# QuickTranslate - ML Dependencies (Optional)
# Required ONLY for fuzzy StrOrigin matching (Strict or Quadruple Fallback).
# Install with: pip install -r requirements-ml.txt
#
# NOT included in the PyInstaller build to keep it light.
# Install these on any machine where you want fuzzy matching.

model2vec>=0.3.0
faiss-cpu>=1.7.0
numpy>=1.24.0
