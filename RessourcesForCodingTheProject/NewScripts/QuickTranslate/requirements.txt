# QuickTranslate Dependencies
# Python 3.11+ required

# XML parsing (lxml for robustness, ElementTree fallback)
lxml>=4.9.0

# Excel output
openpyxl>=3.1.0
xlsxwriter>=3.1.0

# GUI (tkinter is built-in, but including for clarity)
# tkinter is included with Python

# Optional: For clipboard support if needed
# pyperclip>=1.8.0

# === Fuzzy KR Match (SBERT + FAISS) ===
# These are OPTIONAL - only needed for Fuzzy KR Match mode.
# Install separately: pip install -r requirements-ml.txt
# NOT bundled in the build to keep it light (~30MB vs ~2GB+).
