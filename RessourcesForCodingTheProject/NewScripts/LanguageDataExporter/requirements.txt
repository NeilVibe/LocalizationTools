# Language XML to Categorized Excel Converter
# Dependencies

# Excel file handling
openpyxl>=3.1.0      # For READING Excel files (vrs_ordering.py)
xlsxwriter>=3.1.0    # For WRITING Excel files (reliable cell protection)

# Optional: faster XML parsing (fallback to stdlib xml.etree if not installed)
lxml>=4.9.0
