# ============================================
# LocalizationTools - Requirements
# ============================================

# -------------------- Client Dependencies --------------------

# Machine Learning & NLP
sentence-transformers==2.7.0
transformers>=4.46.0
torch==2.3.1
torchvision==0.18.1
faiss-cpu==1.8.0  # Use faiss-gpu if GPU available
model2vec>=0.3.0  # Fast multilingual embeddings (Model2Vec)

# Data Processing
pandas==2.2.2
numpy==1.26.4
openpyxl==3.1.5
xlrd==2.0.1

# HTTP Client
requests==2.32.3
httpx==0.27.0

# System & Utilities
psutil==6.0.0  # For performance metrics
uuid==1.30

# -------------------- Server Dependencies --------------------

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-socketio==5.11.0  # WebSocket support

# Database
sqlalchemy==2.0.32
aiosqlite==0.20.0  # Async SQLite support
psycopg2-binary==2.9.9  # PostgreSQL (sync)
asyncpg>=0.29.0  # PostgreSQL (async)
alembic==1.13.2  # Database migrations

# XML Processing
lxml>=4.9.0  # XML parsing for QA tools

# Authentication & Security
python-jose[cryptography]==3.3.0  # JWT (jose)
PyJWT==2.9.0  # JWT (jwt) - used by auth.py
passlib[bcrypt]==1.7.4  # Password hashing
bcrypt==4.2.0
python-dotenv==1.0.1  # Environment variables

# Validation
pydantic==2.9.0
pydantic-settings==2.5.2
email-validator==2.2.0

# -------------------- Shared Dependencies --------------------

# Date/Time
python-dateutil==2.9.0.post0

# Logging
loguru==0.7.2

# Configuration
pyyaml==6.0.2
toml==0.10.2

# Progress Bars
tqdm==4.66.5

# -------------------- Development Dependencies --------------------

# Testing
pytest==8.3.2
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pytest-xdist==3.5.0  # Parallel test execution
httpx==0.27.0  # For FastAPI testing

# Code Quality
black==24.8.0  # Code formatter
flake8==7.1.1  # Linter
mypy==1.11.2  # Type checker
isort==5.13.2  # Import sorter

# Build & Distribution
pyinstaller==6.10.0  # For creating executables
setuptools==74.0.0
wheel==0.44.0

# -------------------- Optional Dependencies --------------------

# Visualization (for admin dashboard)
plotly==5.24.0
matplotlib==3.9.2
seaborn==0.13.2

# Excel Advanced Features
xlsxwriter==3.2.0

# Monitoring (production)
# sentry-sdk==2.14.0  # Error tracking (uncomment for production)

# -------------------- Platform-specific Notes --------------------
# For Windows:
#   - torch may require manual installation from PyTorch website
#   - faiss-cpu works out of the box
#
# For macOS (Apple Silicon):
#   - Use: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# For Linux:
#   - All packages should work with default pip install
#
# For GPU support:
#   - Replace faiss-cpu with faiss-gpu
#   - Install appropriate CUDA-enabled torch version
