# ============================================
# LocalizationTools - LIGHT Requirements
# ============================================
# For LIGHT builds - excludes ML packages (~800MB savings)
# ML features (KR Similar, AI translation) require manual install
# or upgrade to FULL build
# ============================================

# -------------------- Server Dependencies --------------------

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart>=0.0.18  # CVE-2024-53981 fix
python-socketio>=5.14.0  # CVE-2025-61765 fix

# Database
sqlalchemy==2.0.32
aiosqlite==0.20.0  # Async SQLite support
psycopg2-binary==2.9.9  # PostgreSQL (sync)
asyncpg>=0.29.0  # PostgreSQL (async)
alembic==1.13.2  # Database migrations

# Data Processing (lightweight)
pandas==2.2.2
numpy==1.26.4
openpyxl==3.1.5
xlrd==2.0.1

# HTTP Client
requests>=2.32.4  # CVE-2024-47081 fix
httpx==0.27.0

# XML Processing
lxml>=4.9.0  # XML parsing for QA tools

# Authentication & Security
python-jose[cryptography]>=3.4.0  # JWT (jose) - CVE fixes
PyJWT==2.9.0  # JWT (jwt)
passlib[bcrypt]==1.7.4  # Password hashing
bcrypt==4.2.0
python-dotenv==1.0.1  # Environment variables

# Validation
pydantic==2.9.0
pydantic-settings==2.5.2
email-validator==2.2.0

# -------------------- Shared Dependencies --------------------

# Date/Time
python-dateutil==2.9.0.post0

# Logging
loguru==0.7.2

# Configuration
pyyaml==6.0.2
toml==0.10.2

# Progress Bars
tqdm==4.66.5

# System & Utilities
psutil==6.0.0

# Excel Advanced Features
xlsxwriter==3.2.0

# -------------------- NOT INCLUDED (LIGHT BUILD) --------------------
# The following are excluded to reduce size by ~800MB:
#
# Machine Learning & NLP (install manually for AI features):
# - torch==2.3.1 (~800MB)
# - torchvision==0.18.1 (~200MB)
# - sentence-transformers==2.7.0 (~100MB)
# - transformers>=4.46.0 (~500MB)
# - faiss-cpu==1.8.0 (~50MB)
# - model2vec>=0.3.0
#
# To enable ML features, run:
#   pip install torch torchvision sentence-transformers transformers faiss-cpu
#
# Or download the FULL build which includes everything.
# ============================================
