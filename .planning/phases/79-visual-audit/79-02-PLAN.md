---
phase: 79-visual-audit
plan: 02
type: execute
wave: 2
depends_on:
  - "79-01"
files_modified:
  - locaNext/src/routes/+layout.svelte
  - locaNext/src/lib/components/apps/LDM.svelte
  - screenshots/final-files-page.webp
  - screenshots/final-gamedev-page.webp
  - screenshots/final-codex-page.webp
  - screenshots/final-map-page.webp
  - screenshots/final-tm-page.webp
  - .planning/phases/79-visual-audit/79-02-final-scores.json
autonomous: true
requirements:
  - UIUX-02
  - UIUX-03

must_haves:
  truths:
    - "All critical UIUX issues identified in Plan 79-01 audit are fixed"
    - "Re-verified pages score 7+/10 on Qwen3-VL review"
    - "Final screenshot gallery saved as milestone evidence"
  artifacts:
    - path: "screenshots/final-files-page.webp"
      provides: "Final Files page screenshot after fixes"
    - path: "screenshots/final-gamedev-page.webp"
      provides: "Final Game Dev page screenshot after fixes"
    - path: "screenshots/final-codex-page.webp"
      provides: "Final Codex page screenshot after fixes"
    - path: "screenshots/final-map-page.webp"
      provides: "Final Map page screenshot after fixes"
    - path: "screenshots/final-tm-page.webp"
      provides: "Final TM page screenshot after fixes"
    - path: ".planning/phases/79-visual-audit/79-02-final-scores.json"
      provides: "Final audit scores proving all pages pass 7+/10"
  key_links:
    - from: ".planning/phases/79-visual-audit/79-01-audit-results.json"
      to: "Svelte component fixes"
      via: "critical_issues list drives fix targets"
      pattern: "critical_issues"
    - from: "Fixed components"
      to: "screenshots/final-*.webp"
      via: "re-screenshot after HMR applies fixes"
      pattern: "final-.*\\.webp"
---

<objective>
Fix all critical UIUX issues identified by the Plan 79-01 visual audit, re-verify with Qwen3-VL, and save final screenshot gallery as milestone evidence.

Purpose: Ensure every page meets the 7+/10 quality bar before closing v9.0 milestone.
Output: Fixed UI components + final screenshot gallery + final scores JSON proving all pages pass.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-visual-audit/79-01-SUMMARY.md

The audit results from Plan 79-01 contain per-page scores and critical issues.
Read `.planning/phases/79-visual-audit/79-01-audit-results.json` to get the exact issues to fix.

Key constraints:
- Svelte 5 Runes ONLY ($state, $derived, $effect)
- Optimistic UI mandatory
- HMR is active at localhost:5173 — changes apply instantly
- Login: admin/admin123
- Do NOT restart servers — HMR auto-applies
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix critical issues from visual audit</name>
  <files>Files determined by audit results — likely Svelte components in locaNext/src/lib/components/</files>
  <read_first>.planning/phases/79-visual-audit/79-01-audit-results.json (the audit results with critical issues per page), .planning/phases/79-visual-audit/79-01-SUMMARY.md (summary of audit findings)</read_first>
  <action>
1. Read the audit results JSON from Plan 79-01
2. Extract all critical issues across all 5 pages
3. Group issues by component/file to minimize edits
4. For each critical issue:
   a. Read the affected Svelte component
   b. Apply the fix (CSS layout, missing data handling, alignment, contrast, etc.)
   c. Verify HMR picks up the change (no server restart needed)
5. Common fix patterns to expect:
   - Empty states: Add "No data loaded" placeholder instead of blank space
   - Truncated text: Adjust overflow/text-overflow CSS
   - Misaligned elements: Fix flexbox/grid alignment
   - Poor contrast: Adjust color values for WCAG compliance
   - Broken layouts: Fix responsive grid/flex issues
   - Missing data indicators: Add loading states or empty state messages
6. If a page already scores 7+/10 with no critical issues, skip fixes for that page
7. Only fix CRITICAL issues. Minor issues are noted but not blocking.
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools/locaNext && npx svelte-check --threshold warning 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>All critical issues from audit results are addressed in Svelte components, no new Svelte compilation errors introduced</acceptance_criteria>
  <done>Critical UIUX issues fixed, HMR applied, ready for re-verification</done>
</task>

<task type="auto">
  <name>Task 2: Re-screenshot all pages and re-verify with Qwen3-VL</name>
  <files>screenshots/final-files-page.webp, screenshots/final-gamedev-page.webp, screenshots/final-codex-page.webp, screenshots/final-map-page.webp, screenshots/final-tm-page.webp, .planning/phases/79-visual-audit/79-02-final-scores.json</files>
  <read_first>.planning/phases/79-visual-audit/79-01-audit-results.json (original scores for comparison)</read_first>
  <action>
1. Use Playwright MCP to navigate to http://localhost:5173 (ONE TIME ONLY per playwright-efficiency rule)
2. Login with admin/admin123
3. For each of the 5 pages (files, gamedev, codex, map, tm):
   a. Click sidebar nav to switch page
   b. Wait 2s for data load
   c. Take screenshot: `screenshots/final-{page}-page.png`
4. Convert all to WebP: `for f in screenshots/final-*-page.png; do cwebp -q 85 "$f" -o "${f%.png}.webp"; done`
5. For each final screenshot, send to Qwen3-VL:
   - Same prompt as Plan 79-01 Task 2
   - Parse score and issues
6. Compile final results into `.planning/phases/79-visual-audit/79-02-final-scores.json`:
```json
{
  "final_audit_date": "2026-03-24",
  "pages": {
    "files": { "score": N, "screenshot": "screenshots/final-files-page.webp", "issues_remaining": [...], "improved_from": M },
    "gamedev": { ... },
    "codex": { ... },
    "map": { ... },
    "tm": { ... }
  },
  "summary": {
    "avg_score": N,
    "all_pass_7plus": true/false,
    "total_critical_remaining": 0
  }
}
```
7. Print comparison table: Page | Before | After | Pass?
8. If any page still below 7/10 with critical issues, apply one more round of fixes and re-screenshot that specific page only.
  </action>
  <verify>
    <automated>cat .planning/phases/79-visual-audit/79-02-final-scores.json | python3 -c "import sys,json; d=json.load(sys.stdin); scores=[p['score'] for p in d['pages'].values()]; assert all(s>=7 for s in scores), f'Failed: {scores}'; print(f'All pass! Scores: {scores}, Avg: {sum(scores)/len(scores):.1f}')"</automated>
  </verify>
  <acceptance_criteria>All 5 pages score 7+/10 on Qwen3-VL re-review, final screenshot gallery saved, comparison showing improvement</acceptance_criteria>
  <done>All pages pass 7+/10 visual quality bar, final screenshots saved as milestone evidence, scores documented</done>
</task>

</tasks>

<verification>
- All critical issues from 79-01 audit addressed
- 5 final WebP screenshots in screenshots/ directory
- final-scores.json shows all pages at 7+/10
- No Svelte compilation errors introduced
</verification>

<success_criteria>
Every page passes AI visual review at 7+/10. Screenshot gallery of all 5 pages with real-looking data exists as evidence. Critical issues fixed and re-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/79-visual-audit/79-02-SUMMARY.md`
</output>
