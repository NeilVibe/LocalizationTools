---
phase: 24-uiux-polish
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - locaNext/src/lib/components/pages/GameDevPage.svelte
  - locaNext/src/lib/components/ldm/FileExplorerTree.svelte
  - locaNext/src/lib/components/pages/CodexPage.svelte
  - locaNext/src/lib/components/ldm/CodexEntityDetail.svelte
  - locaNext/src/lib/components/ldm/CodexSearchBar.svelte
  - locaNext/src/lib/components/pages/WorldMapPage.svelte
  - locaNext/src/lib/components/ldm/MapCanvas.svelte
  - locaNext/src/lib/components/ldm/MapDetailPanel.svelte
  - locaNext/src/lib/components/ldm/MapTooltip.svelte
  - locaNext/src/lib/components/ldm/AISuggestionsTab.svelte
  - locaNext/src/lib/components/ldm/NamingPanel.svelte
  - locaNext/src/lib/components/ldm/CategoryFilter.svelte
  - locaNext/src/lib/components/ldm/QAInlineBadge.svelte
autonomous: false
requirements: [UX-01, UX-02, UX-03, UX-04, UX-05]

must_haves:
  truths:
    - "All v3.0 interactive elements have visible focus indicators"
    - "Error states show actionable messages, not raw error strings"
    - "Long text content wraps gracefully in all v3.0 panels"
    - "Empty states provide clear guidance on what to do next"
    - "Loading states are consistent across all v3.0 components"
  artifacts:
    - path: "locaNext/src/lib/components/ldm/AISuggestionsTab.svelte"
      provides: "Hardened error states and loading text"
    - path: "locaNext/src/lib/components/ldm/NamingPanel.svelte"
      provides: "Improved empty state and error copy"
    - path: "locaNext/src/lib/components/ldm/QAInlineBadge.svelte"
      provides: "Keyboard-accessible badge interaction"
  key_links:
    - from: "All v3.0 components"
      to: "Carbon Design System"
      via: "CSS custom properties"
      pattern: "var\\(--cds-"
---

<objective>
Run skill-driven UIUX review (audit, harden, clarify) across all v3.0 components and implement the findings. Focus on accessibility hardening, error state improvements, and UX copy clarity.

Purpose: Elevate all v3.0 features from "functional" to "production-ready" quality. Catch issues that per-requirement fixes miss.
Output: Hardened components with better a11y, error handling, and UX copy across the entire v3.0 feature set.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-uiux-polish/24-01-SUMMARY.md

@locaNext/src/lib/components/pages/GameDevPage.svelte
@locaNext/src/lib/components/ldm/AISuggestionsTab.svelte
@locaNext/src/lib/components/ldm/NamingPanel.svelte
@locaNext/src/lib/components/ldm/QAInlineBadge.svelte
@locaNext/src/lib/components/ldm/CategoryFilter.svelte
@locaNext/src/lib/components/ldm/CodexSearchBar.svelte
@locaNext/src/lib/components/ldm/CodexEntityDetail.svelte
@locaNext/src/lib/components/ldm/MapCanvas.svelte
@locaNext/src/lib/components/ldm/MapTooltip.svelte
@locaNext/src/lib/components/pages/WorldMapPage.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit + Harden all v3.0 components (a11y, error states, edge cases)</name>
  <files>
    locaNext/src/lib/components/pages/GameDevPage.svelte
    locaNext/src/lib/components/ldm/AISuggestionsTab.svelte
    locaNext/src/lib/components/ldm/NamingPanel.svelte
    locaNext/src/lib/components/ldm/QAInlineBadge.svelte
    locaNext/src/lib/components/ldm/CategoryFilter.svelte
    locaNext/src/lib/components/ldm/CodexSearchBar.svelte
    locaNext/src/lib/components/ldm/CodexEntityDetail.svelte
    locaNext/src/lib/components/ldm/MapCanvas.svelte
    locaNext/src/lib/components/ldm/MapTooltip.svelte
    locaNext/src/lib/components/pages/WorldMapPage.svelte
    locaNext/src/lib/components/pages/CodexPage.svelte
    locaNext/src/lib/components/ldm/MapDetailPanel.svelte
    locaNext/src/lib/components/ldm/FileExplorerTree.svelte
  </files>
  <action>
Run audit skill and harden skill mentally across ALL v3.0 components. Read each component, identify and fix issues in these categories:

**Accessibility (audit):**
- Add `aria-label` to icon-only buttons (close buttons, refresh buttons, navigation icons)
- Add `role="status"` or `aria-live="polite"` to loading indicators and error banners
- Ensure all custom buttons have visible `:focus` styles (2px solid var(--cds-focus))
- Add `aria-label` to CodexSearchBar input if missing
- Ensure MapCanvas has `role="img"` or appropriate ARIA for the canvas element
- Add `role="tablist"` to CodexPage `.codex-tabs` container and `role="tab"` + `aria-selected` to each tab button

**Error hardening (harden):**
- Review all `catch` blocks -- ensure error messages shown to users are human-readable, not raw HTTP status codes
- Add empty state messaging where missing (e.g., "No suggestions yet -- select a text entry to see AI suggestions")
- Ensure text overflow is handled in ALL text-displaying elements (add `overflow-wrap: break-word` where missing)
- Review MapTooltip positioning -- ensure it does not render off-screen

**UX copy (clarify):**
- Review loading text: use action-oriented text ("Loading suggestions..." not just "Loading...")
- Review error messages: be specific about what failed and what to try ("Failed to load entity list -- check your network connection" not just "Error")
- Review empty states: guide the user ("Select a file to see AI translation suggestions" not "No data")
- Review button labels: ensure they describe the action ("Open in Codex" is good, verify others)

Implement all findings directly. Do NOT create a separate report -- fix the code. Prioritize by impact:
1. Missing focus styles (breaks keyboard navigation)
2. Missing ARIA attributes (breaks screen readers)
3. Raw error messages (confuses users)
4. Missing empty states (leaves users lost)
5. Text overflow (breaks layouts)

Skip any changes that would conflict with Plan 01's modifications (PlaceholderImage, layout tabs -- those are already fixed).
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && grep -r "aria-label\|aria-live\|aria-selected\|role=" locaNext/src/lib/components/ldm/AISuggestionsTab.svelte locaNext/src/lib/components/ldm/QAInlineBadge.svelte locaNext/src/lib/components/ldm/CodexSearchBar.svelte locaNext/src/lib/components/pages/CodexPage.svelte | wc -l</automated>
  </verify>
  <done>
    - All icon-only buttons have aria-label attributes
    - Loading indicators use aria-live for screen reader announcements
    - CodexPage tabs have role="tablist" and role="tab" with aria-selected
    - Error messages are human-readable across all v3.0 components
    - Empty states provide actionable guidance text
    - All custom buttons have visible :focus styles
    - Text overflow handled in all text-displaying elements
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete UIUX polish across all v3.0 features: 5 specific fixes (a11y attributes, tab dividers, image fallbacks, Chromium compatibility, text wrapping) plus comprehensive audit/harden/clarify pass across 13 components.</what-built>
  <how-to-verify>
    1. Start DEV server: `cd locaNext && npm run dev`
    2. Open http://localhost:5173 and login (admin/admin123)
    3. **Tab dividers (UX-02):** Look at the top nav bar -- Files|TM|Game Dev|Codex|Map should all have dividers between them
    4. **Codex images (UX-03):** Click Codex tab, check entity cards -- broken images should show placeholder icons, not blank spaces
    5. **Game Dev tree (UX-01):** Click Game Dev tab, expand/collapse folders -- use browser DevTools to verify aria-expanded changes
    6. **Map panel (UX-05):** Click Map tab, select a region -- check that long description text wraps properly in the detail panel
    7. **Keyboard nav:** Tab through the Codex page -- all buttons should show visible focus outlines
    8. **Error states:** Disconnect backend, check that error messages are human-readable (not "HTTP 500")
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `grep -r "aria-expanded" locaNext/src/lib/components/ldm/FileExplorerTree.svelte` shows attribute on folder buttons
2. `grep -r "role=\"tablist\"" locaNext/src/lib/components/pages/CodexPage.svelte` shows tab accessibility
3. `grep -r "aria-label" locaNext/src/lib/components/ldm/QAInlineBadge.svelte` shows button labels
4. `cd locaNext && npx svelte-check --threshold warning 2>&1 | tail -10` shows no new errors
</verification>

<success_criteria>
All v3.0 components pass accessibility and UX quality bar:
- Zero icon-only buttons without aria-label
- All interactive containers have appropriate ARIA roles
- Error states show human-readable messages with recovery guidance
- Empty states guide users to the next action
- Text wraps gracefully at all viewport widths
- Visual verification by user confirms production-ready quality
</success_criteria>

<output>
After completion, create `.planning/phases/24-uiux-polish/24-02-SUMMARY.md`
</output>
