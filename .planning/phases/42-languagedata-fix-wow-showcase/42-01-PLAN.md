---
phase: 42-languagedata-fix-wow-showcase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/tools/ldm/routes/files.py
  - server/tools/ldm/routes/rows.py
  - server/tools/ldm/routes/platforms.py
  - server/tools/ldm/routes/projects.py
  - locaNext/src/lib/components/pages/FilesPage.svelte
  - locaNext/src/lib/components/pages/GridPage.svelte
autonomous: true
requirements: [LDM-FIX-01, LDM-FIX-02, LDM-FIX-03]

must_haves:
  truths:
    - "API endpoints for platforms, projects, files, and rows return 200 with valid data (not 422)"
    - "Double-clicking a file in FilesPage navigates to GridPage showing file rows"
    - "Upload -> Open -> Edit -> Save cycle completes without errors"
  artifacts:
    - path: "server/tools/ldm/routes/files.py"
      provides: "File CRUD endpoints returning valid responses"
    - path: "server/tools/ldm/routes/rows.py"
      provides: "Row listing and editing endpoints"
    - path: "locaNext/src/lib/components/pages/FilesPage.svelte"
      provides: "File browser with working double-click to open"
    - path: "locaNext/src/lib/components/pages/GridPage.svelte"
      provides: "Grid page receiving fileId and rendering rows"
  key_links:
    - from: "FilesPage.svelte"
      to: "/api/ldm/platforms"
      via: "fetch in onMount"
      pattern: "fetch.*api/ldm/platforms"
    - from: "FilesPage.svelte"
      to: "/api/ldm/projects/{project_id}/files"
      via: "fetch on project select"
      pattern: "fetch.*api/ldm/projects.*files"
    - from: "FilesPage.svelte"
      to: "GridPage.svelte"
      via: "onFileSelect callback or navigation store"
      pattern: "onFileSelect|openFile"
    - from: "GridPage.svelte"
      to: "/api/ldm/files/{file_id}/rows"
      via: "VirtualGrid fetch"
      pattern: "files.*rows"
---

<objective>
Fix the LDM grid editor regression -- the grid opens but shows "No file selected" after double-clicking a file. Backend API returns 422 validation errors on file list endpoints.

Purpose: Restore core LDM functionality that regressed, which is prerequisite for all showcase work.
Output: Working LDM file browser and grid editor (upload, open, edit, save).
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-languagedata-fix-wow-showcase/42-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Debug and fix backend API 422 errors</name>
  <files>server/tools/ldm/routes/files.py, server/tools/ldm/routes/rows.py, server/tools/ldm/routes/platforms.py, server/tools/ldm/routes/projects.py, server/tools/ldm/schemas/file.py, server/tools/ldm/schemas/project.py</files>
  <read_first>
    - server/tools/ldm/routes/files.py (full file -- understand all endpoints and their parameter validation)
    - server/tools/ldm/routes/rows.py (full file -- understand row listing endpoint params)
    - server/tools/ldm/routes/platforms.py (full file -- understand platform listing)
    - server/tools/ldm/routes/projects.py (full file -- understand project listing and params)
    - server/tools/ldm/schemas/file.py (response schemas that may have changed)
    - server/tools/ldm/schemas/project.py (response schemas)
    - server/tools/ldm/schemas/common.py (shared schemas)
    - testing_toolkit/api_test_protocol.sh (API testing patterns)
  </read_first>
  <action>
    GDP-level systematic API debugging. Test the full navigation chain with curl:

    1. Start with health check: `curl -s http://localhost:8888/api/ldm/health`
    2. Test login: `curl -s -X POST http://localhost:8888/api/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"admin123"}'` -- save the token
    3. Test platform list: `curl -s http://localhost:8888/api/ldm/platforms -H "Authorization: Bearer $TOKEN"`
    4. Test project list: `curl -s http://localhost:8888/api/ldm/projects -H "Authorization: Bearer $TOKEN"`
    5. Test file list for a project: `curl -s "http://localhost:8888/api/ldm/projects/1/files" -H "Authorization: Bearer $TOKEN"`
    6. Test row list for a file: `curl -s "http://localhost:8888/api/ldm/files/1/rows" -H "Authorization: Bearer $TOKEN"`

    For each 422 error, read the response body to identify which Pydantic field validation failed. Common causes:
    - Schema field added/removed that doesn't match DB columns
    - Optional field now required in response model
    - Type mismatch (int vs str) in serialization

    Fix each validation error at the source -- either fix the schema to match what the repository returns, or fix the repository to return what the schema expects. Do NOT change DB models.

    After fixes, re-run all 6 curl commands and confirm 200 responses.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:8888/api/ldm/health | grep -q 200 && echo "Health OK"</automated>
  </verify>
  <acceptance_criteria>
    - `curl http://localhost:8888/api/ldm/platforms` returns HTTP 200 with JSON array
    - `curl http://localhost:8888/api/ldm/projects` returns HTTP 200 with JSON array
    - `curl "http://localhost:8888/api/ldm/projects/{id}/files"` returns HTTP 200 (not 422)
    - `curl "http://localhost:8888/api/ldm/files/{id}/rows"` returns HTTP 200 with `items` array
    - No 422 errors in server logs for any LDM endpoint in the file navigation flow
  </acceptance_criteria>
  <done>All LDM API endpoints in the file navigation chain return 200 with valid JSON. Zero 422 validation errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix frontend file selection and grid loading flow</name>
  <files>locaNext/src/lib/components/pages/FilesPage.svelte, locaNext/src/lib/components/pages/GridPage.svelte, locaNext/src/lib/stores/navigation.js</files>
  <read_first>
    - locaNext/src/lib/components/pages/FilesPage.svelte (full file -- understand double-click handler, onFileSelect callback, navigation flow)
    - locaNext/src/lib/components/pages/GridPage.svelte (full file -- understand how fileId prop is received and used to load rows)
    - locaNext/src/lib/stores/navigation.js (openFile store that bridges FilesPage -> GridPage)
    - locaNext/src/lib/components/ldm/VirtualGrid.svelte (first 100 lines -- understand props and how it loads rows)
    - locaNext/src/lib/components/ldm/ExplorerGrid.svelte (first 50 lines -- understand double-click event handling)
  </read_first>
  <action>
    Trace the file selection flow from FilesPage to GridPage:

    1. In FilesPage.svelte, find the double-click handler on files (likely in ExplorerGrid's ondblclick or a handleOpen function). Verify it calls `onFileSelect` with the file's ID and metadata.

    2. Trace `onFileSelect` up to the parent component (likely LDM.svelte or App.svelte) -- verify it updates the `openFile` store with `{ id: fileId, name: fileName, file_type: ... }`.

    3. In GridPage.svelte, verify the `fileId` prop is bound to `$openFile?.id` and that `VirtualGrid` receives it.

    4. In VirtualGrid.svelte, verify the `$effect` or `onMount` that triggers row loading when `fileId` changes -- it should call `GET /api/ldm/files/{fileId}/rows`.

    Common regression causes:
    - Store update not propagating (Svelte 5 runes migration broke $store syntax)
    - fileId passed as undefined due to prop name mismatch
    - VirtualGrid `$effect` not reacting to fileId change
    - API base URL wrong or auth headers missing

    Fix the broken link in the chain. After fix, verify with Playwright:
    - Navigate to Files page
    - Double-click a file
    - Confirm GridPage renders with rows visible

    Upload a test file via curl first if no files exist:
    ```bash
    curl -X POST "http://localhost:8888/api/ldm/files/upload" \
      -H "Authorization: Bearer $TOKEN" \
      -F "file=@tests/fixtures/mock_gamedata/stringtable/export__/System/characterinfo_npc.loc.xml" \
      -F "project_id=1" \
      -F "storage=server"
    ```
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools/locaNext && npx playwright test --grep "file.*open|grid.*load" --timeout 30000 2>/dev/null || echo "Manual curl verification needed"</automated>
  </verify>
  <acceptance_criteria>
    - FilesPage.svelte contains a working file open handler that updates the openFile store (grep for `openFile.set` or `$openFile =`)
    - GridPage.svelte receives fileId and passes it to VirtualGrid (grep for `fileId` prop binding)
    - VirtualGrid.svelte fetches rows when fileId changes (grep for `files/${fileId}/rows` or equivalent fetch call)
    - No "No file selected" shown after double-clicking a file that has rows
  </acceptance_criteria>
  <done>Double-clicking a file in FilesPage opens GridPage with VirtualGrid showing the file's translation rows. Upload, open, edit cell, save all work end-to-end.</done>
</task>

</tasks>

<verification>
1. Start dev servers: backend on :8888, frontend on :5173
2. curl test all 6 API endpoints in the navigation chain -- all return 200
3. Open browser at localhost:5173, navigate to Files page
4. Upload a test XML file if none exist
5. Double-click the file -- GridPage should show rows
6. Click a cell, edit, save -- verify the change persists on reload
</verification>

<success_criteria>
- All LDM API endpoints return 200 (zero 422 errors)
- File browser navigates correctly (platforms -> projects -> files)
- Double-click file opens grid with rows visible
- Edit and save cycle works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/42-languagedata-fix-wow-showcase/42-01-SUMMARY.md`
</output>
