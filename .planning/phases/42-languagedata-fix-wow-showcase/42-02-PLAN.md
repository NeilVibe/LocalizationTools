---
phase: 42-languagedata-fix-wow-showcase
plan: 02
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - tests/fixtures/mock_gamedata/localization/showcase_ui_strings.xlsx
  - tests/fixtures/mock_gamedata/localization/showcase_dialogue.txt
  - tests/fixtures/mock_gamedata/localization/showcase_items.loc.xml
  - server/tools/ldm/services/mock_tm_loader.py
autonomous: true
requirements: [LDM-MOCK-01, LDM-MOCK-02, LDM-MOCK-03, LDM-MOCK-04]

must_haves:
  truths:
    - "Three mock files exist in 3 different formats (xlsx, txt, xml) with game-themed strings"
    - "Each mock file has 25+ strings with StringID, Source (ENG), Translation (KR)"
    - "Mock files can be uploaded to the LDM grid and rows display correctly"
    - "30+ TM entries exist that match the mock file strings with varying similarity scores"
  artifacts:
    - path: "tests/fixtures/mock_gamedata/localization/showcase_ui_strings.xlsx"
      provides: "Excel format mock file with 25+ strings and metadata columns"
    - path: "tests/fixtures/mock_gamedata/localization/showcase_dialogue.txt"
      provides: "Tab-separated mock file with 25+ dialogue strings"
    - path: "tests/fixtures/mock_gamedata/localization/showcase_items.loc.xml"
      provides: "XML LocStr format mock file with 25+ item/character strings including <br/> tags"
    - path: "server/tools/ldm/services/mock_tm_loader.py"
      provides: "Script to load 30+ TM entries matching mock file strings"
  key_links:
    - from: "showcase_ui_strings.xlsx"
      to: "server/tools/ldm/file_handlers/excel_handler.py"
      via: "Upload parses Excel columns"
      pattern: "source_col.*target_col"
    - from: "showcase_dialogue.txt"
      to: "server/tools/ldm/file_handlers/txt_handler.py"
      via: "Upload parses tab-separated"
      pattern: "split.*\\t"
    - from: "showcase_items.loc.xml"
      to: "server/tools/ldm/file_handlers/xml_handler.py"
      via: "Upload parses LocStr XML"
      pattern: "LocStr|StrOrigin"
---

<objective>
Create 3-format mock localization data (Excel, TXT, XML) with game-themed strings that cross-reference the Codex universe, plus 30+ TM entries for the showcase demo.

Purpose: Prove LocaNext is format-agnostic and provide rich data for the right panel WOW demo.
Output: 3 mock files + TM entry loader script, all uploadable and displayable in the LDM grid.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-languagedata-fix-wow-showcase/42-CONTEXT.md
@.planning/phases/42-languagedata-fix-wow-showcase/42-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3-format mock localization files</name>
  <files>tests/fixtures/mock_gamedata/localization/showcase_ui_strings.xlsx, tests/fixtures/mock_gamedata/localization/showcase_dialogue.txt, tests/fixtures/mock_gamedata/localization/showcase_items.loc.xml</files>
  <read_first>
    - server/tools/ldm/file_handlers/excel_handler.py (understand expected Excel column layout: source_col, target_col, stringid_col, has_header)
    - server/tools/ldm/file_handlers/txt_handler.py (understand expected TXT format: tab-separated, which column index is source vs target)
    - server/tools/ldm/file_handlers/xml_handler.py (understand expected XML LocStr format: StringID attr, Str attr, StrOrigin, language attrs like KR, ENG)
    - server/tools/ldm/file_handlers/__init__.py (handler selection logic)
    - tests/fixtures/mock_gamedata/StaticInfo/characterinfo/characterinfo_showcase.staticinfo.xml (existing character names to cross-reference)
  </read_first>
  <action>
    Create a Python script at `tests/fixtures/mock_gamedata/localization/generate_showcase_data.py` that generates all 3 files. Run it to produce the files.

    **Excel file (showcase_ui_strings.xlsx):**
    Use xlsxwriter (project standard for writing Excel). Columns: StringID | Source (ENG) | Translation (KR) | Status | Translator | ReviewState
    - 25-30 UI strings: menu items, button labels, system messages, tooltips
    - Mix of statuses: "AI-Translated" (8), "Human-Reviewed" (10), "Machine-Translated" (4), "Needs-Review" (3)
    - StringIDs like: UI_MENU_FILE, UI_MENU_EDIT, UI_BTN_SAVE, UI_MSG_WELCOME, UI_TIP_SEARCH, etc.
    - Include Korean translations that are natural and game-themed
    - Example: StringID=UI_MSG_WELCOME, Source="Welcome to the Realm of Stars", KR="별의 왕국에 오신 것을 환영합니다"

    **TXT file (showcase_dialogue.txt):**
    Tab-separated. First row is header: StringID\tSource\tTranslation\tSpeaker\tContext
    - 25-30 dialogue lines from the 5 characters (Elder Varon, Kira, Grimjaw, Lune, Drakmar)
    - Cross-reference character names matching Codex entities
    - StringIDs like: DLG_VARON_001, DLG_KIRA_001, DLG_GRIMJAW_001, etc.
    - Include dramatic/emotional dialogue that showcases the fantasy universe
    - Example: DLG_VARON_001\tThe stars speak of a great darkness approaching.\t별들이 다가오는 거대한 어둠에 대해 이야기합니다.\tElder Varon\tAct 1 Opening

    **XML file (showcase_items.loc.xml):**
    Standard LocStr format matching xml_handler expectations:
    ```xml
    <?xml version="1.0" encoding="utf-8"?>
    <LanguageData>
      <LocStr StringID="ITEM_BLACKSTAR_SWORD_NAME" ENG="Blackstar Sword" KR="흑성검"/>
      <LocStr StringID="ITEM_BLACKSTAR_SWORD_DESC" ENG="A blade forged in the heart of a dying star.&lt;br/&gt;Its edge cuts through both flesh and shadow." KR="죽어가는 별의 심장에서 단조된 검.&lt;br/&gt;그 날은 살과 그림자를 모두 베어냅니다."/>
    </LocStr>
    ```
    - 25-30 strings for items, regions, skills from the Codex universe
    - Include strings with `<br/>` tags to demonstrate linebreak handling (use `&lt;br/&gt;` in XML attributes or `<br/>` in text content depending on xml_handler expectations)
    - Cross-reference: Blackstar Sword, Moonstone Amulet, Plague Cure items; Varon, Kira characters; skill names
    - Mix descriptions (long, with linebreaks) and names (short)

    Run the generation script: `cd /home/neil1988/LocalizationTools && python3 tests/fixtures/mock_gamedata/localization/generate_showcase_data.py`

    Verify each file:
    - xlsx: `python3 -c "import openpyxl; wb=openpyxl.load_workbook('tests/fixtures/mock_gamedata/localization/showcase_ui_strings.xlsx'); print(f'Rows: {wb.active.max_row}')"` -- should show 26+ rows
    - txt: `wc -l tests/fixtures/mock_gamedata/localization/showcase_dialogue.txt` -- should show 26+ lines
    - xml: `grep -c 'LocStr' tests/fixtures/mock_gamedata/localization/showcase_items.loc.xml` -- should show 25+
  </action>
  <verify>
    <automated>python3 -c "import os; files=['tests/fixtures/mock_gamedata/localization/showcase_ui_strings.xlsx','tests/fixtures/mock_gamedata/localization/showcase_dialogue.txt','tests/fixtures/mock_gamedata/localization/showcase_items.loc.xml']; [print(f'{f}: {os.path.getsize(f)} bytes') for f in files if os.path.exists(f)]; assert all(os.path.exists(f) for f in files), 'Missing files'"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/fixtures/mock_gamedata/localization/showcase_ui_strings.xlsx` exists and has 25+ data rows with columns StringID, Source, Translation, Status, Translator, ReviewState
    - File `tests/fixtures/mock_gamedata/localization/showcase_dialogue.txt` exists with 25+ tab-separated lines and header row containing StringID, Source, Translation, Speaker, Context
    - File `tests/fixtures/mock_gamedata/localization/showcase_items.loc.xml` exists with 25+ LocStr elements, valid XML, contains `<br/>` or `&lt;br/&gt;` in at least 3 description strings
    - All StringIDs in dialogue reference one of the 5 characters: Varon, Kira, Grimjaw, Lune, Drakmar
    - All StringIDs in items reference Codex entities (Blackstar Sword, Moonstone Amulet, etc.)
  </acceptance_criteria>
  <done>3 mock localization files created covering Excel, TXT, and XML formats with 75+ total game-themed strings cross-referencing the Codex universe.</done>
</task>

<task type="auto">
  <name>Task 2: Create TM entry loader and upload mock files</name>
  <files>server/tools/ldm/services/mock_tm_loader.py</files>
  <read_first>
    - server/tools/ldm/routes/tm_crud.py (understand TM creation endpoint: POST /api/ldm/tm)
    - server/tools/ldm/routes/tm_entries.py (understand TM entry creation: POST /api/ldm/tm/{tm_id}/entries)
    - server/tools/ldm/schemas/tm.py (TMEntry schema -- required fields: source, target, similarity, etc.)
    - server/tools/ldm/routes/files.py (upload endpoint params: project_id, folder_id, file, storage, source_col, target_col, etc.)
    - tests/fixtures/mock_gamedata/localization/showcase_items.loc.xml (generated in Task 1)
    - tests/fixtures/mock_gamedata/localization/showcase_dialogue.txt (generated in Task 1)
  </read_first>
  <action>
    Create `server/tools/ldm/services/mock_tm_loader.py` -- a standalone script that:

    1. Authenticates with the API (POST /api/auth/login with admin/admin123)
    2. Creates a project "Showcase Demo" if not exists (POST /api/ldm/projects)
    3. Uploads all 3 mock files to the project:
       - Excel: POST /api/ldm/files/upload with source_col=1, target_col=2, stringid_col=0, has_header=true
       - TXT: POST /api/ldm/files/upload (auto-detected by extension)
       - XML: POST /api/ldm/files/upload (auto-detected by extension)
    4. Creates a TM "Showcase TM" (POST /api/ldm/tm with name="Showcase TM", source_lang="en", target_lang="ko")
    5. Loads 30+ TM entries into the TM via POST /api/ldm/tm/{tm_id}/entries. Each entry has:
       - source: English text (variations of the mock file strings -- similar but not identical for fuzzy matching)
       - target: Korean translation
       - context: category tag (UI, Dialogue, Item, Skill, Region)

    TM entry design for showcase:
    - 10 exact matches (100% similarity) -- same source text as mock files
    - 10 fuzzy matches (70-95% similarity) -- slightly modified source text (word order change, synonym substitution)
    - 10 semantic matches (50-70% similarity) -- different phrasing, same meaning
    - Include entries that match strings about characters (so Image/Audio tabs light up)

    Run the loader: `cd /home/neil1988/LocalizationTools && python3 server/tools/ldm/services/mock_tm_loader.py`

    The script should:
    - Use `requests` library (stdlib-adjacent, already in requirements)
    - Print progress: "Uploading showcase_ui_strings.xlsx... OK", "Creating TM entries... 30/30"
    - Be idempotent: check if "Showcase Demo" project already exists before creating
    - Handle errors gracefully with clear messages
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python3 -c "import requests; r=requests.post('http://localhost:8888/api/auth/login', json={'username':'admin','password':'admin123'}); token=r.json()['access_token']; h={'Authorization':f'Bearer {token}'}; projects=requests.get('http://localhost:8888/api/ldm/projects',headers=h).json(); showcase=[p for p in projects if 'Showcase' in p.get('name','')]; print(f'Showcase project: {len(showcase) > 0}'); files=requests.get(f'http://localhost:8888/api/ldm/projects/{showcase[0][\"id\"]}/files',headers=h).json() if showcase else []; print(f'Files uploaded: {len(files)}')"</automated>
  </verify>
  <acceptance_criteria>
    - File `server/tools/ldm/services/mock_tm_loader.py` exists and is executable with `python3`
    - Running the loader creates a "Showcase Demo" project visible at GET /api/ldm/projects
    - 3 files appear under the Showcase Demo project at GET /api/ldm/projects/{id}/files
    - A "Showcase TM" exists at GET /api/ldm/tm with 30+ entries
    - TM entries include exact (10), fuzzy (10), and semantic (10) match variations
  </acceptance_criteria>
  <done>Mock data loader creates Showcase Demo project with 3 uploaded files and 30+ TM entries. All data visible in the LDM grid and TM panel.</done>
</task>

</tasks>

<verification>
1. All 3 mock files exist on disk with correct formats
2. Mock data loader runs without errors
3. Open FilesPage in browser -- Showcase Demo project visible
4. Open each file -- grid shows rows with correct columns
5. TM search for a source string returns matches from Showcase TM
</verification>

<success_criteria>
- 3 mock files (xlsx, txt, xml) with 75+ total strings
- Showcase Demo project with all 3 files uploaded
- 30+ TM entries with exact/fuzzy/semantic distribution
- All files open in the grid and display correctly
</success_criteria>

<output>
After completion, create `.planning/phases/42-languagedata-fix-wow-showcase/42-02-SUMMARY.md`
</output>
