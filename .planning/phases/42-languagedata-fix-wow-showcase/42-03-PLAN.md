---
phase: 42-languagedata-fix-wow-showcase
plan: 03
type: execute
wave: 3
depends_on: [42-02]
files_modified:
  - locaNext/src/lib/components/ldm/TMTab.svelte
  - locaNext/src/lib/components/ldm/ImageTab.svelte
  - locaNext/src/lib/components/ldm/AudioTab.svelte
  - locaNext/src/lib/components/ldm/ContextTab.svelte
  - locaNext/src/lib/components/ldm/RightPanel.svelte
  - locaNext/src/lib/components/pages/GridPage.svelte
autonomous: true
requirements: [LDM-WOW-01, LDM-WOW-02, LDM-WOW-03, LDM-WOW-04]

must_haves:
  truths:
    - "Selecting a row in the grid triggers the right panel to show TM matches with 6-tier cascade labels"
    - "When editing a character string, the Image tab shows the character portrait from Codex"
    - "When editing a character string, the Audio tab offers voice playback from TTS"
    - "AI Context tab shows Korean AI summary for the selected string's entity"
  artifacts:
    - path: "locaNext/src/lib/components/ldm/TMTab.svelte"
      provides: "TM tab with 6-tier cascade display (split-line/whole x exact/fuzzy/semantic)"
    - path: "locaNext/src/lib/components/ldm/ImageTab.svelte"
      provides: "Image tab showing entity portraits from Codex when cross-ref detected"
    - path: "locaNext/src/lib/components/ldm/AudioTab.svelte"
      provides: "Audio tab with voice playback from TTS for character strings"
    - path: "locaNext/src/lib/components/ldm/ContextTab.svelte"
      provides: "AI Context tab with Korean entity summary"
  key_links:
    - from: "GridPage.svelte"
      to: "RightPanel.svelte"
      via: "selectedRow prop binding"
      pattern: "selectedRow.*sidePanelSelectedRow"
    - from: "RightPanel.svelte"
      to: "TMTab.svelte"
      via: "tmMatches prop"
      pattern: "tmMatches"
    - from: "TMTab.svelte"
      to: "/api/ldm/tm/suggest"
      via: "fetch on selectedRow change"
      pattern: "tm/suggest"
    - from: "ImageTab.svelte"
      to: "/api/ldm/codex/entities"
      via: "fetch entity by name from source text"
      pattern: "codex.*entities|codex.*search"
    - from: "AudioTab.svelte"
      to: "/api/ldm/codex/generate-voice"
      via: "fetch or existing audio URL"
      pattern: "generate-voice|audio"
---

<objective>
Wire the right panel tabs (TM, Image, Audio, AI Context) to the showcase mock data so that selecting a row in the grid lights up all panels with rich, cross-referenced content.

Purpose: Make the LDM page feel like a professional CAT tool (SDL Trados / memoQ level) for the executive demo.
Output: Fully functional right panel with 4 active tabs showing TM cascade, entity images, voice audio, and AI context.
</objective>

<execution_context>
@/home/<USERNAME>/.claude/get-shit-done/workflows/execute-plan.md
@/home/<USERNAME>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-languagedata-fix-wow-showcase/42-CONTEXT.md
@.planning/phases/42-languagedata-fix-wow-showcase/42-01-SUMMARY.md
@.planning/phases/42-languagedata-fix-wow-showcase/42-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance TMTab with 6-tier cascade display and wire row selection to TM search</name>
  <files>locaNext/src/lib/components/ldm/TMTab.svelte, locaNext/src/lib/components/pages/GridPage.svelte, locaNext/src/lib/components/ldm/RightPanel.svelte</files>
  <read_first>
    - locaNext/src/lib/components/ldm/TMTab.svelte (full file -- understand current TM display, props, how matches are rendered)
    - locaNext/src/lib/components/ldm/RightPanel.svelte (full file -- understand tab switching, selectedRow prop, how tmMatches are passed)
    - locaNext/src/lib/components/pages/GridPage.svelte (full file -- understand sidePanelSelectedRow, TM loading flow, loadTMSuggestions function)
    - server/tools/ldm/routes/tm_search.py (full file -- understand /api/ldm/tm/suggest endpoint params and response format)
    - server/tools/ldm/routes/tm_leverage.py (understand /api/ldm/files/{id}/leverage-stats if exists)
    - locaNext/src/lib/utils/api.js (getAuthHeaders, getApiBase)
  </read_first>
  <action>
    Two changes:

    **A) GridPage.svelte -- wire row selection to TM search:**
    Verify that when a row is clicked in VirtualGrid, `sidePanelSelectedRow` updates and triggers `loadTMSuggestions()`. The function should call:
    ```
    GET /api/ldm/tm/suggest?source={row.source}&tm_id={activeTMs[0]?.id}&file_id={fileId}&threshold=0.5&max_results=10
    ```
    If loadTMSuggestions does not exist or is broken, create/fix it. It must:
    - Read `sidePanelSelectedRow.source` (the source text of the selected row)
    - Use the first active TM ID if available, otherwise search by file_id
    - Set `sidePanelTMLoading = true` before fetch, `false` after
    - Set `sidePanelTMMatches` to the response array
    - Debounce at 200ms to avoid rapid fire during keyboard navigation

    **B) TMTab.svelte -- add 6-tier cascade grouping:**
    The TM matches from the API already have a `similarity` field (0.0-1.0). Group and label them:
    - **Tier 1 - Exact Match** (similarity >= 0.99): Green badge, "100%" label
    - **Tier 2 - High Fuzzy** (0.85-0.99): Yellow badge, show percentage
    - **Tier 3 - Medium Fuzzy** (0.70-0.85): Orange badge, show percentage
    - **Tier 4 - Low Fuzzy** (0.50-0.70): Gray badge, show percentage
    - **Tier 5 - Semantic** (matches from vector search, flagged by `match_type: "semantic"`): Blue badge, "Semantic" label
    - **Tier 6 - Split Line** (matches from segment-level, flagged by `match_type: "segment"`): Purple badge, "Segment" label

    For each match, display:
    - Tier badge (colored dot + label)
    - Source text with diff highlighting against selected row's source (highlight differences in red)
    - Target text (the translation suggestion)
    - "Apply" button that calls `onApplyTM(match)` to insert into the grid

    Use Svelte 5 runes: `let groupedMatches = $derived(groupByTier(tmMatches))`.
    Style with Carbon design tokens (--cds-text-01, --cds-support-success, etc.).
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools/locaNext && grep -c "groupByTier\|Tier\|Exact Match\|Fuzzy\|Semantic\|Segment" src/lib/components/ldm/TMTab.svelte</automated>
  </verify>
  <acceptance_criteria>
    - TMTab.svelte contains `$derived` grouping logic with at least 4 tier categories (grep for `Exact Match`, `Fuzzy`, `Semantic`)
    - TMTab.svelte renders tier badges with colored indicators (grep for `tier-badge` or `match-tier`)
    - TMTab.svelte has an "Apply" button per match (grep for `onApplyTM`)
    - GridPage.svelte has a function that calls `/api/ldm/tm/suggest` when selectedRow changes (grep for `tm/suggest`)
    - RightPanel.svelte passes tmMatches to TMTab (grep for `tmMatches`)
  </acceptance_criteria>
  <done>Selecting a row in the grid triggers TM search, and TMTab displays results grouped in 6 tiers with colored badges, diff highlighting, and Apply buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Image, Audio, and AI Context tabs to Codex entity cross-references</name>
  <files>locaNext/src/lib/components/ldm/ImageTab.svelte, locaNext/src/lib/components/ldm/AudioTab.svelte, locaNext/src/lib/components/ldm/ContextTab.svelte, locaNext/src/lib/components/ldm/RightPanel.svelte</files>
  <read_first>
    - locaNext/src/lib/components/ldm/ImageTab.svelte (full file -- understand current state, props, placeholder behavior)
    - locaNext/src/lib/components/ldm/AudioTab.svelte (full file -- understand current state, props, audio player)
    - locaNext/src/lib/components/ldm/ContextTab.svelte (full file -- understand current state, AI context display)
    - locaNext/src/lib/components/ldm/RightPanel.svelte (full file -- understand how selectedRow is passed to tabs)
    - server/tools/ldm/routes/codex.py (understand entity search/lookup endpoints)
    - server/tools/ldm/routes/context.py (understand AI context endpoint)
    - server/tools/ldm/services/codex_service.py (entity registry, how to look up by name)
    - server/tools/ldm/services/ai_summary_service.py (AI summary generation)
  </read_first>
  <action>
    All 3 tabs need to detect when the selected row's StringID or source text references a Codex entity (character, item, region, skill), then display relevant content.

    **Entity detection helper (add to RightPanel.svelte or a shared util):**
    Create a function `detectEntity(row)` that:
    - Checks row.string_id for known prefixes: DLG_VARON -> "Character_ElderVaron", ITEM_BLACKSTAR -> "Item_BlackstarSword", etc.
    - Falls back to scanning row.source for entity names: "Elder Varon", "Kira", "Blackstar Sword", etc.
    - Returns `{ type: "character"|"item"|"region"|"skill", strkey: "Character_ElderVaron", name: "Elder Varon" }` or null
    - Use a simple lookup map, NOT an API call (fast, synchronous)

    The entity lookup map should include all 5 characters + key items from the Codex:
    ```javascript
    const ENTITY_MAP = {
      'VARON': { type: 'character', strkey: 'Character_ElderVaron', name: 'Elder Varon' },
      'KIRA': { type: 'character', strkey: 'Character_Kira', name: 'Kira' },
      'GRIMJAW': { type: 'character', strkey: 'Character_Grimjaw', name: 'Grimjaw' },
      'LUNE': { type: 'character', strkey: 'Character_Lune', name: 'Lune' },
      'DRAKMAR': { type: 'character', strkey: 'Character_Drakmar', name: 'Drakmar' },
      'BLACKSTAR': { type: 'item', strkey: 'Item_BlackstarSword', name: 'Blackstar Sword' },
      'MOONSTONE': { type: 'item', strkey: 'Item_MoonstoneAmulet', name: 'Moonstone Amulet' },
      'PLAGUE': { type: 'item', strkey: 'Item_PlagueCure', name: 'Plague Cure' },
    };
    ```

    **ImageTab.svelte:**
    - Accept `selectedRow` and `detectedEntity` as props
    - When `detectedEntity` is not null, fetch the entity image: `GET /api/ldm/codex/entities/{strkey}/image` or use the known image path `/api/ldm/gamedata/media/{strkey}` (check which endpoint serves images)
    - Display the image with shimmer-to-reveal loading (reuse pattern from Phase 37: imageShimmer/imageReveal CSS animations)
    - Show entity name + type badge below the image
    - When no entity detected, show empty state: "Select a string related to a character or item to see its portrait"

    **AudioTab.svelte:**
    - Accept `selectedRow` and `detectedEntity` as props
    - When entity is a character, show "Play Voice" button
    - On click, fetch audio: `GET /api/ldm/codex/entities/{strkey}/audio` or `POST /api/ldm/codex/generate-voice/{strkey}`
    - Display HTML5 `<audio>` player with controls after audio loads
    - Show character name + "Voice Preview" label
    - When no character detected, show: "Select a character dialogue string to hear their voice"
    - When entity is non-character (item, region), show: "Voice available for character strings only"

    **ContextTab.svelte:**
    - Accept `selectedRow` and `detectedEntity` as props
    - When entity detected, fetch AI context: `GET /api/ldm/context/entity/{strkey}` or the gamedata context endpoint
    - Display the Korean AI summary in a formatted card with:
      - Entity name as header
      - Category badge (Character / Item / Region / Skill)
      - AI-generated description (Korean text)
      - Related entities list (if available from cross-refs)
    - When no entity detected, show: "Select a string to see AI-generated context"
    - Use LRU cache pattern (reuse from Phase 37: Map-based cache, 100 entries) to avoid refetching

    **RightPanel.svelte:**
    - Import the `detectEntity` function
    - Compute `let detectedEntity = $derived(selectedRow ? detectEntity(selectedRow) : null)`
    - Pass `detectedEntity` to ImageTab, AudioTab, ContextTab as a prop
    - Add small indicator dot on tab icons when that tab has content (e.g., green dot on Image tab when image available)

    All components use Svelte 5 runes ($state, $derived, $effect). Use `$effect` for fetching when detectedEntity changes. Clean up with return function in $effect.
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools/locaNext && grep -c "detectedEntity\|ENTITY_MAP\|detectEntity" src/lib/components/ldm/RightPanel.svelte src/lib/components/ldm/ImageTab.svelte src/lib/components/ldm/AudioTab.svelte src/lib/components/ldm/ContextTab.svelte</automated>
  </verify>
  <acceptance_criteria>
    - RightPanel.svelte contains `detectEntity` function or imports it, and passes `detectedEntity` to child tabs (grep for `detectedEntity`)
    - RightPanel.svelte contains ENTITY_MAP with at least 5 character entries and 3 item entries (grep for `ENTITY_MAP`)
    - ImageTab.svelte fetches entity image when detectedEntity is not null (grep for `codex.*image\|gamedata.*media`)
    - ImageTab.svelte shows empty state text "Select a string" when no entity (grep for `Select a string`)
    - AudioTab.svelte has audio player element (grep for `<audio` or `Audio`)
    - AudioTab.svelte checks entity type is character before showing voice (grep for `type.*character\|character.*voice`)
    - ContextTab.svelte fetches AI context for detected entity (grep for `context.*entity\|entity.*context`)
    - ContextTab.svelte uses cache for context results (grep for `cache\|Map\|LRU`)
  </acceptance_criteria>
  <done>Image tab shows entity portraits, Audio tab plays character voices, AI Context tab shows Korean summaries -- all triggered by row selection in the grid with cross-reference detection.</done>
</task>

</tasks>

<verification>
1. Open GridPage with a showcase file (e.g., showcase_dialogue.txt)
2. Click on a "DLG_VARON_001" row
3. TM tab shows matches grouped by tier with colored badges
4. Image tab shows Elder Varon portrait
5. Audio tab shows "Play Voice" button for character
6. AI Context tab shows Korean context summary
7. Click on an item string (ITEM_BLACKSTAR_SWORD_NAME)
8. Image tab shows item portrait, Audio tab shows "character only" message
9. Click on a UI string (UI_BTN_SAVE) -- all tabs show empty states gracefully
</verification>

<success_criteria>
- TM tab displays 6-tier cascade with colored badges and Apply buttons
- Image tab shows entity portraits when cross-reference detected
- Audio tab plays character voices for character strings
- AI Context tab shows Korean AI summaries
- All tabs show graceful empty states when no entity cross-reference found
- No console errors during tab switching or row navigation
</success_criteria>

<output>
After completion, create `.planning/phases/42-languagedata-fix-wow-showcase/42-03-SUMMARY.md`
</output>
