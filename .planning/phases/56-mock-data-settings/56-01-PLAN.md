---
phase: 56-mock-data-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/setup_mock_data.py
  - tests/test_mock_data.py
autonomous: true
requirements: [MOCK-01, MOCK-02, MOCK-03, MOCK-04]

must_haves:
  truths:
    - "Running `python scripts/setup_mock_data.py --confirm-wipe` creates a fresh DB with 1 platform and 3 projects"
    - "project_FRE auto-detects as French, project_ENG as English, project_MULTI as Multi-Language"
    - "project_MULTI has corrections_FRE and corrections_ENG subfolders"
    - "test123 languagedata files are loadable when LOC PATH points to them"
  artifacts:
    - path: "scripts/setup_mock_data.py"
      provides: "CLI mock DB setup with --confirm-wipe flag"
      contains: "argparse"
    - path: "tests/test_mock_data.py"
      provides: "Tests for mock data creation and language detection"
      contains: "test_wipe_and_create"
  key_links:
    - from: "scripts/setup_mock_data.py"
      to: "server/data/offline.db"
      via: "sqlite3 direct connection"
      pattern: "sqlite3\\.connect"
    - from: "scripts/setup_mock_data.py"
      to: "server/database/models.py"
      via: "table names match ORM model __tablename__"
      pattern: "ldm_platforms|ldm_projects|ldm_folders"
---

<objective>
Create the CLI mock data script that wipes the SQLite database and populates it with 3 mock projects (project_FRE, project_ENG, project_MULTI) under a single platform, with language auto-detection from project name suffixes and subfolders for multi-language merge testing.

Purpose: Every subsequent phase (transfer adapter, merge API, merge UI) needs mock data to operate on. This is the foundation.
Output: `scripts/setup_mock_data.py` CLI script + tests
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-mock-data-settings/56-RESEARCH.md
@server/database/models.py
@server/config.py
@scripts/create_admin.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create mock data CLI script with language detection</name>
  <files>scripts/setup_mock_data.py, tests/test_mock_data.py</files>
  <read_first>
    - server/database/models.py (LDMPlatform at line 541, LDMProject at line 577, LDMFolder at line 613 — get exact column names and FK constraints)
    - server/config.py (SQLITE_DATABASE_PATH at line 204, DATA_DIR at line 48 — get DB path)
    - scripts/create_admin.py (existing CLI script pattern — sys.path setup, loguru usage)
    - server/repositories/sqlite/project_repo.py (table name pattern: self._table('projects') resolves to ldm_projects in SERVER mode)
  </read_first>
  <behavior>
    - test_language_detection: detect_language_from_name("project_FRE") returns ("FRE", "French")
    - test_language_detection: detect_language_from_name("project_ENG") returns ("ENG", "English")
    - test_language_detection: detect_language_from_name("project_MULTI") returns ("MULTI", "Multi-Language")
    - test_language_detection: detect_language_from_name("some_random") returns ("UNKNOWN", "Unknown")
    - test_wipe_and_create: after wipe_and_create(), DB has exactly 1 row in ldm_platforms with name "v6.0 Showcase"
    - test_wipe_and_create: after wipe_and_create(), DB has exactly 3 rows in ldm_projects with names project_FRE, project_ENG, project_MULTI
    - test_multi_project_folders: after wipe_and_create(), ldm_folders has 2 rows for project_id=3 with names corrections_FRE and corrections_ENG
    - test_confirm_wipe_required: running without --confirm-wipe exits with error message
    - test_dry_run: running with --dry-run prints what would happen without modifying DB
  </behavior>
  <action>
    Create `scripts/setup_mock_data.py` as a standalone CLI script using sync `sqlite3` (NOT async aiosqlite):

    1. Add `sys.path.insert(0, str(Path(__file__).parent.parent))` at top for imports
    2. Import `from loguru import logger` (NEVER print())
    3. Define `LANGUAGE_SUFFIX_MAP` dict with these exact entries:
       ```
       "FRE": "French", "ENG": "English", "GER": "German",
       "ITA": "Italian", "JPN": "Japanese", "KOR": "Korean",
       "POL": "Polish", "POR-BR": "Portuguese (BR)", "RUS": "Russian",
       "SPA-ES": "Spanish (ES)", "SPA-MX": "Spanish (MX)",
       "TUR": "Turkish", "ZHO-CN": "Chinese (Simplified)",
       "ZHO-TW": "Chinese (Traditional)", "MULTI": "Multi-Language"
       ```
    4. Define `detect_language_from_name(project_name: str) -> tuple[str, str]`:
       - Split on `_` from the right (rsplit("_", 1))
       - Check if suffix is in LANGUAGE_SUFFIX_MAP
       - Return (suffix_code, display_name) or ("UNKNOWN", "Unknown")
    5. Define `DB_PATH = Path(__file__).parent.parent / "server" / "data" / "offline.db"`
    6. Define `wipe_and_create(db_path: Path, dry_run: bool = False)`:
       - Connect with `sqlite3.connect(str(db_path))`
       - Execute `PRAGMA foreign_keys=ON`
       - Delete from tables in order: ldm_rows, ldm_files, ldm_folders, ldm_projects, ldm_platforms (FK order)
       - Ensure admin user exists: `INSERT OR IGNORE INTO users (user_id, username, password_hash, role, is_active, created_at) VALUES (1, 'admin', '$2b$12$LJ3m4ys3Lk0TSwHjbMkJYOQZmSGfXMBkVfPvBNqYaKj2lHgGzVPi6', 'admin', 1, ?)`
       - Insert platform: `INSERT INTO ldm_platforms (id, name, owner_id, description, created_at, updated_at) VALUES (1, 'v6.0 Showcase', 1, 'Mock platform for offline transfer demo', ?, ?)`
       - Insert 3 projects into ldm_projects with columns: id, name, owner_id (1), platform_id (1), description, is_restricted (0), created_at, updated_at
         - (1, "project_FRE", "French localization project")
         - (2, "project_ENG", "English localization project")
         - (3, "project_MULTI", "Multi-language merge testing")
       - Insert 2 folders into ldm_folders for project_id=3: (1, 3, "corrections_FRE", NULL, now) and (2, 3, "corrections_ENG", NULL, now)
         - ldm_folders columns: id, project_id, parent_id, name, created_at
       - If dry_run, rollback; else commit
       - Log summary with logger.success()
    7. argparse main:
       - `--confirm-wipe` required flag (store_true)
       - `--dry-run` optional flag
       - `--db-path` optional override (default: DB_PATH)
       - If not --confirm-wipe, logger.error("Must pass --confirm-wipe to proceed") and sys.exit(1)

    Create `tests/test_mock_data.py`:
    - Import detect_language_from_name and wipe_and_create from scripts.setup_mock_data
    - Use a temporary SQLite DB (tmp_path fixture) for integration tests
    - Before wipe_and_create tests, create the required tables (ldm_platforms, ldm_projects, ldm_folders, users) with the exact schema from models.py
    - Test all behaviors listed above
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python -m pytest tests/test_mock_data.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LANGUAGE_SUFFIX_MAP" scripts/setup_mock_data.py` returns at least 1
    - `grep -c "detect_language_from_name" scripts/setup_mock_data.py` returns at least 1
    - `grep -c "argparse" scripts/setup_mock_data.py` returns at least 1
    - `grep -c "--confirm-wipe" scripts/setup_mock_data.py` returns at least 1
    - `grep "ldm_platforms" scripts/setup_mock_data.py` finds DELETE and INSERT statements
    - `grep "ldm_projects" scripts/setup_mock_data.py` finds DELETE and INSERT statements
    - `grep "ldm_folders" scripts/setup_mock_data.py` finds DELETE and INSERT statements
    - `grep "corrections_FRE" scripts/setup_mock_data.py` finds the folder name
    - `grep "corrections_ENG" scripts/setup_mock_data.py` finds the folder name
    - `grep "from loguru import logger" scripts/setup_mock_data.py` succeeds (no print())
    - `grep -c "def test_" tests/test_mock_data.py` returns at least 5
    - `python -m pytest tests/test_mock_data.py -x -q --tb=short` exits 0
  </acceptance_criteria>
  <done>
    - detect_language_from_name correctly maps FRE/ENG/MULTI suffixes to display names
    - wipe_and_create creates exactly 1 platform, 3 projects, 2 folders in a temp DB
    - --confirm-wipe is required, --dry-run previews without writing
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify mock script runs against real offline.db</name>
  <files>scripts/setup_mock_data.py</files>
  <read_first>
    - scripts/setup_mock_data.py (just created in Task 1)
    - server/data/offline.db (check if exists; if not, the script needs to handle table creation)
    - server/database/db_setup.py (how tables are created — may need to call setup_database() or create tables manually)
  </read_first>
  <action>
    Run the actual mock script against the real offline.db to verify it works end-to-end:

    1. First check if `server/data/offline.db` exists. If not, ensure the script creates the `server/data/` directory and initializes tables.
    2. Add a `ensure_tables(conn)` function to `setup_mock_data.py` that creates required tables if they don't exist:
       - `CREATE TABLE IF NOT EXISTS users (user_id INTEGER PRIMARY KEY, username TEXT UNIQUE, password_hash TEXT, email TEXT, full_name TEXT, department TEXT, team TEXT, language TEXT, role TEXT DEFAULT 'user', is_active INTEGER DEFAULT 1, created_at TEXT, created_by INTEGER, last_login TEXT, last_password_change TEXT, must_change_password INTEGER DEFAULT 0)`
       - `CREATE TABLE IF NOT EXISTS ldm_platforms (id INTEGER PRIMARY KEY, name TEXT NOT NULL, owner_id INTEGER NOT NULL, description TEXT, created_at TEXT, updated_at TEXT, FOREIGN KEY (owner_id) REFERENCES users(user_id))`
       - `CREATE TABLE IF NOT EXISTS ldm_projects (id INTEGER PRIMARY KEY, name TEXT NOT NULL, owner_id INTEGER NOT NULL, platform_id INTEGER, description TEXT, is_restricted INTEGER DEFAULT 0, created_at TEXT, updated_at TEXT, FOREIGN KEY (owner_id) REFERENCES users(user_id), FOREIGN KEY (platform_id) REFERENCES ldm_platforms(id))`
       - `CREATE TABLE IF NOT EXISTS ldm_folders (id INTEGER PRIMARY KEY, project_id INTEGER NOT NULL, parent_id INTEGER, name TEXT NOT NULL, created_at TEXT, FOREIGN KEY (project_id) REFERENCES ldm_projects(id), FOREIGN KEY (parent_id) REFERENCES ldm_folders(id))`
    3. Call `ensure_tables(conn)` at the start of `wipe_and_create()`, before any DELETE statements
    4. Run: `python scripts/setup_mock_data.py --confirm-wipe`
    5. Verify with sqlite3 query: `sqlite3 server/data/offline.db "SELECT id, name FROM ldm_projects ORDER BY id"` should show 3 rows
    6. Verify folders: `sqlite3 server/data/offline.db "SELECT id, project_id, name FROM ldm_folders ORDER BY id"` should show 2 rows for project_id=3
    7. Run: `python scripts/setup_mock_data.py --confirm-wipe --dry-run` — should NOT modify DB (verify project count unchanged)
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python scripts/setup_mock_data.py --confirm-wipe && sqlite3 server/data/offline.db "SELECT COUNT(*) FROM ldm_projects" | grep -q "3"</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/setup_mock_data.py --confirm-wipe` exits 0
    - `sqlite3 server/data/offline.db "SELECT name FROM ldm_projects ORDER BY id"` outputs project_FRE, project_ENG, project_MULTI (one per line)
    - `sqlite3 server/data/offline.db "SELECT name FROM ldm_folders ORDER BY id"` outputs corrections_FRE, corrections_ENG
    - `sqlite3 server/data/offline.db "SELECT name FROM ldm_platforms WHERE id=1"` outputs "v6.0 Showcase"
    - `grep "ensure_tables" scripts/setup_mock_data.py` finds the function
    - Running the script twice in a row succeeds (idempotent wipe+create)
  </acceptance_criteria>
  <done>
    - Mock script creates a working offline.db with 1 platform, 3 projects, 2 folders
    - Script is idempotent (safe to run multiple times)
    - Tables are auto-created if offline.db doesn't exist yet
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_mock_data.py -x -q --tb=short` — all tests green
- `python scripts/setup_mock_data.py --confirm-wipe` — creates mock data without errors
- `sqlite3 server/data/offline.db "SELECT COUNT(*) FROM ldm_projects"` — returns 3
- `sqlite3 server/data/offline.db "SELECT COUNT(*) FROM ldm_folders WHERE project_id=3"` — returns 2
</verification>

<success_criteria>
- CLI script exists at scripts/setup_mock_data.py with --confirm-wipe and --dry-run flags
- Language detection function correctly maps project name suffixes to display names
- 3 mock projects visible in DB after script runs
- project_MULTI has corrections_FRE and corrections_ENG subfolders
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/56-mock-data-settings/56-01-SUMMARY.md`
</output>
