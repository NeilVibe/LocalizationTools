---
phase: 15-mock-gamedata-universe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/mock_gamedata/generate_mock_universe.py
  - tests/fixtures/mock_gamedata/StaticInfo/iteminfo/iteminfo_weapon.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/iteminfo/iteminfo_armor.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/iteminfo/iteminfo_consumable.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/iteminfo/iteminfo_accessory.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/characterinfo/characterinfo_npc.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/characterinfo/characterinfo_npc_shop.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/characterinfo/characterinfo_monster.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/skillinfo/skillinfo_pc.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/skillinfo/SkillTreeInfo.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_character.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_item.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_region.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_contents.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/factioninfo/FactionInfo.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/factioninfo/NodeWaypointInfo/NodeWaypointInfo.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/gimmickinfo/Background/GimmickInfo_Background_Door.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/gimmickinfo/Item/GimmickInfo_Item_Chest.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/gimmickinfo/Puzzle/GimmickInfo_Puzzle_Seal.staticinfo.xml
  - tests/fixtures/mock_gamedata/textures/
  - tests/fixtures/mock_gamedata/audio/
  - tests/unit/test_mock_universe_structure.py
  - tests/unit/test_mock_volume.py
  - tests/unit/test_mock_media_stubs.py
  - tests/unit/test_mock_map_data.py
  - tests/integration/test_mock_crossref.py
autonomous: true
requirements:
  - MOCK-01
  - MOCK-02
  - MOCK-03
  - MOCK-06
  - MOCK-07

must_haves:
  truths:
    - "Generator script creates a full StaticInfo folder tree matching real gamedata patterns"
    - "Every KnowledgeKey, LearnKnowledgeKey, and SkillKey cross-reference resolves to a valid target"
    - "Every entity with a UITextureName has a matching DDS stub file in textures/"
    - "Region FactionNodes have spatially distributed WorldPosition coordinates"
    - "Volume targets are met: 120+ items, 35+ characters, 55+ skills, 12+ regions, 25+ gimmicks"
  artifacts:
    - path: "tests/fixtures/mock_gamedata/generate_mock_universe.py"
      provides: "Deterministic mock data generator script"
      min_lines: 300
    - path: "tests/fixtures/mock_gamedata/StaticInfo/iteminfo/iteminfo_weapon.staticinfo.xml"
      provides: "Weapon items with Key, StrKey, ItemName, ItemDesc, KnowledgeKey"
      contains: "ItemInfo"
    - path: "tests/fixtures/mock_gamedata/StaticInfo/skillinfo/SkillTreeInfo.staticinfo.xml"
      provides: "Skill trees with SkillNode children referencing SkillInfo.StrKey"
      contains: "SkillNode"
    - path: "tests/fixtures/mock_gamedata/StaticInfo/factioninfo/FactionInfo.staticinfo.xml"
      provides: "Region hierarchy with WorldPosition coordinates"
      contains: "FactionNode"
    - path: "tests/unit/test_mock_universe_structure.py"
      provides: "Structure validation tests for MOCK-01"
      contains: "def test_"
    - path: "tests/integration/test_mock_crossref.py"
      provides: "Cross-reference chain validation for MOCK-02"
      contains: "def test_"
  key_links:
    - from: "generate_mock_universe.py"
      to: "StaticInfo/**/*.xml"
      via: "lxml etree generation with deterministic seed"
      pattern: "etree\\.SubElement"
    - from: "ItemInfo.KnowledgeKey"
      to: "KnowledgeInfo.StrKey"
      via: "cross-reference lookup table in generator"
      pattern: "KnowledgeKey"
    - from: "SkillNode.SkillKey"
      to: "SkillInfo.StrKey"
      via: "StrKey (NOT numeric Key)"
      pattern: "SkillKey"
    - from: "KnowledgeInfo.UITextureName"
      to: "textures/*.dds"
      via: "filename stem match"
      pattern: "UITextureName"
---

<objective>
Build the Mock Gamedata Universe generator and all StaticInfo XML files, binary media stubs, and cross-reference integrity validation.

Purpose: Create the foundation data layer that every v3.0 phase (16-21) depends on -- realistic XML gamedata with valid cross-references, binary stubs, and spatial data.
Output: Generator script (`generate_mock_universe.py`), full StaticInfo folder tree, DDS/WEM stubs, and validation tests for structure/volume/cross-refs/media/map data.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mock-gamedata-universe/15-RESEARCH.md

@tests/fixtures/mock_gamedata/iteminfo_sample.xml
@tests/fixtures/mock_gamedata/characterinfo_sample.xml
@tests/fixtures/mock_gamedata/knowledge/knowledgeinfo_sample.xml
@tests/fixtures/mock_gamedata/regioninfo_sample.xml
@tests/integration/test_mock_gamedata_pipeline.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generator script + all StaticInfo XML + binary stubs</name>
  <files>
    tests/fixtures/mock_gamedata/generate_mock_universe.py
    tests/fixtures/mock_gamedata/StaticInfo/ (entire tree)
    tests/fixtures/mock_gamedata/textures/ (50+ DDS stubs)
    tests/fixtures/mock_gamedata/audio/ (15+ WEM stubs)
  </files>
  <behavior>
    - MOCK-01: StaticInfo/ folder has subdirectories matching real gamedata: iteminfo/, characterinfo/, skillinfo/, knowledgeinfo/, factioninfo/, factioninfo/NodeWaypointInfo/, gimmickinfo/Background/, gimmickinfo/Item/, gimmickinfo/Puzzle/
    - MOCK-02: Every ItemInfo.KnowledgeKey maps to a KnowledgeInfo.StrKey. Every CharacterInfo.KnowledgeKey maps to a KnowledgeInfo.StrKey. Every SkillInfo.LearnKnowledgeKey maps to a KnowledgeInfo.StrKey. Every SkillNode.SkillKey maps to a SkillInfo.StrKey (NOT numeric Key). Every FactionNode.KnowledgeKey maps to a KnowledgeInfo.StrKey.
    - MOCK-03: Every entity with UITextureName in KnowledgeInfo has a matching .dds file in textures/. Voice-acted characters have matching .wem files in audio/.
    - MOCK-06: FactionNode elements have WorldPosition attributes with coordinates spread across X:500-5000, Z:500-5000. NodeWaypointInfo entries connect FactionNodes with intermediate WorldPosition waypoints.
    - MOCK-07: 120+ items across 4 files, 35+ characters across 3 files, 55+ skills + 5 skill trees, 80+ knowledge entries across 4 files, 12+ region nodes in faction hierarchy, 25+ gimmicks across 3 folders.
  </behavior>
  <action>
Create `tests/fixtures/mock_gamedata/generate_mock_universe.py`:

1. **Seed and structure**: Use `random.seed(42)` for deterministic generation. Define OUTPUT_DIR = `Path(__file__).parent`. Create all subdirectories under `StaticInfo/` matching the research folder structure.

2. **Korean text corpus**: Build dictionaries of ~30 Korean name templates and ~20 Korean description templates per entity type (characters, items, skills, regions, gimmicks). Use parametric substitution to generate 300+ unique Korean strings. Descriptions that span multiple lines MUST use `<br/>` (literal in Python string, which lxml will escape to `&lt;br/&gt;` in XML attributes).

3. **Generation order** (dependency-safe):
   a. Generate Knowledge entries FIRST (80+ entries: ~35 character, ~25 item, ~12 region, ~10 skill)
   b. Generate Items (120+) referencing knowledge_map for KnowledgeKey
   c. Generate Characters (35+) referencing knowledge_map for KnowledgeKey
   d. Generate Skills (55+) referencing knowledge_map for LearnKnowledgeKey (NOT KnowledgeKey!)
   e. Generate SkillTrees (5+) referencing skills by StrKey (NOT numeric Key!)
   f. Generate Factions/Regions (12+ nodes) with WorldPosition coordinates spread across X:500-5000, Z:500-5000 with 100+ unit separation
   g. Generate NodeWaypointInfo connecting adjacent FactionNodes with 2-4 intermediate waypoints
   h. Generate Gimmicks (25+) across 3 folders with SealData.Desc children

4. **XML generation**: Use `lxml.etree` for all XML. Each entity type gets its own `write_xml()` call. Use `xml_declaration=True, encoding="utf-8", pretty_print=True`. Match EXACT attribute names from research (Key, StrKey, ItemName, ItemDesc, KnowledgeKey, LearnKnowledgeKey, SkillKey, UIPositionXY, WorldPosition, etc.).

5. **Binary stubs**: Copy existing DDS template (1152 bytes from `textures/character_varon.dds`) for all new DDS files. Copy existing WEM template (~22KB from `audio/varon_greeting.wem`) for all new WEM files. Create 50+ DDS files (one per UITextureName in KnowledgeInfo). Create 15+ WEM files (one per voice-acted character).

6. **Cross-reference registry**: Maintain a `CrossRefRegistry` dict that tracks ALL generated StrKeys, KnowledgeKeys, SkillKeys. After generation, run a self-validation pass that asserts every cross-reference resolves. Print summary counts to stdout.

7. **Existing fixtures preservation**: Do NOT modify or delete existing files (iteminfo_sample.xml, characterinfo_sample.xml, etc.). The new StaticInfo/ folder is ADDITIVE alongside existing flat files.

8. **Script must be idempotent**: Running it twice produces identical output (deterministic seed + overwrite existing generated files).
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python tests/fixtures/mock_gamedata/generate_mock_universe.py && echo "Generation OK" && python -c "
from pathlib import Path
d = Path('tests/fixtures/mock_gamedata/StaticInfo')
assert d.exists(), 'StaticInfo missing'
xml_files = list(d.rglob('*.xml'))
assert len(xml_files) >= 15, f'Only {len(xml_files)} XML files (need 15+)'
dds = list(Path('tests/fixtures/mock_gamedata/textures').glob('*.dds'))
assert len(dds) >= 50, f'Only {len(dds)} DDS files (need 50+)'
print(f'OK: {len(xml_files)} XML files, {len(dds)} DDS stubs')
"</automated>
  </verify>
  <done>Generator script runs deterministically, produces StaticInfo/ tree with 15+ XML files, 50+ DDS stubs, 15+ WEM stubs. All cross-references resolve internally. Existing fixtures untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validation test suite for structure, volume, cross-refs, media, map data</name>
  <files>
    tests/unit/test_mock_universe_structure.py
    tests/unit/test_mock_volume.py
    tests/unit/test_mock_media_stubs.py
    tests/unit/test_mock_map_data.py
    tests/integration/test_mock_crossref.py
  </files>
  <behavior>
    - test_mock_universe_structure.py: Verifies all expected directories exist under StaticInfo/, all expected XML files exist with correct naming conventions, root elements are parseable
    - test_mock_volume.py: Counts entities per type and asserts minimums (120 items, 35 chars, 55 skills, 80 knowledge, 12 regions, 25 gimmicks, 5 skill trees)
    - test_mock_media_stubs.py: Every UITextureName in KnowledgeInfo has matching DDS file, DDS files start with b"DDS " magic, WEM files start with b"RIFF" magic, file sizes match expected (DDS=1152, WEM~22KB)
    - test_mock_map_data.py: FactionNode elements have WorldPosition attribute, coordinates are in valid range (500-5000 for X/Z), NodeWaypointInfo entries reference valid FactionNode StrKeys, waypoints have 2+ intermediate positions
    - test_mock_crossref.py: All 6 cross-reference chains from research validate (Item->Knowledge, Character->Knowledge, Skill->Knowledge, SkillTree->Skill, FactionNode->Knowledge, Entity->Texture)
  </behavior>
  <action>
Create 5 test files that validate the generated mock universe. All tests use `from __future__ import annotations`. All import `pytest` and `lxml.etree`. Define `MOCK_DIR = Path(__file__).parent.parent / "fixtures" / "mock_gamedata"` and `STATIC_DIR = MOCK_DIR / "StaticInfo"`.

**tests/unit/test_mock_universe_structure.py** (MOCK-01):
- `test_staticinfo_directories_exist()`: Assert all 9 expected subdirectories exist (iteminfo, characterinfo, skillinfo, knowledgeinfo, factioninfo, factioninfo/NodeWaypointInfo, gimmickinfo/Background, gimmickinfo/Item, gimmickinfo/Puzzle)
- `test_xml_files_parseable()`: Iterate all `*.xml` files under StaticInfo/, parse each with lxml, assert no parse errors
- `test_filename_conventions()`: All files match `*.staticinfo.xml` pattern

**tests/unit/test_mock_volume.py** (MOCK-07):
- `test_item_count()`: Count `<ItemInfo>` elements across all iteminfo files >= 120
- `test_character_count()`: Count `<CharacterInfo>` elements >= 35
- `test_skill_count()`: Count `<SkillInfo>` elements >= 55
- `test_knowledge_count()`: Count `<KnowledgeInfo>` elements >= 80
- `test_region_count()`: Count `<FactionNode>` elements >= 12
- `test_gimmick_count()`: Count `<GimmickInfo>` elements >= 25
- `test_skill_tree_count()`: Count `<SkillTreeInfo>` elements >= 5

**tests/unit/test_mock_media_stubs.py** (MOCK-03):
- `test_dds_files_have_valid_headers()`: All .dds files start with `b"DDS "`
- `test_wem_files_have_valid_headers()`: All .wem files start with `b"RIFF"`
- `test_knowledge_texture_refs_have_dds()`: Every UITextureName in KnowledgeInfo resolves to a .dds file
- `test_dds_file_count()`: At least 50 DDS files

**tests/unit/test_mock_map_data.py** (MOCK-06):
- `test_faction_nodes_have_world_position()`: Every FactionNode has WorldPosition attribute
- `test_world_positions_in_valid_range()`: X and Z coordinates between 500-5000
- `test_node_separation()`: No two nodes within 50 units of each other
- `test_waypoints_reference_valid_nodes()`: FromNodeKey and ToNodeKey in NodeWaypointInfo match FactionNode StrKeys
- `test_waypoints_have_intermediate_positions()`: Each NodeWaypointInfo has 2+ WorldPosition children

**tests/integration/test_mock_crossref.py** (MOCK-02):
- Build lookup maps: knowledge_map (StrKey -> entry), skill_map (StrKey -> entry), faction_node_set, texture_set
- `test_item_knowledge_refs()`: Every ItemInfo.KnowledgeKey is in knowledge_map
- `test_character_knowledge_refs()`: Every CharacterInfo.KnowledgeKey is in knowledge_map
- `test_skill_knowledge_refs()`: Every SkillInfo.LearnKnowledgeKey is in knowledge_map
- `test_skilltree_skill_refs()`: Every SkillNode.SkillKey is in skill_map (by StrKey, NOT numeric Key)
- `test_faction_knowledge_refs()`: Every FactionNode.KnowledgeKey is in knowledge_map
- `test_texture_refs()`: Every KnowledgeInfo.UITextureName has a matching .dds file (case-insensitive)
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python -m pytest tests/unit/test_mock_universe_structure.py tests/unit/test_mock_volume.py tests/unit/test_mock_media_stubs.py tests/unit/test_mock_map_data.py tests/integration/test_mock_crossref.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>All 5 test files pass. MOCK-01, MOCK-02, MOCK-03, MOCK-06, MOCK-07 are validated by automated tests. Existing test suite (478 tests) still passes.</done>
</task>

</tasks>

<verification>
Run generator then full test suite:
```bash
cd /home/neil1988/LocalizationTools
python tests/fixtures/mock_gamedata/generate_mock_universe.py
python -m pytest tests/unit/test_mock_universe_structure.py tests/unit/test_mock_volume.py tests/unit/test_mock_media_stubs.py tests/unit/test_mock_map_data.py tests/integration/test_mock_crossref.py -x -v
```
</verification>

<success_criteria>
1. Generator script is deterministic (running twice produces identical output)
2. StaticInfo/ folder tree has all 9 expected subdirectories with 15+ XML files
3. 120+ items, 35+ characters, 55+ skills, 80+ knowledge, 12+ regions, 25+ gimmicks
4. All 6 cross-reference chains resolve (0 broken references)
5. 50+ DDS stubs with valid headers, 15+ WEM stubs with valid headers
6. WorldPosition coordinates distributed across 500-5000 range
7. Existing fixtures (iteminfo_sample.xml, etc.) untouched
8. All 5 new test files pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-mock-gamedata-universe/15-01-SUMMARY.md`
</output>
