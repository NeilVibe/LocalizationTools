---
phase: 40-cross-cutting-wow-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - locaNext/src/lib/components/common/CommandPalette.svelte
  - locaNext/src/lib/components/common/ToastContainer.svelte
  - locaNext/src/routes/+layout.svelte
  - locaNext/src/lib/stores/toastStore.js
autonomous: true
requirements:
  - WOW-16
  - WOW-17

must_haves:
  truths:
    - "Ctrl+K opens a command palette modal for global entity search"
    - "Toast notifications slide in from top-right with auto-dismiss after 3 seconds"
  artifacts:
    - path: "locaNext/src/lib/components/common/CommandPalette.svelte"
      provides: "Global Ctrl+K command palette with entity search"
      contains: "backdrop-filter"
    - path: "locaNext/src/lib/components/common/ToastContainer.svelte"
      provides: "Redesigned toast container with slide-in animation and progress bar"
      contains: "slide-in"
  key_links:
    - from: "locaNext/src/routes/+layout.svelte"
      to: "locaNext/src/lib/components/common/CommandPalette.svelte"
      via: "import and mount in app-wrapper"
      pattern: "CommandPalette"
    - from: "locaNext/src/lib/components/common/CommandPalette.svelte"
      to: "/api/ldm/gamedata/dictionary-lookup"
      via: "fetch POST with debounced search text"
      pattern: "dictionary-lookup"
    - from: "locaNext/src/lib/components/common/ToastContainer.svelte"
      to: "$lib/stores/toastStore.js"
      via: "subscribes to toasts store for reactive rendering"
      pattern: "toasts"
---

<objective>
Ctrl+K command palette for global entity search and redesigned toast notification system.

Purpose: Users can quickly find any entity across all data via Ctrl+K, and toast notifications have polished slide-in animation with auto-dismiss progress bars.
Output: CommandPalette.svelte, redesigned ToastContainer.svelte, updated +layout.svelte mounting both.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-03-17-wow-showcase-phases38-40-design.md

<interfaces>
<!-- From server/tools/ldm/schemas/gamedata.py -->
class DictionaryLookupRequest(BaseModel):
    text: str
    threshold: float = 0.80
    top_k: int = 10

class DictionaryMatch(BaseModel):
    source: str
    target: str = ""
    score: float
    tier: int
    tier_name: str
    match_type: str  # "exact", "similar", "fuzzy"

class DictionaryLookupResponse(BaseModel):
    results: List[DictionaryMatch]
    query: str
    search_time_ms: float

<!-- API endpoint: POST /api/ldm/gamedata/dictionary-lookup -->

<!-- From locaNext/src/lib/stores/toastStore.js -->
export function addToast({ message, kind, title, duration })
export function removeToast(id)
export const toasts  // writable store of toast objects
export const toast   // convenience: toast.success(), toast.error(), toast.info(), toast.warning()

<!-- From locaNext/src/lib/stores/navigation.js -->
export const currentPage = writable('files');
export function goToCodex(searchQuery = '')
export function goToGameDev()

<!-- From locaNext/src/lib/api/client.js -->
// api.post(url, body) for authenticated requests

<!-- Existing GlobalToast.svelte is already in +layout.svelte -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CommandPalette with glassmorphism modal and entity search</name>
  <files>locaNext/src/lib/components/common/CommandPalette.svelte, locaNext/src/routes/+layout.svelte</files>
  <read_first>
    locaNext/src/routes/+layout.svelte
    locaNext/src/lib/stores/navigation.js
    locaNext/src/lib/api/client.js
    locaNext/src/lib/stores/toastStore.js
    locaNext/src/lib/components/GlobalToast.svelte
  </read_first>
  <action>
    Create CommandPalette.svelte as a Svelte 5 Runes component:

    **State management (Svelte 5 Runes):**
    ```
    let isOpen = $state(false);
    let query = $state('');
    let results = $state([]);
    let selectedIndex = $state(0);
    let isSearching = $state(false);
    ```

    **Keyboard handling:**
    - `$effect()` to add/remove document keydown listener
    - Ctrl+K (or Cmd+K on Mac) toggles open/close
    - When open: ArrowUp/ArrowDown navigates results, Enter selects, Escape closes
    - `event.preventDefault()` on Ctrl+K to prevent browser default

    **Search implementation:**
    - On query change, debounce 150ms, then POST to `/api/ldm/gamedata/dictionary-lookup` with `{ text: query, top_k: 8, threshold: 0.6 }`
    - Use `api.post()` from `$lib/api/client.js` for authenticated requests
    - Display results as: entity source name + match_type badge + target translation (if available)
    - Empty state: show "Type to search entities..." when query is empty
    - No results state: show "No matches found" with search icon

    **Result selection:**
    - On Enter or click: navigate to Codex page with search query pre-populated via `goToCodex(result.source)`
    - Close palette after selection
    - Reset query and results on close

    **Visual design (glassmorphism):**
    - Backdrop overlay: `background: rgba(0, 0, 0, 0.6); backdrop-filter: blur(4px);` covering full viewport, z-index: 9500
    - Modal container: `background: rgba(26, 26, 26, 0.95); backdrop-filter: blur(20px); border: 1px solid rgba(212, 154, 92, 0.2); border-radius: 12px; max-width: 560px; width: 90vw; margin: 15vh auto 0;`
    - Search input: full width, 48px height, `background: var(--cds-field-01); border: 1px solid var(--cds-border-subtle-01); border-radius: 8px; padding: 0 1rem 0 2.5rem;` with Search icon (16px) positioned absolute left
    - Font: `font-size: 1rem; color: var(--cds-text-01);`
    - Results list: max-height 320px, overflow-y auto, 4px gap between items
    - Result item: `padding: 0.625rem 1rem; border-radius: 6px; cursor: pointer;`
    - Selected/hover result: `background: rgba(212, 154, 92, 0.12);`
    - Match type badge: `font-size: 0.6875rem; padding: 2px 8px; border-radius: 10px; background: var(--cds-layer-02); color: var(--cds-text-03);`
    - Source text: `font-weight: 500; color: var(--cds-text-01);`
    - Target text: `font-size: 0.8125rem; color: var(--cds-text-03);`
    - Keyboard hint at bottom: `font-size: 0.75rem; color: var(--cds-text-04); padding: 0.5rem 1rem; border-top: 1px solid var(--cds-border-subtle-01);` showing "arrow-keys navigate / enter select / esc close"

    **Animation:**
    - Modal entrance: CSS transition `opacity 0->1 (150ms ease-out)` + `transform: translateY(-8px) -> translateY(0) (150ms ease-out)`
    - Use Svelte `transition:fade={{ duration: 150 }}` on backdrop
    - Use Svelte `transition:fly={{ y: -8, duration: 150 }}` on modal container

    **Accessibility:**
    - `role="dialog"` on modal, `aria-modal="true"`
    - `role="listbox"` on results, `role="option"` on each result, `aria-selected` on active
    - Auto-focus search input on open
    - `aria-label="Command palette"` on dialog

    **Mount in +layout.svelte:**
    - Import CommandPalette from `$lib/components/common/CommandPalette.svelte`
    - Add `<CommandPalette />` inside the `.app-wrapper` div, AFTER GlobalToast (just before closing `</div>`)
    - CommandPalette handles its own visibility via isOpen state, so no props needed
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools/locaNext && grep -q "CommandPalette" src/routes/+layout.svelte && grep -q "backdrop-filter" src/lib/components/common/CommandPalette.svelte && grep -q "Ctrl+K\|ctrlKey.*[kK]\|metaKey.*[kK]" src/lib/components/common/CommandPalette.svelte && grep -q "dictionary-lookup" src/lib/components/common/CommandPalette.svelte && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "CommandPalette" locaNext/src/routes/+layout.svelte returns match
    - grep "backdrop-filter" locaNext/src/lib/components/common/CommandPalette.svelte returns match
    - grep "ctrlKey\|metaKey" locaNext/src/lib/components/common/CommandPalette.svelte returns match (keyboard shortcut)
    - grep "dictionary-lookup" locaNext/src/lib/components/common/CommandPalette.svelte returns match (API integration)
    - grep "role=\"dialog\"" locaNext/src/lib/components/common/CommandPalette.svelte returns match (accessibility)
    - grep "goToCodex" locaNext/src/lib/components/common/CommandPalette.svelte returns match (navigation on select)
  </acceptance_criteria>
  <done>Ctrl+K opens glassmorphism command palette, searches entities via dictionary-lookup API with 150ms debounce, arrow keys navigate results, Enter navigates to Codex, Escape closes</done>
</task>

<task type="auto">
  <name>Task 2: Redesign toast system with slide-in animation and progress bar</name>
  <files>locaNext/src/lib/components/common/ToastContainer.svelte, locaNext/src/lib/stores/toastStore.js, locaNext/src/routes/+layout.svelte</files>
  <read_first>
    locaNext/src/lib/components/GlobalToast.svelte
    locaNext/src/lib/stores/toastStore.js
    locaNext/src/routes/+layout.svelte
  </read_first>
  <action>
    Create a new ToastContainer.svelte that replaces the existing GlobalToast with a polished slide-in design matching the WOW spec.

    **Update toastStore.js:**
    - Change DEFAULT_DURATION from 4000 to 3000 (spec says 3 seconds)
    - Add a `duration` field to each toast object (stored alongside id, message, kind, title, timestamp)
    - Keep all existing exports and convenience functions intact

    **Create ToastContainer.svelte (Svelte 5 Runes):**

    Container positioning:
    - `position: fixed; top: 60px; right: 16px; z-index: 9200;` (below header, above content)
    - `display: flex; flex-direction: column; gap: 8px;`
    - `pointer-events: none;` on container, `pointer-events: auto;` on each toast
    - `max-width: 360px; width: 100%;`

    Each toast item design:
    - `background: var(--cds-layer-01); border: 1px solid var(--cds-border-subtle-01); border-radius: 8px; overflow: hidden;`
    - Left accent border: `border-left: 4px solid {color};` where color depends on kind:
      - success: `#24a148`
      - error: `#da1e28`
      - warning: `#f1c21b`
      - info: `#d49a5c` (warm copper per spec)
    - Content area: `padding: 0.75rem 1rem; display: flex; align-items: flex-start; gap: 0.5rem;`
    - Icon (left): use Carbon icons (CheckmarkFilled, ErrorFilled, WarningFilled, InformationFilled) at 20px, colored to match accent
    - Text area: title in `font-weight: 600; font-size: 0.8125rem; color: var(--cds-text-01);` and message in `font-size: 0.75rem; color: var(--cds-text-02); margin-top: 2px;`
    - Close button (X): `position: absolute; top: 8px; right: 8px;` using Carbon Close icon at 16px, `color: var(--cds-text-03);`

    Progress bar (auto-dismiss indicator):
    - `position: absolute; bottom: 0; left: 0; height: 2px; background: {accent_color};`
    - CSS animation: `width: 100% -> 0%` over the toast's duration (3000ms default)
    - `animation: toast-progress {duration}ms linear forwards;`
    - `@keyframes toast-progress { from { width: 100%; } to { width: 0%; } }`

    Entrance/exit animation:
    - Use Svelte `transition:fly={{ x: 360, duration: 200 }}` for slide-in from right
    - Easing: `cubicOut` from `svelte/easing` (closest to ease-out-quart)

    **Update +layout.svelte:**
    - Replace `<GlobalToast />` import with `<ToastContainer />` from `$lib/components/common/ToastContainer.svelte`
    - Remove the GlobalToast import line
    - Keep the existing GlobalToast.svelte file (do not delete -- other code may import its websocket listeners)
    - Actually: read GlobalToast.svelte carefully. If it has WebSocket subscription logic, keep it mounted but hidden (no visual output). Or better: move the WS subscription into ToastContainer.svelte.
    - Decision: Mount ToastContainer.svelte in place of GlobalToast. Copy the WebSocket subscription logic (onMount with websocket.on for operation_start, operation_complete, operation_failed) from GlobalToast.svelte into ToastContainer.svelte so the WS-driven toasts still work. Then remove the GlobalToast import from +layout.svelte.

    **prefers-reduced-motion:**
    - Disable slide animation (instant appear/disappear)
    - Keep progress bar (informational, not decorative)
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools/locaNext && grep -q "ToastContainer" src/routes/+layout.svelte && grep -q "toast-progress" src/lib/components/common/ToastContainer.svelte && grep -q "transition:fly" src/lib/components/common/ToastContainer.svelte && grep -q "3000\|DEFAULT_DURATION = 3000" src/lib/stores/toastStore.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "ToastContainer" locaNext/src/routes/+layout.svelte returns match
    - grep "GlobalToast" locaNext/src/routes/+layout.svelte returns NO match (replaced)
    - grep "toast-progress" locaNext/src/lib/components/common/ToastContainer.svelte returns match (progress bar keyframes)
    - grep "transition:fly" locaNext/src/lib/components/common/ToastContainer.svelte returns match (slide-in animation)
    - grep "border-left.*4px" locaNext/src/lib/components/common/ToastContainer.svelte returns match (accent border)
    - grep "3000" locaNext/src/lib/stores/toastStore.js returns match (updated default duration)
    - grep "websocket" locaNext/src/lib/components/common/ToastContainer.svelte returns match (WS subscriptions preserved)
  </acceptance_criteria>
  <done>Toast notifications slide in from right with warm accent border, progress bar counts down to auto-dismiss at 3s, close button for manual dismiss, WebSocket operation toasts preserved</done>
</task>

</tasks>

<verification>
1. CommandPalette.svelte exists with glassmorphism styling, Ctrl+K shortcut, dictionary-lookup search
2. ToastContainer.svelte exists with slide-in animation, progress bar, warm accent borders
3. +layout.svelte imports both CommandPalette and ToastContainer (GlobalToast replaced)
4. toastStore.js default duration updated to 3000ms
5. Keyboard navigation works in command palette (arrow keys, enter, escape)
6. WebSocket operation notifications still work through ToastContainer
</verification>

<success_criteria>
- Ctrl+K opens a glassmorphism command palette modal
- Typing in the palette searches entities via dictionary-lookup API with 150ms debounce
- Arrow keys navigate results, Enter navigates to Codex, Escape closes
- Toast notifications slide in from top-right with 4px warm accent left border
- Progress bar shrinks over 3 seconds before auto-dismiss
- Close button (X) allows manual dismiss
- All existing WebSocket-driven operation toasts continue working
</success_criteria>

<output>
After completion, create `.planning/phases/40-cross-cutting-wow-polish/40-02-SUMMARY.md`
</output>
