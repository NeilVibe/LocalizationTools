---
phase: 40-cross-cutting-wow-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - locaNext/src/lib/components/common/PageTransition.svelte
  - locaNext/src/routes/+layout.svelte
  - locaNext/src/lib/components/pages/GameDevPage.svelte
  - locaNext/src/lib/components/pages/CodexPage.svelte
  - locaNext/src/lib/components/pages/WorldMapPage.svelte
  - locaNext/src/lib/components/pages/GridPage.svelte
  - locaNext/src/lib/components/pages/TMPage.svelte
  - locaNext/src/lib/components/pages/FilesPage.svelte
autonomous: true
requirements:
  - WOW-14
  - WOW-15

must_haves:
  truths:
    - "Navigating between pages has a visible crossfade transition (not instant swap)"
    - "All loading states use shimmer skeletons matching content dimensions (no spinners visible)"
  artifacts:
    - path: "locaNext/src/lib/components/common/PageTransition.svelte"
      provides: "Crossfade page transition wrapper component"
      contains: "transition:fade"
    - path: "locaNext/src/routes/+layout.svelte"
      provides: "PageTransition wrapper around page content"
      contains: "PageTransition"
  key_links:
    - from: "locaNext/src/routes/+layout.svelte"
      to: "locaNext/src/lib/components/common/PageTransition.svelte"
      via: "import and wrapping {@render children()}"
      pattern: "PageTransition"
    - from: "locaNext/src/lib/components/common/PageTransition.svelte"
      to: "$lib/stores/navigation.js"
      via: "currentPage subscription for transition key"
      pattern: "currentPage"
---

<objective>
Page crossfade transitions and shimmer loading choreography across all pages.

Purpose: Navigation between pages feels cinematic (crossfade, not instant swap) and all loading states use shimmer skeletons matching content dimensions (zero spinners).
Output: PageTransition.svelte wrapper, updated +layout.svelte, replaced InlineLoading spinners in page-level components with shimmer skeletons.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-03-17-wow-showcase-phases38-40-design.md

<interfaces>
<!-- From locaNext/src/lib/stores/navigation.js -->
export const currentPage = writable('files');
// Values: 'files', 'tm', 'grid', 'tm-entries', 'gamedev', 'codex', 'worldmap'

<!-- From locaNext/src/routes/+layout.svelte -->
// Content area renders children inside <Content>{@render children()}</Content>
// GlobalToast already mounted at bottom of app-wrapper

<!-- From locaNext/src/lib/components/common/SkeletonCard.svelte -->
// Existing shimmer pattern: @keyframes skeleton-shimmer with opacity 0.4->0.8
// Uses --skeleton-bg CSS variable and --card-gap, --card-padding, --card-radius tokens
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PageTransition wrapper and integrate crossfade in layout</name>
  <files>locaNext/src/lib/components/common/PageTransition.svelte, locaNext/src/routes/+layout.svelte</files>
  <read_first>
    locaNext/src/routes/+layout.svelte
    locaNext/src/lib/stores/navigation.js
    locaNext/src/lib/components/common/SkeletonCard.svelte
    locaNext/src/app.css
  </read_first>
  <action>
    Create PageTransition.svelte as a Svelte 5 component that wraps page content with a crossfade effect keyed on the current page.

    PageTransition.svelte implementation:
    - Import `currentPage` from `$lib/stores/navigation.js`
    - Use `{#key $currentPage}` block to trigger transition on page change
    - Inside the key block, use `transition:fade={{ duration: 150 }}` from `svelte/transition`
    - The component accepts a `children` snippet via `$props()` and renders it: `{@render children()}`
    - Add `prefers-reduced-motion` media query to disable transition (set duration to 0)
    - Wrapper div gets `position: relative; width: 100%; height: 100%;`

    In +layout.svelte:
    - Import PageTransition from `$lib/components/common/PageTransition.svelte`
    - Wrap the `{@render children()}` call inside `<Content>` with `<PageTransition>{@render children()}</PageTransition>`
    - Do NOT touch any other part of the layout (auth, header, modals, etc.)

    CSS for PageTransition:
    ```css
    .page-transition-wrapper {
      width: 100%;
      height: 100%;
    }
    @media (prefers-reduced-motion: reduce) {
      .page-transition-wrapper { animation-duration: 0ms !important; }
    }
    ```

    Use Svelte `fade` transition (not CSS animation) for proper enter/exit choreography. Duration: 150ms. This gives a 300ms total crossfade (150ms out + 150ms in).
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools/locaNext && grep -q "PageTransition" src/routes/+layout.svelte && grep -q "transition:fade" src/lib/components/common/PageTransition.svelte && grep -q "currentPage" src/lib/components/common/PageTransition.svelte && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep "PageTransition" locaNext/src/routes/+layout.svelte returns match
    - grep "transition:fade" locaNext/src/lib/components/common/PageTransition.svelte returns match
    - grep "prefers-reduced-motion" locaNext/src/lib/components/common/PageTransition.svelte returns match
    - grep "{#key" locaNext/src/lib/components/common/PageTransition.svelte returns match
  </acceptance_criteria>
  <done>PageTransition component exists with fade crossfade keyed on currentPage, layout wraps children with it, reduced-motion respected</done>
</task>

<task type="auto">
  <name>Task 2: Replace remaining page-level spinners with shimmer skeletons</name>
  <files>locaNext/src/lib/components/pages/GameDevPage.svelte, locaNext/src/lib/components/pages/WorldMapPage.svelte, locaNext/src/lib/components/pages/TMPage.svelte, locaNext/src/lib/components/pages/FilesPage.svelte</files>
  <read_first>
    locaNext/src/lib/components/pages/GameDevPage.svelte
    locaNext/src/lib/components/pages/WorldMapPage.svelte
    locaNext/src/lib/components/pages/TMPage.svelte
    locaNext/src/lib/components/pages/FilesPage.svelte
    locaNext/src/lib/components/common/SkeletonCard.svelte
  </read_first>
  <action>
    Audit each page component for remaining spinner/InlineLoading usage in PAGE-LEVEL loading states (not inline field spinners -- those are fine). Replace page-level loading indicators with shimmer skeletons matching content dimensions.

    For each page, add a shimmer skeleton that matches the page's content layout. Use the existing `@keyframes skeleton-shimmer` pattern (opacity 0.4 to 0.8, 1.5s ease-in-out infinite) from SkeletonCard.svelte.

    Shimmer patterns per page:
    - **GameDevPage**: 8 shimmer tree rows (height: 28px, widths: 60%, 45%, 70%, 55%, 40%, 65%, 50%, 35%) stacked vertically with 4px gap
    - **WorldMapPage**: Single shimmer rectangle (100% width, 400px height, border-radius: 8px) representing map area
    - **TMPage**: 3 shimmer match cards (height: 80px each, 8px gap, border-radius: 6px) representing TM suggestions
    - **FilesPage**: 6 shimmer rows (height: 36px, alternating widths 80%/60%) representing file list rows

    Implementation approach:
    - Create inline `<style>` shimmer blocks in each page (NOT new components -- these are page-specific layouts)
    - Use CSS classes `.shimmer-skeleton` with the shared keyframes pattern
    - Background: `var(--skeleton-bg, var(--cds-layer-02))`
    - Only replace the OUTERMOST loading state (the one shown when the page first loads). Keep inline InlineLoading for specific actions (search loading, TM lookup, etc.)
    - If a page already has skeleton loading (from v3.3 Phase 34 polish), leave it as-is

    Do NOT modify: VirtualGrid.svelte (sacred), CodexPage (already has SkeletonCard from Phase 33), GridPage (already has skeleton rows from Phase 34).
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools/locaNext && grep -l "shimmer-skeleton\|skeleton-shimmer" src/lib/components/pages/GameDevPage.svelte src/lib/components/pages/WorldMapPage.svelte src/lib/components/pages/TMPage.svelte src/lib/components/pages/FilesPage.svelte 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep "shimmer" in GameDevPage.svelte, WorldMapPage.svelte, TMPage.svelte, FilesPage.svelte returns matches (at least 2 pages modified)
    - grep "skeleton-shimmer" finds the keyframes animation in modified page files
    - No changes to VirtualGrid.svelte, CodexPage.svelte, or GridPage.svelte
  </acceptance_criteria>
  <done>All page-level loading states use shimmer skeletons matching content dimensions; no page shows a spinner on initial load</done>
</task>

</tasks>

<verification>
1. PageTransition.svelte exists and uses `transition:fade` with `{#key $currentPage}`
2. +layout.svelte imports and wraps children with PageTransition
3. At least 2 page components have shimmer skeleton loading states replacing spinners
4. No changes to VirtualGrid.svelte (sacred file)
5. prefers-reduced-motion is respected in PageTransition
</verification>

<success_criteria>
- Navigating between tabs triggers a visible crossfade (150ms fade out + 150ms fade in)
- All page-level loading states show shimmer skeletons matching content dimensions
- No spinners visible on initial page load for any of the 5 main pages
- Reduced motion preference disables transitions
</success_criteria>

<output>
After completion, create `.planning/phases/40-cross-cutting-wow-polish/40-01-SUMMARY.md`
</output>
