---
phase: 74-mock-data-foundation
plan: 02
type: execute
wave: 2
depends_on: ["74-01"]
files_modified:
  - tests/fixtures/mock_gamedata/loc/languagedata_JPN.xml
  - tests/unit/test_mock_media_stubs.py
autonomous: true
requirements: [MOCK-12, MOCK-09, MOCK-10, MOCK-11]

must_haves:
  truths:
    - "Japanese language data XML exists with translations for all existing StringIds"
    - "Tests verify DDS files are valid images at correct Perforce-mapped paths"
    - "Tests verify WEM files are valid audio at correct paths for all 3 languages"
    - "All 11 PerforcePathService template directories exist with expected content"
  artifacts:
    - path: "tests/fixtures/mock_gamedata/loc/languagedata_JPN.xml"
      provides: "Japanese language data with br-tag encoding"
      contains: "LanguageData"
    - path: "tests/unit/test_mock_media_stubs.py"
      provides: "Corrected tests at real Perforce-mapped paths"
      contains: "texture/image"
  key_links:
    - from: "tests/unit/test_mock_media_stubs.py"
      to: "tests/fixtures/mock_gamedata/texture/image/*.dds"
      via: "DDS_DIR = MOCK_DIR / 'texture' / 'image'"
      pattern: 'texture.*image'
    - from: "tests/unit/test_mock_media_stubs.py"
      to: "tests/fixtures/mock_gamedata/sound/windows/*/*.wem"
      via: "Tests iterate all 3 language audio folders"
      pattern: 'sound.*windows'
    - from: "tests/fixtures/mock_gamedata/loc/languagedata_JPN.xml"
      to: "tests/fixtures/mock_gamedata/loc/languagedata_KOR.xml"
      via: "Same StringIds, different language content"
      pattern: "StringId"
---

<objective>
Add Japanese language data and fix verification tests to use correct Perforce-mapped paths.

Purpose: MOCK-12 requires mock .loc.xml files with EN/KR/JP minimum. Currently only EN and KR exist. The existing test file `test_mock_media_stubs.py` references wrong paths (`audio` instead of `sound/windows/English(US)`, `textures` instead of `texture/image`). This plan adds JP data and creates a comprehensive verification test that validates all MOCK-09 through MOCK-12 requirements against real PerforcePathService paths.

Output: languagedata_JPN.xml with Japanese translations. Updated tests that validate the entire mock data foundation at correct paths.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-mock-data-foundation/74-RESEARCH.md
@.planning/phases/74-mock-data-foundation/74-01-SUMMARY.md
@tests/fixtures/mock_gamedata/loc/languagedata_KOR.xml
@tests/fixtures/mock_gamedata/loc/languagedata_ENG.xml
@tests/unit/test_mock_media_stubs.py

<interfaces>
From server/tools/ldm/services/perforce_path_service.py:
```python
# configure_for_mock_gamedata() maps these keys to these mock subdirectories:
# "texture_folder" -> mock_gamedata / "texture" / "image"
# "audio_folder" -> mock_gamedata / "sound" / "windows" / "English(US)"
# "audio_folder_kr" -> mock_gamedata / "sound" / "windows" / "Korean"
# "audio_folder_zh" -> mock_gamedata / "sound" / "windows" / "Chinese(PRC)"
# "loc_folder" -> mock_gamedata / "loc"
# "knowledge_folder" -> mock_gamedata / "StaticInfo" / "knowledgeinfo"
# etc. (11 total)
```

From tests/unit/test_mock_media_stubs.py (CURRENT - HAS WRONG PATHS):
```python
TEXTURES_DIR = MOCK_DIR / "textures"       # WRONG - should be "texture" / "image"
AUDIO_DIR = MOCK_DIR / "audio"             # WRONG - should be "sound" / "windows" / "English(US)"
```

XML br-tag encoding rule (from CLAUDE.md):
```xml
KR="첫 번째 줄&lt;br/&gt;두 번째 줄"    <!-- correct in XML attributes -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Japanese language data XML</name>
  <files>tests/fixtures/mock_gamedata/loc/languagedata_JPN.xml</files>
  <read_first>
    - tests/fixtures/mock_gamedata/loc/languagedata_KOR.xml (get all StringIds and structure)
    - tests/fixtures/mock_gamedata/loc/languagedata_ENG.xml (get English translations for reference)
    - .planning/phases/74-mock-data-foundation/74-RESEARCH.md (JP content examples at line 188-194)
  </read_first>
  <action>
Create `tests/fixtures/mock_gamedata/loc/languagedata_JPN.xml` containing Japanese translations for EVERY StringId present in languagedata_KOR.xml. The file must:

1. Use identical XML structure: `<?xml version='1.0' encoding='UTF-8'?>` root `<LanguageData>` with `<LocStr>` children
2. Each `<LocStr>` has attributes: `StringId`, `StrOrigin` (Japanese text), `Str` (same as StrOrigin)
3. Use `&lt;br/&gt;` for line breaks in attribute values (NOT literal `<br/>`)
4. Include Japanese translations that are contextually appropriate for a fantasy RPG game universe

Key translations (from research, line 192):
- Character_ElderVaron name: "長老バロン"
- Character_ShadowKira name: "影の暗殺者キラ"
- Character_Grimjaw name: "鍛冶師グリムジョー"
- Character_Lune name: "斥候ルーネ"
- Character_Drakmar name: "魔法使いドラクマール"

For descriptions, translate the Korean descriptions to natural Japanese. Use `&lt;br/&gt;` wherever Korean uses `&lt;br/&gt;`.

For items, skills, knowledge entries — provide Japanese translations matching the StringId pattern. Every StringId in the KOR file MUST have a corresponding entry in the JPN file.
  </action>
  <verify>
    <automated>python3 -c "
from lxml import etree
from pathlib import Path

jpn = Path('tests/fixtures/mock_gamedata/loc/languagedata_JPN.xml')
kor = Path('tests/fixtures/mock_gamedata/loc/languagedata_KOR.xml')
assert jpn.exists(), 'JPN file missing'

jpn_tree = etree.parse(str(jpn))
kor_tree = etree.parse(str(kor))

jpn_ids = {e.get('StringId') for e in jpn_tree.getroot().findall('.//LocStr')}
kor_ids = {e.get('StringId') for e in kor_tree.getroot().findall('.//LocStr')}

missing = kor_ids - jpn_ids
assert not missing, f'Missing StringIds in JPN: {missing}'

# Check br-tag encoding
content = jpn.read_text(encoding='utf-8')
assert '<br/>' not in content or '&lt;br/&gt;' in content, 'Raw <br/> found — must use &lt;br/&gt;'
print(f'PASS: JPN has {len(jpn_ids)} StringIds matching KOR ({len(kor_ids)})')
"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/fixtures/mock_gamedata/loc/languagedata_JPN.xml` exists
    - Root element is `<LanguageData>`
    - Every StringId from languagedata_KOR.xml has a corresponding `<LocStr>` in JPN file
    - StrOrigin attributes contain Japanese text (not English or Korean)
    - Line breaks encoded as `&lt;br/&gt;` (verified by grep for literal `<br/>` returns 0 hits outside XML escaping)
    - File is valid XML parseable by lxml
  </acceptance_criteria>
  <done>languagedata_JPN.xml exists with Japanese translations for all StringIds, properly XML-encoded br-tags</done>
</task>

<task type="auto">
  <name>Task 2: Fix and expand test_mock_media_stubs.py to verify all MOCK requirements at correct paths</name>
  <files>tests/unit/test_mock_media_stubs.py</files>
  <read_first>
    - tests/unit/test_mock_media_stubs.py (current test with wrong paths)
    - server/tools/ldm/services/perforce_path_service.py (correct path mappings at line 146-158)
    - tests/fixtures/mock_gamedata/loc/ (ls to see all language data files including new JPN)
  </read_first>
  <action>
Rewrite `tests/unit/test_mock_media_stubs.py` to:

1. Fix path constants to match PerforcePathService.configure_for_mock_gamedata() mappings:
   - `DDS_DIR = MOCK_DIR / "texture" / "image"` (was `MOCK_DIR / "textures"`)
   - `AUDIO_DIR_EN = MOCK_DIR / "sound" / "windows" / "English(US)"` (was `MOCK_DIR / "audio"`)
   - `AUDIO_DIR_KR = MOCK_DIR / "sound" / "windows" / "Korean"`
   - `AUDIO_DIR_ZH = MOCK_DIR / "sound" / "windows" / "Chinese(PRC)"`
   - `LOC_DIR = MOCK_DIR / "loc"`

2. Keep `TEXTURES_DIR = MOCK_DIR / "textures"` as a SEPARATE constant for the PNG fallback directory (it still exists and is used by the thumbnail endpoint fallback).

3. Update TestDdsFiles:
   - `test_dds_file_count`: Check DDS_DIR (not TEXTURES_DIR), assert >= 20
   - `test_dds_files_have_valid_headers`: Check DDS_DIR files
   - NEW `test_dds_files_openable_by_pillow`: Use `Image.open()` on each DDS, assert 64x64 RGBA

4. Update TestWemFiles:
   - `test_wem_file_count`: Check AUDIO_DIR_EN, assert >= 10
   - `test_wem_files_have_valid_headers`: Check for RIFF header (WAV content)
   - NEW `test_korean_audio_folder_exists`: Assert AUDIO_DIR_KR exists with >= 10 .wem files
   - NEW `test_chinese_audio_folder_exists`: Assert AUDIO_DIR_ZH exists with >= 10 .wem files
   - NEW `test_all_languages_have_same_filenames`: Assert set of .wem stems matches across all 3 folders

5. Update TestTextureReferences:
   - Keep existing logic but check both DDS_DIR and TEXTURES_DIR for matches (DDS at Perforce path, PNG at fallback path)

6. NEW TestLanguageData class:
   - `test_eng_kor_jpn_exist`: Assert all 3 languagedata XML files exist in LOC_DIR
   - `test_jpn_has_all_stringids`: Parse JPN and KOR, assert same StringId sets
   - `test_br_tags_properly_encoded`: Read each XML, assert no raw `<br/>` in attribute values

7. NEW TestPerforcePathResolution class:
   - `test_all_11_template_dirs_exist`: Call `configure_for_mock_gamedata(MOCK_DIR)`, then iterate `get_all_resolved()` and assert each Path exists as a directory
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python -m pytest tests/unit/test_mock_media_stubs.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `DDS_DIR` constant equals `MOCK_DIR / "texture" / "image"` (grep verifiable)
    - `AUDIO_DIR_EN` constant equals `MOCK_DIR / "sound" / "windows" / "English(US)"` (grep verifiable)
    - `AUDIO_DIR_KR` constant equals `MOCK_DIR / "sound" / "windows" / "Korean"` (grep verifiable)
    - `AUDIO_DIR_ZH` constant equals `MOCK_DIR / "sound" / "windows" / "Chinese(PRC)"` (grep verifiable)
    - Class `TestLanguageData` exists with methods `test_eng_kor_jpn_exist` and `test_jpn_has_all_stringids`
    - Class `TestPerforcePathResolution` exists with method `test_all_11_template_dirs_exist`
    - All tests pass (`pytest tests/unit/test_mock_media_stubs.py` exits 0)
  </acceptance_criteria>
  <done>All mock media and language data tests pass at correct Perforce-mapped paths, covering MOCK-09 through MOCK-12</done>
</task>

</tasks>

<verification>
1. All tests pass: `python -m pytest tests/unit/test_mock_media_stubs.py -v`
2. JPN file has same StringIds as KOR: `python3 -c "from lxml import etree; j=set(e.get('StringId') for e in etree.parse('tests/fixtures/mock_gamedata/loc/languagedata_JPN.xml').getroot()); k=set(e.get('StringId') for e in etree.parse('tests/fixtures/mock_gamedata/loc/languagedata_KOR.xml').getroot()); print('Match' if j==k else f'Missing: {k-j}')"`
3. All 11 PerforcePathService paths resolve: test_all_11_template_dirs_exist passes
</verification>

<success_criteria>
- languagedata_JPN.xml exists with JP translations for all StringIds (EN/KR/JP minimum met)
- test_mock_media_stubs.py uses correct Perforce-mapped paths (not old wrong paths)
- All tests pass covering DDS validity, WEM validity, 3-language audio, JP language data, and 11-template path resolution
- No test references the old `MOCK_DIR / "audio"` or `MOCK_DIR / "textures"` for DDS verification
</success_criteria>

<output>
After completion, create `.planning/phases/74-mock-data-foundation/74-02-SUMMARY.md`
</output>
