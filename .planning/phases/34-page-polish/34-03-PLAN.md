---
phase: 34-page-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - locaNext/src/lib/components/pages/WorldMapPage.svelte
  - locaNext/src/lib/components/ldm/MapCanvas.svelte
  - locaNext/src/lib/components/ldm/MapTooltip.svelte
  - locaNext/src/lib/components/ldm/MapDetailPanel.svelte
autonomous: true
requirements: [WMP-01, WMP-02]

must_haves:
  truths:
    - "World Map page uses shared EmptyState when no map data is loaded"
    - "World Map uses shared ErrorState with retry for API failures"
    - "Map header styling is consistent with other page headers"
    - "Node styling, route lines, and tooltips look correct in dark mode"
    - "MapDetailPanel has polished spacing and dark mode safe colors"
  artifacts:
    - path: "locaNext/src/lib/components/pages/WorldMapPage.svelte"
      provides: "Polished WorldMap page with EmptyState, ErrorState, consistent header"
    - path: "locaNext/src/lib/components/ldm/MapCanvas.svelte"
      provides: "Dark mode safe canvas colors for nodes and routes"
    - path: "locaNext/src/lib/components/ldm/MapTooltip.svelte"
      provides: "Dark mode safe tooltip styling"
    - path: "locaNext/src/lib/components/ldm/MapDetailPanel.svelte"
      provides: "Polished detail panel with consistent spacing"
  key_links:
    - from: "WorldMapPage.svelte"
      to: "$lib/components/common"
      via: "import { EmptyState, ErrorState }"
      pattern: "EmptyState|ErrorState"
---

<objective>
Polish the World Map page (WorldMapPage + MapCanvas + MapTooltip + MapDetailPanel) for dark mode, empty/error states, and visual consistency.

Purpose: World Map is the smallest page (227 + 298 + 144 + 307 = 976 lines). Quick pass to replace inline error/loading with shared components, fix dark mode, and ensure tooltip/detail panel consistency.

Output: 4 polished component files with consistent v3.3 design language.
</objective>

<execution_context>
@/home/<USERNAME>/.claude/get-shit-done/workflows/execute-plan.md
@/home/<USERNAME>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-design-token-foundation/32-01-SUMMARY.md
</context>

<interfaces>
<!-- Shared components from Phase 32 -->
From locaNext/src/lib/components/common/index.js:
```javascript
export { default as EmptyState } from './EmptyState.svelte';
export { default as ErrorState } from './ErrorState.svelte';
```

EmptyState props: `{ icon: Component, headline: string, description?: string, ctaLabel?: string, onaction?: () => void }`
ErrorState props: `{ message?: string, onretry?: () => void }`

<!-- WorldMapPage current state (from code review): -->
- Header (lines 104-112): Custom .worldmap-header with hardcoded padding `12px 16px` and gap `8px`
- Error state (lines 115-118): Plain div with just text, no retry button, no icon
- Loading (lines 119-122): InlineLoading -- acceptable but could be better
- Empty state: MISSING -- no state for when nodes=[] after successful load
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: WorldMapPage empty/error states + header polish</name>
  <files>locaNext/src/lib/components/pages/WorldMapPage.svelte</files>
  <action>
**WorldMapPage.svelte (227 lines):**

1. Import shared components: `import { EmptyState, ErrorState } from '$lib/components/common';`

2. Replace error state (lines 115-118) with ErrorState component:
   - Remove the `.worldmap-error` div
   - Replace with: `<ErrorState message={apiError} onretry={fetchMapData} />`
   - Wrap in a flex container to center it: `<div class="worldmap-state-container"><ErrorState ... /></div>`

3. Add empty state after loading completes with zero nodes (WMP-02 requirement):
   After `{:else}` (line 123), before the map content, add:
   ```svelte
   {:else if nodes.length === 0}
     <div class="worldmap-state-container">
       <EmptyState
         icon={Earth}
         headline="No map data available"
         description="Configure a gamedata folder in Game Data page to load world map regions"
       />
     </div>
   {:else}
   ```
   Earth icon is already imported.

4. Header polish -- convert hardcoded px to rem:
   - `.worldmap-header` padding: `12px 16px` -> `0.75rem 1rem`
   - `.header-title` gap: `8px` -> `0.5rem`
   - `.worldmap-loading` and `.worldmap-error` padding: `32px` -> `2rem`

5. Add a `.worldmap-state-container` style for centering states:
   ```css
   .worldmap-state-container {
     display: flex;
     align-items: center;
     justify-content: center;
     flex: 1;
   }
   ```

6. Remove dead CSS for `.worldmap-error` (replaced by ErrorState) and `.worldmap-error p` selector.

7. Dark mode: Header already uses `--cds-*` tokens. Node count badge uses `--cds-text-02`. All clean. No changes needed.
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools/locaNext && npx svelte-check --threshold warning 2>&1 | grep "WorldMapPage" | head -5; echo "---"; grep -c "EmptyState\|ErrorState" src/lib/components/pages/WorldMapPage.svelte</automated>
  </verify>
  <done>WorldMapPage uses ErrorState with retry for API errors. EmptyState shows when nodes=0 with helpful guidance. Header uses rem values. No dead CSS remains.</done>
</task>

<task type="auto">
  <name>Task 2: MapCanvas + MapTooltip + MapDetailPanel dark mode + polish</name>
  <files>locaNext/src/lib/components/ldm/MapCanvas.svelte, locaNext/src/lib/components/ldm/MapTooltip.svelte, locaNext/src/lib/components/ldm/MapDetailPanel.svelte</files>
  <action>
**MapCanvas.svelte (298 lines):**

1. Read the full file first to understand the canvas rendering approach.

2. Canvas elements are drawn via JavaScript (Canvas 2D API), not CSS. Dark mode for canvas requires checking if colors used in `ctx.fillStyle`, `ctx.strokeStyle`, `ctx.fillRect` etc. are hardcoded.

3. For canvas-rendered nodes and routes:
   - If node colors are hardcoded hex/rgb, replace with CSS custom property values read at render time:
     ```javascript
     // Get computed CSS token values for canvas rendering
     const style = getComputedStyle(document.documentElement);
     const nodeColor = style.getPropertyValue('--cds-interactive').trim() || '#0f62fe';
     const routeColor = style.getPropertyValue('--cds-border-strong-01').trim() || '#6f6f6f';
     const labelColor = style.getPropertyValue('--cds-text-01').trim() || '#161616';
     const bgColor = style.getPropertyValue('--cds-background').trim() || '#ffffff';
     ```
   - Cache these in a `$derived` or compute once in `$effect` that re-runs when theme changes.
   - Use these computed values in all canvas draw calls.

4. If the canvas already uses dynamic colors, verify they work in dark theme by checking the variable names used.

5. Node hover state: Ensure highlighted nodes use a brighter/larger version of the node color, not a hardcoded highlight.

6. Route lines: Ensure route stroke color uses `--cds-border-subtle-01` or similar for subtle lines.

**MapTooltip.svelte (144 lines):**

1. Read the full file.

2. Tooltip styling: Ensure background uses `var(--cds-layer-02)` (elevated surface), border uses `var(--cds-border-subtle-01)`, text uses `var(--cds-text-01)`, and box-shadow uses a dark-mode-safe value like `var(--card-shadow)` from design tokens.

3. Replace any hardcoded colors with `--cds-*` tokens.

4. Add subtle border-radius: `var(--card-radius)` if not already rounded.

5. Ensure `z-index` is high enough (tooltip should be above map but below modals).

**MapDetailPanel.svelte (307 lines):**

1. Read the full file.

2. Spacing polish: Replace hardcoded px padding/margin with rem values. Key areas:
   - Panel header (close button, title)
   - Section spacing between detail fields
   - Field label/value pairs

3. Typography: Section headers should use `font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.5px; color: var(--cds-text-02)`. Values should use `font-size: 0.875rem; color: var(--cds-text-01)`.

4. Dark mode: Replace hardcoded colors with `--cds-*` tokens. Panel background should use `var(--cds-layer-01)`. Border should use `var(--cds-border-subtle-01)`.

5. Close button: Ensure it has proper hover (`var(--cds-layer-hover-01)`) and focus (`outline: 2px solid var(--cds-focus)`) states.

6. Connected routes section (if exists): Ensure route labels use consistent styling with the rest of the panel.
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools/locaNext && npx svelte-check --threshold warning 2>&1 | grep -E "(MapCanvas|MapTooltip|MapDetailPanel)" | head -10; echo "---"; grep -c "rgba\|rgb(" src/lib/components/ldm/MapCanvas.svelte src/lib/components/ldm/MapTooltip.svelte src/lib/components/ldm/MapDetailPanel.svelte 2>/dev/null; echo "check done"</automated>
  </verify>
  <done>MapCanvas uses CSS token values for node/route/label colors (dark mode safe). MapTooltip uses --cds-* tokens for background/text/border. MapDetailPanel has consistent typography hierarchy and rem spacing. All 3 files pass svelte-check with zero errors. No hardcoded colors remain except canvas fallback defaults.</done>
</task>

</tasks>

<verification>
1. `cd locaNext && npx svelte-check --threshold warning` -- zero new errors in all 4 modified files
2. `grep -rn "rgba\|rgb(" src/lib/components/pages/WorldMapPage.svelte src/lib/components/ldm/MapCanvas.svelte src/lib/components/ldm/MapTooltip.svelte src/lib/components/ldm/MapDetailPanel.svelte` -- only canvas fallback defaults should remain
3. `grep -c "EmptyState\|ErrorState" src/lib/components/pages/WorldMapPage.svelte` -- count >= 2 (one EmptyState, one ErrorState)
</verification>

<success_criteria>
- WorldMapPage uses ErrorState with retry for API errors
- WorldMapPage shows EmptyState when no map data (0 nodes)
- WorldMapPage header uses rem values, not px
- MapCanvas draws nodes/routes using CSS token colors for dark mode
- MapTooltip uses Carbon tokens for background, border, text
- MapDetailPanel has polished typography and rem spacing
- All 4 files pass svelte-check with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/34-page-polish/34-03-SUMMARY.md`
</output>
