---
phase: 34-page-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - locaNext/src/lib/components/pages/GameDevPage.svelte
  - locaNext/src/lib/components/ldm/NodeDetailPanel.svelte
  - locaNext/src/lib/components/ldm/GameDataContextPanel.svelte
  - locaNext/src/lib/components/ldm/GameDataTree.svelte
autonomous: true
requirements: [GDT-01, GDT-02, GDT-03, GDT-04]

must_haves:
  truths:
    - "GameData Tree page has consistent spacing using design tokens from Phase 32"
    - "Node detail panel shows proper typography hierarchy and smooth transitions"
    - "Context Intelligence Panel tabs have loading states and empty states for each section"
    - "Tree expand/collapse remains smooth with 1000+ nodes (no new DOM overhead added)"
    - "All hardcoded colors replaced with --cds-* tokens for dark mode"
    - "Empty explorer shows EmptyState component instead of plain paragraph"
  artifacts:
    - path: "locaNext/src/lib/components/pages/GameDevPage.svelte"
      provides: "Polished GameDev page with EmptyState, ErrorState, design token spacing"
    - path: "locaNext/src/lib/components/ldm/NodeDetailPanel.svelte"
      provides: "Typography hierarchy, transition polish, dark mode safe colors"
    - path: "locaNext/src/lib/components/ldm/GameDataContextPanel.svelte"
      provides: "Loading/empty states per tab, polished tab styling"
    - path: "locaNext/src/lib/components/ldm/GameDataTree.svelte"
      provides: "Loading state for initial tree load, dark mode fixes"
  key_links:
    - from: "GameDevPage.svelte"
      to: "$lib/components/common"
      via: "import { EmptyState, ErrorState } from '$lib/components/common'"
      pattern: "EmptyState|ErrorState"
    - from: "GameDataContextPanel.svelte"
      to: "carbon-components-svelte"
      via: "SkeletonText for tab loading states"
      pattern: "SkeletonText"
---

<objective>
Polish the GameData Tree page (GameDevPage + GameDataTree + NodeDetailPanel + GameDataContextPanel) for visual consistency, dark mode, loading/empty states, and typography.

Purpose: GameData Tree is the largest and most complex page (526 + 1242 + 567 + 974 = 3300+ lines across 4 components). It needs polished loading states, empty states using shared components, consistent spacing via design tokens, and dark mode safe colors.

Output: 4 polished component files with consistent v3.3 design language.
</objective>

<execution_context>
@/home/<USERNAME>/.claude/get-shit-done/workflows/execute-plan.md
@/home/<USERNAME>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-design-token-foundation/32-01-SUMMARY.md
</context>

<interfaces>
<!-- Shared components from Phase 32. Import via barrel export. -->

From locaNext/src/lib/components/common/index.js:
```javascript
export { default as PageHeader } from './PageHeader.svelte';
export { default as SkeletonCard } from './SkeletonCard.svelte';
export { default as EmptyState } from './EmptyState.svelte';
export { default as ErrorState } from './ErrorState.svelte';
export { default as InfiniteScroll } from './InfiniteScroll.svelte';
```

PageHeader props: `{ icon: Component, title: string, children: snippet }`
EmptyState props: `{ icon: Component, headline: string, description?: string, ctaLabel?: string, onaction?: () => void }`
ErrorState props: `{ message?: string, onretry?: () => void }`

Design tokens available in app.css :root:
- `--page-header-height`, `--page-header-padding`
- `--panel-border`, `--panel-bg`
- `--card-padding`, `--card-radius`, `--card-shadow`
- `--transition-fast`, `--transition-normal`
- `--skeleton-shimmer-duration`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: GameDevPage + GameDataTree loading/empty/error states</name>
  <files>locaNext/src/lib/components/pages/GameDevPage.svelte, locaNext/src/lib/components/ldm/GameDataTree.svelte</files>
  <action>
**GameDevPage.svelte (526 lines):**

1. Import `{ EmptyState, ErrorState }` from `$lib/components/common` and `{ FolderOpen, TreeView }` from carbon-icons-svelte (FolderOpen already imported).

2. Replace the `.explorer-placeholder` div (line ~258-260, plain `<p>` tag) with:
   ```svelte
   <EmptyState icon={FolderOpen} headline="No gamedata folder" description="Enter a gamedata folder path above to browse XML files" />
   ```

3. Replace the `.grid-placeholder` div (line ~290-294, inline icon + `<p>`) with:
   ```svelte
   <EmptyState icon={TreeView} headline="No file selected" description="Select a file from the explorer to view its tree structure" />
   ```

4. CSS polish -- replace hardcoded pixel values with design tokens:
   - `.explorer-header` padding `0.5rem 0.75rem` is fine (matches --cds-spacing-03/04)
   - `.path-input-row` gap `4px` -> `0.25rem`
   - `.resize-handle` width `4px` -> keep (functional, not design)
   - Add `transition: background var(--transition-fast)` to `.icon-button:hover` and `.browse-button:hover`
   - Remove `border-radius: 4px` from buttons, use `var(--card-radius)` for consistency (card-radius is 6px, but these are small buttons -- keep 4px, just ensure consistency)

5. Dark mode check: All colors already use `--cds-*` tokens. Verify `.auto-load-dot` background uses `var(--cds-interactive)` (already does). No changes needed.

**GameDataTree.svelte (1242 lines):**

1. Import `{ SkeletonText }` from `carbon-components-svelte` (if not already imported).

2. Find the initial loading state (when tree data is being fetched). If there's a loading indicator, replace spinner/InlineLoading with SkeletonText lines to mimic the tree structure:
   ```svelte
   {#if loading}
     <div class="tree-loading">
       {#each Array(8) as _}
         <div class="tree-skeleton-row">
           <SkeletonText width="60%" />
         </div>
       {/each}
     </div>
   ```
   Add `.tree-skeleton-row { padding: 0.375rem 1rem; }` for proper spacing.

3. Add an EmptyState for when tree loads but has zero nodes:
   ```svelte
   {:else if treeData && treeData.length === 0}
     <EmptyState icon={TreeView} headline="Empty file" description="This XML file contains no data nodes" />
   ```
   Import `{ EmptyState }` from `$lib/components/common` and `{ TreeView }` from carbon-icons-svelte.

4. Add an ErrorState for when tree fetch fails:
   ```svelte
   {:else if error}
     <ErrorState message={error} onretry={reload} />
   ```
   Import `{ ErrorState }` from `$lib/components/common`.

5. DO NOT modify the tree rendering logic, virtual scrolling, or expand/collapse performance. Only add states around the existing content block.

6. Dark mode: Scan for any hardcoded `#hex` or `rgb()` colors in the `<style>` section. Replace with nearest `--cds-*` token. Common pattern: background highlights on selected nodes may use hardcoded colors -- replace with `var(--cds-layer-selected-01)`.
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools/locaNext && npx svelte-check --threshold warning 2>&1 | grep -E "(GameDevPage|GameDataTree)" | head -20; echo "---"; grep -c "EmptyState\|ErrorState" src/lib/components/pages/GameDevPage.svelte src/lib/components/ldm/GameDataTree.svelte</automated>
  </verify>
  <done>GameDevPage uses EmptyState for both placeholders. GameDataTree has skeleton loading, empty state, and error state. No hardcoded colors remain in either file's style section. Zero svelte-check errors in both files.</done>
</task>

<task type="auto">
  <name>Task 2: NodeDetailPanel + GameDataContextPanel polish</name>
  <files>locaNext/src/lib/components/ldm/NodeDetailPanel.svelte, locaNext/src/lib/components/ldm/GameDataContextPanel.svelte</files>
  <action>
**NodeDetailPanel.svelte (567 lines):**

1. Typography hierarchy polish: Ensure the node tag name (h3/h4) uses `font-size: 1rem; font-weight: 600; color: var(--cds-text-01)`. Attribute labels should use `font-size: 0.75rem; color: var(--cds-text-02); text-transform: uppercase; letter-spacing: 0.5px`. Attribute values should use `font-size: 0.875rem; color: var(--cds-text-01)`.

2. Scan `TYPE_COLORS` map (line 33-48) -- these hardcoded hex colors are intentional (entity type accents) and should remain. But where they're APPLIED, ensure they work in dark mode by using them as subtle accents (border-left or small badge) not as text or background colors that would reduce contrast.

3. Add `transition: all var(--transition-fast)` to interactive elements (editable fields, child navigation links).

4. Spacing audit: Ensure padding/margins use `--cds-spacing-*` tokens or rem values. Replace any hardcoded `px` padding with rem equivalents. Keep functional pixel values (1px borders, 2px outlines).

5. Children section at bottom: Ensure clickable child nodes have `cursor: pointer`, hover state `background: var(--cds-layer-hover-01)`, and `border-radius: 4px`.

6. Saving state: If save indicators exist, ensure they use `var(--cds-support-success)` for success and `var(--cds-support-error)` for errors.

**GameDataContextPanel.svelte (974 lines):**

1. Import `{ EmptyState }` from `$lib/components/common` (if not already).

2. For each tab (Details, Cross-Refs, Related, Media), add proper empty states:
   - Cross-Refs empty: `<EmptyState icon={ConnectionSignal} headline="No cross-references" description="This node has no incoming or outgoing references" />`
   - Related empty: `<EmptyState icon={Search} headline="No related items" description="No semantically similar items found" />`
   - Media empty: `<EmptyState icon={Image} headline="No media" description="No images or audio associated with this node" />`
   (Icons ConnectionSignal, Search, Image are already imported in the component)

3. For each tab loading state, verify SkeletonText is used (it's already imported from carbon-components-svelte). If any tab uses InlineLoading as the primary loading indicator, add 3-4 SkeletonText lines alongside it for better perceived loading.

4. Tab styling polish:
   - Active tab: `border-bottom: 2px solid var(--cds-interactive); color: var(--cds-text-01); font-weight: 600`
   - Inactive tab: `color: var(--cds-text-02); border-bottom: 2px solid transparent`
   - Tab hover: `color: var(--cds-text-01); background: var(--cds-layer-hover-01)`
   - Tab container: `border-bottom: 1px solid var(--cds-border-subtle-01)`

5. Collapse/expand toggle: Ensure the ChevronLeft/ChevronRight toggle has `transition: transform var(--transition-fast)` for smooth icon rotation.

6. Dark mode scan: Replace any hardcoded `rgba()` or `#hex` in styles with `--cds-*` tokens. Exception: intentional type accent colors (same as NodeDetailPanel).

7. DO NOT change the API call logic, caching strategy, or progressive loading architecture. Polish is visual only.
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools/locaNext && npx svelte-check --threshold warning 2>&1 | grep -E "(NodeDetailPanel|GameDataContextPanel)" | head -20; echo "---"; grep -c "EmptyState" src/lib/components/ldm/GameDataContextPanel.svelte; grep -c "hardcoded-color-check" src/lib/components/ldm/NodeDetailPanel.svelte src/lib/components/ldm/GameDataContextPanel.svelte || echo "grep done"</automated>
  </verify>
  <done>NodeDetailPanel has consistent typography hierarchy (3 tiers: tag name, attribute label, attribute value) with transitions on interactive elements. GameDataContextPanel has EmptyState for all 4 tabs, polished tab styling with active/inactive/hover states, SkeletonText loading per tab. No hardcoded colors in either file except intentional TYPE_COLORS accents.</done>
</task>

</tasks>

<verification>
1. `cd locaNext && npx svelte-check --threshold warning` -- zero new errors in the 4 modified files
2. `grep -rn "rgb\|rgba\|#[0-9a-fA-F]\{3,6\}" src/lib/components/pages/GameDevPage.svelte src/lib/components/ldm/NodeDetailPanel.svelte src/lib/components/ldm/GameDataContextPanel.svelte src/lib/components/ldm/GameDataTree.svelte` -- only TYPE_COLORS map entries should remain
3. `grep -c "EmptyState\|ErrorState\|SkeletonText" src/lib/components/pages/GameDevPage.svelte src/lib/components/ldm/GameDataTree.svelte src/lib/components/ldm/GameDataContextPanel.svelte` -- all files show non-zero counts
</verification>

<success_criteria>
- GameDevPage placeholders replaced with EmptyState components
- GameDataTree has skeleton loading, empty state, error state with retry
- NodeDetailPanel has polished typography hierarchy and transitions
- GameDataContextPanel has per-tab empty states and polished tab styling
- All 4 files pass svelte-check with zero errors
- No hardcoded colors except TYPE_COLORS accents
- Tree performance unchanged (no new per-node DOM overhead)
</success_criteria>

<output>
After completion, create `.planning/phases/34-page-polish/34-01-SUMMARY.md`
</output>
