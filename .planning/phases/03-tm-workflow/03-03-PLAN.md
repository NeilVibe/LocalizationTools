---
phase: 03-tm-workflow
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - locaNext/src/lib/components/ldm/TMTab.svelte
  - locaNext/src/lib/components/pages/GridPage.svelte
  - locaNext/src/lib/components/ldm/TMExplorerTree.svelte
  - locaNext/src/lib/components/pages/TMPage.svelte
  - locaNext/tests/tm-auto-mirror.spec.ts
  - locaNext/tests/tm-explorer-polish.spec.ts
autonomous: false
requirements: [TM-01, TM-02, TM-04, UI-02, UI-03]

must_haves:
  truths:
    - "After uploading files, the TM tree shows a corresponding TM entry (auto-mirrored)"
    - "User can assign a TM to a folder through the TM explorer tree and the assignment persists"
    - "Per-file leverage statistics are visible (exact%, fuzzy%, new%) in the TM tab or grid header"
    - "File explorer tree looks polished and professional"
    - "TM explorer tree looks polished with clear assignment UI"
  artifacts:
    - path: "locaNext/tests/tm-auto-mirror.spec.ts"
      provides: "E2E test verifying auto-mirror creates TM on file upload"
    - path: "locaNext/tests/tm-explorer-polish.spec.ts"
      provides: "Visual quality tests for tree polish"
  key_links:
    - from: "locaNext/src/lib/components/ldm/TMTab.svelte"
      to: "GET /api/ldm/files/{file_id}/leverage"
      via: "fetch leverage stats when file context changes"
      pattern: "fetch.*leverage"
    - from: "locaNext/tests/tm-auto-mirror.spec.ts"
      to: "server/tools/ldm/routes/files.py"
      via: "E2E upload triggers auto-mirror, verifies TM in tree"
      pattern: "upload.*tm.*tree"
---

<objective>
End-to-end integration: wire auto-mirror to UI, leverage stats display, TM assignment verification, and visual quality checkpoint.

Purpose: Connect the backend capabilities (Plan 01) with the frontend components (Plan 02) into a complete TM workflow. Verify the full user journey: upload files -> TM auto-created -> select segment -> see color-coded matches with leverage stats -> assign TM manually. Conclude with human visual verification.

Output: Fully wired TM workflow with E2E tests and human-verified visual quality.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tm-workflow/03-RESEARCH.md
@.planning/phases/03-tm-workflow/03-01-SUMMARY.md
@.planning/phases/03-tm-workflow/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 (backend): -->
<!-- POST /api/ldm/files/upload -> auto-creates TM for folder -->
<!-- GET /api/ldm/files/{file_id}/leverage -> { exact, fuzzy, new, total, exact_pct, fuzzy_pct, new_pct } -->

<!-- From Plan 02 (frontend): -->
<!-- RightPanel.svelte: tabbed interface, receives selectedRow, tmMatches, tmLoading -->
<!-- TMTab.svelte: color-coded matches, word diff, getMatchColor() -->
<!-- wordDiff.js: tokenize(), computeWordDiff() -->

<!-- Existing: -->
<!-- TMExplorerTree.svelte: drag-drop assignment, context menu, activation -->
<!-- GridPage.svelte: now uses RightPanel instead of TMQAPanel -->
<!-- TMPage.svelte: TM management with TMExplorerGrid -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire leverage stats to TMTab + auto-mirror E2E test</name>
  <files>locaNext/src/lib/components/ldm/TMTab.svelte, locaNext/src/lib/components/pages/GridPage.svelte, locaNext/tests/tm-auto-mirror.spec.ts</files>
  <action>
    1. Add leverage stats display to TMTab.svelte:
       - Add a "Leverage" section at the top of the TM tab (before match list)
       - Shows a compact horizontal bar: green segment (exact%), yellow segment (fuzzy%), gray segment (new%)
       - Below bar: text "45% exact | 30% fuzzy | 25% new" in small muted text
       - Props: add `leverageStats` prop (object or null)
       - When leverageStats is null, show nothing (not loaded yet)
       - When leverageStats.total === 0, show "No segments to analyze"

    2. Add leverage fetching to GridPage.svelte:
       - When a file is opened (fileId changes), fetch `GET /api/ldm/files/${fileId}/leverage`
       - Store in `let leverageStats = $state(null)`
       - Pass leverageStats to RightPanel -> TMTab
       - Fetch on-demand (not blocking file open -- use $effect to trigger after fileId is set)
       - Handle errors gracefully (log warning, show nothing)

    3. Create `locaNext/tests/tm-auto-mirror.spec.ts`:
       - Test: Upload a file via the UI (use existing upload flow from Phase 2 tests)
       - Test: After upload, navigate to TM page and verify a TM exists with name matching the folder
       - Test: Upload a second file to the same folder -- verify no duplicate TM created
       - Use API-seeded approach if UI upload is complex: POST to file upload endpoint, then verify TM tree via UI
       - Follow Playwright login/navigation patterns from existing tests

    4. Verify TM assignment works end-to-end:
       - In tm-auto-mirror.spec.ts, also verify that the auto-created TM is assigned (check via API: GET /api/ldm/files/{file_id}/active-tms returns the auto-created TM)
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools/locaNext && npx playwright test tests/tm-auto-mirror.spec.ts -x</automated>
  </verify>
  <done>Leverage stats bar visible in TM tab. Auto-mirror E2E verified: file upload creates TM, appears in tree, no duplicates on second upload.</done>
</task>

<task type="auto">
  <name>Task 2: TM assignment verification + explorer visual polish tests</name>
  <files>locaNext/tests/tm-explorer-polish.spec.ts, locaNext/src/lib/components/pages/TMPage.svelte</files>
  <action>
    1. Create `locaNext/tests/tm-explorer-polish.spec.ts`:
       - Test: Navigate to TM page, verify tree renders with proper indentation and icons
       - Test: Verify TM assignment UI is accessible (right-click context menu or drag target visible)
       - Test: Screenshot capture of TM explorer tree for human visual review (save to /tmp/)
       - Test: Screenshot capture of file explorer for human visual review (save to /tmp/)
       - Test: Verify tab bar in right panel has all 4 tabs (TM, Image, Audio, AI Context)
       - Test: Verify switching tabs changes content area

    2. If TMPage.svelte needs minor CSS adjustments to align with the polished TMExplorerTree from Plan 02, apply them (CSS only, no JS logic changes).

    3. Ensure all existing Playwright tests still pass (run full suite).
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools/locaNext && npx playwright test tests/tm-explorer-polish.spec.ts -x && npx playwright test --project=chromium</automated>
  </verify>
  <done>TM explorer tree and file explorer pass visual quality tests. TM assignment UI accessible and functional. All existing tests still green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of Phase 3 TM Workflow</name>
  <files>none</files>
  <action>
    User visually verifies the complete Phase 3 TM Workflow implementation:
    1. Start dev servers: `./scripts/start_all_servers.sh --with-vite`
    2. Login at http://localhost:5173 (admin/admin123)
    3. Navigate to a project with files -- verify the file explorer tree looks polished (not raw defaults)
    4. Open a file with translations -- verify the right panel shows TM tab with:
       - Color-coded percentage badges (green for exact, yellow/orange for fuzzy)
       - Word-level diff highlighting on fuzzy matches (Korean text should diff at syllable level)
       - Leverage stats bar at top (exact%/fuzzy%/new%)
    5. Click the other tabs (Image, Audio, AI Context) -- should show placeholder text
    6. Navigate to TM page -- verify TM explorer tree looks polished with proper styling
    7. Verify a TM exists that was auto-created when files were uploaded
    8. Screenshots saved to /tmp/ for review

    Expected: Professional, executive-demo-ready TM workflow. Color coding makes match quality instantly clear. Diff highlighting shows exactly what changed.
  </action>
  <verify>Human approval of visual quality and complete TM workflow</verify>
  <done>User approves TM workflow as demo-ready: color-coded matches, word diff, leverage stats, tabbed panel, polished trees.</done>
</task>

</tasks>

<verification>
Full test suite:
```bash
cd /home/neil1988/LocalizationTools/locaNext && npx playwright test --project=chromium
cd /home/neil1988/LocalizationTools && python3 -m pytest tests/api/test_tm_auto_mirror.py tests/api/test_leverage.py tests/api/test_tm_search.py -x -v
```
</verification>

<success_criteria>
- Auto-mirror creates TM on file upload, visible in TM tree (E2E verified)
- TM assignment works via tree (existing drag-drop + context menu)
- Leverage stats displayed in TM tab as colored bar with percentages
- Color-coded matches and word diff working end-to-end with real data
- Explorer trees polished to executive-demo quality
- Human approves visual quality of complete TM workflow
- All Playwright tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/03-tm-workflow/03-03-SUMMARY.md`
</output>
