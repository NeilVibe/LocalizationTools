---
phase: 60-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_merge_pipeline.py
  - tests/integration/conftest_merge.py
autonomous: false
requirements:
  - MOCK-01
  - MOCK-02
  - MOCK-03
  - MOCK-04
  - SET-01
  - SET-02
  - SET-03
  - XFER-01
  - XFER-07
  - API-01
  - API-02
  - API-03
  - API-04

must_haves:
  truths:
    - "Mock data setup script creates 3 projects and all are queryable via API"
    - "Single-project merge preview returns match counts without modifying files"
    - "Single-project merge execute streams SSE progress events ending with complete"
    - "Multi-language merge preview returns per-language breakdown with scan data"
    - "Multi-language merge execute produces per-language matched/skipped counts"
    - "SSE stream contains progress, log, and complete event types in order"
    - "Settings path validation endpoint accepts test123 path and rejects invalid paths"
  artifacts:
    - path: "tests/integration/conftest_merge.py"
      provides: "Shared fixtures for merge tests (admin auth, mock data, temp dirs)"
      contains: "def mock_data_ready"
    - path: "tests/integration/test_merge_pipeline.py"
      provides: "E2E pipeline tests for single + multi-language merge + settings validation"
      contains: "def test_full_single_language_workflow"
  key_links:
    - from: "tests/integration/test_merge_pipeline.py"
      to: "/api/merge/preview"
      via: "requests.post with JSON body"
      pattern: "requests\\.post.*merge/preview"
    - from: "tests/integration/test_merge_pipeline.py"
      to: "/api/merge/execute"
      via: "requests.post stream=True for SSE"
      pattern: "requests\\.post.*merge/execute.*stream=True"
    - from: "tests/integration/test_merge_pipeline.py"
      to: "/api/settings/validate-path"
      via: "requests.post with path body"
      pattern: "requests\\.post.*settings/validate-path"
---

<objective>
End-to-end merge pipeline integration tests covering mock data setup, settings path validation, single-project merge (preview + execute with SSE), and multi-language merge workflow.

Purpose: Verify that all v6.0 phases (56-59) work together as a complete pipeline -- mock data is queryable, path settings validate correctly (SET-01/02/03), merge preview returns correct summaries, merge execute streams SSE events, and multi-language mode produces per-language breakdowns.

Output: Two test files (conftest_merge.py + test_merge_pipeline.py) with 8-10 integration tests that run against a live server.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-integration-testing/60-RESEARCH.md

@server/api/merge.py
@server/services/transfer_adapter.py
@tests/conftest.py
@scripts/setup_mock_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create merge test fixtures and E2E pipeline tests</name>
  <files>tests/integration/conftest_merge.py, tests/integration/test_merge_pipeline.py</files>
  <read_first>
    - tests/conftest.py (admin token pattern: get_admin_token_with_retry, BASE_URL, ADMIN_USERNAME/PASSWORD)
    - server/api/merge.py (MergePreviewRequest fields: source_path, target_path, export_path, match_mode, only_untranslated, stringid_all_categories, multi_language; MergePreviewResponse fields: files_processed, total_corrections, total_matched, total_updated, total_not_found, total_skipped, overwrite_warnings, errors, per_language, scan; SSE event types: progress, log, complete, error, ping)
    - server/services/transfer_adapter.py (MATCH_MODES dict keys: "stringid_only", "strict", "strorigin_filename")
    - scripts/setup_mock_data.py (--confirm-wipe flag, DB_PATH, creates project_FRE, project_ENG, project_MULTI)
    - server/api/settings.py (validate-path endpoint: POST /api/settings/validate-path with {"path": "..."} body, returns {"valid": bool, "error": str|null})
  </read_first>
  <action>
Create two files:

**tests/integration/conftest_merge.py** — shared fixtures:

1. `BASE_URL = "http://localhost:8888"` constant
2. `TEST123_PATH = "/mnt/c/Users/MYCOM/Desktop/oldoldVold/test123"` constant
3. `PROJECT_ROOT = Path(__file__).parent.parent.parent` (resolves to repo root)

4. `@pytest.fixture(scope="module") def server_running()`:
   - Call `requests.get(f"{BASE_URL}/health", timeout=5)`
   - If ConnectionError, `pytest.skip("Server not running at localhost:8888")`

5. `@pytest.fixture(scope="module") def admin_headers(server_running)`:
   - Import `get_admin_token_with_retry` from `tests.conftest`
   - Call it, build `{"Authorization": f"Bearer {token}"}` dict
   - Return headers dict

6. `@pytest.fixture(scope="module") def mock_data_ready(server_running)`:
   - Run `subprocess.run(["python3", "scripts/setup_mock_data.py", "--confirm-wipe"], capture_output=True, text=True, cwd=str(PROJECT_ROOT))`
   - Assert returncode == 0, yield result.stdout

7. `@pytest.fixture def merge_temp_target(tmp_path)`:
   - Create `tmp_path / "merge_target"` directory
   - Copy `TEST123_PATH / "languagedata_fr PC 0904 1847.txt"` to it as `languagedata_FRE.xml` (if TEST123_PATH exists)
   - If TEST123_PATH not available, create a minimal XML file:
     ```xml
     <?xml version="1.0" encoding="utf-8"?>
     <LanguageData>
       <Row StringID="TEST_001" StrOrigin="Hello" Str="" Category="SCRIPT"/>
       <Row StringID="TEST_002" StrOrigin="World" Str="" Category="SCRIPT"/>
     </LanguageData>
     ```
   - Return `tmp_path / "merge_target"` path

8. `@pytest.fixture def merge_temp_source(tmp_path)`:
   - Create `tmp_path / "merge_source"` directory
   - Create a corrections XML file `corrections.xml`:
     ```xml
     <?xml version="1.0" encoding="utf-8"?>
     <LanguageData>
       <Row StringID="TEST_001" StrOrigin="Hello" Str="Bonjour" Category="SCRIPT"/>
     </LanguageData>
     ```
   - Return path

9. All fixtures use `@pytest.mark.integration` marker

**tests/integration/test_merge_pipeline.py** — E2E tests:

```python
"""E2E merge pipeline integration tests.

Requires: DEV_MODE=true python3 server/main.py running on localhost:8888
Run: python3 -m pytest tests/integration/test_merge_pipeline.py -x -v --timeout=120
"""
from __future__ import annotations

import json
import pytest
import requests

from .conftest_merge import BASE_URL, TEST123_PATH

pytestmark = [pytest.mark.integration]
```

Tests to create:

1. `test_mock_data_setup(mock_data_ready)`:
   - Assert "project_FRE" in mock_data_ready stdout (or verify via API)
   - Call `GET /api/v2/ldm/projects` with admin_headers
   - Assert response contains at least 1 project (status 200)

2. `test_health_endpoint(server_running)`:
   - `requests.get(f"{BASE_URL}/health")` returns 200

3. `test_settings_path_validation(admin_headers)`:
   - POST `/api/settings/validate-path` with `{"path": TEST123_PATH}` and admin_headers
   - If TEST123_PATH exists on this machine: assert status 200, assert `data["valid"]` is True
   - If TEST123_PATH does not exist: assert status 200, assert `data["valid"]` is False (path doesn't exist)
   - POST `/api/settings/validate-path` with `{"path": "/nonexistent/fake/path/12345"}`
   - Assert status 200 and `data["valid"]` is False
   - This covers SET-01 (LOC PATH configurable), SET-02 (EXPORT PATH configurable), SET-03 (path validation with exists + contains languagedata check)

4. `test_preview_single_language(admin_headers, merge_temp_target, merge_temp_source)`:
   - POST `/api/merge/preview` with body:
     ```json
     {
       "source_path": str(merge_temp_source),
       "target_path": str(merge_temp_target),
       "export_path": str(merge_temp_target),
       "match_mode": "strict",
       "only_untranslated": false,
       "multi_language": false
     }
     ```
   - Assert status 200
   - Assert `"total_matched" in data` and `"errors" in data`
   - Assert `data["errors"]` is a list (may be empty or have items)

5. `test_preview_invalid_match_mode(admin_headers, merge_temp_target, merge_temp_source)`:
   - POST `/api/merge/preview` with `match_mode="invalid_mode"`
   - Assert status 422
   - Assert `"Invalid match_mode"` in response text

6. `test_execute_streams_sse(admin_headers, merge_temp_target, merge_temp_source)`:
   - POST `/api/merge/execute` with `stream=True, timeout=120`
   - Same body as preview but `match_mode="strict"`
   - Parse SSE events line by line:
     ```python
     events = []
     current_event = "message"
     for line in resp.iter_lines(decode_unicode=True):
         if not line:
             continue
         if line.startswith("event:"):
             current_event = line[6:].strip()
         elif line.startswith("data:"):
             events.append({"event": current_event, "data": line[5:].strip()})
             if current_event in ("complete", "error"):
                 break
     ```
   - Assert `len(events) >= 1` (at least complete event)
   - Assert any event has `event == "complete"` or `event == "error"`
   - If complete event found, parse its data as JSON, assert it has `"total_matched"` key

7. `test_execute_concurrent_guard(admin_headers, merge_temp_target, merge_temp_source)`:
   - Skip this test with `@pytest.mark.skip(reason="Concurrent guard test needs careful threading")` -- document for future

8. `test_multi_language_preview(admin_headers)`:
   - Skip if TEST123_PATH doesn't exist: `pytest.importorskip` or manual check
   - POST `/api/merge/preview` with `multi_language=True`, source_path=TEST123_PATH, target_path=TEST123_PATH, export_path=TEST123_PATH
   - Assert status 200
   - data = resp.json(); assert `"errors" in data`
   - (Multi-language scan may return 0 languages if test123 has no lang-suffixed subfolders -- that's OK, we verify the endpoint doesn't crash)

9. `test_sse_event_types_ordered(admin_headers, merge_temp_target, merge_temp_source)`:
   - POST execute with stream=True
   - Collect all events
   - Assert "complete" is the LAST event type (or "error")
   - Assert no events appear AFTER complete/error

Add `from __future__ import annotations` at top of both files.
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python3 -m pytest tests/integration/test_merge_pipeline.py -x -v --timeout=120 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/integration/conftest_merge.py contains `def mock_data_ready`
    - tests/integration/conftest_merge.py contains `def admin_headers`
    - tests/integration/conftest_merge.py contains `def merge_temp_target`
    - tests/integration/conftest_merge.py contains `def merge_temp_source`
    - tests/integration/conftest_merge.py contains `BASE_URL = "http://localhost:8888"`
    - tests/integration/test_merge_pipeline.py contains `def test_mock_data_setup`
    - tests/integration/test_merge_pipeline.py contains `def test_settings_path_validation`
    - tests/integration/test_merge_pipeline.py contains `def test_preview_single_language`
    - tests/integration/test_merge_pipeline.py contains `def test_execute_streams_sse`
    - tests/integration/test_merge_pipeline.py contains `def test_multi_language_preview`
    - tests/integration/test_merge_pipeline.py contains `def test_sse_event_types_ordered`
    - tests/integration/test_merge_pipeline.py contains `requests.post` calls to `/api/merge/preview` and `/api/merge/execute`
    - tests/integration/test_merge_pipeline.py contains `validate-path` call
    - tests/integration/test_merge_pipeline.py contains `stream=True` for execute endpoint
    - tests/integration/test_merge_pipeline.py contains `from __future__ import annotations`
    - All tests pass when server is running (or skip gracefully when server is not running)
  </acceptance_criteria>
  <done>
    E2E pipeline tests exist and pass: mock data setup verified, settings path validation tested (SET-01/02/03), single-language preview returns match counts, execute streams SSE events with complete terminator, multi-language preview endpoint responds without error. Tests skip gracefully when server is not running.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify E2E test suite passes with live server</name>
  <files>tests/integration/test_merge_pipeline.py</files>
  <action>User verifies integration tests pass against a running server.</action>
  <what-built>Integration test suite for the complete v6.0 merge pipeline covering mock data, settings path validation, preview, SSE execute, and multi-language modes</what-built>
  <how-to-verify>
    1. Ensure server is running: `DEV_MODE=true python3 server/main.py` in another terminal
    2. Run: `cd /home/neil1988/LocalizationTools && python3 -m pytest tests/integration/test_merge_pipeline.py -v --timeout=120`
    3. Verify all tests pass (or skip with clear reason if server not running)
    4. Check output shows test names matching: test_mock_data_setup, test_settings_path_validation, test_preview_single_language, test_execute_streams_sse, test_multi_language_preview
  </how-to-verify>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python3 -m pytest tests/integration/test_merge_pipeline.py -v --timeout=120 2>&1 | tail -20</automated>
  </verify>
  <done>User confirms all pipeline tests pass with the live server.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/integration/test_merge_pipeline.py -v --timeout=120` -- all tests green
- `grep -c "def test_" tests/integration/test_merge_pipeline.py` -- returns 7+
- `grep "stream=True" tests/integration/test_merge_pipeline.py` -- returns at least 1 match
- `grep "validate-path" tests/integration/test_merge_pipeline.py` -- returns at least 1 match
</verification>

<success_criteria>
- All E2E pipeline tests pass with server running
- Tests skip gracefully when server is not available
- SSE streaming verified with event parsing
- Multi-language preview endpoint tested
- Mock data setup validated via API query
- Settings path validation tested (valid path, invalid path)
</success_criteria>

<output>
After completion, create `.planning/phases/60-integration-testing/60-01-SUMMARY.md`
</output>
