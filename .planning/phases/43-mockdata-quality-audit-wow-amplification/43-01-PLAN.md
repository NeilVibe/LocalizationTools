---
phase: 43-mockdata-quality-audit-wow-amplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/mock_gamedata/StaticInfo/characterinfo/characterinfo_showcase.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/factioninfo/FactionInfo.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/factioninfo/NodeWaypointInfo/NodeWaypointInfo.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/skillinfo/skillinfo_showcase.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/regioninfo/regioninfo_showcase.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/questinfo/questinfo_showcase.staticinfo.xml
autonomous: true
requirements:
  - MOCK-AUDIT-01
  - MOCK-AUDIT-02
  - MOCK-AUDIT-03

must_haves:
  truths:
    - "All 5 characters have complete cross-ref sets: ItemKey, RegionKey, SkillKey, FactionKey, KnowledgeKey"
    - "3 new XML entity files exist: SkillInfo (3 skills), RegionInfo (5 regions), QuestInfo (3 quests)"
    - "FactionNode StrKeys use Region_ prefix PascalCase format"
    - "NodeWaypointInfo FromNodeKey/ToNodeKey match new FactionNode StrKeys"
    - "All routes have 2-3 waypoint coordinates for curved paths"
  artifacts:
    - path: "tests/fixtures/mock_gamedata/StaticInfo/skillinfo/skillinfo_showcase.staticinfo.xml"
      provides: "3 skill entities with Korean names and cross-refs"
      contains: "SkillInfoList"
    - path: "tests/fixtures/mock_gamedata/StaticInfo/regioninfo/regioninfo_showcase.staticinfo.xml"
      provides: "5 region entities with Korean names and cross-refs"
      contains: "RegionInfoList"
    - path: "tests/fixtures/mock_gamedata/StaticInfo/questinfo/questinfo_showcase.staticinfo.xml"
      provides: "3 quest entities connecting the storyline"
      contains: "QuestInfoList"
  key_links:
    - from: "characterinfo_showcase.staticinfo.xml"
      to: "skillinfo_showcase.staticinfo.xml"
      via: "SkillKey attribute matches SkillInfo StrKey"
      pattern: "SkillKey=\"Skill_"
    - from: "characterinfo_showcase.staticinfo.xml"
      to: "FactionInfo.staticinfo.xml"
      via: "RegionKey attribute matches FactionNode StrKey"
      pattern: "RegionKey=\"Region_"
    - from: "NodeWaypointInfo.staticinfo.xml"
      to: "FactionInfo.staticinfo.xml"
      via: "FromNodeKey/ToNodeKey match FactionNode StrKey"
      pattern: "FromNodeKey=\"Region_"
---

<objective>
Create missing XML entity files (SkillInfo, RegionInfo, QuestInfo), fix all cross-reference format mismatches, complete character cross-ref sets, and add waypoints to map routes for curved paths.

Purpose: Without these fixes, the Codex relationship graph shows generic "related" links instead of typed links, character cross-refs resolve to nothing, and map routes render as straight lines.
Output: 6 XML files (3 new, 3 modified) with consistent PascalCase cross-ref keys and complete entity interconnections.
</objective>

<execution_context>
@/home/<USERNAME>/.claude/get-shit-done/workflows/execute-plan.md
@/home/<USERNAME>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-mockdata-quality-audit-wow-amplification/43-CONTEXT.md
@.planning/phases/43-mockdata-quality-audit-wow-amplification/43-RESEARCH.md

<interfaces>
<!-- Cross-ref key format (MUST match exactly for codex_service resolution): -->
<!-- EntityType_EntityName PascalCase: Character_ElderVaron, Skill_SacredFlame, Region_SealedLibrary, etc. -->

<!-- ENTITY_TAG_MAP from codex_service.py (what XML tags register as entities): -->
<!-- CharacterInfo, ItemInfo, SkillInfo, GimmickGroupInfo, KnowledgeInfo, FactionNode -->
<!-- QuestInfo and RegionInfo are NOT in ENTITY_TAG_MAP — they show in GameData Tree but NOT in Codex graph -->

<!-- REL_TYPE_MAP from codex_service.py (cross-ref attr -> relationship type): -->
<!-- ItemKey -> "owns", SkillKey -> "knows", FactionKey -> "member_of", RegionKey -> "located_in", KnowledgeKey -> "described_by" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SkillInfo, RegionInfo, QuestInfo XML files and fix character cross-refs</name>
  <files>
    tests/fixtures/mock_gamedata/StaticInfo/skillinfo/skillinfo_showcase.staticinfo.xml,
    tests/fixtures/mock_gamedata/StaticInfo/regioninfo/regioninfo_showcase.staticinfo.xml,
    tests/fixtures/mock_gamedata/StaticInfo/questinfo/questinfo_showcase.staticinfo.xml,
    tests/fixtures/mock_gamedata/StaticInfo/characterinfo/characterinfo_showcase.staticinfo.xml
  </files>
  <read_first>
    tests/fixtures/mock_gamedata/StaticInfo/characterinfo/characterinfo_showcase.staticinfo.xml,
    tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_showcase.staticinfo.xml,
    .planning/phases/43-mockdata-quality-audit-wow-amplification/43-CONTEXT.md,
    .planning/phases/43-mockdata-quality-audit-wow-amplification/43-RESEARCH.md
  </read_first>
  <action>
1. Create `skillinfo_showcase.staticinfo.xml` with 3 skills. Use the exact StrKey values characters reference:

```xml
<?xml version='1.0' encoding='UTF-8'?>
<SkillInfoList>
  <SkillInfo Key="SKILL_001" StrKey="Skill_SacredFlame"
      SkillName="성스러운 불꽃"
      SkillDesc="현자의 결사에서 전승되는 신성한 화염 마법.&lt;br/&gt;어둠의 존재를 정화하는 힘을 지닌다.&lt;br/&gt;사용 시 주변의 그림자를 소멸시킨다."
      CooldownSec="8" Damage="450" ManaCost="30" RequireLevel="10"
      CharacterKey="Character_ElderVaron"
      KnowledgeKey="Knowledge_SacredFlame"
      UITextureName="skill_0001" />

  <SkillInfo Key="SKILL_002" StrKey="Skill_ShadowStrike"
      SkillName="그림자 일격"
      SkillDesc="어둠의 힘을 응축하여 적에게 치명적인 일격을 가한다.&lt;br/&gt;그림자 암살자만이 사용할 수 있는 비전 기술.&lt;br/&gt;대상의 방어력을 무시하고 급소를 공격한다."
      CooldownSec="12" Damage="680" ManaCost="45" RequireLevel="15"
      CharacterKey="Character_ShadowKira"
      KnowledgeKey="Knowledge_ShadowStrike"
      UITextureName="skill_0002" />

  <SkillInfo Key="SKILL_003" StrKey="Skill_HolyShield"
      SkillName="성스러운 방패"
      SkillDesc="신성한 빛으로 아군을 보호하는 방어 마법.&lt;br/&gt;모든 어둠의 공격을 일정 시간 무효화한다.&lt;br/&gt;봉인된 도서관의 고대 방어 마법에서 유래했다."
      CooldownSec="20" Damage="0" ManaCost="60" RequireLevel="20"
      CharacterKey="Character_Drakmar"
      KnowledgeKey="Knowledge_HolyShield"
      UITextureName="skill_0003" />
</SkillInfoList>
```

2. Create `regioninfo_showcase.staticinfo.xml` with 5 regions:

```xml
<?xml version='1.0' encoding='UTF-8'?>
<RegionInfoList>
  <RegionInfo Key="REGION_001" StrKey="Region_SealedLibrary"
      RegionName="봉인된 도서관" RegionType="Dungeon"
      RegionDesc="고대 현자들이 금지된 지식을 봉인한 장소.&lt;br/&gt;강력한 마법 결계로 보호되어 있으며&lt;br/&gt;오직 현자의 결사만이 출입할 수 있다."
      RecommendLevel="40" KnowledgeKey="Knowledge_SealedLibrary"
      UITextureName="region_fortress" />

  <RegionInfo Key="REGION_002" StrKey="Region_BlackstarVillage"
      RegionName="흑성 마을" RegionType="Town"
      RegionDesc="모험가들의 거점이 되는 평화로운 마을.&lt;br/&gt;검은별 광석이 처음 발견된 곳으로&lt;br/&gt;대장장이 그림죠의 대장간이 있다."
      RecommendLevel="1" KnowledgeKey="Knowledge_BlackstarVillage"
      UITextureName="region_blackstar_village" />

  <RegionInfo Key="REGION_003" StrKey="Region_DragonTomb"
      RegionName="용의 무덤" RegionType="Dungeon"
      RegionDesc="태고의 용이 잠든 위험한 무덤.&lt;br/&gt;고대의 보물이 잠들어 있지만&lt;br/&gt;강력한 수호 마법이 침입자를 가로막는다."
      RecommendLevel="50" KnowledgeKey="Knowledge_DragonTomb"
      UITextureName="region_dragon_tomb" />

  <RegionInfo Key="REGION_004" StrKey="Region_SageTower"
      RegionName="현자의 탑" RegionType="Main"
      RegionDesc="세계의 균형을 지키는 현자들의 탑.&lt;br/&gt;현자의 결사 본부이자&lt;br/&gt;장로 바론이 수련하던 곳이다."
      RecommendLevel="30" KnowledgeKey="Knowledge_SageTower"
      UITextureName="region_sage_tower" />

  <RegionInfo Key="REGION_005" StrKey="Region_DarkCultHQ"
      RegionName="어둠의 교단 본부" RegionType="Fortress"
      RegionDesc="어둠의 세력이 모이는 비밀 요새.&lt;br/&gt;그림자 암살자 키라가 훈련받은 곳이며&lt;br/&gt;봉인된 도서관을 향한 음모가 꾸며지고 있다."
      RecommendLevel="60" KnowledgeKey="Knowledge_DarkCultHQ"
      UITextureName="region_dark_cult_hq" />
</RegionInfoList>
```

3. Create `questinfo_showcase.staticinfo.xml` with 3 quests:

```xml
<?xml version='1.0' encoding='UTF-8'?>
<QuestInfoList>
  <QuestInfo Key="QUEST_001" StrKey="Quest_SealedLibraryMain"
      QuestName="봉인의 비밀"
      QuestDesc="장로 바론이 봉인된 도서관에서 사라진 고대 두루마리의 행방을 조사해달라고 요청한다.&lt;br/&gt;어둠의 교단이 두루마리를 노리고 있다는 정보가 있다.&lt;br/&gt;도서관 깊숙한 곳에서 단서를 찾아야 한다."
      QuestType="Main" RequireLevel="35"
      RewardKey="Item_SealScroll" RegionKey="Region_SealedLibrary"
      NPCKey="Character_ElderVaron"
      KnowledgeKey="Knowledge_SealedLibrary" />

  <QuestInfo Key="QUEST_002" StrKey="Quest_BlackstarForge"
      QuestName="검은별의 단조"
      QuestDesc="대장장이 그림죠가 검은별 대검의 수리를 위해 희귀한 화산 광석을 구해달라고 부탁한다.&lt;br/&gt;화산 지대에서 광석을 채굴하고 무사히 돌아와야 한다."
      QuestType="Sub" RequireLevel="25"
      RewardKey="Item_BlackstarSword" RegionKey="Region_BlackstarVillage"
      NPCKey="Character_Grimjaw"
      KnowledgeKey="Knowledge_BlackstarSword" />

  <QuestInfo Key="QUEST_003" StrKey="Quest_ShadowInvestigation"
      QuestName="그림자의 추적"
      QuestDesc="정찰병 루네가 안개의 숲에서 어둠의 교단의 수상한 움직임을 발견한다.&lt;br/&gt;루네와 함께 숲을 탐색하고 교단의 음모를 파악해야 한다.&lt;br/&gt;위험한 적들이 곳곳에 도사리고 있으니 주의가 필요하다."
      QuestType="Sub" RequireLevel="20"
      RewardKey="Item_PlagueCure" RegionKey="Region_MistForest"
      NPCKey="Character_Lune"
      KnowledgeKey="Knowledge_MistForest" />
</QuestInfoList>
```

4. Fix character cross-refs in `characterinfo_showcase.staticinfo.xml`:
   - **Grimjaw** (CHAR_003): Add `RegionKey="Region_BlackstarVillage"` and `SkillKey="Skill_SacredFlame"` (learned from Elder Varon as Sage Order member — or use a different mapping if preferred, but must have the attribute)
   - Actually, Grimjaw is a Blacksmith, not a caster. Give him no specific skill from the 3 — but the locked decision says ALL 5 must have SkillKey. Assign `SkillKey="Skill_HolyShield"` (he forges the shield enchantments).
   - **Lune** (CHAR_004): Add `ItemKey="Item_SealScroll"` and `SkillKey="Skill_ShadowStrike"` (scouts learn shadow techniques for stealth). Actually better: assign `SkillKey="Skill_SacredFlame"` (as Sage Order member she learned protective magic). Use `SkillKey="Skill_SacredFlame"`.
   - **Drakmar** (CHAR_005): Add `ItemKey="Item_PlagueCure"` and `RegionKey="Region_SageTower"`.

   Final cross-ref table (all 5 complete):
   | Character | ItemKey | RegionKey | SkillKey | FactionKey | KnowledgeKey |
   |-----------|---------|-----------|----------|------------|--------------|
   | ElderVaron | Item_SageStaff | Region_SealedLibrary | Skill_SacredFlame | Faction_SageOrder | Knowledge_ElderVaron |
   | ShadowKira | Item_ShadowDagger | Region_SealedLibrary | Skill_ShadowStrike | Faction_DarkCult | Knowledge_ShadowKira |
   | Grimjaw | Item_BlackstarSword | Region_BlackstarVillage | Skill_HolyShield | Faction_SageOrder | Knowledge_Grimjaw |
   | Lune | Item_SealScroll | Region_SealedLibrary | Skill_SacredFlame | Faction_SageOrder | Knowledge_Lune |
   | Drakmar | Item_PlagueCure | Region_SageTower | Skill_HolyShield | Faction_SageOrder | Knowledge_Drakmar |

   Ensure the `skillinfo_showcase` directory exists: `mkdir -p tests/fixtures/mock_gamedata/StaticInfo/skillinfo` (and regioninfo, questinfo).
  </action>
  <verify>
    <automated>python3 -c "
from lxml import etree
files = [
  'tests/fixtures/mock_gamedata/StaticInfo/skillinfo/skillinfo_showcase.staticinfo.xml',
  'tests/fixtures/mock_gamedata/StaticInfo/regioninfo/regioninfo_showcase.staticinfo.xml',
  'tests/fixtures/mock_gamedata/StaticInfo/questinfo/questinfo_showcase.staticinfo.xml',
  'tests/fixtures/mock_gamedata/StaticInfo/characterinfo/characterinfo_showcase.staticinfo.xml',
]
for f in files:
  tree = etree.parse(f)
  print(f'{f}: OK ({len(tree.getroot())} elements)')
# Verify all 5 chars have complete cross-refs
tree = etree.parse(files[3])
for char in tree.getroot():
  if char.tag != 'CharacterInfo': continue
  name = char.get('StrKey')
  missing = [k for k in ['ItemKey','RegionKey','SkillKey','FactionKey','KnowledgeKey'] if not char.get(k)]
  status = 'COMPLETE' if not missing else f'MISSING: {missing}'
  print(f'  {name}: {status}')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `tests/fixtures/mock_gamedata/StaticInfo/skillinfo/skillinfo_showcase.staticinfo.xml` exists and contains `<SkillInfoList>` with 3 `<SkillInfo>` elements
    - File `tests/fixtures/mock_gamedata/StaticInfo/regioninfo/regioninfo_showcase.staticinfo.xml` exists and contains `<RegionInfoList>` with 5 `<RegionInfo>` elements
    - File `tests/fixtures/mock_gamedata/StaticInfo/questinfo/questinfo_showcase.staticinfo.xml` exists and contains `<QuestInfoList>` with 3 `<QuestInfo>` elements
    - Every `<SkillInfo>` has attributes: Key, StrKey, SkillName, SkillDesc, CooldownSec, Damage, ManaCost, RequireLevel, CharacterKey, KnowledgeKey, UITextureName
    - Every `<RegionInfo>` has attributes: Key, StrKey, RegionName, RegionDesc, RegionType, RecommendLevel, KnowledgeKey, UITextureName
    - Every `<QuestInfo>` has attributes: Key, StrKey, QuestName, QuestDesc, QuestType, RequireLevel, RewardKey, RegionKey, NPCKey, KnowledgeKey
    - In characterinfo_showcase: grep for `StrKey="Character_Grimjaw"` line contains `RegionKey="Region_BlackstarVillage"` and `SkillKey=`
    - In characterinfo_showcase: grep for `StrKey="Character_Lune"` line contains `ItemKey=` and `SkillKey=`
    - In characterinfo_showcase: grep for `StrKey="Character_Drakmar"` line contains `ItemKey=` and `RegionKey=`
    - All XML files parse without errors via `etree.parse()`
  </acceptance_criteria>
  <done>3 new XML entity files created with Korean names/descriptions/cross-refs. All 5 characters have complete cross-ref sets (ItemKey, RegionKey, SkillKey, FactionKey, KnowledgeKey).</done>
</task>

<task type="auto">
  <name>Task 2: Standardize FactionNode StrKeys to Region_ prefix and add route waypoints</name>
  <files>
    tests/fixtures/mock_gamedata/StaticInfo/factioninfo/FactionInfo.staticinfo.xml,
    tests/fixtures/mock_gamedata/StaticInfo/factioninfo/NodeWaypointInfo/NodeWaypointInfo.staticinfo.xml
  </files>
  <read_first>
    tests/fixtures/mock_gamedata/StaticInfo/factioninfo/FactionInfo.staticinfo.xml,
    tests/fixtures/mock_gamedata/StaticInfo/factioninfo/NodeWaypointInfo/NodeWaypointInfo.staticinfo.xml,
    .planning/phases/43-mockdata-quality-audit-wow-amplification/43-CONTEXT.md
  </read_first>
  <action>
1. In `FactionInfo.staticinfo.xml`, update ALL 10 FactionNode elements:
   - Change StrKey from lowercase to PascalCase with Region_ prefix
   - Change KnowledgeKey from lowercase to PascalCase with Knowledge_ prefix

   Mapping (old -> new):
   | Old StrKey | New StrKey | Old KnowledgeKey | New KnowledgeKey |
   |------------|-----------|------------------|------------------|
   | mist_forest | Region_MistForest | knowledge_mist_forest | Knowledge_MistForest |
   | sealed_library | Region_SealedLibrary | knowledge_sealed_library | Knowledge_SealedLibrary |
   | blackstar_village | Region_BlackstarVillage | knowledge_blackstar_village | Knowledge_BlackstarVillage |
   | dragon_tomb | Region_DragonTomb | knowledge_dragon_tomb | Knowledge_DragonTomb |
   | sage_tower | Region_SageTower | knowledge_sage_tower | Knowledge_SageTower |
   | dark_cult_hq | Region_DarkCultHQ | knowledge_dark_cult_hq | Knowledge_DarkCultHQ |
   | wind_canyon | Region_WindCanyon | knowledge_wind_canyon | Knowledge_WindCanyon |
   | forgotten_fortress | Region_ForgottenFortress | knowledge_forgotten_fortress | Knowledge_ForgottenFortress |
   | moonlight_lake | Region_MoonlightLake | knowledge_moonlight_lake | Knowledge_MoonlightLake |
   | volcanic_zone | Region_VolcanicZone | knowledge_volcanic_zone | Knowledge_VolcanicZone |

2. In `NodeWaypointInfo.staticinfo.xml`, update ALL 13 routes:
   - Change FromNodeKey and ToNodeKey to match new FactionNode StrKeys
   - Add 2-3 `<Waypoint>` child elements to 6 routes for curved paths

   Waypoint coordinate calculation: take WorldPosition of from/to nodes, create intermediate points offset perpendicular to the line between them.

   Routes that MUST get waypoints (the 6 longest/most interesting routes):
   - Region_MistForest -> Region_SealedLibrary: waypoints at (200,0,180) and (280,0,160)
   - Region_BlackstarVillage -> Region_DarkCultHQ: waypoints at (560,0,260) and (600,0,230)
   - Region_MoonlightLake -> Region_DragonTomb: waypoints at (130,0,430) and (160,0,470)
   - Region_SageTower -> Region_WindCanyon: waypoints at (370,0,520) and (340,0,580)
   - Region_WindCanyon -> Region_ForgottenFortress: waypoints at (400,0,640) and (480,0,600)
   - Region_VolcanicZone -> Region_DarkCultHQ: waypoints at (720,0,380) and (700,0,300)

   Also add 4 new map nodes as FactionNode entries for spatial density:
   - Region_TradingPost: Type="Town", WorldPosition="250,0,350", DangerLevel="1", NameKR="교역소", NameEN="Trading Post"
   - Region_AncientTemple: Type="Dungeon", WorldPosition="500,0,500", DangerLevel="3", NameKR="고대 신전", NameEN="Ancient Temple"
   - Region_Watchtower: Type="Fortress", WorldPosition="450,0,100", DangerLevel="2", NameKR="감시탑", NameEN="Watchtower"
   - Region_MiningCamp: Type="Sub", WorldPosition="700,0,600", DangerLevel="2", NameKR="광산 캠프", NameEN="Mining Camp"

   Each new node needs a Polygon (hexagon shape centered on WorldPosition) and KnowledgeKey in PascalCase format.

   Add 4 new routes connecting the new nodes to existing ones.
  </action>
  <verify>
    <automated>python3 -c "
from lxml import etree
# Verify FactionInfo
tree = etree.parse('tests/fixtures/mock_gamedata/StaticInfo/factioninfo/FactionInfo.staticinfo.xml')
nodes = tree.getroot().findall('FactionNode')
print(f'FactionNodes: {len(nodes)}')
for n in nodes:
  sk = n.get('StrKey','')
  kk = n.get('KnowledgeKey','')
  ok = sk.startswith('Region_') and kk.startswith('Knowledge_')
  print(f'  {sk}: KnowledgeKey={kk} {\"OK\" if ok else \"BROKEN\"} ')

# Verify routes
tree2 = etree.parse('tests/fixtures/mock_gamedata/StaticInfo/factioninfo/NodeWaypointInfo/NodeWaypointInfo.staticinfo.xml')
routes = tree2.getroot().findall('NodeWaypointInfo')
print(f'Routes: {len(routes)}')
with_waypoints = sum(1 for r in routes if len(r.findall('Waypoint')) > 0)
print(f'Routes with waypoints: {with_waypoints}')
for r in routes:
  fk = r.get('FromNodeKey','')
  tk = r.get('ToNodeKey','')
  ok = fk.startswith('Region_') and tk.startswith('Region_')
  wps = len(r.findall('Waypoint'))
  print(f'  {fk} -> {tk}: {wps} waypoints {\"OK\" if ok else \"BROKEN\"}')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - All FactionNode StrKey values start with "Region_" (grep for `StrKey="Region_` should return 14 matches — 10 original + 4 new)
    - All FactionNode KnowledgeKey values start with "Knowledge_" (grep for `KnowledgeKey="Knowledge_` returns 14 matches)
    - Zero FactionNode StrKeys use lowercase format (grep for `StrKey="mist_forest"` returns 0 matches)
    - All NodeWaypointInfo FromNodeKey/ToNodeKey start with "Region_" (no lowercase keys remain)
    - At least 6 routes have `<Waypoint>` child elements
    - At least 14 FactionNode entries exist (10 original + 4 new)
    - At least 17 routes exist (13 original + 4 new)
    - Both XML files parse without errors via `etree.parse()`
  </acceptance_criteria>
  <done>FactionNode StrKeys standardized to Region_ PascalCase. NodeWaypointInfo keys updated to match. 6 routes have curved waypoints. 4 new map nodes added for spatial density.</done>
</task>

</tasks>

<verification>
All XML files in `tests/fixtures/mock_gamedata/StaticInfo/` parse cleanly with lxml. Cross-ref StrKey values use consistent `{EntityType}_{EntityName}` PascalCase format. All 5 characters have complete cross-ref attribute sets.
</verification>

<success_criteria>
- 3 new XML files created (SkillInfo, RegionInfo, QuestInfo) with Korean content
- All 5 characters have ItemKey, RegionKey, SkillKey, FactionKey, KnowledgeKey
- FactionNode/NodeWaypointInfo keys use Region_ prefix format
- 6+ routes have waypoint coordinates for curved rendering
- 4 new map nodes add spatial density
</success_criteria>

<output>
After completion, create `.planning/phases/43-mockdata-quality-audit-wow-amplification/43-01-SUMMARY.md`
</output>
