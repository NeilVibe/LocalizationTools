---
phase: 43-mockdata-quality-audit-wow-amplification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/mock_gamedata/localization/showcase_items.loc.xml
  - tests/fixtures/mock_gamedata/localization/showcase_dialogue.txt
  - tests/fixtures/mock_gamedata/localization/generate_showcase_data.py
  - server/tools/ldm/services/mock_tm_loader.py
autonomous: true
requirements:
  - MOCK-AUDIT-04

must_haves:
  truths:
    - "XML localization file has 40+ LocStr entries including quest strings and placeholder variables"
    - "TXT dialogue file has 30+ lines including multi-line entries with <br/>"
    - "Excel generator adds 3 rows with empty Korean translation for untranslated highlighting demo"
    - "TM loader has ~50 entries including multi-line, dialogue, near-duplicate fuzzy pairs, and Quest/Dialogue context tags"
    - "Grimjaw Korean name is '그림죠' consistently in all files"
  artifacts:
    - path: "tests/fixtures/mock_gamedata/localization/showcase_items.loc.xml"
      provides: "40+ LocStr entries with quest strings, placeholders, multi-line descriptions"
      contains: "QuestInfoList"
    - path: "server/tools/ldm/services/mock_tm_loader.py"
      provides: "~50 TM entries with Quest/Dialogue contexts"
      contains: "Dialogue"
  key_links:
    - from: "showcase_items.loc.xml"
      to: "mock_tm_loader.py"
      via: "TM source strings overlap with LocStr content for demo matching"
      pattern: "source.*Sealed Library"
---

<objective>
Enrich localization strings across all 3 formats (XML, TXT, XLSX) with quest content, placeholder variables, and multi-line entries. Expand TM from 35 to ~50 entries with dialogue contexts, near-duplicate fuzzy pairs, and multi-line content.

Purpose: The LanguageData Grid demo needs rich, diverse strings that showcase every TM matching tier (exact, high-fuzzy, medium-fuzzy, semantic) and format-specific features (placeholders, multi-line, untranslated rows).
Output: Enriched localization files + expanded TM entry loader.
</objective>

<execution_context>
@/home/<USERNAME>/.claude/get-shit-done/workflows/execute-plan.md
@/home/<USERNAME>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-mockdata-quality-audit-wow-amplification/43-CONTEXT.md
@.planning/phases/43-mockdata-quality-audit-wow-amplification/43-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add quest strings, placeholders, and multi-line entries to localization files</name>
  <files>
    tests/fixtures/mock_gamedata/localization/showcase_items.loc.xml,
    tests/fixtures/mock_gamedata/localization/showcase_dialogue.txt,
    tests/fixtures/mock_gamedata/localization/generate_showcase_data.py
  </files>
  <read_first>
    tests/fixtures/mock_gamedata/localization/showcase_items.loc.xml,
    tests/fixtures/mock_gamedata/localization/showcase_dialogue.txt,
    tests/fixtures/mock_gamedata/localization/generate_showcase_data.py,
    .planning/phases/43-mockdata-quality-audit-wow-amplification/43-CONTEXT.md
  </read_first>
  <action>
1. Add these LocStr entries to `showcase_items.loc.xml` (append before closing `</LanguageData>`):

**Quest strings (8 entries):**
```xml
<LocStr StringID="QUEST_SEALED_LIBRARY_NAME" StrOrigin="The Sealed Library" Str="봉인의 비밀"/>
<LocStr StringID="QUEST_SEALED_LIBRARY_OBJ" StrOrigin="Find Elder Varon in the Sealed Library and retrieve the ancient scroll." Str="봉인된 도서관에서 장로 바론을 찾아 고대 두루마리를 되찾으세요."/>
<LocStr StringID="QUEST_SEALED_LIBRARY_DONE" StrOrigin="Thank you, {CharacterName}. The scroll is safe once more." Str="{CharacterName}, 감사합니다. 두루마리가 다시 안전해졌습니다."/>
<LocStr StringID="QUEST_BLACKSTAR_FORGE_NAME" StrOrigin="The Blackstar Forge" Str="검은별의 단조"/>
<LocStr StringID="QUEST_BLACKSTAR_FORGE_OBJ" StrOrigin="Collect {0} pieces of volcanic ore from the Volcanic Zone for Grimjaw." Str="그림죠를 위해 화산 지대에서 화산 광석 {0}개를 수집하세요."/>
<LocStr StringID="QUEST_SHADOW_INVESTIGATION_NAME" StrOrigin="Shadow Investigation" Str="그림자의 추적"/>
<LocStr StringID="QUEST_SHADOW_INVESTIGATION_OBJ" StrOrigin="Investigate the Dark Cult activity in the Mist Forest with Scout Lune." Str="정찰병 루네와 함께 안개의 숲에서 어둠의 교단 활동을 조사하세요."/>
<LocStr StringID="QUEST_SHADOW_INVESTIGATION_DONE" StrOrigin="We found their hideout, {CharacterName}! Report back to Elder Varon immediately." Str="은신처를 찾았습니다, {CharacterName}! 즉시 장로 바론에게 보고하세요."/>
```

**Multi-line description (2 entries):**
```xml
<LocStr StringID="REGION_SEALED_LIBRARY_DESC" StrOrigin="An ancient library sealed by the Sage Order.&lt;br/&gt;Contains forbidden knowledge too dangerous for mortals.&lt;br/&gt;Only those with the Sage Order's blessing may enter." Str="현자의 결사가 봉인한 고대 도서관.&lt;br/&gt;필멸자에게는 너무 위험한 금지된 지식이 담겨 있다.&lt;br/&gt;현자의 결사의 축복을 받은 자만이 들어갈 수 있다."/>
<LocStr StringID="SKILL_SACRED_FLAME_LORE" StrOrigin="The Sacred Flame has been passed down through generations of the Sage Order.&lt;br/&gt;It purifies darkness and protects the innocent.&lt;br/&gt;Elder Varon is the current master of this ancient art." Str="성스러운 불꽃은 현자의 결사에서 대대로 전승되어 왔다.&lt;br/&gt;어둠을 정화하고 무고한 자를 보호한다.&lt;br/&gt;장로 바론이 이 고대 기술의 현재 달인이다."/>
```

Total after additions: 30 existing + 10 new = 40 LocStr entries.

2. Add multi-line dialogue to `showcase_dialogue.txt` (append at end):

```
[Grimjaw] 검은별 대검은 내 필생의 역작이야.<br/>이 검을 들 자격이 있는 자는 아직 나타나지 않았지.<br/>하지만 곧 그때가 올 거야.
[Lune] 숲의 동쪽에서 수상한 기운이 느껴져.<br/>어둠의 교단이 움직이기 시작한 것 같아.
[Drakmar] 이 고대 문서에 따르면 봉인의 열쇠는 세 가지야.<br/>성스러운 불꽃, 검은별 금속, 그리고 현자의 의지.
[Varon] {CharacterName}, 봉인된 도서관에 오신 것을 환영합니다.<br/>여기에는 세계의 비밀이 담겨 있습니다.
```

3. Update `generate_showcase_data.py` to add 3 rows with EMPTY Korean translation to the XLSX for untranslated highlighting demo. Find the section that generates Excel rows and add:

```python
# Untranslated rows for demo
{"string_id": "UI_NEW_FEATURE_LABEL", "source": "New Feature Coming Soon", "target": ""},
{"string_id": "TOOLTIP_UNKNOWN_ITEM", "source": "This item has not been identified yet.", "target": ""},
{"string_id": "QUEST_HIDDEN_OBJECTIVE", "source": "Discover the hidden path to the Dragon's Tomb.", "target": ""},
```

Verify Grimjaw is "그림죠" (not "그림조") in all 3 localization files. It's already "그림죠" in showcase_items.loc.xml (CHAR_GRIMJAW_NAME line). Check dialogue.txt and fix if needed.
  </action>
  <verify>
    <automated>python3 -c "
from lxml import etree
# Count LocStr entries
tree = etree.parse('tests/fixtures/mock_gamedata/localization/showcase_items.loc.xml')
entries = tree.getroot().findall('LocStr')
print(f'LocStr entries: {len(entries)}')
quest = [e for e in entries if 'QUEST' in e.get('StringID','')]
placeholder = [e for e in entries if '{' in (e.get('Str','') + e.get('StrOrigin',''))]
multiline = [e for e in entries if '&lt;br/&gt;' in (e.get('Str','') or '') or '<br/>' in (e.get('Str','') or '')]
print(f'Quest strings: {len(quest)}')
print(f'Placeholder strings: {len(placeholder)}')
print(f'Multi-line strings: {len(multiline)}')

# Count dialogue lines
with open('tests/fixtures/mock_gamedata/localization/showcase_dialogue.txt') as f:
  lines = [l for l in f.readlines() if l.strip()]
print(f'Dialogue lines: {len(lines)}')
br_lines = [l for l in lines if '<br/>' in l]
print(f'Multi-line dialogue entries: {len(br_lines)}')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `showcase_items.loc.xml` contains 40+ `<LocStr>` elements (grep -c "LocStr" returns >= 40)
    - At least 6 entries have StringID starting with "QUEST_" (grep -c 'StringID="QUEST_' returns >= 6)
    - At least 2 entries contain `{CharacterName}` or `{0}` placeholder variables
    - At least 4 entries contain `&lt;br/&gt;` for multi-line content
    - `showcase_dialogue.txt` has 30+ non-empty lines
    - At least 2 dialogue lines contain `<br/>` for multi-line
    - `generate_showcase_data.py` contains 3 rows with empty target string `"target": ""`
    - Grimjaw Korean name "그림죠" appears consistently (not "그림조")
  </acceptance_criteria>
  <done>XML has 40+ strings with quest/placeholder/multi-line content. TXT has 30+ lines with multi-line dialogue. XLSX generator adds 3 untranslated rows.</done>
</task>

<task type="auto">
  <name>Task 2: Expand TM entries to ~50 with dialogue, near-duplicates, and Quest/Dialogue contexts</name>
  <files>
    server/tools/ldm/services/mock_tm_loader.py
  </files>
  <read_first>
    server/tools/ldm/services/mock_tm_loader.py,
    .planning/phases/43-mockdata-quality-audit-wow-amplification/43-CONTEXT.md
  </read_first>
  <action>
Add 15 new TM entries to the `entries` list in `load_tm_entries()` function, bringing total from 35 to 50. The new entries must include:

**Multi-line content with `<br/>` (5 entries):**
```python
{"source": "A blade forged in the heart of a dying star.<br/>Its edge cuts through both flesh and shadow.", "target": "죽어가는 별의 심장에서 단조된 검.<br/>그 날은 살과 그림자를 모두 베어냅니다.", "context": "Item"},
{"source": "The Sealed Library holds ancient knowledge.<br/>Only the Sage Order may enter its halls.", "target": "봉인된 도서관은 고대의 지식을 간직하고 있다.<br/>현자의 결사만이 그 전당에 들어갈 수 있다.", "context": "Region"},
{"source": "Sacred Flame purifies darkness.<br/>It has been passed down for generations.", "target": "성스러운 불꽃은 어둠을 정화한다.<br/>대대로 전승되어 온 마법이다.", "context": "Skill"},
{"source": "The mist never lifts in this ancient forest.<br/>Strange creatures lurk in the shadows.", "target": "이 고대 숲에서는 안개가 걷히지 않는다.<br/>기이한 생물들이 그림자 속에 도사리고 있다.", "context": "Region"},
{"source": "Grimjaw's forge burns day and night.<br/>The sound of his hammer echoes through the village.", "target": "그림죠의 대장간은 밤낮으로 타오른다.<br/>그의 망치 소리가 마을 전체에 울려 퍼진다.", "context": "Dialogue"},
```

**Dialogue-related entries (5 entries):**
```python
{"source": "Welcome to the Sealed Library, traveler.", "target": "봉인된 도서관에 오신 것을 환영합니다, 여행자여.", "context": "Dialogue"},
{"source": "The Dark Cult grows stronger each day.", "target": "어둠의 교단은 날마다 강해지고 있습니다.", "context": "Dialogue"},
{"source": "Take this scroll to Elder Varon.", "target": "이 두루마리를 장로 바론에게 가져가세요.", "context": "Dialogue"},
{"source": "Be careful in the Mist Forest at night.", "target": "밤에 안개의 숲에서는 조심하세요.", "context": "Dialogue"},
{"source": "The blackstar ore can only be found in the volcanic zone.", "target": "검은별 광석은 화산 지대에서만 찾을 수 있습니다.", "context": "Dialogue"},
```

**Near-duplicate fuzzy pairs (3 entries, 95%+ match with existing):**
```python
{"source": "Welcome to the Realm of the Stars!", "target": "별의 왕국에 오신 것을 환영합니다!", "context": "UI"},
{"source": "The Blackstar Sword", "target": "흑성검", "context": "Item"},
{"source": "Elder Varon the Sage", "target": "현자 장로 바론", "context": "Character"},
```

**Quest context entries (2 entries):**
```python
{"source": "Find the ancient scroll in the Sealed Library.", "target": "봉인된 도서관에서 고대 두루마리를 찾으세요.", "context": "Quest"},
{"source": "Collect volcanic ore for the blacksmith.", "target": "대장장이를 위해 화산 광석을 수집하세요.", "context": "Quest"},
```

Also update the idempotency check: change `if count >= 30:` to `if count >= 45:` so re-running loads the new entries.

Update the docstring to say "50+ entries" instead of "30+ entries".
  </action>
  <verify>
    <automated>grep -c '"source"' server/tools/ldm/services/mock_tm_loader.py</automated>
  </verify>
  <acceptance_criteria>
    - `mock_tm_loader.py` contains 50 entries in the `entries` list (grep -c '"source"' returns 50)
    - At least 5 entries have `"context": "Dialogue"` (grep -c '"Dialogue"' returns >= 5)
    - At least 2 entries have `"context": "Quest"` (grep -c '"Quest"' returns >= 2)
    - At least 5 entries contain `<br/>` in source or target strings
    - The idempotency check uses `>= 45` (grep for "count >= 45" returns 1 match)
    - Grimjaw Korean name "그림죠" used consistently in TM entries (not "그림조")
  </acceptance_criteria>
  <done>TM expanded from 35 to 50 entries. New entries include multi-line with `<br/>`, dialogue context, quest context, and near-duplicate fuzzy pairs for fine-grained scoring demo.</done>
</task>

</tasks>

<verification>
All 3 localization files have 25+ entries each. TM has ~50 entries covering exact, fuzzy, semantic, multi-line, dialogue, and quest contexts. Placeholder variables and untranslated rows exist for demo features.
</verification>

<success_criteria>
- XML: 40+ LocStr entries with quest strings, placeholders, multi-line
- TXT: 30+ dialogue lines with multi-line entries
- XLSX generator: 3 untranslated rows added
- TM: 50 entries with Dialogue/Quest contexts, near-duplicate fuzzy pairs, multi-line content
- Grimjaw = "그림죠" everywhere
</success_criteria>

<output>
After completion, create `.planning/phases/43-mockdata-quality-audit-wow-amplification/43-02-SUMMARY.md`
</output>
