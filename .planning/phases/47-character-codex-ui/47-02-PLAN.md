---
phase: 47-character-codex-ui
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - locaNext/src/lib/components/pages/CharacterCodexPage.svelte
  - locaNext/src/lib/components/ldm/CharacterCodexDetail.svelte
  - locaNext/src/lib/stores/navigation.js
  - locaNext/src/lib/components/apps/LDM.svelte
autonomous: true
requirements: [CHAR-01, CHAR-02, CHAR-03, CHAR-04]
must_haves:
  truths:
    - "User sees a card grid of characters with portrait images, Korean names, and category badges"
    - "User can click category tabs (All, NPC, MONSTER, etc.) to filter characters"
    - "User can type in search box and see results update as they type"
    - "Clicking a character card opens a detail panel with Race/Gender/Age/Job and knowledge tabs"
    - "Characters page is accessible from LDM sidebar navigation"
  artifacts:
    - path: "locaNext/src/lib/components/pages/CharacterCodexPage.svelte"
      provides: "Character Codex main page with grid, tabs, search, infinite scroll"
      min_lines: 200
    - path: "locaNext/src/lib/components/ldm/CharacterCodexDetail.svelte"
      provides: "Character detail panel with metadata and knowledge resolution tabs"
      min_lines: 150
    - path: "locaNext/src/lib/stores/navigation.js"
      provides: "goToCharacterCodex navigation function"
      contains: "goToCharacterCodex"
    - path: "locaNext/src/lib/components/apps/LDM.svelte"
      provides: "Character Codex page rendering and sidebar entry"
      contains: "CharacterCodexPage"
  key_links:
    - from: "locaNext/src/lib/components/pages/CharacterCodexPage.svelte"
      to: "/api/ldm/codex/characters"
      via: "fetch calls to backend API"
      pattern: "codex/characters"
    - from: "locaNext/src/lib/components/apps/LDM.svelte"
      to: "locaNext/src/lib/components/pages/CharacterCodexPage.svelte"
      via: "dynamic import and currentPage === 'character-codex'"
      pattern: "character-codex"
---

<objective>
Create Character Codex frontend UI -- Svelte 5 page with card grid, category tabs, search, infinite scroll, and detail panel with knowledge resolution.

Purpose: Give users a visual character encyclopedia, following the identical pattern as Phase 46 ItemCodexPage/ItemCodexDetail.
Output: 2 Svelte components + navigation wiring into LDM app.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-character-codex-ui/47-CONTEXT.md
@.planning/phases/47-character-codex-ui/47-01-SUMMARY.md

<interfaces>
<!-- API contract from Plan 01 -->

GET /api/ldm/codex/characters/categories -> CharacterCategoryResponse:
```json
{
  "categories": [{"category": "NPC", "count": 3500}, {"category": "MONSTER", "count": 2000}],
  "total_characters": 8000
}
```

GET /api/ldm/codex/characters?offset=0&limit=50&category=NPC&q=search&lang=eng -> CharacterListResponse:
```json
{
  "characters": [{
    "strkey": "CharInfo_Varon",
    "name_kr": "바론",
    "name_translated": "Varon",
    "desc_kr": "...",
    "category": "NPC",
    "race": "Human",
    "gender": "Male",
    "image_url": "/api/ldm/mapdata/thumbnail/cd_knowledgeimage_varon",
    "source_file": "characterinfo_npc.staticinfo.xml"
  }],
  "total": 3500,
  "offset": 0,
  "limit": 50,
  "has_more": true,
  "category_filter": "NPC"
}
```

GET /api/ldm/codex/characters/{strkey}?lang=eng -> CharacterDetailResponse:
```json
{
  "strkey": "CharInfo_Varon",
  "name_kr": "바론",
  "name_translated": "Varon",
  "desc_kr": "...",
  "category": "NPC",
  "race": "Human",
  "gender": "Male",
  "age": "Adult",
  "job": "Job_Scholar",
  "use_macro": "Macro_NPC_Human_Male",
  "image_url": "...",
  "source_file": "...",
  "knowledge_key": "...",
  "knowledge_pass_0": [],
  "knowledge_pass_1": [],
  "knowledge_pass_2": [],
  "related_entities": []
}
```

<!-- Svelte components to reuse (already exist) -->
From locaNext/src/lib/components/ldm/CodexCard.svelte:
  Props: entity, index, apiBase, failedImages, onclick, onfailimage

From locaNext/src/lib/components/common/InfiniteScroll.svelte:
  Props: onloadmore, loading, hasMore

From locaNext/src/lib/components/common/SkeletonCard.svelte:
  Props: count

From locaNext/src/lib/components/common (barrel export):
  PageHeader, ErrorState

From locaNext/src/lib/stores/navigation.js:
  currentPage writable store, pattern: goToItemCodex sets currentPage to 'item-codex'

From locaNext/src/lib/components/apps/LDM.svelte:
  Pattern: import page component, add {:else if $currentPage === 'page-id'} block
  Sidebar: add goToCharacterCodex to imports + add sidebar button
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CharacterCodexPage + CharacterCodexDetail Svelte components</name>
  <files>locaNext/src/lib/components/pages/CharacterCodexPage.svelte, locaNext/src/lib/components/ldm/CharacterCodexDetail.svelte</files>
  <read_first>
    - locaNext/src/lib/components/pages/ItemCodexPage.svelte (EXACT pattern to replicate)
    - locaNext/src/lib/components/ldm/ItemCodexDetail.svelte (EXACT detail pattern to replicate)
  </read_first>
  <action>
**CharacterCodexPage.svelte** -- Clone ItemCodexPage.svelte with these changes:

1. **Imports:** Replace ItemCodexDetail with CharacterCodexDetail. Import `UserMultiple` icon from carbon-icons-svelte (instead of Catalog). Keep CodexCard, InfiniteScroll, SkeletonCard, PageHeader, ErrorState.

2. **State:** Same $state variables as ItemCodexPage (categories instead of groups, activeCategory instead of activeGroup). Same PAGE_SIZE=50, same debounced search via $effect, same abort controller pattern.

3. **fetchCategories()** -- replaces fetchGroups():
   - GET `${API_BASE}/api/ldm/codex/characters/categories`
   - Store categories array and total_characters count

4. **fetchCharacters(page, category, query)** -- replaces fetchItems():
   - GET `${API_BASE}/api/ldm/codex/characters?offset=...&limit=...&category=...&q=...`
   - Same pagination/append logic as fetchItems

5. **fetchCharacterDetail(strkey)** -- replaces fetchItemDetail():
   - GET `${API_BASE}/api/ldm/codex/characters/${strkey}`

6. **Category tabs:** Render from categories array (not tree). Each tab shows category name + count. "All" tab shows total. activeCategory filters the list.

7. **toCardEntity(char):** Transform CharacterCardResponse to CodexCard entity shape:
   - entity_type: 'character'
   - description: build from name_translated + race/gender badges if present

8. **Card grid, InfiniteScroll, detail view:** Identical structure to ItemCodexPage. Detail view uses CharacterCodexDetail component.

9. **PageHeader:** icon={UserMultiple} title="Character Codex"

10. **Styles:** Copy all styles from ItemCodexPage, rename .item-codex-* to .character-codex-*

**CharacterCodexDetail.svelte** -- Clone ItemCodexDetail.svelte with these changes:

1. **Props:** `let { character = null, onback = () => {}, onsimilar = () => {} } = $props();` (renamed from `item`)

2. **Character-specific metadata section** (replaces group_name display):
   - Race badge (Tag type="teal"): character.race if present
   - Gender badge (Tag type="purple"): character.gender if present
   - Age badge (Tag type="warm-gray"): character.age if present (strip "Age_" prefix if present)
   - Job badge (Tag type="cyan"): character.job if present (strip "Job_" prefix if present)
   - Category badge (Tag type="magenta"): character.category

3. **Knowledge tabs:** Same 4-tab pattern as ItemCodexDetail:
   - Knowledge tab: Pass 0 + Pass 1 (same rendering)
   - Related tab: Pass 2 (same rendering)
   - NO InspectData tab (characters don't have InspectData) -- replace with Attributes tab showing use_macro raw value
   - Info tab: StrKey, Knowledge Key, Source File, Use Macro

4. **Tab counts derived:** knowledgeCount, relatedCount (same), attributeCount = use_macro ? 1 : 0

5. **Entity type tag:** `<Tag type="teal" size="sm">character</Tag>` instead of cyan/item

6. **Styles:** Copy from ItemCodexDetail, rename .item-detail to .character-detail

Use Svelte 5 Runes ONLY ($state, $derived, $effect, $props). ALWAYS use keys in {#each}.
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools/locaNext && npx svelte-check --threshold warning --filter-warnings "src/lib/components/pages/CharacterCodexPage.svelte" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "character-codex-page" locaNext/src/lib/components/pages/CharacterCodexPage.svelte
    - grep -q "codex/characters/categories" locaNext/src/lib/components/pages/CharacterCodexPage.svelte
    - grep -q "codex/characters" locaNext/src/lib/components/pages/CharacterCodexPage.svelte
    - grep -q "CharacterCodexDetail" locaNext/src/lib/components/pages/CharacterCodexPage.svelte
    - grep -q "\\$state" locaNext/src/lib/components/pages/CharacterCodexPage.svelte
    - grep -q "\\$props" locaNext/src/lib/components/ldm/CharacterCodexDetail.svelte
    - grep -q "race" locaNext/src/lib/components/ldm/CharacterCodexDetail.svelte
    - grep -q "gender" locaNext/src/lib/components/ldm/CharacterCodexDetail.svelte
    - grep -q "knowledge_pass_0" locaNext/src/lib/components/ldm/CharacterCodexDetail.svelte
  </acceptance_criteria>
  <done>CharacterCodexPage renders card grid with category tabs, search, infinite scroll. CharacterCodexDetail shows character metadata + knowledge resolution tabs.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Character Codex into LDM navigation</name>
  <files>locaNext/src/lib/stores/navigation.js, locaNext/src/lib/components/apps/LDM.svelte</files>
  <read_first>
    - locaNext/src/lib/stores/navigation.js (find goToItemCodex pattern near line 142)
    - locaNext/src/lib/components/apps/LDM.svelte (find ItemCodexPage import, routing block, sidebar button)
  </read_first>
  <action>
**navigation.js:**
Add `goToCharacterCodex` function right after `goToItemCodex` (around line 146):
```javascript
/**
 * Phase 47: Navigate to Character Codex page
 */
export function goToCharacterCodex() {
  currentPage.set('character-codex');
  openFile.set(null);
}
```

**LDM.svelte:**

1. **Import goToCharacterCodex** -- add to the navigation import line (same line as goToItemCodex)

2. **Import CharacterCodexPage** -- add after ItemCodexPage import:
   ```
   // Phase 47: Character Codex page
   import CharacterCodexPage from "$lib/components/pages/CharacterCodexPage.svelte";
   ```

3. **Add routing block** -- add `{:else if $currentPage === 'character-codex'}` block right after the `item-codex` block:
   ```svelte
   {:else if $currentPage === 'character-codex'}
     <!-- Phase 47: Character Codex -->
     <CharacterCodexPage />
   ```

4. **Add sidebar button** -- find the Item Codex sidebar button (look for goToItemCodex onclick), add a Character Codex button right after it with:
   - Same button structure/classes
   - onclick={() => goToCharacterCodex()}
   - Icon: UserMultiple from carbon-icons-svelte (add to imports if not present)
   - Label: "Characters"
   - active class: $currentPage === 'character-codex'

Import UserMultiple from carbon-icons-svelte if not already imported.
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools/locaNext && npx svelte-check --threshold error 2>&1 | grep -c "Error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "goToCharacterCodex" locaNext/src/lib/stores/navigation.js
    - grep -q "character-codex" locaNext/src/lib/stores/navigation.js
    - grep -q "CharacterCodexPage" locaNext/src/lib/components/apps/LDM.svelte
    - grep -q "goToCharacterCodex" locaNext/src/lib/components/apps/LDM.svelte
    - grep -q "character-codex" locaNext/src/lib/components/apps/LDM.svelte
  </acceptance_criteria>
  <done>Character Codex accessible from LDM sidebar, page renders when navigated to, routing works alongside Item Codex</done>
</task>

</tasks>

<verification>
Full frontend build check:
```bash
cd /home/neil1988/LocalizationTools/locaNext && npx svelte-check --threshold error 2>&1 | tail -10
```

Navigation wiring check:
```bash
grep -c "character-codex" locaNext/src/lib/components/apps/LDM.svelte
# Expected: at least 2 (routing + sidebar)
```
</verification>

<success_criteria>
- CharacterCodexPage.svelte displays card grid with portraits, Korean/translated names, category badges
- Category tabs (All, NPC, MONSTER, etc.) filter the character list with counts
- Search box filters across character names, StrKey, and attribute fields with debounced updates
- Clicking a card opens CharacterCodexDetail with Race/Gender/Age/Job badges and knowledge tabs
- Character Codex is reachable from LDM sidebar navigation
- All Svelte 5 Runes used ($state, $derived, $effect, $props), keys in all {#each} blocks
- svelte-check passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/47-character-codex-ui/47-02-SUMMARY.md`
</output>
