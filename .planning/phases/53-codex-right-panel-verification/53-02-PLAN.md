---
phase: 53-codex-right-panel-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - locaNext/src/lib/components/ldm/RightPanel.svelte
  - locaNext/src/lib/components/ldm/ImageTab.svelte
  - locaNext/src/lib/components/ldm/AudioTab.svelte
  - locaNext/src/lib/components/pages/GridPage.svelte
autonomous: true
requirements: [RPANEL-01, RPANEL-02]

must_haves:
  truths:
    - "Image tab shows entity DDS portrait when selecting a LDM row with linked StringID"
    - "Audio tab plays WEM audio with script text when selecting a row with available audio"
  artifacts:
    - path: "screenshots/53-rpanel-image.png"
      provides: "Visual proof Image tab shows entity portrait for linked StringID"
    - path: "screenshots/53-rpanel-audio.png"
      provides: "Visual proof Audio tab shows audio player with script text"
  key_links:
    - from: "GridPage.svelte"
      to: "RightPanel.svelte"
      via: "selectedRow prop"
      pattern: "selectedRow"
    - from: "ImageTab.svelte"
      to: "/api/ldm/codex/items/{strkey}"
      via: "MegaIndex C7->C1 chain (StringID->entity->image)"
      pattern: "fetch.*image|strkey_to_image"
    - from: "AudioTab.svelte"
      to: "/api/ldm/audio/stream"
      via: "MegaIndex C3 (StringID->audio path)"
      pattern: "fetch.*audio|stringid_to_audio"
---

<objective>
Verify RightPanel Image and Audio tabs display linked entity data when selecting LanguageData rows.

Purpose: Confirm the Phase 50 StringID-to-Audio/Image integration works end-to-end: selecting a LDM grid row with a linked entity shows its DDS portrait in Image tab, and selecting a row with available audio shows the WEM player with script text in Audio tab.
Output: 2+ Playwright screenshots proving Image and Audio tabs light up with real mock data. Any wiring bugs found are fixed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-dev-init-megaindex-wiring/52-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify RightPanel Image tab shows entity portrait for linked StringIDs</name>
  <read_first>
    locaNext/src/lib/components/ldm/ImageTab.svelte
    locaNext/src/lib/components/ldm/RightPanel.svelte
    locaNext/src/lib/components/pages/GridPage.svelte
  </read_first>
  <files>
    locaNext/src/lib/components/ldm/ImageTab.svelte
    locaNext/src/lib/components/ldm/RightPanel.svelte
    locaNext/src/lib/components/pages/GridPage.svelte
  </files>
  <action>
    1. Ensure DEV servers are running (port 8888 backend, port 5173 frontend).
       Wait for MegaIndex build log confirming 35 dicts populated.

    2. Identify a StringID that links to an entity with an image:
       - `curl -s "http://localhost:8888/api/ldm/codex/items?limit=5"` to find item StrKeys
       - Note a StrKey that has image_path populated (from MegaIndex strkey_to_image_path C1)
       - Find which XML file contains that StrKey to know which LDM file to open

    3. Alternatively, check the MegaIndex dicts directly via the debug/stats endpoint:
       - `curl -s http://localhost:8888/api/ldm/megaindex/stats` (if available)
       - Or check which mock XML files exist: `ls server/mock_gamedata/` to find LocStr files

    4. Use Playwright MCP to:
       - Navigate to http://localhost:5173 and login (admin/admin123)
       - Open a LanguageData XML file that contains StringIDs linked to entities:
         Look in the file explorer panel for XML files. Click to open one that contains game strings (e.g., item names).
       - The grid should load with rows. Click on a row that has a StringID linked to an entity.
       - Click the "Image" tab in the RightPanel (right side panel)
       - Take screenshot: `screenshots/53-rpanel-image.png`
       - Verify: The Image tab should show a DDS portrait/image for the linked entity

    5. If Image tab shows "No image available" or is blank:
       - Check ImageTab.svelte to see how it resolves the image from selectedRow
       - The chain is: selectedRow.string_id -> MegaIndex C7 (stringid_to_entity) -> entity.strkey -> MegaIndex C1 (strkey_to_image_path) -> DDS image path -> proxy endpoint
       - Verify each step of the chain returns data via curl
       - Fix any broken link in the chain

    6. If Image tab shows a broken image (red X):
       - The DDS proxy endpoint may not be resolving the image path correctly
       - Check if the image file exists at the resolved path under mock_gamedata
       - Fix the proxy endpoint or image path resolution
  </action>
  <verify>
    <automated>curl -s "http://localhost:8888/api/ldm/codex/items?limit=1" | python3 -c "import sys,json; d=json.load(sys.stdin); items=d.get('items',d.get('data',[])); assert len(items)>0 and items[0].get('image_path','')!='', 'No items with image_path'" && echo "Items have image paths"</automated>
  </verify>
  <acceptance_criteria>
    - At least one item in the Codex API response has a non-empty image_path field
    - screenshots/53-rpanel-image.png exists showing the Image tab with a visible entity portrait
    - The Image tab correctly resolves StringID -> entity -> image via MegaIndex chain
  </acceptance_criteria>
  <done>Image tab in RightPanel displays entity DDS portrait when selecting a LanguageData row with a linked StringID</done>
</task>

<task type="auto">
  <name>Task 2: Verify RightPanel Audio tab shows WEM player with script text for linked StringIDs</name>
  <read_first>
    locaNext/src/lib/components/ldm/AudioTab.svelte
    locaNext/src/lib/components/ldm/RightPanel.svelte
  </read_first>
  <files>
    locaNext/src/lib/components/ldm/AudioTab.svelte
    locaNext/src/lib/components/ldm/RightPanel.svelte
  </files>
  <action>
    1. Identify a StringID that links to audio:
       - `curl -s "http://localhost:8888/api/ldm/codex/audio?limit=5"` to find audio entries
       - Note which StringIDs have audio events linked (from MegaIndex C3: stringid_to_audio_path)
       - Find which XML file contains that StringID

    2. Use Playwright MCP (browser should still be open from Task 1):
       - Navigate to the LanguageData grid (click "Files" in nav, then open an XML file containing audio-linked StringIDs)
       - If unsure which file: open any LocStr XML file and click through rows until the Audio tab lights up
       - Click the "Audio" tab in the RightPanel
       - Take screenshot: `screenshots/53-rpanel-audio.png`
       - Verify: The Audio tab should show:
         a) WEM file path or audio event name
         b) Play/Stop button for inline playback
         c) Script text (KOR + ENG lines) from the event chain

    3. If Audio tab shows "No audio available":
       - Check AudioTab.svelte for how it resolves audio from selectedRow
       - The chain is: selectedRow.string_id -> MegaIndex C3 (stringid_to_audio_path) -> WEM path -> streaming endpoint
       - Verify C3 dict has entries: `curl -s http://localhost:8888/api/ldm/megaindex/stats` or check API
       - Fix any broken link

    4. Test audio playback (if WEM files exist in mock_gamedata):
       - Click play button on the audio entry
       - If playback fails: vgmstream-cli may not be installed in DEV — this is acceptable
       - The UI should gracefully show the audio info even if playback fails
       - Take screenshot after clicking play: `screenshots/53-rpanel-audio-play.png`

    5. Verify script text overlay:
       - The Audio tab should show KOR and ENG script text from MegaIndex event_to_script_kr/eng dicts
       - If script text missing: check the MegaIndex dicts and AudioTab.svelte wiring
  </action>
  <verify>
    <automated>curl -s "http://localhost:8888/api/ldm/codex/audio?limit=1" | python3 -c "import sys,json; d=json.load(sys.stdin); entries=d.get('entries',d.get('data',d.get('audio',[]))); assert isinstance(entries,(list,dict)), 'Invalid audio response'" && echo "Audio API returns data"</automated>
  </verify>
  <acceptance_criteria>
    - Audio Codex API returns at least one audio entry
    - screenshots/53-rpanel-audio.png exists showing the Audio tab with audio info (path/event, play button, script text)
    - The Audio tab correctly resolves StringID -> audio event -> WEM path via MegaIndex C3 chain
    - Script text (KOR/ENG) is displayed when available
  </acceptance_criteria>
  <done>Audio tab in RightPanel displays WEM audio info with script text when selecting a LanguageData row with linked audio</done>
</task>

</tasks>

<verification>
- RightPanel Image tab shows entity portrait for at least one LDM row (via MegaIndex C7->C1 chain)
- RightPanel Audio tab shows audio info with script text for at least one LDM row (via MegaIndex C3 chain)
- 2+ screenshots saved proving the tabs render with real data
- Any wiring bugs in the StringID->entity->image or StringID->audio chains are fixed
</verification>

<success_criteria>
- RPANEL-01: Image tab shows DDS portrait for a LDM row with linked entity, verified by screenshot
- RPANEL-02: Audio tab shows WEM audio info with script text for a LDM row with linked audio, verified by screenshot
</success_criteria>

<output>
After completion, create `.planning/phases/53-codex-right-panel-verification/53-02-SUMMARY.md`
</output>
