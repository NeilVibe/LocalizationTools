---
phase: 53-codex-right-panel-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - locaNext/src/lib/components/pages/ItemCodexPage.svelte
  - locaNext/src/lib/components/pages/CharacterCodexPage.svelte
  - locaNext/src/lib/components/pages/AudioCodexPage.svelte
  - locaNext/src/lib/components/pages/RegionCodexPage.svelte
  - server/tools/ldm/routes/codex_items.py
  - server/tools/ldm/routes/codex_characters.py
  - server/tools/ldm/routes/codex_audio.py
  - server/tools/ldm/routes/codex_regions.py
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03, VERIFY-04]

must_haves:
  truths:
    - "Item Codex page renders a card grid with images, group hierarchy tabs, and search"
    - "Character Codex page renders a card grid with portraits, category tabs, and detail fields"
    - "Audio Codex page renders a list with category tree sidebar and inline play buttons"
    - "Region Codex page renders a split layout with faction tree and d3-zoom map"
  artifacts:
    - path: "screenshots/53-item-codex.png"
      provides: "Visual proof Item Codex renders correctly"
    - path: "screenshots/53-character-codex.png"
      provides: "Visual proof Character Codex renders correctly"
    - path: "screenshots/53-audio-codex.png"
      provides: "Visual proof Audio Codex renders correctly"
    - path: "screenshots/53-region-codex.png"
      provides: "Visual proof Region Codex renders correctly"
  key_links:
    - from: "ItemCodexPage.svelte"
      to: "/api/ldm/codex/items"
      via: "fetch in onMount"
      pattern: "fetch.*api/ldm/codex/items"
    - from: "CharacterCodexPage.svelte"
      to: "/api/ldm/codex/characters"
      via: "fetch in onMount"
      pattern: "fetch.*api/ldm/codex/characters"
    - from: "AudioCodexPage.svelte"
      to: "/api/ldm/codex/audio"
      via: "fetch in onMount"
      pattern: "fetch.*api/ldm/codex/audio"
    - from: "RegionCodexPage.svelte"
      to: "/api/ldm/codex/regions"
      via: "fetch in onMount"
      pattern: "fetch.*api/ldm/codex/regions"
---

<objective>
Verify all 4 Codex UIs (Item, Character, Audio, Region) render correctly with MegaIndex mock data in DEV mode.

Purpose: Confirm that Phase 52's MegaIndex auto-build wiring correctly populates the Codex API endpoints, and that each Codex page displays the expected UI elements (cards, tabs, search, map) with real mock data.
Output: 4 Playwright screenshots proving each Codex page renders with populated data. Any rendering bugs found are fixed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-dev-init-megaindex-wiring/52-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Item and Character Codex pages render with mock data</name>
  <read_first>
    locaNext/src/lib/components/pages/ItemCodexPage.svelte
    locaNext/src/lib/components/pages/CharacterCodexPage.svelte
    server/tools/ldm/routes/codex_items.py
    server/tools/ldm/routes/codex_characters.py
  </read_first>
  <files>
    locaNext/src/lib/components/pages/ItemCodexPage.svelte
    locaNext/src/lib/components/pages/CharacterCodexPage.svelte
    server/tools/ldm/routes/codex_items.py
    server/tools/ldm/routes/codex_characters.py
  </files>
  <action>
    1. Ensure DEV servers are running:
       - Backend: `DEV_MODE=true python3 server/main.py` (port 8888)
       - Frontend: `cd locaNext && npm run dev` (port 5173)
       Wait for MegaIndex build log output confirming 35 dicts populated.

    2. Verify Item Codex API returns data:
       - `curl -s http://localhost:8888/api/ldm/codex/items/groups` — should return group hierarchy
       - `curl -s "http://localhost:8888/api/ldm/codex/items?limit=10"` — should return item list with items
       - If API returns empty or errors, investigate the codex_items.py route and MegaIndex wiring

    3. Verify Character Codex API returns data:
       - `curl -s http://localhost:8888/api/ldm/codex/characters/categories` — should return category list
       - `curl -s "http://localhost:8888/api/ldm/codex/characters?limit=10"` — should return characters
       - If API returns empty or errors, investigate the codex_characters.py route

    4. Use Playwright MCP to navigate to the app:
       - Navigate to http://localhost:5173
       - Login with admin/admin123
       - Click the "Items" nav button in the top navigation bar (it triggers goToItemCodex)
       - Take screenshot: `screenshots/53-item-codex.png`
       - Verify the screenshot shows: card grid with item images, group/category tabs, search bar
       - If blank or broken: read the console errors, fix the Svelte component or API route

    5. Navigate to Character Codex:
       - Click the "Characters" nav button in the top navigation bar
       - Take screenshot: `screenshots/53-character-codex.png`
       - Verify the screenshot shows: card grid with character portraits, category tabs
       - Click on a character card to open detail panel
       - Take screenshot: `screenshots/53-character-codex-detail.png`
       - Verify detail shows Race/Gender/Age/Job fields
       - If broken: fix the component or API route

    6. Fix any rendering bugs found. Common issues to check:
       - API returning empty arrays (MegaIndex not populated or wrong dict keys)
       - Image paths not resolving (DDS proxy endpoint may need path fix)
       - Component expecting different data shape than API provides
       - Svelte 5 reactivity issues ($state/$derived not updating)
  </action>
  <verify>
    <automated>curl -s "http://localhost:8888/api/ldm/codex/items?limit=1" | python3 -c "import sys,json; d=json.load(sys.stdin); assert len(d.get('items',d.get('data',[]))) > 0, 'No items returned'" && curl -s "http://localhost:8888/api/ldm/codex/characters?limit=1" | python3 -c "import sys,json; d=json.load(sys.stdin); assert len(d.get('characters',d.get('data',[]))) > 0, 'No characters returned'" && echo "APIs return data"</automated>
  </verify>
  <acceptance_criteria>
    - Item Codex API returns at least 1 item with name and image fields
    - Character Codex API returns at least 1 character with name and category fields
    - screenshots/53-item-codex.png exists and shows populated card grid (not blank/error)
    - screenshots/53-character-codex.png exists and shows populated card grid
    - screenshots/53-character-codex-detail.png shows Race/Gender/Age/Job fields
  </acceptance_criteria>
  <done>Item and Character Codex pages render with populated mock data, screenshots saved as proof</done>
</task>

<task type="auto">
  <name>Task 2: Verify Audio and Region Codex pages render with mock data</name>
  <read_first>
    locaNext/src/lib/components/pages/AudioCodexPage.svelte
    locaNext/src/lib/components/pages/RegionCodexPage.svelte
    locaNext/src/lib/components/ldm/RegionCodexMap.svelte
    server/tools/ldm/routes/codex_audio.py
    server/tools/ldm/routes/codex_regions.py
  </read_first>
  <files>
    locaNext/src/lib/components/pages/AudioCodexPage.svelte
    locaNext/src/lib/components/pages/RegionCodexPage.svelte
    locaNext/src/lib/components/ldm/RegionCodexMap.svelte
    server/tools/ldm/routes/codex_audio.py
    server/tools/ldm/routes/codex_regions.py
  </files>
  <action>
    1. Verify Audio Codex API returns data:
       - `curl -s http://localhost:8888/api/ldm/codex/audio/categories` — should return category tree
       - `curl -s "http://localhost:8888/api/ldm/codex/audio?limit=10"` — should return audio entries
       - If empty: check MegaIndex wem_by_event, event_to_stringid dicts are populated

    2. Verify Region Codex API returns data:
       - `curl -s http://localhost:8888/api/ldm/codex/regions/tree` — should return faction tree
       - If empty: check MegaIndex region_by_strkey, faction dicts are populated

    3. Use Playwright MCP to navigate (browser should already be logged in from Task 1):
       - Click the "Audio" nav button in the top navigation bar
       - Take screenshot: `screenshots/53-audio-codex.png`
       - Verify screenshot shows: audio list entries with play buttons, category tree sidebar, script text
       - If broken: investigate console errors, fix component or API

    4. Navigate to Region Codex:
       - Click the "Regions" nav button in the top navigation bar
       - Take screenshot: `screenshots/53-region-codex.png`
       - Verify screenshot shows: faction tree sidebar on left, d3-zoom map on right with position nodes
       - Click on a region in the tree to verify detail panel renders
       - Take screenshot: `screenshots/53-region-codex-detail.png`
       - If d3 map is blank: check RegionCodexMap.svelte for WorldPosition coordinate handling
       - If tree is empty: check faction tree API endpoint

    5. Fix any rendering bugs found. Common issues:
       - Audio WEM file paths not resolving (vgmstream-cli may not be available in DEV)
       - Audio play button may error if WEM streaming endpoint is misconfigured — fix or gracefully handle
       - Region map may show no nodes if WorldPosition coordinates are missing from mock data
       - D3-zoom map may not render if SVG dimensions are 0 (check container sizing)
  </action>
  <verify>
    <automated>curl -s "http://localhost:8888/api/ldm/codex/audio/categories" | python3 -c "import sys,json; d=json.load(sys.stdin); assert isinstance(d, (list,dict)), 'Invalid audio categories response'" && curl -s "http://localhost:8888/api/ldm/codex/regions/tree" | python3 -c "import sys,json; d=json.load(sys.stdin); assert isinstance(d, (list,dict)), 'Invalid regions tree response'" && echo "APIs return data"</automated>
  </verify>
  <acceptance_criteria>
    - Audio Codex API returns valid categories and audio entry data
    - Region Codex API returns valid faction tree data
    - screenshots/53-audio-codex.png exists and shows audio list with play buttons
    - screenshots/53-region-codex.png exists and shows faction tree + d3 map
    - screenshots/53-region-codex-detail.png shows region detail with coordinates
  </acceptance_criteria>
  <done>Audio and Region Codex pages render with populated mock data, screenshots saved as proof</done>
</task>

</tasks>

<verification>
- All 4 Codex API endpoints return populated data (not empty arrays or errors)
- 4+ screenshots saved in screenshots/ directory showing each Codex page with rendered content
- No JavaScript console errors on any Codex page (check via Playwright console capture if possible)
- Any bugs found during testing are fixed and committed
</verification>

<success_criteria>
- VERIFY-01: Item Codex screenshot shows card grid with images, tabs, search
- VERIFY-02: Character Codex screenshot shows card grid with portraits, category tabs, detail with Race/Gender/Age/Job
- VERIFY-03: Audio Codex screenshot shows list with category tree, play buttons, script text
- VERIFY-04: Region Codex screenshot shows faction tree + d3-zoom map with position nodes
</success_criteria>

<output>
After completion, create `.planning/phases/53-codex-right-panel-verification/53-01-SUMMARY.md`
</output>
