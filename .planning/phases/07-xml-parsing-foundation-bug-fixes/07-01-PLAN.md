---
phase: 07-xml-parsing-foundation-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/tools/ldm/services/xml_parsing.py
  - server/tools/ldm/file_handlers/xml_handler.py
  - tests/unit/ldm/test_xml_parsing.py
  - tests/fixtures/xml/malformed_sample.xml
  - tests/fixtures/xml/locstr_sample.xml
  - tests/fixtures/xml/languagedata_eng.xml
  - tests/fixtures/xml/languagedata_kor.xml
autonomous: true
requirements: [XML-04, XML-06, XML-07]

must_haves:
  truths:
    - "Malformed XML files parse with recovery instead of crashing"
    - "Language table files yield correct language-code-to-translation mappings"
    - "StringIdConsumer deduplicates per language in document order"
    - "XML files with malformed content (bare ampersands, control characters, unclosed tags) open correctly without crashing"
  artifacts:
    - path: "server/tools/ldm/services/xml_parsing.py"
      provides: "XMLParsingEngine with sanitize() and parse_file() methods"
      exports: ["XMLParsingEngine", "get_xml_parsing_engine", "STRINGID_ATTRS", "STRORIGIN_ATTRS", "STR_ATTRS", "DESC_ATTRS", "DESCORIGIN_ATTRS", "LOCSTR_TAGS", "StringIdConsumer"]
    - path: "server/tools/ldm/file_handlers/xml_handler.py"
      provides: "lxml-based XML file parser replacing stdlib ET"
      contains: "from lxml import etree"
    - path: "tests/unit/ldm/test_xml_parsing.py"
      provides: "Unit tests for sanitizer, language tables, StringIdConsumer, cross-ref"
      min_lines: 100
  key_links:
    - from: "server/tools/ldm/file_handlers/xml_handler.py"
      to: "server/tools/ldm/services/xml_parsing.py"
      via: "import get_xml_parsing_engine"
      pattern: "get_xml_parsing_engine\\(\\)"
    - from: "server/tools/ldm/services/xml_parsing.py"
      to: "lxml.etree"
      via: "lxml parsing with recover=True fallback"
      pattern: "etree\\.XMLParser\\(recover=True"
---

<objective>
Create the XMLParsingEngine service and migrate xml_handler.py from stdlib ET to lxml.

Purpose: This is the parsing foundation for all v2.0 features. Every downstream phase (merge, export, media, AI summaries) depends on reliable XML parsing with sanitization and recovery. The current xml_handler.py uses stdlib ET which crashes on malformed game data.

Output: New `xml_parsing.py` service with sanitizer + parser + attribute constants + language table parser + StringIdConsumer. Migrated `xml_handler.py` using lxml via XMLParsingEngine. Test suite covering all three requirements.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-xml-parsing-foundation-bug-fixes/07-RESEARCH.md

@server/tools/ldm/file_handlers/xml_handler.py
@server/tools/ldm/services/mapdata_service.py
@server/tools/ldm/services/glossary_service.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From server/tools/ldm/file_handlers/xml_handler.py (CURRENT -- to be migrated):
```python
def parse_xml_file(file_content: bytes, filename: str) -> List[Dict]:
    # Returns list of {"row_num", "string_id", "source", "target", "status", "extra_data"}
def get_file_metadata() -> Dict:
    # Returns {"encoding", "xml_declaration", "root_element", "root_attributes", "element_tag", "element_count", "format_version"}
def get_source_language() -> str:
def get_file_format() -> str:
```

From server/tools/ldm/services/glossary_service.py (already uses lxml):
```python
@staticmethod
def _parse_xml(path: Path):
    from lxml import etree
    parser = etree.XMLParser(recover=True, encoding="utf-8")
    tree = etree.parse(str(path), parser)
    return tree.getroot()
```

From server/utils/text_utils.py:
```python
def normalize_text(text: str) -> str:
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Create XMLParsingEngine service with sanitizer, language tables, and StringIdConsumer</name>
  <files>
    server/tools/ldm/services/xml_parsing.py,
    tests/unit/ldm/test_xml_parsing.py,
    tests/fixtures/xml/malformed_sample.xml,
    tests/fixtures/xml/locstr_sample.xml,
    tests/fixtures/xml/languagedata_eng.xml,
    tests/fixtures/xml/languagedata_kor.xml
  </files>
  <behavior>
    - test_sanitizer_removes_control_chars: input with \x00\x08 chars -> chars removed
    - test_sanitizer_fixes_bad_entities: "Rock & Roll" -> "Rock &amp; Roll"
    - test_sanitizer_preserves_valid_entities: "&lt;" and "&amp;" pass through unchanged
    - test_sanitizer_fixes_unescaped_lt_in_attrs: value="a<b" -> value="a&lt;b"
    - test_parse_file_strict_success: valid XML -> returns lxml Element root
    - test_parse_file_recovery_fallback: malformed XML (unclosed tag) -> returns root via recovery mode, logs warning
    - test_parse_file_returns_none_on_total_failure: binary garbage -> returns None
    - test_language_table_parsing: languagedata_eng.xml with LocStr elements -> dict mapping StringId to {Str, StrOrigin, Desc, DescOrigin} per language code
    - test_language_table_discovers_files: folder with languagedata_eng.xml and languagedata_kor.xml -> returns {"eng": Path, "kor": Path}
    - test_stringid_consumer_basic: ordered index with 3 entries -> consume() returns StringIDs in document order
    - test_stringid_consumer_dedup: same normalized_kor appears twice -> first consume returns first StringID, second returns second
    - test_stringid_consumer_exhausted: consume past available -> returns None
    - test_stringid_consumer_fresh_per_language: two consumers from same index -> independent consumption pointers
    - test_attribute_constants: STRINGID_ATTRS contains ['StringId', 'StringID', 'stringid', 'STRINGID', 'Stringid', 'stringId']
    - test_locstr_tags: LOCSTR_TAGS contains ['LocStr', 'locstr', 'LOCSTR', 'LOCStr', 'Locstr']
    - test_get_attr_helper: get_attr(elem, STRINGID_ATTRS) finds attribute regardless of case variant used
    - test_iter_locstr_elements: XML with mixed-case LocStr tags -> all found
  </behavior>
  <action>
    1. Create XML fixture files in tests/fixtures/xml/:
       - malformed_sample.xml: XML with control chars, bare &, unclosed tags
       - locstr_sample.xml: valid LocStr XML with StringId, StrOrigin, Str, Desc attributes (multiple case variants)
       - languagedata_eng.xml: sample language file with LocStr elements and Str attributes
       - languagedata_kor.xml: sample language file with LocStr elements and StrOrigin (Korean source)

    2. Create tests/unit/ldm/test_xml_parsing.py with all tests from <behavior> above. Tests import from server.tools.ldm.services.xml_parsing.

    3. Create server/tools/ldm/services/xml_parsing.py implementing:
       - Centralized attribute constants: STRINGID_ATTRS, STRORIGIN_ATTRS, STR_ATTRS, DESC_ATTRS, DESCORIGIN_ATTRS, LOCSTR_TAGS (ported from QuickTranslate)
       - `get_attr(elem, attr_variants: list) -> Optional[str]`: case-variant attribute lookup helper
       - `iter_locstr_elements(root) -> Iterator[Element]`: finds LocStr elements using all LOCSTR_TAGS variants
       - XMLParsingEngine class:
         - `sanitize(raw: str) -> str`: 5-step pipeline ported from QuickTranslate sanitize_xml_content (control chars, bad entities, seg newlines via _preprocess_newlines, unescaped < in attrs, tag stack repair via _repair_tag_stack)
         - `parse_file(path: Path) -> Optional[Element]`: strict-first with recover=True fallback, calls sanitize first, uses huge_tree=True
         - `parse_bytes(content: bytes, filename: str) -> Tuple[Optional[Element], str]`: parse from bytes with encoding detection (XML declaration first, then cascade), returns (root, detected_encoding)
         - `discover_language_files(loc_folder: Path) -> Dict[str, Path]`: ported from QuickTranslate language_loader.py, matches languagedata_*.xml pattern
         - `build_translation_lookup(lang_file: Path) -> Dict[str, Dict[str, str]]`: parse a language file and return {StringId: {Str, StrOrigin, Desc, DescOrigin}}
       - StringIdConsumer class (ported from QACompiler generators/base.py):
         - `__init__(ordered_index: Dict[str, Dict[str, List[str]]])`: takes {export_key: {normalized_kor: [sid_list]}}
         - `consume(normalized_kor: str, export_key: str) -> Optional[str]`: returns next StringID in document order, advances pointer
       - Singleton: `get_xml_parsing_engine() -> XMLParsingEngine`

    Important: Use `from __future__ import annotations` in all files. Use loguru logger, never print(). The _preprocess_newlines and _repair_tag_stack helper methods can be simplified stubs initially -- the critical path is sanitize's 5-step structure and parse_file's strict+recovery pattern.
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python -m pytest tests/unit/ldm/test_xml_parsing.py -x --no-header -q</automated>
  </verify>
  <done>
    XMLParsingEngine service exists with sanitize(), parse_file(), parse_bytes(), discover_language_files(), build_translation_lookup(). StringIdConsumer class works with document-order dedup. All attribute constants exported. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate xml_handler.py from stdlib ET to lxml via XMLParsingEngine</name>
  <files>
    server/tools/ldm/file_handlers/xml_handler.py
  </files>
  <action>
    Rewrite xml_handler.py to use XMLParsingEngine instead of stdlib xml.etree.ElementTree:

    1. Replace `import xml.etree.ElementTree as ET` with imports from xml_parsing service:
       ```python
       from server.tools.ldm.services.xml_parsing import (
           get_xml_parsing_engine, get_attr, iter_locstr_elements,
           STRINGID_ATTRS, STRORIGIN_ATTRS, STR_ATTRS,
       )
       ```

    2. In parse_xml_file():
       - Replace encoding detection cascade with XMLParsingEngine.parse_bytes(file_content, filename) which returns (root, detected_encoding)
       - Replace `root = ET.fromstring(text_content)` with the parse_bytes call
       - Replace `root.findall('.//LocStr')` with `iter_locstr_elements(root)` for case-insensitive tag matching
       - Replace direct `attribs.get('StringId')` with `get_attr(loc, STRINGID_ATTRS)` for case-variant lookup
       - Replace direct `attribs.get('StrOrigin')` with `get_attr(loc, STRORIGIN_ATTRS)`
       - Replace direct `attribs.get('Str')` with `get_attr(loc, STR_ATTRS)`

    3. Eliminate module-level `_file_metadata` state:
       - Change parse_xml_file to return a tuple: `(rows, metadata)` instead of just rows
       - Remove `global _file_metadata` and `get_file_metadata()` function
       - Callers that use get_file_metadata() will receive metadata from the parse_xml_file return value

    4. Replace `except ET.ParseError` with `except Exception` (lxml exceptions are different)

    5. Keep get_source_language() and get_file_format() unchanged.

    IMPORTANT: Preserve the existing return format for rows (row_num, string_id, source, target, status, extra_data). The migration changes HOW parsing works, not WHAT it returns. Keep CORE_ATTRIBUTES set.

    NOTE: Since parse_xml_file's signature changes (returns tuple), grep for callers and update them. Expected caller: server/tools/ldm/routes/file_routes.py. The caller should destructure: `rows, metadata = parse_xml_file(content, filename)`.
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python -m pytest tests/unit/ldm/test_routes_files.py tests/unit/ldm/test_xml_parsing.py -x --no-header -q</automated>
  </verify>
  <done>
    xml_handler.py uses lxml via XMLParsingEngine. No stdlib ET import remains. Module-level _file_metadata eliminated. parse_xml_file returns (rows, metadata) tuple. Existing file route tests still pass. Case-insensitive attribute lookup via get_attr().
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python -m pytest tests/unit/ldm/ -x --no-header -q` -- full LDM test suite passes
2. `grep -r "import xml.etree" server/tools/ldm/` -- returns empty (no stdlib ET in LDM)
3. `python -c "from server.tools.ldm.services.xml_parsing import XMLParsingEngine, StringIdConsumer; print('OK')"` -- imports work
</verification>

<success_criteria>
- XMLParsingEngine sanitizes and parses malformed XML without crashing
- Language table discovery and parsing extracts correct per-language data
- StringIdConsumer deduplicates in document order with fresh instances per language
- xml_handler.py is fully migrated to lxml -- zero stdlib ET usage in server/tools/ldm/
- All existing tests continue to pass (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/07-xml-parsing-foundation-bug-fixes/07-01-SUMMARY.md`
</output>