---
phase: 10-export-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/tools/ldm/services/export_service.py
  - tests/unit/ldm/test_export_service.py
  - server/tools/ldm/routes/files.py
autonomous: true
requirements:
  - TMERGE-05
  - TMERGE-06
  - TMERGE-07
must_haves:
  truths:
    - "XML export preserves attribute casing (StringId, StrOrigin, Str) exactly"
    - "XML export round-trips br-tags correctly (in-memory br-tags become escaped on disk)"
    - "Excel export produces 14-column EU structure with header formatting"
    - "Text export produces tab-delimited StringID + source + translation"
    - "None values are skipped, empty strings are preserved as attributes"
  artifacts:
    - path: "server/tools/ldm/services/export_service.py"
      provides: "ExportService with export_xml, export_excel, export_text methods"
      exports: ["ExportService"]
    - path: "tests/unit/ldm/test_export_service.py"
      provides: "Unit tests for all 3 export formats"
      contains: "TestXMLExport, TestExcelExport, TestTextExport"
  key_links:
    - from: "server/tools/ldm/services/export_service.py"
      to: "lxml.etree"
      via: "etree.SubElement + etree.tostring"
      pattern: "etree\\.tostring.*pretty_print"
    - from: "server/tools/ldm/services/export_service.py"
      to: "xlsxwriter"
      via: "xlsxwriter.Workbook"
      pattern: "xlsxwriter\\.Workbook"
    - from: "server/tools/ldm/services/export_service.py"
      to: "server/tools/ldm/services/korean_detection.py"
      via: "is_korean_text for Text State column"
      pattern: "from.*korean_detection.*import.*is_korean_text"
---

<objective>
Create ExportService with XML, Excel, and plain text export methods, fully tested, and wire into download route.

Purpose: Replace the broken inline `_build_*_from_dicts` functions (stdlib ET for XML, openpyxl for Excel) with a proper service using lxml and xlsxwriter, matching proven patterns from ExtractAnything and LanguageDataExporter.

Output: `server/tools/ldm/services/export_service.py` with 3 export methods + comprehensive unit tests + route wiring.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-export-pipeline/10-RESEARCH.md

@server/tools/ldm/routes/files.py (lines 1252-1380 -- existing _build_*_from_dicts to replace)
@server/tools/ldm/services/korean_detection.py (is_korean_text for Text State column)
@server/tools/ldm/services/xml_parsing.py (XMLParsingEngine pattern reference)

<interfaces>
<!-- Row dict structure from repository (input to all export methods) -->
```python
# Each row is a dict with these keys:
row = {
    "string_id": "STR_001",         # StringId attribute
    "source": "Original text",      # StrOrigin attribute
    "target": "Translated text",    # Str attribute
    "extra_data": {                  # All other attributes preserved
        "Desc": "Description",
        "DescOrigin": "원래 설명",
        "Category": "UI",
        "Memo": "Translator note",
        # ... any other XML attributes
    }
}

# file_metadata dict (from file.extra_data):
metadata = {
    "root_element": "LangData",     # XML root tag
    "root_attributes": {"xmlns": "..."}, # Root element attributes
    "element_tag": "LocStr",        # Per-row element tag
    "encoding": "utf-8",            # File encoding
    "total_columns": 7,             # For TXT files
}
```

From server/tools/ldm/services/korean_detection.py:
```python
def is_korean_text(text: str | None) -> bool:
    """Check if text contains Korean characters."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ExportService with XML, Excel, and text export methods</name>
  <files>server/tools/ldm/services/export_service.py, tests/unit/ldm/test_export_service.py</files>
  <behavior>
    - TestXMLExport::test_basic_xml_output: rows with StringId/StrOrigin/Str produce valid XML with correct attribute casing
    - TestXMLExport::test_brtag_roundtrip: in-memory br-tags in source/target serialize as escaped entities on disk
    - TestXMLExport::test_extra_data_preserved: extra_data dict attributes (Desc, Category, etc.) appear in output elements
    - TestXMLExport::test_none_vs_empty_string: None values skip attribute, empty string writes attr=""
    - TestXMLExport::test_root_attributes: root_attributes from metadata appear on root element
    - TestXMLExport::test_custom_element_tag: element_tag from metadata controls per-row tag name
    - TestExcelExport::test_14_column_headers: output has exactly 14 EU columns in correct order
    - TestExcelExport::test_header_formatting: headers have bold, #DAEEF3 background, centered, bordered
    - TestExcelExport::test_data_mapping: source maps to StrOrigin (col 0), target to Str (col 2), string_id to StringID (col 11)
    - TestExcelExport::test_text_state_korean: Korean text in target produces "KOREAN" in Text State column
    - TestExcelExport::test_text_state_translated: Non-Korean text produces "TRANSLATED" in Text State column
    - TestExcelExport::test_stringid_text_format: StringID column uses text format (num_format='@') to prevent scientific notation
    - TestExcelExport::test_freeze_panes: worksheet has frozen panes at row 1
    - TestTextExport::test_basic_tab_delimited: output is tab-delimited with StringID, source, target per line
    - TestTextExport::test_utf8_encoding: output bytes are valid UTF-8
    - TestTextExport::test_empty_fields: missing fields produce empty strings, not "None"
  </behavior>
  <action>
    1. Create `tests/unit/ldm/test_export_service.py` with test classes TestXMLExport, TestExcelExport, TestTextExport covering all behaviors above. Use `openpyxl` (reading only) in tests to verify Excel output. Use `lxml.etree.fromstring` to verify XML output.

    2. Create `server/tools/ldm/services/export_service.py` with:
       - `from __future__ import annotations` at top
       - `ExportService` class with three methods:
         a. `export_xml(rows, metadata)` -- uses lxml `etree.Element` + `etree.SubElement` with raw attribute dicts. Maps row keys: string_id->StringId, source->StrOrigin, target->Str. Merges extra_data attributes. Uses `etree.tostring(pretty_print=True, xml_declaration=True)`. Do NOT manually escape br-tags -- lxml handles it.
         b. `export_excel(rows, metadata)` -- uses xlsxwriter with EU column order: StrOrigin, ENG, Str, Correction, Text State, STATUS, COMMENT, MEMO1, MEMO2, Category, FileName, StringID, DescOrigin, Desc. Header format: bold, #DAEEF3 bg, centered, border 1. Cell format: text_wrap, border 1, valign top. StringID column: num_format='@'. Text State: use `is_korean_text(target)` -> "KOREAN" or "TRANSLATED". Freeze panes row 1. Autofilter on all columns.
         c. `export_text(rows, metadata)` -- tab-delimited: `StringID\tStrOrigin\tStr` per line. UTF-8 encoded bytes.
       - All methods return `bytes`.
       - Use loguru `logger` for logging, never print().

    3. Run tests RED first, then implement until GREEN.
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python3 -m pytest tests/unit/ldm/test_export_service.py -x -q</automated>
  </verify>
  <done>All 16+ tests pass. ExportService produces correct XML (lxml, casing preserved, br-tags safe), Excel (xlsxwriter, 14 EU columns, formatting), and text (tab-delimited) output.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ExportService into download route and verify roundtrip</name>
  <files>server/tools/ldm/routes/files.py</files>
  <action>
    1. In `server/tools/ldm/routes/files.py`:
       - Add import: `from server.tools.ldm.services.export_service import ExportService`
       - Replace calls to `_build_xml_file_from_dicts(rows, file_metadata)` with `ExportService().export_xml(rows, file_metadata)` in the download endpoint (line ~881), merge endpoint (line ~1022), and convert endpoint (line ~1107).
       - Replace calls to `_build_excel_file_from_dicts(rows, file_metadata)` with `ExportService().export_excel(rows, file_metadata)` in download (line ~885) and convert (line ~1103).
       - Replace calls to `_build_txt_file_from_dicts(rows, file_metadata)` with `ExportService().export_text(rows, file_metadata)` in download (line ~877) and convert (line ~1111).
       - Keep the old `_build_*_from_dicts` functions at the bottom of the file but add a `# DEPRECATED: Use ExportService instead` comment. Do NOT delete them yet (backward compat for any edge callers).
       - Remove the `import xml.etree.ElementTree as ET` and `from xml.dom import minidom` imports from inside `_build_xml_file_from_dicts` (they are no longer needed by ExportService).

    2. Run the existing integration test to verify round-trip still works.
    3. Run the full unit test suite to verify no regressions.
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python3 -m pytest tests/unit/ldm/test_export_service.py tests/integration/test_export_roundtrip.py tests/unit/ldm/test_routes_files.py -x -q</automated>
  </verify>
  <done>Download endpoint uses ExportService for all 3 formats. Existing integration roundtrip test passes. No regressions in route tests.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/unit/ldm/test_export_service.py -x -q` -- all export unit tests pass
- `python3 -m pytest tests/integration/test_export_roundtrip.py -x -q` -- roundtrip validation passes
- `python3 -m pytest tests/unit/ldm/ -x -q` -- full LDM unit suite green
</verification>

<success_criteria>
1. XML export uses lxml (not stdlib ET), preserves attribute casing, auto-escapes br-tags
2. Excel export uses xlsxwriter (not openpyxl), produces 14-column EU structure with formatting
3. Text export produces clean tab-delimited StringID + source + translation
4. Download route delegates to ExportService for all 3 formats
5. All tests pass (unit + integration roundtrip)
</success_criteria>

<output>
After completion, create `.planning/phases/10-export-pipeline/10-01-SUMMARY.md`
</output>
