---
phase: 99-svelte5-event-migration
plan: 02
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - locaNext/src/lib/components/common/ConfirmModal.svelte
  - locaNext/src/lib/components/common/InputModal.svelte
  - locaNext/src/lib/components/ChangePassword.svelte
  - locaNext/src/lib/components/UpdateModal.svelte
  - locaNext/src/lib/components/admin/AccessControl.svelte
  - locaNext/src/lib/components/ldm/TMUploadModal.svelte
  - locaNext/src/lib/components/ldm/FilePickerDialog.svelte
  - locaNext/src/lib/components/ldm/PretranslateModal.svelte
  - locaNext/src/lib/components/ldm/FileMergeModal.svelte
  - locaNext/src/lib/components/ldm/TMManager.svelte
autonomous: true
requirements: [EVT-04, EVT-05]

must_haves:
  truths:
    - "All 10 small/medium modal files use AppModal instead of Carbon Modal directly"
    - "Zero on:click:button--primary or on:click:button--secondary in these files"
    - "All modal button handlers still fire correctly (same business logic)"
  artifacts:
    - path: "locaNext/src/lib/components/common/ConfirmModal.svelte"
      provides: "Migrated to AppModal with onprimary/onsecondary"
      contains: "AppModal"
    - path: "locaNext/src/lib/components/common/InputModal.svelte"
      provides: "Migrated to AppModal with onprimary/onsecondary"
      contains: "AppModal"
    - path: "locaNext/src/lib/components/ldm/TMManager.svelte"
      provides: "3 modals migrated to AppModal"
      contains: "AppModal"
  key_links:
    - from: "All 10 files in this plan"
      to: "locaNext/src/lib/components/common/AppModal.svelte"
      via: "import AppModal"
      pattern: "import AppModal from"
---

<objective>
Migrate 10 small/medium modal files from Carbon Modal on:event syntax to AppModal wrapper with Svelte 5 callback props.

Purpose: Eliminate on:click:button--primary and on:click:button--secondary from 10 files. Each migration is mechanical: replace Modal import with AppModal import, replace on:click:button--primary with onprimary, replace on:click:button--secondary with onsecondary.

Output: 10 files migrated, zero on:event patterns in these files
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/99-svelte5-event-migration/99-01-SUMMARY.md

<interfaces>
<!-- AppModal.svelte API (created in Plan 01) -->
```svelte
<!-- Usage: -->
<AppModal
  bind:open
  modalHeading="Title"
  primaryButtonText="OK"
  secondaryButtonText="Cancel"
  onprimary={handlePrimary}
  onsecondary={handleSecondary}
  onclose={handleClose}
  danger
  size="sm"
>
  <p>Content</p>
</AppModal>

<!-- All Carbon Modal props pass through. Only change: -->
<!-- on:click:button--primary={fn}  -->  <!-- becomes: onprimary={fn} -->
<!-- on:click:button--secondary={fn} --> <!-- becomes: onsecondary={fn} -->
<!-- on:close={fn} -->                   <!-- becomes: onclose={fn} -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate common modals + ChangePassword + UpdateModal + AccessControl</name>
  <files>
    locaNext/src/lib/components/common/ConfirmModal.svelte,
    locaNext/src/lib/components/common/InputModal.svelte,
    locaNext/src/lib/components/ChangePassword.svelte,
    locaNext/src/lib/components/UpdateModal.svelte,
    locaNext/src/lib/components/admin/AccessControl.svelte
  </files>
  <read_first>
    locaNext/src/lib/components/common/ConfirmModal.svelte
    locaNext/src/lib/components/common/InputModal.svelte
    locaNext/src/lib/components/ChangePassword.svelte
    locaNext/src/lib/components/UpdateModal.svelte
    locaNext/src/lib/components/admin/AccessControl.svelte
  </read_first>
  <action>
For EACH of the 5 files, apply this mechanical transformation:

1. **Change import:** Replace `import { Modal } from 'carbon-components-svelte';` with `import AppModal from './common/AppModal.svelte';` (adjust relative path per file location)
   - ConfirmModal.svelte: `import AppModal from './AppModal.svelte';` (same directory)
   - InputModal.svelte: `import AppModal from './AppModal.svelte';` (same directory)
   - ChangePassword.svelte: `import AppModal from './common/AppModal.svelte';`
   - UpdateModal.svelte: `import AppModal from './common/AppModal.svelte';`
   - AccessControl.svelte: `import AppModal from '../common/AppModal.svelte';`
   - NOTE: If the file imports OTHER things from carbon-components-svelte (like Button, TextInput), keep that import line and only remove Modal from it. E.g. `import { Modal, Button } from 'carbon-components-svelte';` becomes `import { Button } from 'carbon-components-svelte';` plus `import AppModal from '...';`

2. **Replace tag:** `<Modal` becomes `<AppModal`, `</Modal>` becomes `</AppModal>` (if closing tag exists; Carbon Modal usually self-closes with slot content)

3. **Replace events:**
   - `on:click:button--primary={handler}` becomes `onprimary={handler}`
   - `on:click:button--secondary={handler}` becomes `onsecondary={handler}`
   - If inline arrow: `on:click:button--primary={() => { ... }}` becomes `onprimary={() => { ... }}`
   - `onclose={handler}` stays as-is (already Svelte 5 syntax if present)

4. **Keep everything else identical:** All props (bind:open, modalHeading, primaryButtonText, secondaryButtonText, danger, size, etc.) stay exactly the same. All business logic stays identical.

Specific changes per file:

**ConfirmModal.svelte** (lines 39-40):
- `on:click:button--primary={handleConfirm}` -> `onprimary={handleConfirm}`
- `on:click:button--secondary={handleCancel}` -> `onsecondary={handleCancel}`
- `onclose={handleCancel}` stays as-is

**InputModal.svelte** (lines 67-68):
- `on:click:button--primary={handleSubmit}` -> `onprimary={handleSubmit}`
- `on:click:button--secondary={handleCancel}` -> `onsecondary={handleCancel}`

**ChangePassword.svelte** (line 80):
- `on:click:button--secondary={handleClose}` -> `onsecondary={handleClose}`
- Note: only has secondary button event (no primary event handler)

**UpdateModal.svelte** (lines 303-304):
- `on:click:button--primary={() => updateState === 'downloaded' ? handleRestartNow() : handleDownload()}` -> `onprimary={() => updateState === 'downloaded' ? handleRestartNow() : handleDownload()}`
- `on:click:button--secondary={handleLater}` -> `onsecondary={handleLater}`

**AccessControl.svelte** (lines 265-266):
- `on:click:button--primary={() => grantAccess(selectedUserId)}` -> `onprimary={() => grantAccess(selectedUserId)}`
- `on:click:button--secondary={() => { showUserPicker = false; selectedUserId = null; }}` -> `onsecondary={() => { showUserPicker = false; selectedUserId = null; }}`
  </action>
  <verify>
    <automated>cd locaNext && for f in src/lib/components/common/ConfirmModal.svelte src/lib/components/common/InputModal.svelte src/lib/components/ChangePassword.svelte src/lib/components/UpdateModal.svelte src/lib/components/admin/AccessControl.svelte; do echo "$f: $(grep -c 'on:click:button' $f) on:click:button"; done</automated>
  </verify>
  <acceptance_criteria>
    - ConfirmModal.svelte: 0 occurrences of `on:click:button`, contains `AppModal`, contains `onprimary`
    - InputModal.svelte: 0 occurrences of `on:click:button`, contains `AppModal`, contains `onprimary`
    - ChangePassword.svelte: 0 occurrences of `on:click:button`, contains `AppModal`, contains `onsecondary`
    - UpdateModal.svelte: 0 occurrences of `on:click:button`, contains `AppModal`, contains `onprimary`
    - AccessControl.svelte: 0 occurrences of `on:click:button`, contains `AppModal`, contains `onprimary`
  </acceptance_criteria>
  <done>5 files migrated to AppModal with zero on:click:button patterns remaining</done>
</task>

<task type="auto">
  <name>Task 2: Migrate LDM modals (TMUploadModal, FilePickerDialog, PretranslateModal, FileMergeModal, TMManager)</name>
  <files>
    locaNext/src/lib/components/ldm/TMUploadModal.svelte,
    locaNext/src/lib/components/ldm/FilePickerDialog.svelte,
    locaNext/src/lib/components/ldm/PretranslateModal.svelte,
    locaNext/src/lib/components/ldm/FileMergeModal.svelte,
    locaNext/src/lib/components/ldm/TMManager.svelte
  </files>
  <read_first>
    locaNext/src/lib/components/ldm/TMUploadModal.svelte
    locaNext/src/lib/components/ldm/FilePickerDialog.svelte
    locaNext/src/lib/components/ldm/PretranslateModal.svelte
    locaNext/src/lib/components/ldm/FileMergeModal.svelte
    locaNext/src/lib/components/ldm/TMManager.svelte
  </read_first>
  <action>
For EACH of the 5 LDM files, apply the same mechanical transformation as Task 1:

1. **Change import:** Remove Modal from carbon-components-svelte import, add `import AppModal from '../common/AppModal.svelte';` (all LDM files are in `ldm/` which is sibling to `common/`)
2. **Replace tag:** `<Modal` -> `<AppModal`
3. **Replace events:** `on:click:button--primary={x}` -> `onprimary={x}`, `on:click:button--secondary={x}` -> `onsecondary={x}`

Specific changes per file:

**TMUploadModal.svelte** (lines 205-206):
- `on:click:button--primary={uploadTM}` -> `onprimary={uploadTM}`
- `on:click:button--secondary={handleClose}` -> `onsecondary={handleClose}`

**FilePickerDialog.svelte** (lines 195-196):
- `on:click:button--primary={confirmSelection}` -> `onprimary={confirmSelection}`
- `on:click:button--secondary={handleClose}` -> `onsecondary={handleClose}`

**PretranslateModal.svelte** (lines 202-203):
- `on:click:button--primary={runPretranslation}` -> `onprimary={runPretranslation}`
- `on:click:button--secondary={handleClose}` -> `onsecondary={handleClose}`

**FileMergeModal.svelte** (lines 169-173):
- `on:click:button--primary={() => { ... }}` -> `onprimary={() => { ... }}` (multiline arrow function — keep the entire function body)
- `on:click:button--secondary={() => open = false}` -> `onsecondary={() => open = false}`

**TMManager.svelte** (lines 609-670, 3 separate Modals):
- Delete confirm modal (lines 609-610): `on:click:button--primary={deleteTM}` -> `onprimary={deleteTM}`, `on:click:button--secondary={...}` -> `onsecondary={...}`
- Build confirm modal (lines 629-630): `on:click:button--primary={executeBuildIndexes}` -> `onprimary={executeBuildIndexes}`, `on:click:button--secondary={...}` -> `onsecondary={...}`
- Export modal (lines 669-670): `on:click:button--primary={executeExport}` -> `onprimary={executeExport}`, `on:click:button--secondary={closeExportModal}` -> `onsecondary={closeExportModal}`
- TMManager has 3 Modal instances — ALL must be changed to AppModal

After ALL changes, run build to verify: `cd locaNext && npm run build`
  </action>
  <verify>
    <automated>cd locaNext && for f in src/lib/components/ldm/TMUploadModal.svelte src/lib/components/ldm/FilePickerDialog.svelte src/lib/components/ldm/PretranslateModal.svelte src/lib/components/ldm/FileMergeModal.svelte src/lib/components/ldm/TMManager.svelte; do echo "$f: $(grep -c 'on:click:button' $f) on:click:button"; done && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - TMUploadModal.svelte: 0 occurrences of `on:click:button`, contains `AppModal`
    - FilePickerDialog.svelte: 0 occurrences of `on:click:button`, contains `AppModal`
    - PretranslateModal.svelte: 0 occurrences of `on:click:button`, contains `AppModal`
    - FileMergeModal.svelte: 0 occurrences of `on:click:button`, contains `AppModal`
    - TMManager.svelte: 0 occurrences of `on:click:button`, contains `AppModal` (3 instances)
    - Build succeeds
  </acceptance_criteria>
  <done>5 LDM files migrated to AppModal. Build passes. Zero on:click:button patterns in LDM directory.</done>
</task>

</tasks>

<verification>
- `grep -rn 'on:click:button' locaNext/src/lib/components/common/ locaNext/src/lib/components/ldm/ locaNext/src/lib/components/ChangePassword.svelte locaNext/src/lib/components/UpdateModal.svelte locaNext/src/lib/components/admin/AccessControl.svelte` returns 0 matches (excluding AppModal.svelte which is the wrapper)
- `cd locaNext && npm run build` succeeds
</verification>

<success_criteria>
1. All 10 files import AppModal instead of Carbon Modal
2. Zero on:click:button--primary or on:click:button--secondary in any of the 10 files
3. All modal button handlers preserved (same function references)
4. Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/99-svelte5-event-migration/99-02-SUMMARY.md`
</output>
