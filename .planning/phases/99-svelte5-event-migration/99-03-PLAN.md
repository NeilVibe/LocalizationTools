---
phase: 99-svelte5-event-migration
plan: 03
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - locaNext/src/lib/components/apps/KRSimilar.svelte
  - locaNext/src/lib/components/apps/QuickSearch.svelte
  - locaNext/src/lib/components/apps/XLSTransfer.svelte
  - locaNext/src/lib/components/pages/FilesPage.svelte
autonomous: true
requirements: [EVT-06, EVT-07]

must_haves:
  truths:
    - "All 4 large app/page files use AppModal instead of Carbon Modal directly"
    - "Zero on:click:button--primary or on:click:button--secondary in these files"
    - "Zero on:event patterns remain ANYWHERE in locaNext/src/ (except AppModal.svelte)"
    - "Build succeeds with zero errors and zero warnings"
  artifacts:
    - path: "locaNext/src/lib/components/apps/KRSimilar.svelte"
      provides: "4 modals migrated from on:click:button to onprimary/onsecondary"
      contains: "AppModal"
    - path: "locaNext/src/lib/components/apps/QuickSearch.svelte"
      provides: "3 modals migrated from on:click:button to onprimary/onsecondary"
      contains: "AppModal"
    - path: "locaNext/src/lib/components/pages/FilesPage.svelte"
      provides: "3 modals migrated from on:click:button to onprimary/onsecondary"
      contains: "AppModal"
  key_links:
    - from: "All 4 files in this plan"
      to: "locaNext/src/lib/components/common/AppModal.svelte"
      via: "import AppModal"
      pattern: "import AppModal from"
---

<objective>
Migrate the 4 large app/page files from Carbon Modal on:event syntax to AppModal wrapper, then run full codebase verification.

Purpose: Complete the Svelte 4-to-5 event migration across all remaining files. These are the largest files (1050-3116 lines) with multiple Modal instances each. After this plan, zero on:event patterns remain anywhere in the codebase except the single AppModal.svelte wrapper.

Output: 4 files migrated, full codebase clean of on:event patterns, build verified
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/99-svelte5-event-migration/99-01-SUMMARY.md

<interfaces>
<!-- AppModal.svelte API (created in Plan 01) -->
<!-- Replace: on:click:button--primary={fn}  -> onprimary={fn} -->
<!-- Replace: on:click:button--secondary={fn} -> onsecondary={fn} -->
<!-- Replace: <Modal -> <AppModal -->
<!-- Import: import AppModal from '../common/AppModal.svelte' or adjust path -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate KRSimilar + QuickSearch + XLSTransfer</name>
  <files>
    locaNext/src/lib/components/apps/KRSimilar.svelte,
    locaNext/src/lib/components/apps/QuickSearch.svelte,
    locaNext/src/lib/components/apps/XLSTransfer.svelte
  </files>
  <read_first>
    locaNext/src/lib/components/apps/KRSimilar.svelte
    locaNext/src/lib/components/apps/QuickSearch.svelte
    locaNext/src/lib/components/apps/XLSTransfer.svelte
  </read_first>
  <action>
For each file, apply the mechanical transformation. All 3 files are in `apps/` directory, so the import path is `'../common/AppModal.svelte'`.

**KRSimilar.svelte** (8 events across 4 Modals):
1. Change import: Remove Modal from `carbon-components-svelte` import, add `import AppModal from '../common/AppModal.svelte';`
2. Find all 4 `<Modal` tags and replace with `<AppModal`
3. Replace 8 event handlers:
   - Line 714: `on:click:button--secondary={() => showCreateDictionaryModal = false}` -> `onsecondary={() => showCreateDictionaryModal = false}`
   - Line 715: `on:click:button--primary={createDictionary}` -> `onprimary={createDictionary}`
   - Line 762: `on:click:button--secondary={() => showLoadDictionaryModal = false}` -> `onsecondary={() => showLoadDictionaryModal = false}`
   - Line 763: `on:click:button--primary={loadDictionary}` -> `onprimary={loadDictionary}`
   - Line 796: `on:click:button--secondary={() => showExtractSimilarModal = false}` -> `onsecondary={() => showExtractSimilarModal = false}`
   - Line 797: `on:click:button--primary={extractSimilar}` -> `onprimary={extractSimilar}`
   - Line 845: `on:click:button--secondary={() => showAutoTranslateModal = false}` -> `onsecondary={() => showAutoTranslateModal = false}`
   - Line 846: `on:click:button--primary={autoTranslate}` -> `onprimary={autoTranslate}`

**QuickSearch.svelte** (6 events across 3 Modals):
1. Change import: Remove Modal from `carbon-components-svelte` import, add `import AppModal from '../common/AppModal.svelte';`
2. Find all 3 `<Modal` tags and replace with `<AppModal`
3. Replace 6 event handlers:
   - Line 1527: `on:click:button--secondary={() => { showCreateDictionaryModal = false; sourceMode = 'files'; createFiles = []; selectedFolderPath = ''; folderFilesCount = 0; }}` -> `onsecondary={() => { showCreateDictionaryModal = false; sourceMode = 'files'; createFiles = []; selectedFolderPath = ''; folderFilesCount = 0; }}`
   - Line 1528: `on:click:button--primary={createDictionary}` -> `onprimary={createDictionary}`
   - Line 1588: `on:click:button--secondary={() => showLoadDictionaryModal = false}` -> `onsecondary={() => showLoadDictionaryModal = false}`
   - Line 1589: `on:click:button--primary={loadDictionary}` -> `onprimary={loadDictionary}`
   - Line 1639: `on:click:button--secondary={() => showSetReferenceModal = false}` -> `onsecondary={() => showSetReferenceModal = false}`
   - Line 1640: `on:click:button--primary={setReference}` -> `onprimary={setReference}`

**XLSTransfer.svelte** (2 events across 1 Modal):
1. Change import: Remove Modal from `carbon-components-svelte` import, add `import AppModal from '../common/AppModal.svelte';`
2. Find the 1 `<Modal` tag and replace with `<AppModal`
3. Replace 2 event handlers:
   - Line 1641: `on:click:button--secondary={closeUploadSettings}` -> `onsecondary={closeUploadSettings}`
   - Line 1642: `on:click:button--primary={executeUploadSettings}` -> `onprimary={executeUploadSettings}`

IMPORTANT: These are large files. Only change the import line and the specific on:click:button lines. Do NOT modify any other code, styling, or business logic. Read each file fully before editing to understand the import structure.
  </action>
  <verify>
    <automated>cd locaNext && for f in src/lib/components/apps/KRSimilar.svelte src/lib/components/apps/QuickSearch.svelte src/lib/components/apps/XLSTransfer.svelte; do echo "$f: $(grep -c 'on:click:button' $f) on:click:button, $(grep -c 'AppModal' $f) AppModal"; done</automated>
  </verify>
  <acceptance_criteria>
    - KRSimilar.svelte: 0 occurrences of `on:click:button`, 4+ occurrences of `AppModal`
    - QuickSearch.svelte: 0 occurrences of `on:click:button`, 3+ occurrences of `AppModal`
    - XLSTransfer.svelte: 0 occurrences of `on:click:button`, 1+ occurrences of `AppModal`
  </acceptance_criteria>
  <done>3 app files migrated to AppModal with zero on:click:button patterns remaining</done>
</task>

<task type="auto">
  <name>Task 2: Migrate FilesPage + full codebase verification</name>
  <files>locaNext/src/lib/components/pages/FilesPage.svelte</files>
  <read_first>
    locaNext/src/lib/components/pages/FilesPage.svelte
  </read_first>
  <action>
**FilesPage.svelte** (6 events across 3 Modals):
1. Change import: Remove Modal from `carbon-components-svelte` import, add `import AppModal from '../common/AppModal.svelte';` (FilesPage is in `pages/`, so path is `'../common/AppModal.svelte'`)
2. Find all 3 `<Modal` tags and replace with `<AppModal`
3. Replace 6 event handlers:
   - Line 2804: `on:click:button--primary={executeAssignPlatform}` -> `onprimary={executeAssignPlatform}`
   - Line 2805: `on:click:button--secondary={() => { showAssignPlatformModal = false; assignPlatformTarget = null; }}` -> `onsecondary={() => { showAssignPlatformModal = false; assignPlatformTarget = null; }}`
   - Line 2853: `on:click:button--primary={registerAsTM}` -> `onprimary={registerAsTM}`
   - Line 2854: `on:click:button--secondary={() => { showTMRegistrationModal = false; }}` -> `onsecondary={() => { showTMRegistrationModal = false; }}`
   - Line 2890: `on:click:button--primary={executeUploadToServer}` -> `onprimary={executeUploadToServer}`
   - Line 2891: `on:click:button--secondary={() => { showUploadToServerModal = false; }}` -> `onsecondary={() => { showUploadToServerModal = false; }}`

**After FilesPage migration, run full codebase verification:**

1. `grep -rn 'on:click:button\|on:select\b' locaNext/src/ --include="*.svelte" | grep -v node_modules | grep -v AppModal.svelte` — must return 0 results
2. `grep -rn 'on:click\b' locaNext/src/ --include="*.svelte" | grep -v node_modules | grep -v AppModal.svelte` — must return 0 results (the Button on:click patterns were fixed in Plan 01)
3. `cd locaNext && npm run build` — must succeed
4. `cd locaNext && npx svelte-check 2>&1 | tail -5` — must show 0 errors, 0 warnings

If any remaining `on:` patterns are found (other than in AppModal.svelte), fix them using the same pattern.
  </action>
  <verify>
    <automated>cd locaNext && echo "=== on:click:button ===" && grep -rn 'on:click:button' src/ --include="*.svelte" | grep -v AppModal.svelte | wc -l && echo "=== on:select ===" && grep -rn 'on:select' src/ --include="*.svelte" | grep -v AppModal.svelte | wc -l && echo "=== on:click ===" && grep -rn '\bon:click\b' src/ --include="*.svelte" | grep -v AppModal.svelte | wc -l && echo "=== Build ===" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - FilesPage.svelte: 0 occurrences of `on:click:button`, 3+ occurrences of `AppModal`
    - CODEBASE: grep for `on:click:button` in locaNext/src/ (excluding AppModal.svelte) returns 0
    - CODEBASE: grep for `on:select` in locaNext/src/ (excluding AppModal.svelte) returns 0
    - CODEBASE: grep for `on:click` in locaNext/src/ (excluding AppModal.svelte) returns 0
    - Build succeeds with no errors
    - svelte-check passes with 0 errors
  </acceptance_criteria>
  <done>FilesPage migrated. Full codebase verification: zero Svelte 4 on:event patterns remain (except in AppModal wrapper). Build and svelte-check both pass.</done>
</task>

</tasks>

<verification>
Full codebase verification (run after all tasks):
1. `grep -rn 'on:click\|on:select\|on:submit\|on:close\|on:change' locaNext/src/ --include="*.svelte" | grep -v node_modules | grep -v AppModal.svelte` — returns 0 results
2. `cd locaNext && npm run build` — succeeds
3. `cd locaNext && npx svelte-check` — 0 errors, 0 warnings
</verification>

<success_criteria>
1. All 4 large files migrated to AppModal
2. ZERO Svelte 4 on:event patterns remain in locaNext/src/ (except AppModal.svelte wrapper)
3. Build passes with zero errors
4. svelte-check passes with zero errors and zero warnings
</success_criteria>

<output>
After completion, create `.planning/phases/99-svelte5-event-migration/99-03-SUMMARY.md`
</output>
