---
phase: 83-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - locaNext/package.json
  - locaNext/vitest.config.ts
  - locaNext/vitest-setup.js
  - locaNext/tests/tagDetector.test.mjs
  - locaNext/tests/tagDetector.e2e.test.mjs
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-06]

must_haves:
  truths:
    - "npx vitest run exits with code 0 from locaNext/ directory"
    - "Existing tagDetector tests pass under Vitest with zero logic changes"
    - "tagDetector tests use Vitest expect() syntax after migration"
    - "Each of the 6 tag patterns has a mutation-killing assertion that fails when the pattern regex is removed"
    - "npm run test:unit runs Vitest with coverage and excludes Playwright spec files"
  artifacts:
    - path: "locaNext/vitest.config.ts"
      provides: "Vitest configuration extending SvelteKit vite config"
      contains: "svelteTesting"
    - path: "locaNext/vitest-setup.js"
      provides: "Test setup with jest-dom matchers"
      contains: "jest-dom/vitest"
    - path: "locaNext/tests/tagDetector.test.mjs"
      provides: "Migrated tagDetector unit tests with expect() syntax"
      contains: "expect("
    - path: "locaNext/tests/tagDetector.e2e.test.mjs"
      provides: "Migrated tagDetector e2e tests with expect() syntax"
      contains: "expect("
  key_links:
    - from: "locaNext/vitest.config.ts"
      to: "locaNext/vite.config.js"
      via: "sveltekit() plugin reuse"
      pattern: "sveltekit\\(\\)"
    - from: "locaNext/package.json"
      to: "locaNext/vitest.config.ts"
      via: "test:unit script"
      pattern: "vitest run --coverage"
---

<objective>
Set up Vitest test infrastructure for the LocaNext Svelte 5 project and migrate existing tagDetector tests from node:assert to Vitest expect() syntax with mutation-killing assertions.

Purpose: Establishes the unit test foundation that all subsequent test plans depend on. Per D-01 through D-08, Vitest is the single test runner with jsdom environment and @testing-library/svelte support.
Output: Working `npm run test:unit` command, vitest.config.ts, migrated tagDetector tests with mutation-killing assertions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-test-infrastructure/83-CONTEXT.md
@.planning/phases/83-test-infrastructure/83-RESEARCH.md

<interfaces>
<!-- Existing vite.config.js that vitest.config.ts must coexist with -->
From locaNext/vite.config.js:
```javascript
import { sveltekit } from '@sveltejs/kit/vite';
import { defineConfig } from 'vite';
export default defineConfig({
  plugins: [sveltekit()],
  base: './',
  server: { host: true, port: 5173, strictPort: true },
  optimizeDeps: { include: ['carbon-components-svelte', 'carbon-icons-svelte'] }
});
```

From locaNext/tests/tagDetector.test.mjs (import pattern):
```javascript
import assert from 'node:assert';
import { describe, it } from 'node:test';
import { detectTags, hasTags } from '../src/lib/utils/tagDetector.js';
```

From locaNext/src/lib/utils/tagDetector.js (exports):
```javascript
// Exports: detectTags(text), hasTags(text)
// 6 patterns: combinedcolor, staticinfo, param, braced, escape, desc
```

From locaNext/package.json (current scripts):
```json
{
  "test": "playwright test",
  "test:headed": "playwright test --headed",
  "test:ui": "playwright test --ui",
  "test:report": "playwright show-report"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest stack and create configuration</name>
  <files>locaNext/package.json, locaNext/vitest.config.ts, locaNext/vitest-setup.js</files>
  <read_first>
    - locaNext/package.json (current deps and scripts)
    - locaNext/vite.config.js (base vite config to extend)
    - locaNext/svelte.config.js (Svelte preprocessing config)
    - .planning/phases/83-test-infrastructure/83-RESEARCH.md (exact versions and patterns)
  </read_first>
  <action>
    1. Install dev dependencies per D-05 (from locaNext/ directory):
       ```bash
       cd locaNext && npm install -D vitest@^4.1.0 @testing-library/svelte@^5.3.0 @testing-library/jest-dom@^6.9.0 jsdom@^29.0.0 @vitest/coverage-v8@^4.1.0
       ```

    2. Create `locaNext/vitest.config.ts` per D-06 with these EXACT contents:
       - Import `defineConfig` from `vitest/config`
       - Import `sveltekit` from `@sveltejs/kit/vite`
       - Import `svelteTesting` from `@testing-library/svelte/vite`
       - Plugins: `[sveltekit(), svelteTesting()]`
       - test.environment: `'jsdom'`
       - test.setupFiles: `['./vitest-setup.js']`
       - test.include: `['tests/**/*.test.{js,mjs,ts}', 'tests/**/*.svelte.test.ts']` per D-07
       - test.exclude: `['tests/**/*.spec.*', 'node_modules/**']` (exclude Playwright tests)
       - coverage.provider: `'v8'`
       - coverage.include: `['src/lib/utils/tagDetector.js', 'src/lib/utils/statusColors.ts', 'src/lib/components/ldm/TagText.svelte']`
       - coverage.reporter: `['text', 'html']`
       - coverage.thresholds: statements 80, branches 70, functions 80, lines 80

    3. Create `locaNext/vitest-setup.js` with single line:
       ```javascript
       import '@testing-library/jest-dom/vitest';
       ```

    4. Add npm scripts to package.json per D-04:
       - `"test:unit": "vitest run --coverage"`
       - `"test:unit:watch": "vitest"`
       Do NOT modify existing `"test"` script (that's Playwright).

    5. Smoke test: Run `cd locaNext && npx vitest run tests/tagDetector.test.mjs` to verify existing tests pass under Vitest with node:assert (per D-02). They should pass with ZERO syntax changes. If they fail on relative imports, the sveltekit() plugin handles module resolution.

    6. Verify Svelte 5 rune support per D-08: Create a minimal file `locaNext/tests/runes-smoke.svelte.test.ts` that imports `$state` to confirm the `.svelte.test.ts` naming convention works. This file should:
       - Use `import { render } from '@testing-library/svelte'`
       - Render a minimal inline component or just verify the test file compiles without `$state is not defined` errors
       - Delete this smoke test after confirming it works, OR keep it as a canary
  </action>
  <verify>
    <automated>cd locaNext && npx vitest run tests/tagDetector.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - locaNext/vitest.config.ts exists and contains `svelteTesting`
    - locaNext/vitest.config.ts contains `environment: 'jsdom'`
    - locaNext/vitest.config.ts contains `'tests/**/*.spec.*'` in exclude array
    - locaNext/vitest-setup.js contains `import '@testing-library/jest-dom/vitest'`
    - locaNext/package.json contains `"test:unit": "vitest run --coverage"`
    - locaNext/package.json contains `"test:unit:watch": "vitest"`
    - `cd locaNext && npx vitest run tests/tagDetector.test.mjs` exits with code 0
    - `cd locaNext && npx vitest run tests/tagDetector.e2e.test.mjs` exits with code 0
    - vitest, @testing-library/svelte, jsdom, @vitest/coverage-v8, @testing-library/jest-dom appear in devDependencies
  </acceptance_criteria>
  <done>Vitest configured, all existing tagDetector tests pass under Vitest with zero changes, npm run test:unit script exists, Svelte 5 rune smoke test passes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate tagDetector tests to expect() syntax and add mutation-killing assertions</name>
  <files>locaNext/tests/tagDetector.test.mjs, locaNext/tests/tagDetector.e2e.test.mjs</files>
  <read_first>
    - locaNext/tests/tagDetector.test.mjs (full file -- 375 lines of current node:assert tests)
    - locaNext/tests/tagDetector.e2e.test.mjs (full file -- 261 lines of current node:assert tests)
    - locaNext/src/lib/utils/tagDetector.js (full file -- source being tested, need to see all 6 patterns)
    - .planning/phases/83-test-infrastructure/83-RESEARCH.md (migration example pattern)
  </read_first>
  <behavior>
    Migration rules (per D-03):
    - `assert.strictEqual(a, b)` -> `expect(a).toBe(b)`
    - `assert.deepStrictEqual(a, b)` -> `expect(a).toEqual(b)`
    - `assert.ok(a)` -> `expect(a).toBeTruthy()`
    - `assert.strictEqual(result.length, N)` -> `expect(result).toHaveLength(N)`
    - Remove `import assert from 'node:assert'` and `import { describe, it } from 'node:test'`
    - Add `import { describe, it, expect } from 'vitest'`
    - Keep relative import `from '../src/lib/utils/tagDetector.js'` (works with sveltekit plugin)

    Mutation-killing assertions (per D-10) -- add one describe block per pattern:
    - combinedcolor: detectTags with PAColor input returns tag with type 'combinedcolor'
    - staticinfo: detectTags with {StaticInfo:...} returns tag with type 'staticinfo'
    - param: detectTags with %1# returns tag with type 'param'
    - braced: detectTags with {0} returns tag with type 'braced'
    - escape: detectTags with \n returns tag with type 'escape'
    - desc: detectTags with &desc; returns tag with type 'desc'
    Each test verifies tag.type is the expected value, which can ONLY be true if that pattern's regex exists.
  </behavior>
  <action>
    1. In tagDetector.test.mjs:
       - Replace `import assert from 'node:assert'` with nothing (remove)
       - Replace `import { describe, it } from 'node:test'` with `import { describe, it, expect } from 'vitest'`
       - Replace ALL `assert.strictEqual(a, b)` with `expect(a).toBe(b)`
       - Replace ALL `assert.deepStrictEqual(a, b)` with `expect(a).toEqual(b)`
       - Replace ALL `assert.strictEqual(result.length, N)` with `expect(result).toHaveLength(N)`
       - Replace ALL `assert.ok(x)` with `expect(x).toBeTruthy()`
       - Preserve ALL existing test structure and grouping (per CONTEXT.md: preserve organization)

    2. Add a new describe block "mutation-killing: pattern X is essential" for each of the 6 patterns at the end of tagDetector.test.mjs. Each test calls `detectTags()` with input that ONLY matches that specific pattern and asserts `result[0].tag.type` equals the pattern name. These tests pass now but would FAIL if the pattern regex were removed from TAG_PATTERNS.

    3. In tagDetector.e2e.test.mjs:
       - Same import replacement as step 1
       - Same assertion syntax migration
       - Preserve all test structure

    4. Run `cd locaNext && npx vitest run tests/tagDetector.test.mjs tests/tagDetector.e2e.test.mjs` to confirm all tests pass after migration.
  </action>
  <verify>
    <automated>cd locaNext && npx vitest run tests/tagDetector.test.mjs tests/tagDetector.e2e.test.mjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - locaNext/tests/tagDetector.test.mjs does NOT contain `import assert from 'node:assert'`
    - locaNext/tests/tagDetector.test.mjs does NOT contain `import { describe, it } from 'node:test'`
    - locaNext/tests/tagDetector.test.mjs contains `import { describe, it, expect } from 'vitest'`
    - locaNext/tests/tagDetector.test.mjs contains `expect(` (at least 30 occurrences)
    - locaNext/tests/tagDetector.test.mjs contains `mutation-killing` (6 pattern blocks)
    - locaNext/tests/tagDetector.e2e.test.mjs contains `import { describe, it, expect } from 'vitest'`
    - locaNext/tests/tagDetector.e2e.test.mjs does NOT contain `import assert from 'node:assert'`
    - `cd locaNext && npx vitest run tests/tagDetector.test.mjs tests/tagDetector.e2e.test.mjs` exits with code 0
  </acceptance_criteria>
  <done>Both tagDetector test files migrated to Vitest expect() syntax, all tests pass, 6 mutation-killing assertion blocks added (one per pattern type)</done>
</task>

</tasks>

<verification>
1. `cd locaNext && npm run test:unit` exits with code 0
2. All tagDetector tests use `expect()` syntax (no `assert.` remaining)
3. vitest.config.ts excludes `*.spec.*` files (Playwright)
4. Coverage report generated showing tagDetector.js coverage
</verification>

<success_criteria>
- `npm run test:unit` from locaNext/ runs Vitest with coverage and exits 0
- Zero `node:assert` or `node:test` imports remain in test files
- 6 mutation-killing assertions exist (one per tag pattern)
- Svelte 5 rune smoke test confirms *.svelte.test.ts files compile correctly
</success_criteria>

<output>
After completion, create `.planning/phases/83-test-infrastructure/83-01-SUMMARY.md`
</output>
