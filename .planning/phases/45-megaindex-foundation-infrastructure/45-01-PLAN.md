---
phase: 45-megaindex-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/tools/ldm/services/perforce_path_service.py
  - server/tools/ldm/services/mega_index_schemas.py
  - server/tools/ldm/services/mapdata_service.py
  - server/tools/ldm/routes/mapdata.py
autonomous: true
requirements: [INFRA-01]
must_haves:
  truths:
    - "PerforcePathService resolves all 11 path templates with configurable drive letter and branch"
    - "MapDataService delegates path resolution to PerforcePathService instead of using inline PATH_TEMPLATES"
    - "Data entry schemas exist for all 10 MegaIndex entity types as frozen dataclasses with slots"
    - "API endpoint allows configuring drive letter and branch, returns resolved paths"
  artifacts:
    - path: "server/tools/ldm/services/perforce_path_service.py"
      provides: "Centralized Perforce path resolution with 11 templates, drive/branch substitution, WSL conversion"
      exports: ["PerforcePathService", "get_perforce_path_service"]
    - path: "server/tools/ldm/services/mega_index_schemas.py"
      provides: "All 10 frozen dataclass schemas for MegaIndex entries"
      contains: "KnowledgeEntry"
    - path: "server/tools/ldm/services/mapdata_service.py"
      provides: "MapDataService modified to import from PerforcePathService"
  key_links:
    - from: "server/tools/ldm/services/perforce_path_service.py"
      to: "server/tools/ldm/services/mapdata_service.py"
      via: "get_perforce_path_service() import"
      pattern: "from.*perforce_path_service.*import"
---

<objective>
Extract PerforcePathService from MapDataService and create MegaIndex data schemas.

Purpose: Eliminate Pitfall #5 (duplicated path templates) by extracting path resolution into a shared service BEFORE any new Codex service is built. Create the frozen dataclass schemas that MegaIndex will populate in Plan 03.
Output: PerforcePathService singleton + 10 dataclass schemas + MapDataService refactored to delegate + settings API endpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/MEGAINDEX_DESIGN.md
@.planning/research/PERFORCE_PATH_TEMPLATES.md
@.planning/research/ARCHITECTURE.md

@server/tools/ldm/services/mapdata_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PerforcePathService + MegaIndex schemas</name>
  <files>server/tools/ldm/services/perforce_path_service.py, server/tools/ldm/services/mega_index_schemas.py</files>
  <read_first>
    - server/tools/ldm/services/mapdata_service.py (source of PATH_TEMPLATES, KNOWN_BRANCHES, generate_paths, convert_to_wsl_path)
    - .planning/research/PERFORCE_PATH_TEMPLATES.md (all 11 templates, drive/branch substitution rules)
    - .planning/research/MEGAINDEX_DESIGN.md (Section 8: Data Entry Schemas -- exact field definitions for all 10 frozen dataclasses)
  </read_first>
  <action>
Create two new files:

**perforce_path_service.py:**
- Move `PATH_TEMPLATES` dict (all 11 entries), `KNOWN_BRANCHES` list, `convert_to_wsl_path()`, and `generate_paths()` from `mapdata_service.py` into this file.
- Add `KNOWN_DRIVES = ["C", "D", "E", "F", "G"]` (from QACompiler config.py).
- Add `LANG_TO_AUDIO_FOLDER = {"eng": "audio_folder", "kor": "audio_folder_kr", "zho-cn": "audio_folder_zh"}` mapping.
- Create `PerforcePathService` class with:
  - `__init__(self)`: stores `_drive = "F"`, `_branch = "mainline"`, `_resolved_paths: Dict[str, Path] = {}`
  - `configure(self, drive: str, branch: str) -> None`: validates drive is single alpha char, branch is in KNOWN_BRANCHES, regenerates resolved paths. Logs with `logger.info(f"[PERFORCE] Configured: drive={drive}, branch={branch}")`
  - `resolve(self, key: str) -> Path`: returns resolved WSL Path for a template key. Raises `KeyError` if key not in PATH_TEMPLATES.
  - `resolve_audio_folder(self, language: str = "eng") -> Path`: uses LANG_TO_AUDIO_FOLDER to pick the right audio template key, returns resolved Path.
  - `get_all_resolved(self) -> Dict[str, Path]`: returns all 11 resolved paths.
  - `get_status(self) -> dict`: returns `{"drive": self._drive, "branch": self._branch, "paths_resolved": len(self._resolved_paths), "known_branches": KNOWN_BRANCHES, "known_drives": KNOWN_DRIVES}`
  - Internal `_regenerate_paths(self) -> None`: calls generate_paths(drive, branch), then convert_to_wsl_path on each, stores as Path objects in `_resolved_paths`.
- Singleton pattern: `_service_instance: Optional[PerforcePathService] = None` and `get_perforce_path_service() -> PerforcePathService`.
- Use `from loguru import logger` (NEVER print()).
- Add `from __future__ import annotations` at top.

**mega_index_schemas.py:**
- Add `from __future__ import annotations` at top.
- Create all 10 frozen dataclass schemas from MEGAINDEX_DESIGN.md Section 8:
  - `KnowledgeEntry(strkey, name, desc, ui_texture_name, group_key, source_file)` -- all `str`
  - `CharacterEntry(strkey, name, desc, knowledge_key, use_macro, age, job, ui_icon_path, source_file)` -- all `str`
  - `ItemEntry(strkey, name, desc, knowledge_key, group_key, source_file, inspect_entries: Tuple[Tuple[str, str, str, str], ...])` -- inspect_entries default `()`
  - `RegionEntry(strkey, name, desc, knowledge_key, world_position: Optional[Tuple[float, float, float]], node_type, parent_strkey, source_file, display_name)` -- world_position default `None`
  - `FactionEntry(strkey, name, knowledge_key, group_strkey, source_file, node_strkeys: Tuple[str, ...])` -- node_strkeys default `()`
  - `FactionGroupEntry(strkey, group_name, knowledge_key, source_file, faction_strkeys: Tuple[str, ...])` -- faction_strkeys default `()`
  - `SkillEntry(strkey, name, desc, learn_knowledge_key, source_file)` -- all `str`
  - `GimmickEntry(strkey, name, desc, seal_desc, source_file)` -- all `str`
  - `ItemGroupNode(strkey, group_name, parent_strkey, child_strkeys: Tuple[str, ...], item_strkeys: Tuple[str, ...])` -- tuples default `()`
  - `KnowledgeGroupNode(strkey, group_name, child_strkeys: Tuple[str, ...])` -- tuple default `()`
- All use `@dataclass(frozen=True, slots=True)`.
- All `str` fields that could be empty default to `""`.
- Import from `dataclasses` and `typing`.
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools && python3 -c "
from server.tools.ldm.services.perforce_path_service import get_perforce_path_service, PerforcePathService, PATH_TEMPLATES, KNOWN_BRANCHES, KNOWN_DRIVES, convert_to_wsl_path
svc = get_perforce_path_service()
svc.configure('F', 'mainline')
p = svc.resolve('knowledge_folder')
assert '/mnt/f/' in str(p), f'WSL path expected, got {p}'
assert len(PATH_TEMPLATES) == 11, f'Expected 11 templates, got {len(PATH_TEMPLATES)}'
ap = svc.resolve_audio_folder('kor')
assert 'Korean' in str(ap), f'Expected Korean audio path, got {ap}'
status = svc.get_status()
assert status['drive'] == 'F'
print('PerforcePathService OK')

from server.tools.ldm.services.mega_index_schemas import KnowledgeEntry, CharacterEntry, ItemEntry, RegionEntry, FactionEntry, FactionGroupEntry, SkillEntry, GimmickEntry, ItemGroupNode, KnowledgeGroupNode
k = KnowledgeEntry(strkey='test', name='n', desc='d', ui_texture_name='t', group_key='g', source_file='f')
assert k.strkey == 'test'
try:
    k.strkey = 'other'
    assert False, 'Should be frozen'
except Exception:
    pass
r = RegionEntry(strkey='r', name='n', desc='d', knowledge_key='k', world_position=(1.0, 2.0, 3.0), node_type='Main', parent_strkey='p', source_file='f', display_name='dn')
assert r.world_position == (1.0, 2.0, 3.0)
print('MegaIndex schemas OK')
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `perforce_path_service.py` exists with `PATH_TEMPLATES` dict containing exactly 11 entries (faction_folder, loc_folder, knowledge_folder, waypoint_folder, texture_folder, character_folder, audio_folder, audio_folder_kr, audio_folder_zh, export_folder, vrs_folder)
    - `perforce_path_service.py` exports `PerforcePathService`, `get_perforce_path_service`, `PATH_TEMPLATES`, `KNOWN_BRANCHES`, `KNOWN_DRIVES`, `convert_to_wsl_path`, `generate_paths`
    - `PerforcePathService.resolve('knowledge_folder')` returns a Path containing `/mnt/f/` when configured with drive=F
    - `mega_index_schemas.py` contains exactly 10 `@dataclass(frozen=True, slots=True)` classes
    - All schema classes are importable and immutable (assignment raises FrozenInstanceError)
    - Both files have `from __future__ import annotations` as first import
    - Both files use `from loguru import logger` (no print statements)
  </acceptance_criteria>
  <done>PerforcePathService resolves all 11 Perforce path templates with drive/branch substitution and WSL conversion. All 10 MegaIndex data entry schemas exist as frozen dataclasses.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor MapDataService to use PerforcePathService + add settings endpoint</name>
  <files>server/tools/ldm/services/mapdata_service.py, server/tools/ldm/routes/mapdata.py</files>
  <read_first>
    - server/tools/ldm/services/mapdata_service.py (current inline PATH_TEMPLATES and generate_paths usage)
    - server/tools/ldm/services/perforce_path_service.py (newly created in Task 1)
    - server/tools/ldm/routes/mapdata.py (existing routes to add settings endpoint)
  </read_first>
  <action>
**mapdata_service.py changes:**
1. Remove `KNOWN_BRANCHES` list (line 24) -- now in perforce_path_service.py
2. Remove `PATH_TEMPLATES` dict (lines 26-38) -- now in perforce_path_service.py
3. Remove `convert_to_wsl_path()` function -- now in perforce_path_service.py
4. Remove `generate_paths()` function -- now in perforce_path_service.py
5. Keep `ImageContext`, `AudioContext`, `KnowledgeLookup` dataclasses, `build_knowledge_table()`, `build_dds_index()` unchanged.
6. In `MapDataService.__init__`, add: `self._path_service = get_perforce_path_service()`
7. In `MapDataService.initialize(branch, drive)`:
   - Replace `paths = generate_paths(drive, branch)` with `self._path_service.configure(drive, branch)` then `knowledge_folder = self._path_service.resolve('knowledge_folder')` and `texture_folder = self._path_service.resolve('texture_folder')`.
   - Remove the `convert_to_wsl_path()` calls (PerforcePathService.resolve() already returns WSL Path).
   - Pass the `Path` objects directly to `build_knowledge_table(knowledge_folder, parser)` and `build_dds_index(texture_folder)`.
8. Add import: `from server.tools.ldm.services.perforce_path_service import get_perforce_path_service`
9. Keep all other methods (`get_image_context`, `get_audio_context`, `get_knowledge_lookup`, `get_status`, `_resolve_image_chains`, `_lazy_load_audio`) unchanged.
10. In `get_status()`, add path_service status: `"path_service": self._path_service.get_status()`

**mapdata.py route changes:**
Add two new endpoints:
1. `GET /api/ldm/mapdata/paths/status` -- returns `get_perforce_path_service().get_status()`
2. `POST /api/ldm/mapdata/paths/configure` -- accepts JSON body `{"drive": "F", "branch": "mainline"}`, calls `get_perforce_path_service().configure(drive, branch)`, then re-initializes MapDataService with the new drive/branch. Returns the new status. Use Pydantic model `PathConfigRequest(drive: str, branch: str)` for validation.

Import `get_perforce_path_service` from `server.tools.ldm.services.perforce_path_service`.
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools && python3 -c "
from server.tools.ldm.services.mapdata_service import MapDataService, get_mapdata_service
from server.tools.ldm.services.perforce_path_service import get_perforce_path_service
import inspect

# Verify PATH_TEMPLATES removed from mapdata_service
src = inspect.getsource(MapDataService)
assert 'PATH_TEMPLATES' not in src, 'PATH_TEMPLATES should be removed from MapDataService class'

# Verify perforce_path_service import exists
mod_src = open('server/tools/ldm/services/mapdata_service.py').read()
assert 'perforce_path_service' in mod_src, 'Should import from perforce_path_service'
assert 'KNOWN_BRANCHES' not in mod_src.split('import')[0], 'KNOWN_BRANCHES should be removed'

# Verify MapDataService still works
svc = get_mapdata_service()
status = svc.get_status()
assert 'loaded' in status
print('MapDataService refactored OK')

# Check route file has new endpoints
route_src = open('server/tools/ldm/routes/mapdata.py').read()
assert 'paths/status' in route_src, 'Missing paths/status endpoint'
assert 'paths/configure' in route_src, 'Missing paths/configure endpoint'
print('Routes OK')
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `mapdata_service.py` no longer contains `PATH_TEMPLATES`, `KNOWN_BRANCHES`, `convert_to_wsl_path`, or `generate_paths` as top-level definitions
    - `mapdata_service.py` imports `get_perforce_path_service` from `server.tools.ldm.services.perforce_path_service`
    - `MapDataService.initialize()` calls `self._path_service.configure(drive, branch)` then `self._path_service.resolve(key)` instead of `generate_paths()` + `convert_to_wsl_path()`
    - `mapdata.py` contains `GET /api/ldm/mapdata/paths/status` endpoint
    - `mapdata.py` contains `POST /api/ldm/mapdata/paths/configure` endpoint accepting `{"drive": str, "branch": str}`
    - Existing `build_knowledge_table`, `build_dds_index`, `ImageContext`, `AudioContext`, `KnowledgeLookup` remain in mapdata_service.py unchanged
  </acceptance_criteria>
  <done>MapDataService delegates all path resolution to PerforcePathService. PATH_TEMPLATES no longer duplicated. Settings API endpoints allow configuring drive/branch.</done>
</task>

</tasks>

<verification>
1. `python3 -c "from server.tools.ldm.services.perforce_path_service import get_perforce_path_service; s=get_perforce_path_service(); s.configure('D','cd_beta'); print(s.resolve('audio_folder_kr'))"` -- prints WSL path containing `/mnt/d/` and `cd_beta` and `Korean`
2. `python3 -c "from server.tools.ldm.services.mapdata_service import MapDataService; import inspect; assert 'PATH_TEMPLATES' not in inspect.getsource(MapDataService); print('No inline PATH_TEMPLATES')"` -- confirms removal
3. `grep -r "PATH_TEMPLATES" server/tools/ldm/services/ --include="*.py" -l` -- should only appear in `perforce_path_service.py`
</verification>

<success_criteria>
- PerforcePathService is the single source of truth for all 11 Perforce path templates
- MapDataService uses PerforcePathService (no inline PATH_TEMPLATES)
- All 10 MegaIndex data entry schemas exist as frozen dataclasses with slots
- Drive/branch configuration is available via API endpoint
- No new dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/45-megaindex-foundation-infrastructure/45-01-SUMMARY.md`
</output>
