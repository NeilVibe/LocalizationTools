---
phase: 45-megaindex-foundation-infrastructure
plan: 04
type: execute
wave: 3
depends_on: ["45-03"]
files_modified:
  - server/tools/ldm/services/codex_service.py
  - server/tools/ldm/services/mapdata_service.py
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03]
must_haves:
  truths:
    - "CodexService reads entity data from MegaIndex instead of scanning XML independently"
    - "MapDataService reads knowledge table and DDS index from MegaIndex instead of building independently"
    - "Existing API endpoints return same data shape as before migration"
    - "FAISS search index still builds correctly from MegaIndex entity data"
    - "get_image_context and get_audio_context delegate to MegaIndex composed dicts"
  artifacts:
    - path: "server/tools/ldm/services/codex_service.py"
      provides: "CodexService wired to MegaIndex for entity data"
      contains: "get_mega_index"
    - path: "server/tools/ldm/services/mapdata_service.py"
      provides: "MapDataService wired to MegaIndex for knowledge/DDS/audio data"
      contains: "get_mega_index"
  key_links:
    - from: "server/tools/ldm/services/codex_service.py"
      to: "server/tools/ldm/services/mega_index.py"
      via: "get_mega_index() import, iterate entities for FAISS"
      pattern: "from.*mega_index.*import"
    - from: "server/tools/ldm/services/mapdata_service.py"
      to: "server/tools/ldm/services/mega_index.py"
      via: "get_mega_index() import, delegate lookups"
      pattern: "from.*mega_index.*import"
---

<objective>
Wire CodexService and MapDataService to consume MegaIndex instead of doing independent XML parsing.

Purpose: Complete the migration path (MEGAINDEX_DESIGN.md Section 10). After this plan, there is ONE parse of game data (MegaIndex.build()) instead of three independent scans. Existing API endpoints continue to work with same response shapes.
Output: CodexService._registry populated from MegaIndex. MapDataService._knowledge_table, _dds_index, _strkey_to_image, _strkey_to_audio all delegated to MegaIndex.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/MEGAINDEX_DESIGN.md

@server/tools/ldm/services/codex_service.py
@server/tools/ldm/services/mapdata_service.py
@server/tools/ldm/services/mega_index.py
@server/tools/ldm/services/mega_index_schemas.py

<interfaces>
From server/tools/ldm/services/mega_index.py:
```python
class MegaIndex:
    # Direct dicts
    knowledge_by_strkey: Dict[str, KnowledgeEntry]
    character_by_strkey: Dict[str, CharacterEntry]
    item_by_strkey: Dict[str, ItemEntry]
    region_by_strkey: Dict[str, RegionEntry]
    skill_by_strkey: Dict[str, SkillEntry]
    gimmick_by_strkey: Dict[str, GimmickEntry]
    dds_by_stem: Dict[str, Path]
    wem_by_event: Dict[str, Path]

    # Composed dicts
    strkey_to_image_path: Dict[str, Path]
    stringid_to_audio_path: Dict[str, Path]
    event_to_script_kr: Dict[str, str]
    event_to_script_eng: Dict[str, str]
    stringid_to_entity: Dict[str, Tuple[str, str]]

    # Reverse dicts
    knowledge_key_to_entities: Dict[str, List[Tuple[str, str]]]
    source_file_to_strkeys: Dict[str, List[Tuple[str, str]]]

    # Methods
    def build(self, preload_langs=None) -> None
    def get_entity(self, entity_type, strkey) -> Optional[Any]
    def entity_counts() -> Dict[str, int]
    def stats() -> Dict[str, Any]
```

From server/tools/ldm/schemas/codex.py:
```python
class CodexEntity(BaseModel):
    entity_type: str
    strkey: str
    name: str
    description: Optional[str]
    knowledge_key: Optional[str]
    image_texture: Optional[str]
    audio_key: Optional[str]
    source_file: str
    attributes: Dict[str, str]
    related_entities: List[str]
    ai_image_url: Optional[str]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire CodexService to MegaIndex</name>
  <files>server/tools/ldm/services/codex_service.py</files>
  <read_first>
    - server/tools/ldm/services/codex_service.py (full file -- understand _scan_entities, _resolve_cross_refs, _find_related_entities, _build_search_index, and all public API methods)
    - server/tools/ldm/services/mega_index.py (entity dict names and data schemas)
    - server/tools/ldm/services/mega_index_schemas.py (KnowledgeEntry, CharacterEntry, etc. field names)
    - server/tools/ldm/schemas/codex.py (CodexEntity Pydantic model that API returns)
  </read_first>
  <action>
Modify `CodexService` to populate its `_registry` from MegaIndex instead of scanning XML independently.

**Changes to `initialize()`:**
1. Before the existing `self._scan_entities()` call, trigger MegaIndex build:
   ```python
   from server.tools.ldm.services.mega_index import get_mega_index
   mega = get_mega_index()
   if not mega._built:
       mega.build()
   ```
2. Replace `self._scan_entities()` with `self._populate_from_mega_index()`.
3. Keep `self._resolve_cross_refs()` -- it still works on `_registry` (but MegaIndex already resolved knowledge, so this is now a no-op for most entities. Keep it for safety/compatibility).
4. Keep `self._find_related_entities()` -- still derives from `_registry`.
5. Keep `self._build_search_index()` -- FAISS still builds from `_registry` entity names+descriptions.

**New method `_populate_from_mega_index(self)`:**
Convert MegaIndex frozen dataclasses to CodexEntity Pydantic models and populate `_registry`.

For each entity type mapping:
- `"knowledge"` <- `mega.knowledge_by_strkey`: `CodexEntity(entity_type="knowledge", strkey=e.strkey, name=e.name, description=e.desc, knowledge_key=None, image_texture=e.ui_texture_name, audio_key=e.strkey, source_file=e.source_file, attributes={"GroupKey": e.group_key}, related_entities=[])`
- `"character"` <- `mega.character_by_strkey`: `CodexEntity(entity_type="character", strkey=e.strkey, name=e.name, description=e.desc, knowledge_key=e.knowledge_key, image_texture=None, audio_key=e.strkey, source_file=e.source_file, attributes={"UseMacro": e.use_macro, "Age": e.age, "Job": e.job, "UIIconPath": e.ui_icon_path}, related_entities=[])`
- `"item"` <- `mega.item_by_strkey`: `CodexEntity(entity_type="item", strkey=e.strkey, name=e.name, description=e.desc, knowledge_key=e.knowledge_key, image_texture=None, audio_key=e.strkey, source_file=e.source_file, attributes={"GroupKey": e.group_key}, related_entities=[])`
- `"region"` <- `mega.region_by_strkey`: include WorldPosition in attributes as `"WorldPosition": f"{e.world_position[0]},{e.world_position[1]},{e.world_position[2]}"` if world_position is not None. Set `name=e.display_name or e.name`.
- `"skill"` <- `mega.skill_by_strkey`: `knowledge_key=e.learn_knowledge_key`.
- `"gimmick"` <- `mega.gimmick_by_strkey`: include `seal_desc` in description.

Also build `self._key_to_strkey` from MegaIndex entities (scan for Key attribute if needed, or skip since MegaIndex handles cross-refs).

**Keep unchanged:**
- `_resolve_cross_refs()` -- still walks knowledge_map for image_texture enrichment (harmless if already resolved)
- `_find_related_entities()` -- still groups by source_file
- `_build_search_index()` -- still builds FAISS from _registry
- ALL public API methods (`search`, `get_entity`, `list_entities`, `get_entity_types`, `get_relationships`) -- they read from `_registry` which is now populated from MegaIndex

**Remove (or comment out with `# LEGACY: replaced by MegaIndex`):**
- `_scan_entities()` method body -- replace with call to `_populate_from_mega_index()`
- `_parse_xml_file()` -- no longer needed
- `_scan_nested()` -- no longer needed
- `_extract_entity()` -- no longer needed

Keep the method signatures as empty stubs or delete entirely. Prefer keeping as deprecated stubs for reference.

**Do NOT change:**
- `ENTITY_TAG_MAP` at module level (other code may reference it)
- `_SKIP_ATTRS` (referenced by existing code)
- Any Pydantic schema (CodexEntity, CodexListResponse, etc.)
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools && python3 -c "
from server.tools.ldm.services.codex_service import CodexService
import inspect

src = inspect.getsource(CodexService)
assert 'mega_index' in src or 'get_mega_index' in src, 'Should import from mega_index'
assert '_populate_from_mega_index' in src, 'Should have _populate_from_mega_index method'

# Verify it still works (empty data)
from pathlib import Path
import tempfile
with tempfile.TemporaryDirectory() as td:
    cs = CodexService(td)
    cs.initialize()
    types = cs.get_entity_types()
    print(f'Entity types (empty): {types}')
    results = cs.search('test')
    print(f'Search results (empty): {results.count}')

print('CodexService migration OK')
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `codex_service.py` imports `get_mega_index` from `mega_index`
    - `_populate_from_mega_index` method exists and converts MegaIndex schemas to CodexEntity models
    - `initialize()` calls `mega.build()` then `_populate_from_mega_index()` instead of `_scan_entities()`
    - All 6 entity types populated from MegaIndex: character, item, skill, gimmick, knowledge, region
    - `_build_search_index()` still builds FAISS from `_registry` (unchanged)
    - Public API methods (`search`, `get_entity`, `list_entities`, `get_entity_types`, `get_relationships`) unchanged
    - `ENTITY_TAG_MAP` and `_SKIP_ATTRS` still present at module level
    - Legacy XML parsing methods removed or marked deprecated
  </acceptance_criteria>
  <done>CodexService reads entity data from MegaIndex. No independent XML scanning. FAISS search still works. All API endpoints return same data shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire MapDataService to MegaIndex</name>
  <files>server/tools/ldm/services/mapdata_service.py</files>
  <read_first>
    - server/tools/ldm/services/mapdata_service.py (current state after Plan 01 refactor -- uses PerforcePathService)
    - server/tools/ldm/services/mega_index.py (strkey_to_image_path, stringid_to_audio_path, knowledge_by_strkey, dds_by_stem)
    - server/tools/ldm/services/mega_index_schemas.py (KnowledgeEntry fields)
  </read_first>
  <action>
Modify `MapDataService` to delegate lookups to MegaIndex instead of building its own indexes.

**Changes to `initialize()`:**
1. After configuring PerforcePathService (keep that), trigger MegaIndex build:
   ```python
   from server.tools.ldm.services.mega_index import get_mega_index
   mega = get_mega_index()
   if not mega._built:
       mega.build()
   ```
2. Remove the calls to `build_knowledge_table()` and `build_dds_index()`.
3. Remove the call to `self._resolve_image_chains()`.
4. Instead, populate internal caches from MegaIndex:
   - `self._knowledge_table` = build from `mega.knowledge_by_strkey`: for each `KnowledgeEntry`, create a `KnowledgeLookup(strkey=e.strkey, name=e.name, desc=e.desc, ui_texture_name=e.ui_texture_name, group_key=e.group_key, source_file=e.source_file)`.
   - `self._dds_index` = `mega.dds_by_stem` (direct reference, same type `Dict[str, Path]`).
   - `self._strkey_to_image` = build from `mega.strkey_to_image_path`: for each `(strkey, dds_path)`, create `ImageContext(texture_name=mega.knowledge_by_strkey[strkey].ui_texture_name, dds_path=str(dds_path), thumbnail_url=f"/api/ldm/mapdata/thumbnail/{mega.knowledge_by_strkey[strkey].ui_texture_name}", has_image=True)`.
5. Set `self._loaded = True`.

**Changes to `get_audio_context()`:**
Replace the lazy WAV scanning stub with MegaIndex-backed real audio context:
1. Try `mega.stringid_to_audio_path.get(string_id)` for direct StringId->WEM lookup (C3).
2. If found, get script lines: `mega.event_to_script_kr` and `mega.event_to_script_eng` using the event name from `mega.stringid_to_event.get(string_id)`.
3. Build `AudioContext(event_name=event, wem_path=str(wem_path), script_kr=script_kr, script_eng=script_eng)`.
4. Keep fuzzy fallback for cases where StringId doesn't have direct audio mapping.
5. Keep `_lazy_load_audio()` as fallback for TTS-generated WAV files.

**Remove:**
- `build_knowledge_table()` function (top-level) -- MOVED to MegaIndex
- `build_dds_index()` function (top-level) -- MOVED to MegaIndex
- `_resolve_image_chains()` method -- REPLACED by MegaIndex C1

**Keep:**
- `ImageContext`, `AudioContext`, `KnowledgeLookup` dataclasses (still used as return types)
- `get_image_context()` public API (still works, now backed by MegaIndex)
- `get_knowledge_lookup()` (still works, reads from `_knowledge_table`)
- `get_status()` (add MegaIndex stats)
- `_lazy_load_audio()` as fallback for TTS WAV files

**In `get_status()`:**
Add MegaIndex build info: `"mega_index_built": mega._built, "mega_index_entity_counts": mega.entity_counts() if mega._built else {}`.
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools && python3 -c "
from server.tools.ldm.services.mapdata_service import MapDataService, get_mapdata_service
import inspect

src = inspect.getsource(MapDataService)
assert 'mega_index' in src or 'get_mega_index' in src, 'Should import mega_index'

# Verify build_knowledge_table and build_dds_index are removed
file_src = open('server/tools/ldm/services/mapdata_service.py').read()
assert 'def build_knowledge_table' not in file_src, 'build_knowledge_table should be removed'
assert 'def build_dds_index' not in file_src, 'build_dds_index should be removed'

# Verify it still works
svc = get_mapdata_service()
status = svc.get_status()
assert 'loaded' in status
print(f'Status: {status}')

# Verify get_audio_context uses MegaIndex
assert 'stringid_to_audio_path' in src or 'mega' in src, 'get_audio_context should use MegaIndex'

print('MapDataService migration OK')
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `mapdata_service.py` imports `get_mega_index` from `mega_index`
    - `initialize()` calls `mega.build()` and populates caches from MegaIndex (not independent XML parsing)
    - `build_knowledge_table()` and `build_dds_index()` top-level functions removed
    - `_resolve_image_chains()` method removed
    - `get_audio_context()` uses MegaIndex C3 (stringid_to_audio_path) and C4/C5 (event_to_script) for real audio context
    - `ImageContext`, `AudioContext`, `KnowledgeLookup` dataclasses still present
    - `get_image_context()`, `get_knowledge_lookup()`, `get_status()` public methods still work
    - `_lazy_load_audio()` kept as TTS WAV fallback
  </acceptance_criteria>
  <done>MapDataService delegates all data lookups to MegaIndex. No independent XML parsing. Audio context now uses real WEM chain instead of TTS-only stub. Image context backed by MegaIndex C1 composed dict.</done>
</task>

</tasks>

<verification>
1. `python3 -c "from server.tools.ldm.services.codex_service import CodexService; from pathlib import Path; import tempfile; cs=CodexService(tempfile.mkdtemp()); cs.initialize(); print(cs.get_entity_types())"` -- works without crashing
2. `python3 -c "from server.tools.ldm.services.mapdata_service import get_mapdata_service; s=get_mapdata_service(); print(s.get_status())"` -- shows MegaIndex info
3. `grep -r "build_knowledge_table\|build_dds_index" server/tools/ldm/services/ --include="*.py"` -- should NOT appear in mapdata_service.py
4. `grep "get_mega_index" server/tools/ldm/services/codex_service.py server/tools/ldm/services/mapdata_service.py` -- both files import MegaIndex
</verification>

<success_criteria>
- Single parse: MegaIndex.build() is the ONE place that parses game data XML
- CodexService and MapDataService both read from MegaIndex
- All existing API endpoints continue to work with same response shapes
- No independent XML scanning in CodexService or MapDataService
- FAISS search index still builds from entity data
- Audio context now has real WEM path resolution (not just TTS stubs)
</success_criteria>

<output>
After completion, create `.planning/phases/45-megaindex-foundation-infrastructure/45-04-SUMMARY.md`
</output>
