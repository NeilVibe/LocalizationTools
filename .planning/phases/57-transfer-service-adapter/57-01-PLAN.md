---
phase: 57-transfer-service-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/transfer_config_shim.py
  - server/services/transfer_adapter.py
  - server/services/__init__.py
  - tests/test_transfer_adapter.py
  - tests/fixtures/transfer/
autonomous: true
requirements: [XFER-01]

must_haves:
  truths:
    - "QuickTranslate core modules import successfully via sys.path with config shim"
    - "Config shim provides LOC_FOLDER, EXPORT_FOLDER, SCRIPT_CATEGORIES, LANGUAGE_NAMES to QT modules"
    - "No Sacred Script code is copied -- adapter uses sys.path import only"
    - "Reconfiguring paths updates the injected config module for project switching"
  artifacts:
    - path: "server/services/transfer_config_shim.py"
      provides: "Synthetic config module creation and injection"
      exports: ["create_config_shim", "inject_config_shim", "reconfigure_paths"]
    - path: "server/services/transfer_adapter.py"
      provides: "QuickTranslate module import wrapper"
      exports: ["init_quicktranslate", "get_qt_modules", "TransferAdapter"]
    - path: "tests/test_transfer_adapter.py"
      provides: "Unit tests for config shim and module import"
      contains: "test_config_shim"
    - path: "tests/fixtures/transfer/source/corrections_FRE.xml"
      provides: "Test XML fixture with known LocStr entries"
    - path: "tests/fixtures/transfer/target/languagedata_FRE.xml"
      provides: "Test target XML fixture for merge verification"
  key_links:
    - from: "server/services/transfer_adapter.py"
      to: "RessourcesForCodingTheProject/NewScripts/QuickTranslate/core/"
      via: "sys.path injection + import"
      pattern: "sys\\.path\\.insert.*QuickTranslate"
    - from: "server/services/transfer_config_shim.py"
      to: "sys.modules[\"config\"]"
      via: "synthetic ModuleType injection"
      pattern: "sys\\.modules\\[.config.\\]"
---

<objective>
Create the adapter import layer that makes QuickTranslate's Sacred Script modules available to LocaNext via sys.path injection and a synthetic config shim, plus test fixtures for all subsequent plans.

Purpose: Foundation for all transfer operations -- without working imports, nothing else in Phase 57 functions.
Output: transfer_config_shim.py, transfer_adapter.py, test fixtures, passing import tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-transfer-service-adapter/57-RESEARCH.md
@.planning/phases/56-mock-data-settings/56-02-SUMMARY.md

<interfaces>
<!-- QuickTranslate config.py attributes needed by core modules (from research) -->
Required config attributes:
- LOC_FOLDER: Path
- EXPORT_FOLDER: Path
- SCRIPT_CATEGORIES: set  (e.g., {"Sequencer", "Dialog"})
- SCRIPT_EXCLUDE_SUBFOLDERS: set  (e.g., set())
- LANGUAGE_NAMES: dict  (e.g., {"FRE": "FRE", "ENG": "ENG"})
- FUZZY_THRESHOLD_DEFAULT: float  (0.85)
- SEQUENCER_FOLDER: Path
- SCRIPT_DIR: Path
- OUTPUT_FOLDER: Path
- SOURCE_FOLDER: Path

<!-- Existing LocaNext service pattern (from server/services/__init__.py) -->
from server.services.sync_service import SyncService
__all__ = ["SyncService"]

<!-- QuickTranslate root path -->
RessourcesForCodingTheProject/NewScripts/QuickTranslate/

<!-- Per-project settings (from 56-02-SUMMARY) -->
locaNext/src/lib/stores/projectSettings.js provides getProjectSettings(projectId)
returns { locPath, exportPath } from localStorage
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config shim module + adapter import layer with test fixtures</name>
  <files>
    server/services/transfer_config_shim.py,
    server/services/transfer_adapter.py,
    server/services/__init__.py,
    tests/test_transfer_adapter.py,
    tests/fixtures/transfer/source/corrections_FRE.xml,
    tests/fixtures/transfer/target/languagedata_FRE.xml,
    tests/fixtures/transfer/export/Dialog/sample.loc.xml
  </files>
  <read_first>
    server/services/__init__.py,
    server/services/sync_service.py,
    RessourcesForCodingTheProject/NewScripts/QuickTranslate/config.py,
    RessourcesForCodingTheProject/NewScripts/QuickTranslate/core/xml_transfer.py (lines 1-50 for imports),
    RessourcesForCodingTheProject/NewScripts/QuickTranslate/core/source_scanner.py (lines 1-30 for imports),
    RessourcesForCodingTheProject/NewScripts/QuickTranslate/core/xml_parser.py (lines 1-30 for LocStr constants)
  </read_first>
  <behavior>
    - Test 1 (test_config_shim_creates_module): create_config_shim("/tmp/loc", "/tmp/export") returns a types.ModuleType with LOC_FOLDER=Path("/tmp/loc"), EXPORT_FOLDER=Path("/tmp/export"), SCRIPT_CATEGORIES={"Sequencer","Dialog"}
    - Test 2 (test_inject_config_shim): After inject_config_shim("/tmp/loc", "/tmp/export"), sys.modules["config"] has LOC_FOLDER attribute
    - Test 3 (test_init_quicktranslate): init_quicktranslate(loc_path, export_path) returns dict with keys "transfer_folder_to_folder", "merge_corrections_to_xml", "merge_corrections_stringid_only", "run_all_postprocess", "scan_source_for_languages"
    - Test 4 (test_reconfigure_paths): After reconfigure_paths(new_loc, new_export), sys.modules["config"].LOC_FOLDER == Path(new_loc)
    - Test 5 (test_no_sacred_script_copied): No file in server/services/ contains "def merge_corrections" or "def run_all_postprocess" (only imports, never copies)
    - Test 6 (test_fixtures_exist): All 3 fixture XML files exist and are valid XML with at least one LocStr element
  </behavior>
  <action>
    **Step 1: Create test fixtures** (3 small XML files for deterministic merge testing):

    `tests/fixtures/transfer/target/languagedata_FRE.xml` -- minimal languagedata with 5 LocStr entries:
    ```xml
    <?xml version="1.0" encoding="utf-8"?>
    <LanguageData>
      <LocStr StringID="STR_HELLO" Str="Bonjour" StrOrigin="Hello" DescOrigin="Greeting"/>
      <LocStr StringID="STR_GOODBYE" Str="" StrOrigin="Goodbye" DescOrigin="Farewell"/>
      <LocStr StringID="STR_YES" Str="" StrOrigin="Yes" DescOrigin="Confirmation"/>
      <LocStr StringID="STR_SCRIPT_01" Str="" StrOrigin="Attack" DescOrigin="Combat action"/>
      <LocStr StringID="STR_DIALOG_01" Str="" StrOrigin="Welcome hero" DescOrigin="NPC dialog"/>
    </LanguageData>
    ```

    `tests/fixtures/transfer/source/corrections_FRE.xml` -- corrections to merge:
    ```xml
    <?xml version="1.0" encoding="utf-8"?>
    <LanguageData>
      <LocStr StringID="STR_HELLO" Str="Salut" StrOrigin="Hello" DescOrigin="Greeting"/>
      <LocStr StringID="STR_GOODBYE" Str="Au revoir" StrOrigin="Goodbye" DescOrigin="Farewell"/>
      <LocStr StringID="STR_YES" Str="Oui" StrOrigin="Yes" DescOrigin="Confirmation"/>
    </LanguageData>
    ```

    `tests/fixtures/transfer/export/Dialog/sample.loc.xml` -- minimal export file for category mapping:
    ```xml
    <?xml version="1.0" encoding="utf-8"?>
    <LanguageData>
      <LocStr StringID="STR_DIALOG_01" Str="Welcome hero" StrOrigin="Welcome hero"/>
      <LocStr StringID="STR_SCRIPT_01" Str="Attack" StrOrigin="Attack"/>
    </LanguageData>
    ```

    **Step 2: Create `server/services/transfer_config_shim.py`**:
    - `from __future__ import annotations`
    - Import: `sys, types, re` from stdlib, `Path` from pathlib, `logger` from loguru
    - `create_config_shim(loc_path: str, export_path: str) -> types.ModuleType`:
      - Creates `types.ModuleType("config")`
      - Sets: `LOC_FOLDER=Path(loc_path)`, `EXPORT_FOLDER=Path(export_path)`, `SCRIPT_CATEGORIES={"Sequencer","Dialog"}`, `SCRIPT_EXCLUDE_SUBFOLDERS=set()`, `LANGUAGE_NAMES={}`, `FUZZY_THRESHOLD_DEFAULT=0.85`, `SEQUENCER_FOLDER=Path(export_path)/"Sequencer"`, `SCRIPT_DIR=Path(loc_path).parent`, `OUTPUT_FOLDER=Path("/tmp/quicktranslate_output")`, `SOURCE_FOLDER=Path("/tmp/quicktranslate_source")`
      - Also set `get_failed_report_dir` as a lambda returning `Path("/tmp/quicktranslate_reports")`
      - Auto-discover language names from LOC folder using `languagedata_*.xml` glob
    - `inject_config_shim(loc_path: str, export_path: str) -> types.ModuleType`:
      - Calls `create_config_shim()`, sets `sys.modules["config"] = shim`, returns shim
    - `reconfigure_paths(loc_path: str, export_path: str)`:
      - Gets existing `sys.modules["config"]`, updates `LOC_FOLDER`, `EXPORT_FOLDER`, `SEQUENCER_FOLDER`
      - If no existing config, calls `inject_config_shim()` instead
      - Tries to call `clear_language_code_cache()` from `core.source_scanner` (wrapped in try/except)

    **Step 3: Create `server/services/transfer_adapter.py`**:
    - `from __future__ import annotations`
    - Import: `sys, Path, logger (loguru)`
    - Import: `from server.services.transfer_config_shim import inject_config_shim, reconfigure_paths`
    - Constant: `QT_ROOT = str(Path(__file__).resolve().parent.parent.parent / "RessourcesForCodingTheProject" / "NewScripts" / "QuickTranslate")`
    - Module-level cache: `_qt_modules: dict | None = None`
    - `init_quicktranslate(loc_path: str, export_path: str) -> dict`:
      1. Call `inject_config_shim(loc_path, export_path)`
      2. Insert `QT_ROOT` at `sys.path[0]` if not already present
      3. Import from `core.xml_transfer`: `transfer_folder_to_folder`, `merge_corrections_to_xml`, `merge_corrections_stringid_only`
      4. Import from `core.postprocess`: `run_all_postprocess`
      5. Import from `core.source_scanner`: `scan_source_for_languages`
      6. Import from `core.language_loader`: `discover_language_files`, `build_translation_lookup`, `build_stringid_to_category`, `build_stringid_to_subfolder`, `build_stringid_to_filepath`
      7. Cache in `_qt_modules`, return dict of all imported functions
      8. Log success with loguru: `logger.info("QuickTranslate modules loaded from {}", QT_ROOT)`
    - `get_qt_modules() -> dict`: Returns cached `_qt_modules` or raises RuntimeError("init_quicktranslate() not called")
    - Class `TransferAdapter`:
      - `__init__(self, loc_path: str, export_path: str)`: stores paths, calls `init_quicktranslate()`
      - `reconfigure(self, loc_path: str, export_path: str)`: calls `reconfigure_paths()`, updates stored paths
      - Properties: `qt_modules -> dict`, `loc_path -> str`, `export_path -> str`

    **Step 4: Update `server/services/__init__.py`**:
    - Add: `from server.services.transfer_adapter import TransferAdapter, init_quicktranslate`
    - Update `__all__` to include `"TransferAdapter"`, `"init_quicktranslate"`

    **Step 5: Write tests** in `tests/test_transfer_adapter.py` per behavior spec above.
    - Use `tmp_path` pytest fixture for isolation where needed
    - For test_init_quicktranslate, use the fixture paths as loc_path/export_path
    - For test_no_sacred_script_copied, grep server/services/*.py for forbidden patterns
    - Use `from __future__ import annotations` in test file
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools && python -m pytest tests/test_transfer_adapter.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `server/services/transfer_config_shim.py` exists and contains `def create_config_shim(loc_path: str, export_path: str)`
    - File `server/services/transfer_config_shim.py` contains `sys.modules["config"]`
    - File `server/services/transfer_config_shim.py` contains `SCRIPT_CATEGORIES = {"Sequencer", "Dialog"}`
    - File `server/services/transfer_adapter.py` exists and contains `QT_ROOT = str(Path`
    - File `server/services/transfer_adapter.py` contains `from core.xml_transfer import transfer_folder_to_folder`
    - File `server/services/transfer_adapter.py` contains `from core.postprocess import run_all_postprocess`
    - File `server/services/transfer_adapter.py` contains `from core.source_scanner import scan_source_for_languages`
    - File `server/services/transfer_adapter.py` contains `class TransferAdapter`
    - File `server/services/__init__.py` contains `TransferAdapter`
    - File `tests/fixtures/transfer/target/languagedata_FRE.xml` exists and contains `<LocStr StringID="STR_HELLO"`
    - File `tests/fixtures/transfer/source/corrections_FRE.xml` exists and contains `<LocStr StringID="STR_HELLO"`
    - `python -m pytest tests/test_transfer_adapter.py -x -v` exits 0 with 6 tests passing
    - No file in `server/services/` contains `def merge_corrections_to_xml` or `def run_all_postprocess` (only import statements)
  </acceptance_criteria>
  <done>Config shim injects synthetic config into sys.modules, adapter imports all 5 core QT module groups, tests pass, fixtures in place, no Sacred Script code copied</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_transfer_adapter.py -x -v` -- all 6 tests green
- `python -c "from server.services.transfer_adapter import TransferAdapter; print('import OK')"` -- no ImportError
- `grep -r "def merge_corrections" server/services/` returns empty (Sacred Scripts not copied)
</verification>

<success_criteria>
- QuickTranslate modules importable via adapter with zero code copying
- Config shim provides all required attributes for QT module-level imports
- Path reconfiguration works for project switching
- Test fixtures provide deterministic XML data for subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/57-transfer-service-adapter/57-01-SUMMARY.md`
</output>
