---
phase: 23-bug-fixes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_mock_gamedata_pipeline.py
  - scripts/api_test.sh
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "test_mock_gamedata_pipeline.py texture tests pass with generated universe filenames"
    - "API test shell script can verify all backend endpoints return expected status codes"
  artifacts:
    - path: "tests/integration/test_mock_gamedata_pipeline.py"
      provides: "Updated texture test for generated filenames"
      contains: "character_0001"
    - path: "scripts/api_test.sh"
      provides: "Shell wrapper for API endpoint health checks"
      contains: "curl"
  key_links:
    - from: "test_mock_gamedata_pipeline.py"
      to: "tests/fixtures/mock_gamedata/textures/"
      via: "file path assertions"
      pattern: "texture_dir"
---

<objective>
Fix the stale test fixture reference in test_mock_gamedata_pipeline.py and create an API testing shell wrapper script for endpoint health checks.

Purpose: The texture test references old named filenames (character_varon.dds) but the generated mock universe uses numbered filenames (character_0001.dds). Also creates a shell script for quick API endpoint auditing per user request.
Output: Updated test file + new api_test.sh script.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tests/integration/test_mock_gamedata_pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update texture test for generated universe filenames (TEST-01)</name>
  <files>tests/integration/test_mock_gamedata_pipeline.py</files>
  <action>
The mock gamedata universe was regenerated in Phase 15 with numbered filenames (character_0001.dds, item_0001.dds, etc.) instead of named filenames (character_varon.dds, item_blackstar_sword.dds).

Update the test file to match the actual fixture filenames:

1. **test_texture_files_exist** (around line 159-167): Update the `expected` list to use numbered filenames that actually exist in `tests/fixtures/mock_gamedata/textures/`:
```python
def test_texture_files_exist(self):
    """All expected DDS files exist in fixtures."""
    texture_dir = FIXTURES / "textures"
    expected = [
        "character_0001.dds", "character_0002.dds",
        "item_0001.dds", "region_0001.dds",
    ]
    for name in expected:
        assert (texture_dir / name).exists(), f"Missing texture: {name}"
```

2. **test_texture_index_build** (around line 169-186): The index build test should still work since it globs *.dds files. Verify the assertion `assert len(dds_index) >= 10` still holds (there are 102 texture files so it will pass). No changes needed here.

3. **test_knowledge_info_parsing** (around line 25-56): Update the UITextureName assertion. Check the actual fixture XML to see what UITextureName values the generated universe uses:
```bash
grep "UITextureName" tests/fixtures/mock_gamedata/knowledge/knowledgeinfo_sample.xml | head -5
```
Update the assertion on line 54 to match the actual UITextureName value in the fixture.

4. **test_character_to_texture_chain** (around line 253-277): This test resolves CharacterInfo -> KnowledgeKey -> UITextureName -> DDS. Update assertions to expect the generated universe texture names. The cross-reference chain should still work if the XML fixture UITextureName values match the DDS filenames.

5. **test_cross_reference_linkage** (around line 119-153): Check the fixture data assertions for texture values and update to match generated universe values.

**IMPORTANT:** Before making changes, read the actual fixture XML files to determine the correct UITextureName values:
- `tests/fixtures/mock_gamedata/knowledge/knowledgeinfo_sample.xml`
- `tests/fixtures/mock_gamedata/characterinfo_sample.xml`

Then update ALL assertions to match those actual values.
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python3 -m pytest tests/integration/test_mock_gamedata_pipeline.py::TestTextureIndex -x -v 2>&1 | tail -10</automated>
  </verify>
  <done>All texture-related tests in test_mock_gamedata_pipeline.py pass with the generated universe fixture filenames. No stale filename references.</done>
</task>

<task type="auto">
  <name>Task 2: Create API endpoint testing shell wrapper script</name>
  <files>scripts/api_test.sh</files>
  <action>
Create a shell script `scripts/api_test.sh` that tests all LocaNext backend API endpoints for availability. This fulfills the user's request for an API testing wrapper.

The script should:

1. Accept an optional `--base-url` argument (default: `http://localhost:8888`)
2. Accept `--auth-token` or read from environment variable `LOCANEXT_TOKEN`
3. Test each endpoint category with curl, checking HTTP status codes
4. Report results in a table format (endpoint, method, status, pass/fail)
5. Exit with non-zero if any endpoint returns unexpected status

Endpoint categories to test (from the codebase API audit):
- **Auth:** POST /api/auth/login, GET /api/auth/me
- **Files:** GET /api/ldm/files, POST /api/ldm/files/upload
- **Rows:** GET /api/ldm/rows/{file_id} (use file_id=1 as test)
- **Search:** GET /api/ldm/search?q=test
- **TM:** GET /api/ldm/tm/search?q=test
- **QA:** GET /api/ldm/qa/run/{file_id} (use file_id=1)
- **AI Suggestions:** GET /api/ldm/ai-suggestions/test_key
- **Gamedata:** POST /api/ldm/gamedata/browse (with body: {"path": "."})
- **Codex:** GET /api/ldm/codex/types
- **World Map:** GET /api/ldm/worldmap/data
- **Naming:** GET /api/ldm/naming/suggest/character?name=test
- **Health:** GET /api/health (if exists)

Use colors for pass/fail output. Include a summary count at the end.

Make the script executable. Add a usage header comment. Use `#!/bin/bash` shebang.

**Important:** This is a health-check script, not a full test suite. It verifies endpoints exist and return non-5xx responses. 4xx responses for auth-protected endpoints without valid tokens are acceptable (means the endpoint exists).
  </action>
  <verify>
    <automated>test -x /home/neil1988/LocalizationTools/scripts/api_test.sh && head -5 /home/neil1988/LocalizationTools/scripts/api_test.sh | grep -q "#!/bin/bash" && echo "PASS: Script exists and is executable"</automated>
  </verify>
  <done>api_test.sh exists, is executable, and can test all major API endpoint categories with colored pass/fail output.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/integration/test_mock_gamedata_pipeline.py::TestTextureIndex -x` passes
- `scripts/api_test.sh` exists and is executable
- `bash -n scripts/api_test.sh` (syntax check) passes
</verification>

<success_criteria>
Texture tests pass with generated universe filenames. API test script provides quick health-check for all backend endpoints.
</success_criteria>

<output>
After completion, create `.planning/phases/23-bug-fixes/23-04-SUMMARY.md`
</output>
