---
phase: 75-build-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 75-01
files_modified:
  - BUILD_TRIGGER.txt
autonomous: false
requirements:
  - BUILD-03
  - BUILD-04

must_haves:
  truths:
    - "GitHub Actions Light Build completes successfully with merge module verification passing"
    - "Downloaded installer installs and launches on offline Windows PC"
  artifacts:
    - path: "BUILD_TRIGGER.txt"
      provides: "Light Build trigger"
      contains: "Build Light"
  key_links:
    - from: "BUILD_TRIGGER.txt"
      to: ".github/workflows/build-electron.yml"
      via: "push trigger on BUILD_TRIGGER.txt path"
      pattern: "BUILD_TRIGGER.txt"
---

<objective>
Trigger a GitHub Actions Light Build and verify the resulting installer works on the offline Windows PC.

Purpose: This validates that the lxml fix from Plan 01 actually works in the CI pipeline, and that the built installer runs the merge workflow on the target machine. This is the end-to-end proof that BUILD-03 and BUILD-04 are satisfied.

Output: Successful Light Build + confirmed working installer on offline PC.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-build-pipeline/75-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger Light Build on GitHub Actions</name>
  <files>BUILD_TRIGGER.txt</files>
  <read_first>
    - BUILD_TRIGGER.txt (current content)
    - .github/workflows/build-electron.yml (lines 44-76 — trigger detection logic)
  </read_first>
  <action>
    Write "Build Light" to BUILD_TRIGGER.txt (the content must start with "Build" per CI trigger format rules). Then commit and push to GitHub to trigger the workflow.

    ```bash
    echo "Build Light" > BUILD_TRIGGER.txt
    git add BUILD_TRIGGER.txt
    git commit -m "ci: trigger Light Build for merge module verification"
    git push origin main
    ```

    After pushing, monitor the GitHub Actions run:
    ```bash
    gh run list --limit 1
    gh run watch $(gh run list --limit 1 --json databaseId --jq '.[0].databaseId')
    ```

    The build takes approximately 15-20 minutes. Key steps to watch:
    1. "Install light backend dependencies" — should now include lxml
    2. "Verify Python Embedded + ALL Dependencies" — should show "[OK] lxml ready"
    3. "Verify Merge Module Imports" — NEW step, should show all merge imports OK
    4. "Build Electron App" — should produce the installer
    5. "Create GitHub Release" — should upload the installer artifact

    If the build fails at the merge module verification step, check the error output to identify which import failed.
  </action>
  <verify>
    <automated>gh run list --limit 1 --json status,conclusion --jq '.[0] | "\(.status) \(.conclusion)"'</automated>
  </verify>
  <acceptance_criteria>
    - gh run list shows the latest run with status "completed" and conclusion "success"
    - The GitHub release contains a LocaNext_v*_Light_Setup.exe file
  </acceptance_criteria>
  <done>Light Build completed successfully on GitHub Actions with merge module verification passing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Download and test installer on offline Windows PC</name>
  <files>N/A</files>
  <action>
    Human verification step. The user downloads the installer from GitHub Releases and tests on their offline Windows PC. Steps:
    1. Go to GitHub Releases: https://github.com/<GIT_USER>/LocalizationTools/releases
    2. Download the latest LocaNext_v*_Light_Setup.exe
    3. Transfer to offline Windows PC (USB drive)
    4. Run the installer
    5. Launch LocaNext
    6. Verify merge module works (open language data, try merge/transfer)
    7. Check app console for ImportError messages
    8. Confirm offline operation (disconnect network)
  </action>
  <what-built>
    A Light Build installer was produced by GitHub Actions. The build includes:
    - Embedded Python with lxml installed
    - All 14 merge module files bundled as extraResources
    - Model2Vec for offline AI embeddings
    - Merge module import verification passed in CI
  </what-built>
  <how-to-verify>
    1. Go to GitHub Releases: https://github.com/<GIT_USER>/LocalizationTools/releases
    2. Download the latest LocaNext_v*_Light_Setup.exe
    3. Transfer to offline Windows PC (USB drive)
    4. Run the installer — should complete without errors
    5. Launch LocaNext — should start and show the UI
    6. Verify merge module works:
       - Open a language data file
       - Try a merge/transfer operation (if mock data available)
       - Check the app console for any ImportError messages
    7. Confirm the app runs fully offline (disconnect from network)
  </how-to-verify>
  <verify>
    <automated>MISSING — human verification required on offline Windows PC</automated>
  </verify>
  <resume-signal>Type "approved" if installer works on offline PC, or describe any issues encountered</resume-signal>
  <acceptance_criteria>
    - Installer runs without errors on Windows
    - App launches and shows UI
    - No ImportError for lxml or merge module components in app logs
  </acceptance_criteria>
  <done>User confirmed installer installs, launches, and runs merge workflow on offline Windows PC without errors</done>
</task>

</tasks>

<verification>
1. GitHub Actions Light Build completed with "success" conclusion
2. GitHub Release contains downloadable installer
3. User confirms installer works on offline Windows PC
</verification>

<success_criteria>
- Light Build triggered and completed successfully
- Merge module verification step passed in CI
- User confirmed installer installs, launches, and runs merge workflow on offline PC
</success_criteria>

<output>
After completion, create `.planning/phases/75-build-pipeline/75-02-SUMMARY.md`
</output>
