---
phase: 75-build-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/build-electron.yml
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02

must_haves:
  truths:
    - "lxml is installed in the embedded Python bundle during Light Build"
    - "All 14 merge module files can be imported by embedded Python"
    - "Merge workflow (match + postprocess) runs without missing module errors"
  artifacts:
    - path: ".github/workflows/build-electron.yml"
      provides: "lxml in pip install list + merge module import verification step"
      contains: "lxml"
  key_links:
    - from: "server/services/merge/tmx_tools.py"
      to: "lxml"
      via: "from lxml import etree"
      pattern: "from lxml import etree"
    - from: "server/services/transfer_adapter.py"
      to: "server/services/merge"
      via: "from server.services.merge import"
      pattern: "from server\\.services\\.merge import"
---

<objective>
Add lxml to the embedded Python pip install and add a merge module import verification step to the GitHub Actions build workflow.

Purpose: The internalized merge module (server/services/merge/) uses lxml extensively (tmx_tools.py, xml_parser.py, postprocess.py, source_scanner.py, eventname_resolver.py) but lxml is NOT in the embedded Python pip install list. Without it, any merge workflow in the built app will crash with ImportError. This plan fixes the dependency gap and adds a CI verification step to catch similar issues in the future.

Output: Updated build-electron.yml with lxml dependency and merge module import check.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/build-electron.yml
@server/services/merge/__init__.py
</context>

<interfaces>
<!-- Key imports and structure the executor needs to understand -->

From .github/workflows/build-electron.yml (line 646-652):
The embedded Python pip install step installs these packages:
```
fastapi uvicorn python-multipart python-socketio
sqlalchemy aiosqlite psycopg2-binary
PyJWT python-jose passlib python-dotenv bcrypt
pydantic pydantic-settings email-validator
pandas openpyxl xlrd
httpx requests loguru tqdm pyyaml
```
MISSING: lxml (used by 6+ merge module files and 10+ other server files)

From server/services/merge/__init__.py:
14 Python files in the merge module:
- __init__.py, _config.py, category_mapper.py, eventname_resolver.py
- excel_io.py, korean_detection.py, language_loader.py, matching.py
- postprocess.py, source_scanner.py, text_utils.py, tmx_tools.py
- xml_io.py, xml_parser.py, xml_transfer.py

Files importing lxml:
- tmx_tools.py (line 24): from lxml import etree
- xml_parser.py (line 18): from lxml import etree as ET
- source_scanner.py (line 23): from lxml import etree
- postprocess.py (line 34): from lxml import etree
- eventname_resolver.py (line 24): from lxml import etree
- xml_transfer.py (line 20): from lxml import etree
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add lxml to embedded Python pip install</name>
  <files>.github/workflows/build-electron.yml</files>
  <read_first>
    - .github/workflows/build-electron.yml (lines 644-656 — the pip install step)
    - server/services/merge/__init__.py (to understand all 14 module files)
    - requirements.txt (to confirm lxml version constraint: lxml>=4.9.0)
  </read_first>
  <action>
    In .github/workflows/build-electron.yml, find the "Install light backend dependencies" step (around line 646).

    Add `lxml` to the pip install command. It should go on the data processing line alongside `pandas openpyxl xlrd`.

    Change:
    ```
    pandas openpyxl xlrd `
    ```
    To:
    ```
    pandas openpyxl xlrd lxml `
    ```

    Then add a verification line after the existing dependency checks (around line 693-703). After the last verification line (`& "tools\python\python.exe" -c "import yaml; ..."`), add:

    ```powershell
    & "tools\python\python.exe" -c "from lxml import etree; print('[OK] lxml ready')"
    ```

    DO NOT change any other lines. This is a surgical one-word addition to an existing pip install command plus one verification line.
  </action>
  <verify>
    <automated>grep -n "lxml" .github/workflows/build-electron.yml | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "lxml" .github/workflows/build-electron.yml returns at least 2 matches (pip install line + verification line)
    - The pip install line contains "pandas openpyxl xlrd lxml"
    - A verification line exists: "from lxml import etree"
  </acceptance_criteria>
  <done>lxml is in the embedded Python pip install list and has a verification check in the build workflow</done>
</task>

<task type="auto">
  <name>Task 2: Add merge module import verification step to build workflow</name>
  <files>.github/workflows/build-electron.yml</files>
  <read_first>
    - .github/workflows/build-electron.yml (lines 738-756 — the "Verify Python setup" area where server imports are tested)
    - server/services/merge/__init__.py (the full list of exports)
    - server/services/transfer_adapter.py (lines 1-30 — how the merge module is consumed)
  </read_first>
  <action>
    In .github/workflows/build-electron.yml, find the step "Testing server can START with embedded Python" (around line 751-756). After that step, add a NEW step that verifies the merge module can be imported:

    ```yaml
      - name: Verify Merge Module Imports
        if: needs.check-build-trigger.outputs.light_mode == 'yes'
        run: |
          Write-Host "=== Verifying internalized merge module ==="
          $env:PYTHONPATH = Get-Location

          # Test all 14 merge module files import successfully
          & "tools\python\python.exe" -c @"
    import sys
    sys.path.insert(0, '.')
    errors = []

    # Core merge package
    try:
        from server.services.merge import (
            configure, get_config,
            normalize_text, sanitize_xml_content,
            parse_xml_file, discover_language_files,
            build_translation_lookup, detect_excel_columns,
            read_corrections_from_excel, merge_corrections_to_xml,
            run_all_postprocess, run_preprocess_excel,
            scan_source_for_languages, get_eventname_mapping,
            clean_segment, combine_xmls_to_tmx,
        )
        print('[OK] All merge module exports imported')
    except ImportError as e:
        errors.append(f'Merge module import failed: {e}')

    # Transfer adapter (consumes merge module)
    try:
        from server.services.transfer_adapter import TransferService
        print('[OK] TransferService (merge consumer) imported')
    except ImportError as e:
        errors.append(f'TransferService import failed: {e}')

    # lxml (critical dependency for merge XML operations)
    try:
        from lxml import etree
        print('[OK] lxml.etree available')
    except ImportError as e:
        errors.append(f'lxml missing: {e}')

    if errors:
        print()
        for e in errors:
            print(f'  FAIL: {e}')
        sys.exit(1)
    else:
        print('[OK] Merge module fully operational')
    "@
          if ($LASTEXITCODE -ne 0) { Write-Error "Merge module verification FAILED!"; exit 1 }
          Write-Host "[OK] Merge module verification passed" -ForegroundColor Green
        shell: pwsh
    ```

    Place this step AFTER the "Testing server can START with embedded Python" step and BEFORE the Light Mode Setup step (the one that checks `light_mode == 'yes'` and installs model2vec).

    The `if: needs.check-build-trigger.outputs.light_mode == 'yes'` condition ensures this only runs for Light builds (where the merge module must work offline). For full builds, the merge module is verified by the full test suite.
  </action>
  <verify>
    <automated>grep -c "Verify Merge Module" .github/workflows/build-electron.yml</automated>
  </verify>
  <acceptance_criteria>
    - grep "Verify Merge Module" .github/workflows/build-electron.yml returns 1
    - The step contains imports from server.services.merge
    - The step contains TransferService import check
    - The step contains lxml.etree check
    - The step has the light_mode condition
  </acceptance_criteria>
  <done>Build workflow has a dedicated merge module verification step that will catch missing imports before the installer is produced</done>
</task>

</tasks>

<verification>
1. `grep "lxml" .github/workflows/build-electron.yml` shows lxml in pip install AND verification
2. `grep "Verify Merge Module" .github/workflows/build-electron.yml` shows the new verification step exists
3. `grep "server.services.merge" .github/workflows/build-electron.yml` shows merge module imports in the verification step
4. The workflow YAML is valid (no syntax errors introduced)
</verification>

<success_criteria>
- lxml added to embedded Python pip install (was missing, merge module needs it)
- Merge module import verification step added to catch future missing dependencies
- No other workflow changes (surgical edit to existing file)
</success_criteria>

<output>
After completion, create `.planning/phases/75-build-pipeline/75-01-SUMMARY.md`
</output>
