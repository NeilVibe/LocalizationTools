---
phase: 84-virtualgrid-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - locaNext/src/lib/components/ldm/grid/gridState.svelte.ts
  - locaNext/src/lib/components/ldm/grid/ScrollEngine.svelte
  - locaNext/src/lib/components/ldm/grid/StatusColors.svelte
  - locaNext/src/lib/components/ldm/VirtualGrid.svelte
autonomous: true
requirements: [GRID-02, GRID-03, GRID-07]

must_haves:
  truths:
    - "gridState.svelte.ts exports a $state object with all shared fields per D-02"
    - "Write-ownership comments exist at top of gridState.svelte.ts per D-03"
    - "ScrollEngine handles virtual scroll, row loading, viewport management in isolation"
    - "StatusColors handles QA flags, TM match state, reference data in isolation"
    - "VirtualGrid.svelte imports from ./grid/ and still exports all 7 functions"
    - "The app loads files and displays rows identically to before extraction"
    - "Existing Vitest unit tests still pass"
  artifacts:
    - path: "locaNext/src/lib/components/ldm/grid/gridState.svelte.ts"
      provides: "Shared reactive state for all grid modules"
      contains: "export const grid = $state"
    - path: "locaNext/src/lib/components/ldm/grid/ScrollEngine.svelte"
      provides: "Virtual scroll, row loading, viewport calculation"
      contains: "export async function loadRows"
    - path: "locaNext/src/lib/components/ldm/grid/StatusColors.svelte"
      provides: "Row status, QA flags, TM matches, reference data"
      contains: "export function updateRowQAFlag"
  key_links:
    - from: "gridState.svelte.ts"
      to: "ScrollEngine.svelte"
      via: "grid.rows, grid.visibleStart, grid.visibleEnd mutation"
      pattern: "import.*gridState"
    - from: "gridState.svelte.ts"
      to: "StatusColors.svelte"
      via: "grid state reads for QA/TM data"
      pattern: "import.*gridState"
    - from: "VirtualGrid.svelte"
      to: "ScrollEngine.svelte"
      via: "bind:this + export function delegation"
      pattern: "scrollEngine\\.loadRows"
    - from: "VirtualGrid.svelte"
      to: "StatusColors.svelte"
      via: "bind:this + export function delegation"
      pattern: "statusColors\\.updateRowQAFlag"
---

<objective>
Batch 1: Create gridState.svelte.ts shared state module, extract ScrollEngine and StatusColors from VirtualGrid.svelte, and rewire the parent to delegate to these new modules.

Purpose: Establishes the shared state foundation (gridState.svelte.ts) that all subsequent batches depend on, and extracts the two modules with zero upstream dependencies (per D-07). After this plan, VirtualGrid.svelte is ~1500 lines smaller and the app still works identically.

Output: 3 new files in locaNext/src/lib/components/ldm/grid/, a slimmer VirtualGrid.svelte, and a passing E2E + unit test run.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-virtualgrid-decomposition/84-CONTEXT.md
@.planning/phases/84-virtualgrid-decomposition/84-RESEARCH.md
@locaNext/src/lib/components/ldm/VirtualGrid.svelte
@locaNext/src/lib/stores/aiCapabilityStore.svelte.ts
@locaNext/src/lib/utils/statusColors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gridState.svelte.ts and extract ScrollEngine</name>
  <files>
    locaNext/src/lib/components/ldm/grid/gridState.svelte.ts,
    locaNext/src/lib/components/ldm/grid/ScrollEngine.svelte,
    locaNext/src/lib/components/ldm/VirtualGrid.svelte
  </files>
  <read_first>
    locaNext/src/lib/components/ldm/VirtualGrid.svelte (FULL — all 4293 lines),
    locaNext/src/lib/stores/aiCapabilityStore.svelte.ts (pattern reference for .svelte.ts),
    locaNext/src/lib/utils/statusColors.ts (Phase 83 extraction, referenced by StatusColors module),
    .planning/phases/84-virtualgrid-decomposition/84-RESEARCH.md (patterns and pitfalls)
  </read_first>
  <action>
    **Step 1 — Create the grid/ directory:**
    ```bash
    mkdir -p locaNext/src/lib/components/ldm/grid
    ```

    **Step 2 — Create gridState.svelte.ts (per D-01, D-02, D-03, D-04):**

    Create `locaNext/src/lib/components/ldm/grid/gridState.svelte.ts` with:

    1. Write-ownership comment block at the top (per D-03) documenting which module writes each field:
       - rows: ScrollEngine (load), InlineEditor (save), parent (WebSocket)
       - total: ScrollEngine
       - visibleStart/visibleEnd: ScrollEngine
       - selectedRowId: SelectionManager
       - hoveredRowId/hoveredCell: SelectionManager
       - inlineEditingRowId: InlineEditor
       - activeFilter: SearchEngine
       - selectedCategories: SearchEngine
       - tmAppliedRows: StatusColors
       - referenceData: StatusColors

    2. `export const grid = $state({...})` with all fields from D-02:
       `rows` (any[]), `total` (number), `visibleStart` (number), `visibleEnd` (number),
       `loading` (boolean), `initialLoading` (boolean), `selectedRowId` (string|null),
       `hoveredRowId` (string|null), `hoveredCell` (string|null),
       `inlineEditingRowId` (string|null), `activeFilter` (string, default 'all'),
       `selectedCategories` (string[])

    3. Additional exported $state objects:
       - `export const rowIndexById = $state(new Map())` — maps row_id string to array index
       - `export const rowHeightCache = $state(new Map())` — maps row index to measured pixel height
       - `export const loadedPages = $state(new Set())` — tracks which pages have been fetched
       - `export const tmAppliedRows = $state(new Map())` — maps row_id to TM match type
       - `export const referenceData = $state(new Map())` — maps row_id to reference text
       - `export const qaFlags = $state(new Map())` — maps row_id to QA flag count

    4. Cross-module derived state (per D-04): `export const visibleRows = $derived(...)` that slices grid.rows[visibleStart..visibleEnd] with placeholder fallback.

    5. Helper functions: `getRowById(rowId)`, `getRowIndexById(rowId)`, `resetGridState()`, `rebuildRowIndex()` (rebuilds rowIndexById from grid.rows).

    6. `measureRowHeight` action function — extract from VirtualGrid.svelte (used as `use:measureRowHeight` on row divs). This is a plain function that reads/writes rowHeightCache. Define it here so both ScrollEngine and CellRenderer can import it.

    7. `cumulativeHeights` array and `rebuildCumulativeHeights()` function — used by ScrollEngine for virtual scroll offset calculations.

    **Step 3 — Create ScrollEngine.svelte (per D-12):**

    Create `locaNext/src/lib/components/ldm/grid/ScrollEngine.svelte` containing:

    1. `<script>` section that imports from `./gridState.svelte.ts` and `$lib/utils/api.js` (or wherever getAuthHeaders/getApiBase live in the codebase).

    2. Props via `$props()` (per D-05): `fileId`, `fileType`, `searchTerm`, `searchMode`, `searchFields`, `activeTMs` — configuration only, no shared reactive state.

    3. Extract these functions from VirtualGrid.svelte (identified in RESEARCH.md as lines 432-835, plus lines 55-68, 130-139, 2247-2363):
       - `loadRows()` — the main page-loading function (fetches from API, updates grid.rows)
       - `loadPage(pageNum)` — loads a single page
       - `calculateVisibleRange()` — determines visibleStart/visibleEnd from scroll position
       - `handleScroll(event)` — scroll event handler
       - `scrollToRowById(rowId)` — scrolls viewport to make a row visible
       - `scrollToRowNum(rowNum)` — scrolls viewport to a row number
       - All scroll-related constants (PAGE_SIZE, MIN_ROW_HEIGHT, BUFFER_ROWS, etc.)
       - Module-local state: `loadingPages` Set, scroll container ref

    4. Export functions that the parent needs to delegate to: `loadRows`, `scrollToRowById`, `scrollToRowNum`.

    5. Minimal markup: just the scroll container `<div class="scroll-container">` with the `on:scroll={handleScroll}` — this component owns the scroll viewport. If the scroll container is deeply interleaved with other markup, use a renderless pattern (no markup, export functions only) and let the parent keep the scroll container div.

    6. Move relevant CSS from VirtualGrid.svelte to ScrollEngine.svelte (scroll container styles, virtual row height/positioning styles).

    **Step 4 — Rewire VirtualGrid.svelte:**

    1. Add imports at top: `import { grid, resetGridState, ... } from './grid/gridState.svelte.ts'` and `import ScrollEngine from './grid/ScrollEngine.svelte'`.

    2. Replace all direct state variable declarations that moved to gridState with imports. For example, `let rows = $state([])` becomes reading from `grid.rows`. Replace ALL references throughout the file: `rows` -> `grid.rows`, `total` -> `grid.total`, `visibleStart` -> `grid.visibleStart`, etc.

    3. Add `let scrollEngine;` ref and `<ScrollEngine bind:this={scrollEngine} {fileId} {fileType} ... />` in the markup.

    4. Update the 7 export functions to delegate where appropriate:
       - `export function loadRows() { return scrollEngine.loadRows(); }`
       - `export function scrollToRowById(id) { return scrollEngine.scrollToRowById(id); }`
       - `export function scrollToRowNum(num) { return scrollEngine.scrollToRowNum(num); }`
       - The other 4 exports stay in VirtualGrid.svelte for now (they'll move in batches 2 and 3).

    5. Remove the extracted scroll functions and state declarations from VirtualGrid.svelte. Leave everything else (CellRenderer logic, SelectionManager logic, InlineEditor logic, StatusColors logic, SearchEngine logic) in place for now.

    6. **CRITICAL:** Do NOT break any existing functionality. Every removed variable must be replaced by the gridState import. Every removed function must be delegated. Run a mental "find all references" for each extracted identifier.

    **Important pitfall avoidance (from RESEARCH.md):**
    - Never reassign exported $state variables — only mutate properties (grid.rows = newArr, NOT grid = {...})
    - measureRowHeight must be importable by whatever component renders row divs
    - Keep WebSocket sync (joinFile/leaveFile/onCellUpdate) in parent per D-19
    - Keep all 7 export functions on VirtualGrid.svelte (delegating wrappers)
  </action>
  <verify>
    <automated>cd locaNext && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export const grid = \$state" locaNext/src/lib/components/ldm/grid/gridState.svelte.ts
    - grep -q "WRITE OWNERSHIP" locaNext/src/lib/components/ldm/grid/gridState.svelte.ts
    - grep -q "export async function loadRows" locaNext/src/lib/components/ldm/grid/ScrollEngine.svelte
    - grep -q "import.*gridState" locaNext/src/lib/components/ldm/grid/ScrollEngine.svelte
    - grep -q "import.*ScrollEngine" locaNext/src/lib/components/ldm/VirtualGrid.svelte
    - grep -q "scrollEngine.loadRows" locaNext/src/lib/components/ldm/VirtualGrid.svelte
    - grep -cq "export function" locaNext/src/lib/components/ldm/VirtualGrid.svelte returns 7 (all exports preserved)
    - Vitest unit tests pass (tagDetector, statusColors, TagText, runes-smoke)
  </acceptance_criteria>
  <done>
    gridState.svelte.ts exists with $state object, write-ownership comments, and helper functions.
    ScrollEngine.svelte exists with loadRows, calculateVisibleRange, scrollToRowById, scrollToRowNum extracted from VirtualGrid.
    VirtualGrid.svelte imports from gridState and delegates scroll functions to ScrollEngine.
    All 7 export functions still work on VirtualGrid.svelte.
    Vitest unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract StatusColors module and verify E2E</name>
  <files>
    locaNext/src/lib/components/ldm/grid/StatusColors.svelte,
    locaNext/src/lib/components/ldm/grid/gridState.svelte.ts,
    locaNext/src/lib/components/ldm/VirtualGrid.svelte
  </files>
  <read_first>
    locaNext/src/lib/components/ldm/VirtualGrid.svelte (current state after Task 1),
    locaNext/src/lib/components/ldm/grid/gridState.svelte.ts (created in Task 1),
    locaNext/src/lib/utils/statusColors.ts (Phase 83 extraction — StatusColors module imports this),
    .planning/phases/84-virtualgrid-decomposition/84-CONTEXT.md (D-16, D-25 decisions)
  </read_first>
  <action>
    **Step 1 — Create StatusColors.svelte (per D-16):**

    Create `locaNext/src/lib/components/ldm/grid/StatusColors.svelte` containing:

    1. Import from `./gridState.svelte.ts`: grid, tmAppliedRows, referenceData, qaFlags, getRowById.
    2. Import from `$lib/utils/statusColors.ts` (Phase 83 extraction per D-25): the status color utility functions.
    3. Import from `$lib/utils/api.js`: getAuthHeaders, getApiBase for API calls.

    4. Props via `$props()` (per D-05): `fileId`, `activeTMs` — configuration only.

    5. Extract from VirtualGrid.svelte (RESEARCH.md lines 224-229, 1018-1069, 1091-1257):
       - `updateRowQAFlag(rowId, flagCount)` — updates QA flag count on a row
       - `markRowAsTMApplied(rowId, matchType)` — marks a row as TM-matched
       - `applyTMToRow(lineNumber, targetText)` — applies TM suggestion to a row's target
       - `fetchReferenceData(fileId)` — loads reference language data for comparison
       - `fetchQAFlags(fileId)` — loads QA flag counts per row
       - `fetchTMMatches(fileId, activeTMs)` — loads TM match data
       - All TM/QA/reference state management (tmAppliedRows Map, referenceData Map, qaFlags Map)
       - Helper functions for row status determination (empty/translated/confirmed logic)

    6. Export functions the parent needs: `updateRowQAFlag`, `applyTMToRow`, `markRowAsTMApplied`.

    7. Move relevant CSS (status-related row coloring, QA badge styles, TM match indicators) from VirtualGrid.svelte to StatusColors.svelte.

    **Step 2 — Update gridState.svelte.ts if needed:**

    If any additional shared state fields are needed for StatusColors that weren't included in Task 1 (e.g., tmAppliedRows, referenceData, qaFlags were already added), verify they exist and adjust types if needed.

    **Step 3 — Rewire VirtualGrid.svelte:**

    1. Add import: `import StatusColors from './grid/StatusColors.svelte'`.
    2. Add `let statusColors;` ref and `<StatusColors bind:this={statusColors} {fileId} {activeTMs} />` in markup.
    3. Update export function delegations:
       - `export function updateRowQAFlag(rowId, flagCount) { return statusColors.updateRowQAFlag(rowId, flagCount); }`
       - `export function applyTMToRow(lineNumber, targetText) { return statusColors.applyTMToRow(lineNumber, targetText); }`
       - `export function markRowAsTMApplied(rowId, matchType) { return statusColors.markRowAsTMApplied(rowId, matchType); }`
    4. Remove extracted StatusColors functions and state from VirtualGrid.svelte.

    **Step 4 — Run E2E tests (per D-11):**

    Run the most relevant Playwright E2E test to verify the grid still works:
    ```bash
    cd locaNext && npx playwright test tests/grid-status-colors.spec.ts --reporter=list 2>&1 | tail -30
    ```
    Also run unit tests to confirm Phase 83 tests still pass:
    ```bash
    cd locaNext && npx vitest run --reporter=verbose 2>&1 | tail -20
    ```

    **Pitfall avoidance:**
    - StatusColors imports the PURE FUNCTION statusColors.ts from $lib/utils/ — do NOT duplicate that logic
    - tmAppliedRows, referenceData, qaFlags are Map objects in gridState — mutate via .set()/.delete(), never reassign
    - The applyTMToRow function likely writes to grid.rows (updating target text) — this is an allowed cross-write per the ownership doc (InlineEditor also writes rows)
  </action>
  <verify>
    <automated>cd locaNext && npx vitest run --reporter=verbose 2>&1 | tail -20 && echo "--- E2E ---" && npx playwright test tests/grid-status-colors.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function updateRowQAFlag" locaNext/src/lib/components/ldm/grid/StatusColors.svelte
    - grep -q "export function applyTMToRow" locaNext/src/lib/components/ldm/grid/StatusColors.svelte
    - grep -q "export function markRowAsTMApplied" locaNext/src/lib/components/ldm/grid/StatusColors.svelte
    - grep -q "import.*statusColors.*from.*statusColors" locaNext/src/lib/components/ldm/grid/StatusColors.svelte
    - grep -q "import StatusColors" locaNext/src/lib/components/ldm/VirtualGrid.svelte
    - grep -q "statusColors.updateRowQAFlag" locaNext/src/lib/components/ldm/VirtualGrid.svelte
    - Vitest unit tests pass (statusColors.test.ts specifically)
    - VirtualGrid.svelte still has exactly 7 export functions
  </acceptance_criteria>
  <done>
    StatusColors.svelte exists with QA flag, TM match, and reference data management extracted from VirtualGrid.
    StatusColors imports from $lib/utils/statusColors.ts (Phase 83 pure functions) and gridState.svelte.ts.
    VirtualGrid.svelte delegates updateRowQAFlag, applyTMToRow, markRowAsTMApplied to StatusColors.
    All 7 export functions still work on VirtualGrid.svelte.
    Vitest unit tests pass. E2E grid-status-colors test passes (or is skipped if it requires running servers — note result either way).
    Batch 1 complete: VirtualGrid.svelte reduced by ~800-1000 lines.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd locaNext && npx vitest run` — all Phase 83 unit tests pass
2. `wc -l locaNext/src/lib/components/ldm/VirtualGrid.svelte` — should be ~3000-3300 lines (down from 4293)
3. `wc -l locaNext/src/lib/components/ldm/grid/gridState.svelte.ts` — should be ~80-120 lines
4. `wc -l locaNext/src/lib/components/ldm/grid/ScrollEngine.svelte` — should be ~300-450 lines
5. `wc -l locaNext/src/lib/components/ldm/grid/StatusColors.svelte` — should be ~350-500 lines
6. `grep -c "export function" locaNext/src/lib/components/ldm/VirtualGrid.svelte` — should be 7
7. No file in grid/ exceeds 800 lines
</verification>

<success_criteria>
- gridState.svelte.ts is the single source of truth for shared grid state
- ScrollEngine owns virtual scroll, row loading, and viewport management
- StatusColors owns QA flags, TM matches, and reference data
- VirtualGrid.svelte delegates to both modules via bind:this
- All 7 exported functions continue to work for GridPage.svelte / LDM.svelte consumers
- Phase 83 Vitest tests pass without modification
- No single file exceeds 800 lines
</success_criteria>

<output>
After completion, create `.planning/phases/84-virtualgrid-decomposition/84-01-SUMMARY.md`
</output>
