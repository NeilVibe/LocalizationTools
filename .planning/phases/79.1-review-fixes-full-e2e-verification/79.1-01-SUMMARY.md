---
phase: 79.1-review-fixes-full-e2e-verification
plan: 01
subsystem: backend, tests, ci, frontend
tags: [bugfix, route-conflict, ci, null-safety]
dependency_graph:
  requires: []
  provides: [merge-route-separation, ci-merge-all-modes, null-safe-tmexplorer]
  affects: [server/tools/ldm/routes/files.py, server/tools/ldm/routes/merge.py, tests/e2e, .github/workflows, locaNext/src/lib/components/ldm/TMExplorerGrid.svelte]
tech_stack:
  added: []
  patterns: [route-rename-for-conflict-resolution, session-scoped-conftest-fixtures]
key_files:
  created: []
  modified:
    - server/tools/ldm/routes/files.py
    - tests/e2e/test_langdata_media.py
    - tests/e2e/test_merge_qa_pipeline.py
    - locaNext/src/lib/components/ldm/TMExplorerGrid.svelte
    - .github/workflows/build-electron.yml
decisions:
  - Renamed files.py merge to /export-merge (semantic correctness) rather than renging merge.py
  - Used function imports for category_mapper (no class export exists)
metrics:
  duration: 3min
  completed: "2026-03-24T03:19:00Z"
  tasks: 2
  files: 5
---

# Phase 79.1 Plan 01: Fix 5 Expert Review Code Issues Summary

Resolved all 5 critical/high issues from 4-expert code review: merge route conflict, test fixture duplication, CI gate, missing CI import, and Svelte null safety.

## What Was Done

### Task 1: Merge route conflict + test fixtures + Svelte null guards (3 fixes)

**FIX-01 (Critical): Merge route conflict resolved.** Renamed the older export-style merge in `files.py` from `/files/{file_id}/merge` to `/files/{file_id}/export-merge`. The TranslatorMergeService endpoint in `merge.py` now correctly owns `/files/{file_id}/merge` without being shadowed. Verified both routes respond (401, not 404).

**FIX-02 (High): Test fixture duplication removed.** Deleted the module-scoped `client()` and `auth_headers()` fixtures from `test_langdata_media.py` (lines 31-59) plus the now-unused `TestClient`/`app` imports. Pytest will automatically use the session-scoped versions from `tests/e2e/conftest.py`.

**FIX-05 (Medium): TMExplorerGrid null guards added.** In `formatEntryCount`, the `unassigned` type now guards `item.tm_count` with `|| 0` to prevent "undefined TMs" in the template literal. In `formatStatus`, the `platform` type now uses `(item.tm_count || 0) > 0` instead of `item.project_count > 0` for correct semantics.

Also removed all `@pytest.mark.xfail` decorators and `XFAIL_REASON` from `test_merge_qa_pipeline.py` since the route conflict is now resolved.

### Task 2: CI merge verification fixes (2 fixes)

**FIX-03 (High): Light Mode gate removed.** Removed `if: env.LIGHT_MODE == 'yes'` from the "Verify Merge Module Imports" CI step. Merge modules are bundled in ALL builds, so verification now runs unconditionally.

**FIX-04 (Medium): category_mapper added to CI.** Added import verification for `categorize_file` and `build_stringid_category_index` from `server.services.merge.category_mapper` to the CI merge verification step.

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| 1 | 47e13b96 | Fix merge route conflict, test fixtures, Svelte null guards |
| 2 | 8d287d38 | Remove CI Light Mode gate, add category_mapper |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] category_mapper has no class export**
- **Found during:** Task 2
- **Issue:** Plan specified `from server.services.merge.category_mapper import TwoTierCategoryMapper` but the module only exports functions, not a class
- **Fix:** Changed CI import to `from server.services.merge.category_mapper import categorize_file, build_stringid_category_index`
- **Files modified:** .github/workflows/build-electron.yml

## Verification

- TranslatorMergeService endpoint at /files/{file_id}/merge responds 401 (not 404) -- PASS
- Export-merge endpoint at /files/{file_id}/export-merge responds 401 (not 404) -- PASS
- test_langdata_media.py has zero local fixture definitions -- PASS
- XFAIL_REASON removed from test_merge_qa_pipeline.py -- PASS
- TMExplorerGrid uses (tm_count || 0) in both formatStatus and formatEntryCount -- PASS
- CI workflow has no LIGHT_MODE gate on merge verification -- PASS
- category_mapper appears in CI verification script -- PASS

## Known Stubs

None -- all fixes are complete implementations, no stubs.

## Self-Check: PASSED

All 5 modified files exist. Both commits (47e13b96, 8d287d38) verified in git log.
