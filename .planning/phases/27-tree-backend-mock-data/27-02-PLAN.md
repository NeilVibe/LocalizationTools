---
phase: 27-tree-backend-mock-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/mock_gamedata/StaticInfo/skillinfo/SkillTreeInfo.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_skill.staticinfo.xml
autonomous: true
requirements: [TREE-07]

must_haves:
  truths:
    - "SkillTreeInfo fixture has multi-branch ParentNodeId nesting (not a single chain) with at least 3 levels deep"
    - "KnowledgeInfo fixture exists with KnowledgeList children linking child entries to parents"
    - "GimmickGroupInfo fixture already has XML-nested structure (verified sufficient -- no changes needed)"
    - "All expanded fixtures parse without errors via lxml"
  artifacts:
    - path: "tests/fixtures/mock_gamedata/StaticInfo/skillinfo/SkillTreeInfo.staticinfo.xml"
      provides: "SkillTreeInfo with multi-branch ParentNodeId nesting"
      contains: "ParentNodeId"
    - path: "tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_skill.staticinfo.xml"
      provides: "KnowledgeInfo with KnowledgeList children referencing parent StrKeys"
      contains: "KnowledgeList"
  key_links:
    - from: "tests/fixtures/mock_gamedata/StaticInfo/skillinfo/SkillTreeInfo.staticinfo.xml"
      to: "server/tools/ldm/services/gamedata_tree_service.py"
      via: "parse_file reads this fixture; ParentNodeId triggers _resolve_parent_references"
      pattern: "ParentNodeId"
    - from: "tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_skill.staticinfo.xml"
      to: "server/tools/ldm/services/gamedata_tree_service.py"
      via: "parse_file reads this fixture; KnowledgeList triggers child-parent resolution"
      pattern: "KnowledgeList"
---

<objective>
Expand mock gamedata fixtures with real hierarchical XML structures that exercise the tree parser's nesting capabilities -- multi-branch skill trees with ParentNodeId, and knowledge entries with KnowledgeList parent references.

Purpose: Phase 28 (Tree UI) needs rich hierarchical data to render meaningful tree views. Current fixtures have basic nesting -- this plan adds realistic multi-branch, multi-level structures based on real game data patterns from exampleofskillgamedata.txt.
Output: Expanded SkillTreeInfo fixture with multi-branch nesting, new KnowledgeInfo skill fixture with KnowledgeList children.
</objective>

<execution_context>
@/home/<USERNAME>/.claude/get-shit-done/workflows/execute-plan.md
@/home/<USERNAME>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-tree-backend-mock-data/27-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand SkillTreeInfo fixture with multi-branch ParentNodeId nesting</name>
  <files>tests/fixtures/mock_gamedata/StaticInfo/skillinfo/SkillTreeInfo.staticinfo.xml</files>
  <read_first>
    tests/fixtures/mock_gamedata/StaticInfo/skillinfo/SkillTreeInfo.staticinfo.xml,
    RessourcesForCodingTheProject/NewScripts/QACompilerNEW/exampleofskillgamedata.txt
  </read_first>
  <action>
    Rewrite `SkillTreeInfo.staticinfo.xml` to have realistic multi-branch nesting based on exampleofskillgamedata.txt patterns. The current fixture has flat ParentNodeId chains (each node points to the previous one in a line). Real game data has BRANCHING trees where multiple nodes share the same parent, creating a proper tree structure.

    Replace the current content with this structure (keep 3 SkillTreeInfo elements for variety, reduce from current 6 to keep file focused):

    **SkillTreeInfo Key="1" StrKey="TREE_001" (Warrior Sword Skills):**
    - Root node: NodeId="100" SkillKey="Skill_SwordMastery" ParentNodeId="0" (root -- no parent)
    - Branch A from root:
      - NodeId="150" SkillKey="Skill_MoveCutting" ParentNodeId="100"
        - NodeId="160" SkillKey="Skill_ShieldAttack" ParentNodeId="150"
          - NodeId="200" SkillKey="Skill_RotationBash" ParentNodeId="160" (3rd level deep)
    - Branch B from root:
      - NodeId="250" SkillKey="Skill_TurnBash" ParentNodeId="100"  (same parent as Branch A)
        - NodeId="300" SkillKey="Skill_Lunge" ParentNodeId="250"
          - NodeId="650" SkillKey="Skill_LightReflection" ParentNodeId="300"
            - NodeId="950" SkillKey="Skill_SwordRush" ParentNodeId="650" (4th level deep)

    **SkillTreeInfo Key="2" StrKey="TREE_002" (Unarmed Combat):**
    - Root node: NodeId="450" SkillKey="Skill_Wrestle" ParentNodeId="0"
      - NodeId="455" SkillKey="Skill_Wrestle_Lariat" ParentNodeId="450"
      - NodeId="460" SkillKey="Skill_Wrestle_AirBodySlam" ParentNodeId="450"
      - NodeId="465" SkillKey="Skill_Wrestle_GiantSwing" ParentNodeId="450"
    - Root node: NodeId="500" SkillKey="Skill_UnArmedMastery" ParentNodeId="0"
      - NodeId="505" SkillKey="Skill_DropKick" ParentNodeId="500"
      - NodeId="510" SkillKey="Skill_DoubleKick" ParentNodeId="500"
      - NodeId="515" SkillKey="Skill_JumpDoubleKick" ParentNodeId="500"
        - NodeId="520" SkillKey="Skill_SkyKick" ParentNodeId="515" (3rd level)

    **SkillTreeInfo Key="3" StrKey="TREE_003" (Mage Skills -- simple flat for contrast):**
    - NodeId="1" SkillKey="Skill_Fireball" ParentNodeId="0"
    - NodeId="2" SkillKey="Skill_IceArrow" ParentNodeId="1"
    - NodeId="3" SkillKey="Skill_Lightning" ParentNodeId="2"

    Each SkillNode should include these attributes: Id (or NodeId), SkillKey, SkillLevel="1", ParentNodeId (use "0" for roots), UIPositionXY (realistic pixel coords like "640 270"), UISkillTreeNodeType="Graymane".

    Each SkillTreeInfo should have: Key, StrKey, CharacterKey, UIPageName (Korean name).

    Use well-formed XML (no self-closing `</>` -- use proper `</SkillNode>`). Root element: `<SkillTreeInfoList>`.
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools && python3 -c "
from lxml import etree
tree = etree.parse('tests/fixtures/mock_gamedata/StaticInfo/skillinfo/SkillTreeInfo.staticinfo.xml')
root = tree.getroot()
trees = list(root)
print(f'SkillTreeInfo count: {len(trees)}')
for t in trees:
    nodes = list(t)
    parents = set(n.get('ParentNodeId','') for n in nodes)
    branching = [p for p in parents if p != '0' and sum(1 for n in nodes if n.get('ParentNodeId')==p) > 0]
    print(f'  {t.get(\"StrKey\")}: {len(nodes)} nodes, parents={parents}')
# Check multi-branch: at least one parent has 2+ children
all_nodes = [n for t in trees for n in list(t)]
from collections import Counter
parent_counts = Counter(n.get('ParentNodeId') for n in all_nodes if n.get('ParentNodeId') != '0')
multi_branch = {k:v for k,v in parent_counts.items() if v >= 2}
print(f'Multi-branch parents (2+ children): {len(multi_branch)}')
assert len(multi_branch) >= 2, 'Need at least 2 multi-branch parents'
# Check depth: at least 3 levels
print('PASS: Multi-branch nesting verified')
"</automated>
  </verify>
  <acceptance_criteria>
    - SkillTreeInfo.staticinfo.xml contains at least 3 SkillTreeInfo elements
    - At least 2 SkillNode elements share the same ParentNodeId (multi-branch)
    - At least one path goes 3+ levels deep (e.g., root -> child -> grandchild -> great-grandchild)
    - All ParentNodeId values are either "0" (root) or match an existing NodeId in the same SkillTreeInfo
    - File parses without errors via `etree.parse()`
    - File contains `ParentNodeId` attribute on SkillNode elements
  </acceptance_criteria>
  <done>
    SkillTreeInfo fixture has multi-branch tree structure with 3+ levels of ParentNodeId nesting, multiple root nodes per tree, and branching where multiple children share a parent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create KnowledgeInfo skill fixture with KnowledgeList parent-child references</name>
  <files>tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_skill.staticinfo.xml</files>
  <read_first>
    tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_character.staticinfo.xml,
    RessourcesForCodingTheProject/NewScripts/QACompilerNEW/exampleofskillgamedata.txt,
    RessourcesForCodingTheProject/NewScripts/QACompilerNEW/generators/skill.py (lines 256-340)
  </read_first>
  <action>
    Create a NEW fixture file `knowledgeinfo_skill.staticinfo.xml` that demonstrates the KnowledgeList parent-child reference pattern from real game data. This pattern is where KnowledgeInfo entries reference their parent via `KnowledgeList="ParentStrKey(N)"` inside LevelData/Learnable elements.

    Based on the exampleofskillgamedata.txt patterns:

    ```xml
    <?xml version='1.0' encoding='UTF-8'?>
    <KnowledgeInfoList>
      <!-- Root knowledge: Unarmed Combat Mastery (parent of all unarmed sub-skills) -->
      <KnowledgeInfo StrKey="Knowledge_UnArmedMastery_I" Name="타격" isDefault="True"
          LearnApplySkillKey="Skill_UnArmedMastery"
          UITextureName="cd_icon_Skill_UnArmedMastery_I"
          Desc="사용 방법 : {Key:Key_FistAttack}&lt;br/&gt;&lt;br/&gt;맨주먹으로 적을 타격하는 기술이다."
          LocalizationDescription="">
        <Dev Memo="기본 습득"/>
        <UseMacroNode Name="Macro_Skill_SkillPoint_Follow_Level5_Basic_01"/>
      </KnowledgeInfo>

      <!-- Child 1: Leg Sweep (references parent via KnowledgeList) -->
      <KnowledgeInfo StrKey="Knowledge_LowerKick" Name="쓸어 차기" isDefault="True"
          UITextureName="cd_icon_Skill_LowerKick"
          Desc="사용 방법 : 구르기 후 {Key:Key_KickAttack}&lt;br/&gt;&lt;br/&gt;하단에서 적을 쓸어 찬다."
          LocalizationDescription="">
        <LevelData Level="1" LearnStyle="LearnedPriorKnowledge" KnowledgeList="Knowledge_UnArmedMastery_I(1)"/>
      </KnowledgeInfo>

      <!-- Child 2: Scissor Takedown -->
      <KnowledgeInfo StrKey="Knowledge_LegSwing" Name="낚아 차기"
          UITextureName="cd_icon_Skill_LegSwing"
          Desc="사용 방법 : 앉은 자세에서 {Key:Key_KickAttack}&lt;br/&gt;&lt;br/&gt;낮은 자세에서 적의 몸을 발로 감아 넘어뜨린다."
          LocalizationDescription="">
        <LevelData Level="1" LearnStyle="LearnedPriorKnowledge" KnowledgeList="Knowledge_UnArmedMastery_I(2)"/>
      </KnowledgeInfo>

      <!-- Child 3: Proficiency (multiple learn conditions) -->
      <KnowledgeInfo StrKey="Knowledge_UnArmedCombination" Name="타격 강화 : 숙련"
          UITextureName="cd_icon_Skill_UnArmedCombination"
          Desc="타격 기술 연계가 추가된다."
          LocalizationDescription="" LearnSpot="cd_m08_m05_npc_etc_01_0017">
        <LevelData Level="1">
          <Learnable LearnStyle="LearnedPriorKnowledge" KnowledgeList="Knowledge_UnArmedMastery_I(3)"/>
          <Learnable LearnStyle="FollowManualLockOn" Condition="Macro(SkillLearn_Battle)" ManualLockOnString="UI_ManualLockOn_Common"/>
        </LevelData>
      </KnowledgeInfo>

      <!-- Child 4: Expertise -->
      <KnowledgeInfo StrKey="Knowledge_UnArmedMastery_Expert" Name="타격 강화 : 통달"
          UITextureName="cd_icon_Skill_UnArmedMastery_Expert"
          Desc="타격술의 극에 다다라, 더욱 다양한 기술을 사용한다."
          LocalizationDescription="">
        <LevelData Level="1" LearnStyle="LearnedPriorKnowledge" KnowledgeList="Knowledge_UnArmedMastery_I(4)"/>
      </KnowledgeInfo>

      <!-- Child 5: Iron Fists (also child of Mastery via level 4) -->
      <KnowledgeInfo StrKey="Knowledge_UnArmedCombination_I" Name="강권"
          UITextureName="cd_icon_Skill_UnArmedCombination_I"
          Desc="사용 방법 : {Key:Key_HardAttack} 연속 입력&lt;br/&gt;&lt;br/&gt;전방으로 주먹을 내지르며 전진한다."
          LocalizationDescription="">
        <LevelData Level="1" LearnStyle="LearnedPriorKnowledge" KnowledgeList="Knowledge_UnArmedMastery_I(4)"/>
      </KnowledgeInfo>

      <!-- Child 6: Flurry of Blows -->
      <KnowledgeInfo StrKey="Knowledge_UnArmedCombination_II" Name="호포"
          UITextureName="cd_icon_Skill_UnArmedCombination_II"
          Desc="사용 방법 : {Key:Key_Skill_1}&lt;br/&gt;&lt;br/&gt;힘을 모아 질풍 같은 권격을 쏟아낸다."
          LocalizationDescription="">
        <LevelData Level="1" LearnStyle="LearnedPriorKnowledge" KnowledgeList="Knowledge_UnArmedMastery_I(5)"/>
      </KnowledgeInfo>

      <!-- Standalone knowledge (no parent reference -- for contrast) -->
      <KnowledgeInfo StrKey="Knowledge_JumpDoubleKick" Name="질풍 차기"
          UITextureName="cd_icon_Skill_DoubleKick_Jump_I"
          Desc="사용 방법 : 공중 낮은 높이에서 {Key:Key_KickAttack}&lt;br/&gt;&lt;br/&gt;공중에서 적에게 날아가 두 번 찍어 찬다."
          LocalizationDescription="">
        <UseMacroNode Name="Macro_Skill_SkillPoint_Level1_Basic_01"/>
      </KnowledgeInfo>

      <!-- Standalone: Meteor Kick with complex learn conditions -->
      <KnowledgeInfo StrKey="Knowledge_SkyKick" Name="벼락 차기"
          UITextureName="cd_icon_Skill_DoubleKick_Jump_II"
          Desc="사용 방법 : 공중 높은 높이에서 {Key:Key_KickAttack}&lt;br/&gt;&lt;br/&gt;높은 곳에서 정확한 위치로 발을 내려찍는다."
          LocalizationDescription="" LearnSpot="Goblin_Master_Doo_OldKliffBattle">
        <LevelData Level="1">
          <Learnable LearnStyle="SkillPoint" ItemKey="Abyss_Artifact" Count="1" GlobalStageSequencerKey="PlayerBehaviorStage_Skill_Basic_03"/>
          <Learnable LearnStyle="Follow" Condition="Macro(SkillLearn_Battle)" RepeatCount="3"/>
        </LevelData>
      </KnowledgeInfo>
    </KnowledgeInfoList>
    ```

    Key requirements:
    - Root element `<KnowledgeInfoList>`
    - Parent knowledge entry: Knowledge_UnArmedMastery_I (the root of the tree)
    - 6 child entries that reference the parent via `KnowledgeList="Knowledge_UnArmedMastery_I(N)"` in their LevelData
    - 2 standalone entries (no KnowledgeList reference) for testing mixed content
    - Use `&lt;br/&gt;` for escaped br-tags in Desc attributes (as per existing fixtures)
    - Include child elements (Dev, UseMacroNode, LevelData, Learnable) to exercise tree depth
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools && python3 -c "
from lxml import etree
tree = etree.parse('tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_skill.staticinfo.xml')
root = tree.getroot()
entries = list(root)
print(f'KnowledgeInfo count: {len(entries)}')
kl_count = 0
for ki in root.iter('KnowledgeInfo'):
    for desc in ki.iter():
        kl = (desc.get('KnowledgeList') or '').strip()
        if kl:
            kl_count += 1
            print(f'  {ki.get(\"StrKey\")} -> KnowledgeList={kl}')
print(f'KnowledgeList references: {kl_count}')
assert len(entries) >= 8, f'Expected >= 8 entries, got {len(entries)}'
assert kl_count >= 6, f'Expected >= 6 KnowledgeList refs, got {kl_count}'
print('PASS: KnowledgeInfo skill fixture verified')
"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_skill.staticinfo.xml
    - File contains `<KnowledgeInfoList>` root element
    - File contains at least 8 KnowledgeInfo elements
    - File contains at least 6 KnowledgeList attribute references (e.g., `KnowledgeList="Knowledge_UnArmedMastery_I(1)"`)
    - File contains `&lt;br/&gt;` escaped br-tags in Desc attributes
    - File parses without errors via `etree.parse()`
    - At least 2 KnowledgeInfo entries have NO KnowledgeList reference (standalone entries)
  </acceptance_criteria>
  <done>
    New KnowledgeInfo skill fixture demonstrates parent-child KnowledgeList reference pattern with 6+ child entries referencing a parent, plus standalone entries for contrast. File parses cleanly with lxml.
  </done>
</task>

</tasks>

<verification>
1. `python3 -c "from lxml import etree; etree.parse('tests/fixtures/mock_gamedata/StaticInfo/skillinfo/SkillTreeInfo.staticinfo.xml')"` -- parses without error
2. `python3 -c "from lxml import etree; etree.parse('tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_skill.staticinfo.xml')"` -- parses without error
3. `grep -c 'ParentNodeId' tests/fixtures/mock_gamedata/StaticInfo/skillinfo/SkillTreeInfo.staticinfo.xml` -- at least 15 occurrences
4. `grep -c 'KnowledgeList' tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_skill.staticinfo.xml` -- at least 6 occurrences
5. GimmickInfo fixture already has XML-nested structure: `grep -c 'GimmickInfo\|SealData' tests/fixtures/mock_gamedata/StaticInfo/gimmickinfo/Item/GimmickInfo_Item_Chest.staticinfo.xml` -- confirms existing nesting
</verification>

<success_criteria>
- SkillTreeInfo fixture has realistic multi-branch ParentNodeId nesting with 3+ depth levels
- New KnowledgeInfo skill fixture has KnowledgeList parent-child reference pattern
- All expanded fixtures parse without errors
- GimmickGroupInfo fixture verified as already sufficient (no changes needed)
- Mock data covers all 3 hierarchy styles: XML-nested (Gimmick), ParentNodeId (Skill), KnowledgeList (Knowledge)
</success_criteria>

<output>
After completion, create `.planning/phases/27-tree-backend-mock-data/27-02-SUMMARY.md`
</output>
