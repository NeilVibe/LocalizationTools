---
phase: 54-tm-flow-faiss-auto-build-grid-colors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - locaNext/src/lib/components/ldm/VirtualGrid.svelte
autonomous: true
requirements:
  - COLOR-01
  - COLOR-02
  - COLOR-03

must_haves:
  truths:
    - "Default/untranslated rows have grey/neutral background (no color)"
    - "Rows with status 'translated' show yellow highlight indicating needs confirmation"
    - "Rows with status 'reviewed' or 'approved' show blue-green highlight indicating confirmed"
  artifacts:
    - path: "locaNext/src/lib/components/ldm/VirtualGrid.svelte"
      provides: "Grid cell color scheme"
      contains: "teal|blue-green|confirmed"
  key_links:
    - from: "VirtualGrid.svelte CSS"
      to: "row.status"
      via: "class:status-translated, class:status-reviewed, class:status-approved"
      pattern: "status-(translated|reviewed|approved)"
---

<objective>
Fix LanguageData grid color scheme to match the design spec: grey (default), yellow (needs confirmation / translated), blue-green (confirmed / reviewed+approved).

Purpose: The current color scheme uses green for confirmed rows but the spec requires blue-green (teal). Default rows are already grey (no styling), and yellow is already used for translated. The green needs to change to blue-green/teal.
Output: Updated VirtualGrid.svelte CSS with corrected color scheme.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@locaNext/src/lib/components/ldm/VirtualGrid.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update grid color scheme — grey/yellow/blue-green</name>
  <files>locaNext/src/lib/components/ldm/VirtualGrid.svelte</files>
  <read_first>locaNext/src/lib/components/ldm/VirtualGrid.svelte (lines 3920-3950 for status color CSS, lines 2855-2875 for class bindings, lines 2169-2175 for getStatusTagType)</read_first>
  <action>
Update the CSS color scheme in VirtualGrid.svelte:

1. **Keep grey default** (no change needed — untranslated rows have no special background, which is grey/neutral).

2. **Keep yellow for "translated" (needs confirmation)** — the current `.cell.target.status-translated` already uses `rgba(198, 163, 0, 0.12)` amber/yellow with `border-left: 3px solid #c6a300`. This is correct for "needs confirmation". No change needed.

3. **Change green to blue-green/teal for "reviewed" and "approved" (confirmed)** — update `.cell.target.status-reviewed` and `.cell.target.status-approved`:
   - Background: change from `rgba(36, 161, 72, 0.15)` (green) to `rgba(0, 157, 154, 0.15)` (teal/blue-green, Carbon Design teal-50)
   - Border-left: change from `3px solid #24a148` (green) to `3px solid #009d9a` (teal/blue-green)

4. **Update the CSS comment** at line ~3920 to reflect the new scheme:
   ```
   /* Status-based cell colors (replaces Status column)
    * 3-state scheme:
    * - Empty (pending, untranslated) = Gray (default, no styling)
    * - Needs Confirmation (translated) = Yellow/amber highlight
    * - Confirmed (reviewed, approved) = Blue-green/teal highlight
    */
   ```

5. **Update the `getStatusTagType` function** (around line 2169) — change the 'reviewed' and 'approved' cases from returning `'green'` to returning `'teal'`. If Carbon's Tag component does not have a 'teal' type, use `'cyan'` or `'blue'` as the closest available. Check the Tag component's valid types first.

6. **Update the inline comment** at line ~2861 from "green=reviewed/approved(confirmed)" to "teal=reviewed/approved(confirmed)".

Do NOT change class names (keep `status-reviewed`, `status-approved`). Only change the CSS color values and comments.
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && grep -n "009d9a\|teal\|blue-green" locaNext/src/lib/components/ldm/VirtualGrid.svelte | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "rgba(0, 157, 154" VirtualGrid.svelte returns 2+ matches (reviewed + approved backgrounds)
    - grep "#009d9a" VirtualGrid.svelte returns 2+ matches (reviewed + approved borders)
    - grep "rgba(36, 161, 72" VirtualGrid.svelte returns 0 matches (old green removed)
    - grep "#24a148" VirtualGrid.svelte returns 0 matches (old green border removed)
    - grep "Needs Confirmation" VirtualGrid.svelte returns 1+ match (updated comment)
    - grep "teal" VirtualGrid.svelte returns 1+ match (updated comment or tag type)
  </acceptance_criteria>
  <done>Grid uses grey (default), yellow (translated/needs confirmation), blue-green/teal (reviewed+approved/confirmed). No green remains in status-based cell colors.</done>
</task>

</tasks>

<verification>
1. Grep confirms teal/blue-green colors replaced green in status CSS
2. Class bindings unchanged (status-translated, status-reviewed, status-approved still used)
3. No build errors: `cd locaNext && npx svelte-check --threshold warning 2>&1 | tail -5`
</verification>

<success_criteria>
- Default rows: grey/neutral (no background color)
- Translated rows: yellow/amber highlight with yellow left border
- Reviewed/approved rows: blue-green/teal highlight with teal left border
- No green color values remain in status-based cell CSS
</success_criteria>

<output>
After completion, create `.planning/phases/54-tm-flow-faiss-auto-build-grid-colors/54-01-SUMMARY.md`
</output>
