---
phase: 07-xml-parsing-foundation-bug-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/repositories/sqlite/folder_repo.py
  - server/tools/ldm/routes/tm_routes.py
  - tests/unit/ldm/test_routes_tm_crud.py
  - tests/unit/ldm/test_routes_folders.py
  - tests/unit/ldm/test_tm_paste.py
autonomous: true
requirements: [FIX-01, FIX-02, FIX-03]

must_haves:
  truths:
    - "Offline TMs appear alongside online TMs in the TM tree response"
    - "TM paste flow transfers clipboard content to target row via API"
    - "Creating a folder then immediately fetching its contents returns 200"
  artifacts:
    - path: "tests/unit/ldm/test_routes_tm_crud.py"
      provides: "Integration test for offline TM visibility"
      contains: "test_offline_tm_in_tree"
    - path: "tests/unit/ldm/test_tm_paste.py"
      provides: "Integration test for TM paste end-to-end"
      min_lines: 30
    - path: "tests/unit/ldm/test_routes_folders.py"
      provides: "Integration test for create-then-get folder"
      contains: "test_create_then_get"
  key_links:
    - from: "server/tools/ldm/routes/tm_routes.py"
      to: "server/repositories"
      via: "tm-tree endpoint queries both online and offline repos"
      pattern: "tm.tree|tm_tree"
---

<objective>
Fix three v1.0 bugs: offline TM visibility in online tree, TM paste end-to-end flow, and folder 404-after-creation.

Purpose: These bugs undermine core CAT tool workflows. Offline TMs not appearing in the tree means users lose work. Broken paste flow blocks the primary translation workflow. Folder 404 after creation causes user confusion.

Output: Three bugs fixed with regression tests. All fixes are independent of the XML parsing work in Plans 01-02.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-xml-parsing-foundation-bug-fixes/07-RESEARCH.md

@server/repositories/sqlite/folder_repo.py

<interfaces>
<!-- Key interfaces for bug fix context -->

From server/repositories/sqlite/folder_repo.py:
```python
class SQLiteFolderRepository(SQLiteBaseRepository, FolderRepository):
    async def get(self, folder_id: int) -> Optional[Dict[str, Any]]: ...
    async def create(self, project_id: int, name: str, parent_id: Optional[int]) -> Dict[str, Any]: ...
    # Creates folders with negative IDs: -int(time.time() * 1000) % 1000000000
```

From server/repositories/interfaces/folder_repository.py:
```python
class FolderRepository(ABC):
    async def get(self, folder_id: int) -> Optional[Dict]: ...
    async def create(self, project_id: int, name: str, parent_id: Optional[int]) -> Dict: ...
    async def get_folder_contents(self, folder_id: int) -> Dict: ...
```

TM tree endpoint (to investigate):
```
GET /api/ldm/tm-tree -> returns tree structure of TM entries
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all three v1.0 bugs (FIX-01, FIX-02, FIX-03)</name>
  <files>
    server/repositories/sqlite/folder_repo.py,
    server/tools/ldm/routes/tm_routes.py,
    tests/unit/ldm/test_routes_tm_crud.py,
    tests/unit/ldm/test_routes_folders.py,
    tests/unit/ldm/test_tm_paste.py
  </files>
  <action>
    **IMPORTANT: Each bug requires investigation before fixing. The research identified likely causes but exact root causes need tracing.**

    **FIX-01: Offline TMs in online TM tree**
    1. Trace the TM tree endpoint: find the route handler for `/api/ldm/tm-tree` (likely in tm_routes.py)
    2. Check whether the handler queries ONLY the primary (PostgreSQL) repository or BOTH online+offline repositories
    3. If it only queries one: update the handler to merge results from both repository instances via the repository factory
    4. If the factory doesn't expose both: check how the factory pattern works (server/repositories/factory.py or similar) and add a method to get offline TMs when in online mode
    5. Add test to test_routes_tm_crud.py: `test_offline_tm_in_tree` -- mock both repos, verify tree response includes entries from both

    **FIX-02: TM Paste end-to-end**
    1. Trace the paste flow: find clipboard store usage in the frontend (locaNext/src/), find the paste API endpoint
    2. Identify the break: is it frontend (Svelte reactivity), API contract mismatch, or repository layer?
    3. Fix the identified issue. Common patterns:
       - If Svelte reactivity: ensure $state updates after paste response
       - If API: ensure paste endpoint accepts the right payload format
       - If repo: ensure the paste operation commits correctly
    4. Create test_tm_paste.py with test covering the paste API endpoint (POST with clipboard payload, verify row updated)

    **FIX-03: Folder 404 after creation**
    1. Read folder_repo.py create() method fully -- it creates folders with negative IDs via `-int(time.time() * 1000) % 1000000000`
    2. Check get_folder_contents() -- does it handle negative IDs? Is there a type mismatch (str vs int)?
    3. Check if the frontend immediately GETs after POST -- is there a race condition with async SQLite?
    4. Fix the identified issue. Likely candidates:
       - get_folder_contents() filtering negative IDs
       - Frontend using the wrong ID from create response
       - Async connection not seeing its own write (need commit)
    5. Add test to test_routes_folders.py: `test_create_then_get` -- create folder via API, immediately fetch contents, verify 200 response

    **For all three bugs:** Use loguru logger for any debug output. Never print().
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python -m pytest tests/unit/ldm/test_routes_tm_crud.py tests/unit/ldm/test_routes_folders.py tests/unit/ldm/test_tm_paste.py -x --no-header -q</automated>
  </verify>
  <done>
    FIX-01: TM tree endpoint returns both online and offline TMs in merged tree structure. Test proves offline entries appear.
    FIX-02: TM paste API endpoint accepts clipboard payload and updates target row. Test proves end-to-end flow works.
    FIX-03: Creating a folder then immediately fetching its contents returns 200 with correct data. Test proves create-then-get works with negative IDs.
  </done>
</task>

</tasks>

<verification>
After task completes:
1. `python -m pytest tests/unit/ldm/ -x --no-header -q` -- full LDM test suite passes
2. All three new test files/test functions exist and pass
</verification>

<success_criteria>
- Offline TMs visible in TM tree alongside online TMs
- TM paste flow works from clipboard to target row
- Folder fetch returns 200 immediately after creation
- All three bugs have regression tests preventing recurrence
</success_criteria>

<output>
After completion, create `.planning/phases/07-xml-parsing-foundation-bug-fixes/07-03-SUMMARY.md`
</output>
