---
phase: 13-ai-summaries
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - locaNext/src/lib/components/ldm/ContextTab.svelte
autonomous: false
requirements: [AISUM-03]

must_haves:
  truths:
    - "User selects a string and sees a 2-line AI summary below the entity cards in the ContextTab"
    - "When Ollama is not running, user sees an 'AI unavailable' badge instead of errors"
    - "When no AI summary is returned, the AI section is simply not rendered (no empty box)"
  artifacts:
    - path: "locaNext/src/lib/components/ldm/ContextTab.svelte"
      provides: "AI summary section + unavailable badge in ContextTab"
      contains: "ai-summary"
  key_links:
    - from: "locaNext/src/lib/components/ldm/ContextTab.svelte"
      to: "/api/ldm/context/{string_id}"
      via: "fetch response parsing ai_summary and ai_status fields"
      pattern: "ai_summary|ai_status"
---

<objective>
Add AI summary display section to ContextTab frontend, showing the 2-line contextual summary from the backend or an "AI unavailable" badge when Ollama is down.

Purpose: Surface AI-generated entity summaries to users in the existing ContextTab, completing the end-to-end AI summary feature.
Output: Updated ContextTab.svelte with AI summary section and graceful fallback badge.
</objective>

<execution_context>
@/home/<USERNAME>/.claude/get-shit-done/workflows/execute-plan.md
@/home/<USERNAME>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-ai-summaries/13-01-SUMMARY.md

@locaNext/src/lib/components/ldm/ContextTab.svelte

<interfaces>
<!-- API response shape from Plan 01 -->
The context endpoint now returns these additional fields:
```json
{
  "entities": [...],
  "detected_in_text": [...],
  "string_id_context": {...},
  "ai_summary": "A brave warrior from the northern kingdom. Known for defending Stormhold Castle.",
  "ai_status": "generated"  // or "cached", "unavailable", "error", null
}
```

ai_summary: string | null -- the 2-line summary text
ai_status: "generated" | "cached" | "unavailable" | "error" | null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AI summary section to ContextTab</name>
  <files>locaNext/src/lib/components/ldm/ContextTab.svelte</files>
  <action>
    1. Add two new $state variables: aiSummary = $state(null) and aiStatus = $state(null)
    2. In the existing fetch .then() success handler, extract ai_summary and ai_status from the response data:
       - aiSummary = data.ai_summary || null
       - aiStatus = data.ai_status || null
    3. Reset aiSummary and aiStatus to null in the cleanup paths (no selectedRow, error, catch)
    4. Import WarningAlt from "carbon-icons-svelte" (add to existing import line)
    5. Add AI summary section in the template, AFTER the entity-list div, inside the context-results block:
       - If aiStatus === 'unavailable': show a small badge div with WarningAlt icon (size=14) and "AI unavailable" text. data-testid="ai-unavailable-badge"
       - Else if aiSummary is truthy: show a summary div with "AI Summary" label and the summary text in a paragraph. data-testid="ai-summary"
       - If neither condition, render nothing (no empty box)
    6. Add CSS styles for .ai-summary-section, .ai-badge-unavailable, .ai-summary, .ai-label, .ai-text:
       - .ai-summary-section: padding 8px, background var(--cds-layer-02), border-radius 4px, border 1px solid var(--cds-border-subtle-01), same pattern as .detected-text
       - .ai-badge-unavailable: display flex, align-items center, gap 4px, font-size 0.6875rem, color var(--cds-text-03), padding 6px 8px, background var(--cds-layer-02), border-radius 4px
       - .ai-label: font-size 0.625rem, font-weight 600, color var(--cds-text-03), text-transform uppercase, letter-spacing 0.5px (same pattern as .detected-label)
       - .ai-text: font-size 0.75rem, color var(--cds-text-01), line-height 1.5, margin 0, white-space pre-line (to preserve line breaks in 2-line summary)
    7. Use Svelte 5 runes only ($state, $derived, $effect). No $: reactive statements.
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools/locaNext && npx svelte-check --threshold error 2>&1 | tail -5</automated>
  </verify>
  <done>ContextTab renders AI summary text when available, shows "AI unavailable" badge when Ollama is down, and renders nothing when no summary exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>AI summary section in ContextTab showing 2-line contextual summaries for game entities, with "AI unavailable" fallback badge when Ollama is not running.</what-built>
  <how-to-verify>
    1. Start dev servers: cd /home/<USERNAME>/LocalizationTools && DEV_MODE=true python3 server/main.py (in one terminal) and cd locaNext && npm run dev (in another)
    2. Open http://localhost:5173, login as admin/admin123
    3. Open a file with entities (e.g., a loc.xml file)
    4. Select a row -- check the Context tab:
       - If Ollama is running with qwen3:4b: should see "AI Summary" label with 2-line text below entity cards
       - If Ollama is NOT running: should see small "AI unavailable" badge with warning icon
    5. Select a different row and return to the original -- second load should be instant (cached)
    6. Verify no console errors in browser DevTools
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
```bash
# Svelte type check passes
cd /home/<USERNAME>/LocalizationTools/locaNext && npx svelte-check --threshold error

# Backend tests still pass
cd /home/<USERNAME>/LocalizationTools && python -m pytest tests/unit/ldm/ -x
```
</verification>

<success_criteria>
- AI summary text appears in ContextTab below entity cards when Ollama returns a summary
- "AI unavailable" badge with WarningAlt icon shows when Ollama is not running
- No empty boxes or error states when AI has no summary to show
- Cached summaries load instantly on re-selection
- All data-testid attributes present for future test automation
</success_criteria>

<output>
After completion, create `.planning/phases/13-ai-summaries/13-02-SUMMARY.md`
</output>
