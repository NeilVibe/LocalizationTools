---
phase: 09-translator-merge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/tools/ldm/services/text_matching.py
  - server/tools/ldm/services/korean_detection.py
  - server/tools/ldm/services/postprocess.py
  - tests/unit/ldm/test_text_matching.py
  - tests/unit/ldm/test_postprocess.py
autonomous: true
requirements: [TMERGE-04]

must_haves:
  truths:
    - "Postprocess pipeline applies all 8 steps in exact order on Str/Desc values"
    - "CJK languages skip ellipsis normalization (step 7)"
    - "br-tag round-trips through postprocess without corruption"
    - "Korean text detection catches syllables, Jamo, and Compat Jamo ranges"
    - "Text normalization for matching uses HTML unescape + whitespace collapse, not LocaNext display normalization"
  artifacts:
    - path: "server/tools/ldm/services/text_matching.py"
      provides: "normalize_text_for_match, normalize_for_matching, normalize_nospace, is_formula_text"
      exports: ["normalize_text_for_match", "normalize_for_matching", "normalize_nospace", "is_formula_text"]
    - path: "server/tools/ldm/services/korean_detection.py"
      provides: "is_korean_text with full Unicode range (syllables + Jamo + Compat Jamo)"
      exports: ["is_korean_text"]
    - path: "server/tools/ldm/services/postprocess.py"
      provides: "PostprocessPipeline with 8 ordered steps"
      exports: ["postprocess_value", "POSTPROCESS_STEPS"]
    - path: "tests/unit/ldm/test_text_matching.py"
      provides: "Unit tests for all normalization functions"
    - path: "tests/unit/ldm/test_postprocess.py"
      provides: "Unit tests for 8-step pipeline, CJK skip, br-tag roundtrip"
  key_links:
    - from: "server/tools/ldm/services/postprocess.py"
      to: "server/tools/ldm/services/korean_detection.py"
      via: "is_korean_text for CJK detection in ellipsis step"
      pattern: "from.*korean_detection.*import.*is_korean_text"
    - from: "server/tools/ldm/services/postprocess.py"
      to: "server/tools/ldm/services/text_matching.py"
      via: "normalize functions for no-translation check"
      pattern: "from.*text_matching.*import"
---

<objective>
Port QuickTranslate's text matching utilities, Korean detection, and 8-step postprocess pipeline into LocaNext service modules.

Purpose: These are foundational utilities that the TranslatorMergeService (Plan 02) depends on. The postprocess pipeline directly satisfies TMERGE-04. The text matching and Korean detection are critical for skip guards and match lookup building in TMERGE-01/02.

Output: Three production modules (text_matching.py, korean_detection.py, postprocess.py) with comprehensive unit tests.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-translator-merge/09-RESEARCH.md

Source references (READ these before implementing):
@RessourcesForCodingTheProject/NewScripts/QuickTranslate/core/text_utils.py
@RessourcesForCodingTheProject/NewScripts/QuickTranslate/core/korean_detection.py
@RessourcesForCodingTheProject/NewScripts/QuickTranslate/core/postprocess.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Text matching utilities + Korean detection</name>
  <files>server/tools/ldm/services/text_matching.py, server/tools/ldm/services/korean_detection.py, tests/unit/ldm/test_text_matching.py</files>
  <behavior>
    - normalize_text_for_match("  Hello   World  ") -> "hello world" (lowercase + whitespace collapse)
    - normalize_text_for_match("test&amp;value") -> "test&value" (HTML unescape)
    - normalize_text_for_match("test&desc;stuff") -> "teststuff" (&desc; removal)
    - normalize_for_matching("  Hello   World  ") -> "hello world" (same as normalize_text_for_match)
    - normalize_nospace("hello world") -> "helloworld" (remove all spaces)
    - is_formula_text("=SUM(A1)") -> True
    - is_formula_text("+1234") -> True
    - is_formula_text("Normal text") -> False
    - is_korean_text("안녕하세요") -> True (syllables)
    - is_korean_text("ㄱㄴㄷ") -> True (Compat Jamo)
    - is_korean_text("Hello") -> False
    - is_korean_text("Hello 안녕") -> True (mixed but has Korean)
  </behavior>
  <action>
    1. READ `RessourcesForCodingTheProject/NewScripts/QuickTranslate/core/text_utils.py` -- port `normalize_text()`, `normalize_for_matching()`, `normalize_nospace()`, `is_formula_text()` into `server/tools/ldm/services/text_matching.py`. CRITICAL: This is NOT the same as LocaNext's existing `server/utils/text_utils.py:normalize_text()` -- QuickTranslate's version does HTML unescape + whitespace collapse + &desc; removal (for merge matching), while LocaNext's strips quotes and removes zero-width chars (for display). Keep them separate. Name the merge version `normalize_text_for_match()` to avoid confusion.

    2. READ `RessourcesForCodingTheProject/NewScripts/QuickTranslate/core/korean_detection.py` -- port `is_korean_text()` into `server/tools/ldm/services/korean_detection.py`. MUST use full regex: `[\uac00-\ud7af\u1100-\u11ff\u3130-\u318f]` (syllables + Jamo + Compat Jamo). NEVER syllables-only.

    3. Both files MUST have `from __future__ import annotations` at top.

    4. Write tests in `tests/unit/ldm/test_text_matching.py` covering all behaviors above plus edge cases: empty string, None-safe, whitespace-only, mixed Korean/English.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/ldm/test_text_matching.py -x -v</automated>
  </verify>
  <done>All normalize and detection functions pass unit tests. Korean detection covers all three Unicode ranges. Formula detection catches =, +, -, @ prefixes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 8-step postprocess pipeline</name>
  <files>server/tools/ldm/services/postprocess.py, tests/unit/ldm/test_postprocess.py</files>
  <behavior>
    - Step 1 (newlines): "\r\n" -> "<br/>", "\n" -> "<br/>", "\r" -> "<br/>", mixed variants normalized
    - Step 2 (empty_strorigin): if source is empty, target becomes empty
    - Step 3 (no_translation): "no translation" -> "" (case-insensitive, whitespace-collapsed)
    - Step 4 (apostrophes): curly quotes (\u2018, \u2019) -> ASCII apostrophe
    - Step 5 (invisible_chars): NBSP (\u00a0) -> space, zero-width chars deleted
    - Step 6 (hyphens): Unicode hyphens (\u2010, \u2011, \u2013, \u2014) -> ASCII hyphen
    - Step 7 (ellipsis): "..." kept as-is for non-CJK; SKIPPED for CJK (keeps Unicode ellipsis)
    - Step 8 (double_escaped): "&amp;amp;" -> "&amp;", "&amp;lt;" -> "<"
    - Pipeline runs all 8 steps in order on Str/Desc only (NOT StrOrigin/DescOrigin)
    - br-tag roundtrip: "<br/>" survives full pipeline intact
  </behavior>
  <action>
    1. READ `RessourcesForCodingTheProject/NewScripts/QuickTranslate/core/postprocess.py` -- port the full `run_all_postprocess_on_tree()` logic. Port each of the 8 step functions exactly. The module-level `POSTPROCESS_STEPS` list defines the order.

    2. Create `server/tools/ldm/services/postprocess.py` with:
       - `POSTPROCESS_STEPS` list of (name, function) tuples in exact order
       - `postprocess_value(value: str, source: str = "", is_cjk: bool = False) -> tuple[str, dict]` -- runs all steps, returns (cleaned_value, stats_dict)
       - `postprocess_rows(rows: list[dict], is_cjk: bool = False) -> tuple[list[dict], dict]` -- batch version for merge results, operates on "target" field only
       - Each step function is private (_normalize_newlines, _enforce_empty_strorigin, etc.)
       - Import `is_korean_text` from `korean_detection.py` for CJK detection if needed
       - `from __future__ import annotations` at top

    3. Step 2 (_enforce_empty_strorigin) needs the source text to check -- `postprocess_value` takes source param for this.

    4. Step 7 (_normalize_ellipsis): for CJK, keep Unicode ellipsis (U+2026). For non-CJK, normalize "..." to "..." (three dots). Skip step entirely when is_cjk=True.

    5. Write tests in `tests/unit/ldm/test_postprocess.py`:
       - Test each step individually
       - Test full pipeline order (steps execute sequentially)
       - Test CJK ellipsis skip
       - Test br-tag roundtrip (string with `<br/>` survives all 8 steps)
       - Test that StrOrigin (source) is never modified
       - Test postprocess_rows batch function
  </action>
  <verify>
    <automated>python -m pytest tests/unit/ldm/test_postprocess.py -x -v</automated>
  </verify>
  <done>All 8 postprocess steps pass individual and pipeline tests. CJK skip works. br-tags survive roundtrip. postprocess_rows batch function works on row dicts.</done>
</task>

</tasks>

<verification>
python -m pytest tests/unit/ldm/test_text_matching.py tests/unit/ldm/test_postprocess.py -x -v
</verification>

<success_criteria>
- text_matching.py exports normalize_text_for_match, normalize_for_matching, normalize_nospace, is_formula_text
- korean_detection.py exports is_korean_text with full 3-range regex
- postprocess.py exports postprocess_value and postprocess_rows with 8 ordered steps
- All unit tests pass
- br-tag roundtrip test confirms no corruption
- CJK ellipsis skip test confirms step 7 is skipped
</success_criteria>

<output>
After completion, create `.planning/phases/09-translator-merge/09-01-SUMMARY.md`
</output>
