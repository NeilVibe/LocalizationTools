---
phase: 25-comprehensive-api-e2e-testing
plan: 08
type: execute
wave: 2
depends_on: [25-03]
files_modified:
  - tests/api/test_ai_intelligence.py
  - tests/api/test_search.py
  - tests/api/test_qa_grammar.py
autonomous: true
requirements:
  - TEST-E2E-15
  - TEST-E2E-16
  - TEST-E2E-17

must_haves:
  truths:
    - "AI suggestion status endpoint returns model availability information"
    - "AI naming and context endpoints respond with meaningful data or graceful degradation"
    - "Search endpoints (explorer, semantic, TM, QuickSearch, KR-Similar) return results with scores"
    - "QA endpoints detect known issues and return categorized results"
    - "Grammar check endpoint processes text and returns correction suggestions"
  artifacts:
    - path: "tests/api/test_ai_intelligence.py"
      provides: "AI suggestion, naming, context, mapdata tests (11 endpoints)"
      min_lines: 150
    - path: "tests/api/test_search.py"
      provides: "Search subsystem tests (9 endpoints)"
      min_lines: 150
    - path: "tests/api/test_qa_grammar.py"
      provides: "QA and grammar check tests (13 endpoints)"
      min_lines: 180
  key_links:
    - from: "tests/api/test_search.py"
      to: "server/tools/ldm/routes/search.py"
      via: "Search endpoints with query parameters"
      pattern: "search"
    - from: "tests/api/test_qa_grammar.py"
      to: "server/tools/ldm/routes/qa.py"
      via: "QA validation endpoints"
      pattern: "qa"
---

<objective>
Write comprehensive pytest API tests for AI Intelligence, Search, and QA/Grammar subsystems.

Purpose: AI suggestions, search (7 search modes), and QA pipeline are the intelligence layer of LocaNext. These endpoints may depend on ML models (FAISS, Qwen) which may not be available in test — tests must handle graceful degradation.

Output: 3 test files covering 33 endpoints with 70+ test cases.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-comprehensive-api-e2e-testing/25-CONTEXT.md

<skills>
- test-master skill (test strategy, graceful degradation patterns)
- test-generator skill (scaffold from endpoint schemas)
- fastapi-expert skill (TestClient, async endpoint testing)
- api-designer skill (REST conventions, search result schemas)
- python-pro skill (pytest parametrize, skip conditions)
- debugging-wizard skill (handle AI model unavailability)
</skills>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AI intelligence tests</name>
  <files>tests/api/test_ai_intelligence.py</files>
  <read_first>
    server/tools/ldm/routes/ai_suggestions.py
    server/tools/ldm/routes/naming.py
    server/tools/ldm/routes/context.py
    server/tools/ldm/routes/mapdata.py
    tests/api/conftest.py
    tests/api/helpers/api_client.py
    tests/api/helpers/assertions.py
  </read_first>
  <action>
    **test_ai_intelligence.py** (25+ tests):
    Read ai_suggestions.py, naming.py, context.py, mapdata.py for all 11 endpoints.

    IMPORTANT: AI endpoints may return error/unavailable if Ollama/Qwen model not running. Tests MUST handle this gracefully — test the endpoint exists and returns valid HTTP response (200 OR 503), not that AI results are perfect.

    1. **AI Suggestions** (5 tests):
       - test_ai_suggestions_status: GET status, assert 200 with model info OR 503 service unavailable
       - test_ai_suggestions_generate: POST generate with Korean text, assert 200 or 503
       - test_ai_suggestions_response_schema: If 200, verify response has suggestions array
       - test_ai_suggestions_empty_text: Empty input, assert 422 or empty suggestions
       - test_ai_suggestions_long_text: Very long input (1000+ chars), assert no crash

    2. **Naming** (6 tests):
       - test_naming_status: GET naming status, assert 200/503
       - test_naming_suggest: POST suggest with entity name, assert response
       - test_naming_similar: POST find similar names, assert list
       - test_naming_suggest_korean: Korean entity name input
       - test_naming_suggest_response_fields: Verify suggestion fields (name, score, reason)
       - test_naming_empty_input: assert 422

    3. **Context** (5 tests):
       - test_context_status: GET context status
       - test_context_detect: POST detect context from text
       - test_context_get: GET context for entity
       - test_context_detect_korean: Korean text context detection
       - test_context_response_schema: Validate response fields

    4. **MapData** (6 tests):
       - test_mapdata_status: GET mapdata status
       - test_mapdata_configure: POST configuration
       - test_mapdata_generate: Trigger map data generation (if endpoint exists)
       - test_mapdata_node_data: Verify node data structure
       - test_mapdata_integration_with_worldmap: MapData feeds WorldMap endpoint
       - test_mapdata_response_schema: Validate all fields

    5. **Graceful Degradation** (3 tests):
       - test_ai_endpoints_respond_when_model_offline: All AI endpoints return valid HTTP (not 500)
       - test_ai_status_indicates_availability: Status endpoints clearly indicate model available/unavailable
       - test_ai_no_server_error: No 500 Internal Server Error from any AI endpoint

    Use `pytest.mark.ai` on all tests. Add `@pytest.mark.skipif` conditions if needed for model availability.
  </action>
  <verify>
    <automated>python3 -c "
import ast
tree = ast.parse(open('tests/api/test_ai_intelligence.py').read())
tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
assert len(tests) >= 20, f'Expected >=20 tests, got {len(tests)}'
names = ' '.join(tests)
assert 'suggestion' in names or 'suggest' in names, 'Missing suggestion tests'
assert 'naming' in names, 'Missing naming tests'
assert 'context' in names, 'Missing context tests'
print(f'OK: test_ai_intelligence.py has {len(tests)} tests')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - >= 20 tests covering all 4 AI subsystems
    - Graceful degradation: tests pass even when AI model is offline
    - No test expects perfect AI output — only valid HTTP responses
    - Korean text input tested for all AI endpoints
    - pytest.mark.ai marker on all tests
  </acceptance_criteria>
  <done>AI intelligence test file with 25+ tests handling model availability gracefully</done>
</task>

<task type="auto">
  <name>Task 2: Create search subsystem tests</name>
  <files>tests/api/test_search.py</files>
  <read_first>
    server/tools/ldm/routes/search.py
    server/tools/ldm/routes/semantic_search.py
    server/tools/ldm/routes/tm_search.py
    tests/api/helpers/assertions.py
    tests/api/helpers/constants.py
  </read_first>
  <action>
    **test_search.py** (22+ tests):
    Read search.py and semantic_search.py for all 9 search endpoints.

    1. **Explorer Search** (4 tests):
       - test_search_explorer_basic: Search with text query, assert results list
       - test_search_explorer_korean: Korean search query
       - test_search_explorer_pagination: Results with offset/limit
       - test_search_explorer_empty: Empty query behavior

    2. **Semantic Search** (4 tests):
       - test_search_semantic_basic: Semantic vector search, assert results with scores
       - test_search_semantic_korean: Korean text semantic search
       - test_search_semantic_score_range: Scores in 0.0-1.0
       - test_search_semantic_unavailable: Handle FAISS index not built gracefully

    3. **TM Search Modes** (5 tests):
       - test_tm_suggest_search: TM suggestion search via search endpoint
       - test_tm_exact_search: Exact match search
       - test_tm_pattern_search: Pattern/regex search
       - test_tm_search_results_sorted: Results sorted by relevance
       - test_tm_search_no_results: Query with no matches

    4. **QuickSearch** (4 tests):
       - test_quicksearch_basic: GET/POST quicksearch with dictionary
       - test_quicksearch_multiline: Multiline text search
       - test_quicksearch_health: Health/status endpoint
       - test_quicksearch_dictionaries: List available dictionaries

    5. **KR-Similar** (3 tests):
       - test_kr_similar_basic: Find similar Korean strings
       - test_kr_similar_score: Results have similarity scores
       - test_kr_similar_empty: No similar strings for gibberish input

    6. **Codex Search** (2 tests):
       - test_codex_search_via_search_api: If codex search is part of search subsystem
       - test_codex_search_korean: Korean codex search

    Use appropriate markers: @pytest.mark.search, @pytest.mark.slow for semantic search.
  </action>
  <verify>
    <automated>python3 -c "
import ast
tree = ast.parse(open('tests/api/test_search.py').read())
tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
assert len(tests) >= 18, f'Expected >=18 tests, got {len(tests)}'
names = ' '.join(tests)
assert 'semantic' in names, 'Missing semantic search tests'
assert 'quicksearch' in names or 'quick_search' in names, 'Missing QuickSearch tests'
print(f'OK: test_search.py has {len(tests)} tests')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - >= 18 tests covering all 9 search endpoints
    - Score validation (0.0-1.0 range) for semantic and similarity searches
    - Sort order validation (descending by relevance)
    - Korean text search tested
    - Graceful handling of FAISS index unavailability
    - QuickSearch and KR-Similar external tools tested
  </acceptance_criteria>
  <done>Search test file with 22+ tests covering all 7 search modes</done>
</task>

<task type="auto">
  <name>Task 3: Create QA and grammar tests</name>
  <files>tests/api/test_qa_grammar.py</files>
  <read_first>
    server/tools/ldm/routes/qa.py
    server/tools/ldm/routes/grammar.py
    server/tools/ldm/schemas/qa.py
    tests/api/helpers/assertions.py
  </read_first>
  <action>
    **test_qa_grammar.py** (25+ tests):
    Read qa.py and grammar.py for all 13 QA/grammar endpoints.

    1. **File-Level QA** (7 tests):
       - test_qa_file_line_check: Run line check QA on file, assert results
       - test_qa_file_pattern_check: Run pattern check on file
       - test_qa_file_term_check: Run term check on file
       - test_qa_file_all_checks: Run all QA checks combined
       - test_qa_results_list: GET QA results for file, assert list of issues
       - test_qa_summary: GET QA summary (counts by category)
       - test_qa_resolve_issue: POST resolve/dismiss a QA issue

    2. **Row-Level QA** (4 tests):
       - test_qa_row_check: QA on single row
       - test_qa_row_with_brtag_issue: Row with br-tag inconsistency
       - test_qa_row_clean: Row with no issues
       - test_qa_row_response_schema: Validate QA result fields (category, severity, message)

    3. **Grammar** (5 tests):
       - test_grammar_check_english: Grammar check on English text
       - test_grammar_check_korean: Grammar check on Korean text
       - test_grammar_check_with_corrections: Text with known grammar issues
       - test_grammar_check_clean_text: Text with no issues
       - test_grammar_check_long_text: Long paragraph (500+ chars)

    4. **QA Categories** (3 tests):
       - test_qa_categories_exist: QA results have category field
       - test_qa_severity_levels: Results have severity (warning, error, info)
       - test_qa_issue_description: Each issue has human-readable description

    5. **QA Tools Integration** (4 tests):
       - test_quicksearch_qa_tools: QuickSearch-based QA (if endpoint exists)
       - test_qa_brtag_validation: QA specifically for br-tag consistency
       - test_qa_korean_text_issues: QA for Korean-specific issues
       - test_qa_empty_file: QA on file with no translatable content

    6. **Edge Cases** (2 tests):
       - test_qa_nonexistent_file: assert 404
       - test_qa_with_special_characters: QA on text with XML special chars

    Use @pytest.mark.qa on all tests.
  </action>
  <verify>
    <automated>python3 -c "
import ast
tree = ast.parse(open('tests/api/test_qa_grammar.py').read())
tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
assert len(tests) >= 20, f'Expected >=20 tests, got {len(tests)}'
names = ' '.join(tests)
assert 'grammar' in names, 'Missing grammar tests'
assert 'qa' in names.lower(), 'Missing QA tests'
print(f'OK: test_qa_grammar.py has {len(tests)} tests')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - >= 20 tests covering file QA, row QA, grammar, categories, tools
    - QA result schema validated (category, severity, message)
    - Grammar check tested for English and Korean
    - br-tag-specific QA validation included
    - Edge cases: empty files, special characters, nonexistent files
  </acceptance_criteria>
  <done>QA/grammar test file with 25+ tests covering all 13 QA endpoints</done>
</task>

</tasks>

<verification>
```bash
python3 -m pytest tests/api/test_ai_intelligence.py tests/api/test_search.py tests/api/test_qa_grammar.py --collect-only 2>&1 | tail -5
```
</verification>

<success_criteria>
- 3 test files with 65+ total tests
- AI tests handle model unavailability gracefully
- Search score validation (range + sort order)
- QA result schema fully validated
- Korean text tested across all subsystems
- All 33 endpoints covered
</success_criteria>

<output>
After completion, create `.planning/phases/25-comprehensive-api-e2e-testing/25-08-SUMMARY.md`
</output>
