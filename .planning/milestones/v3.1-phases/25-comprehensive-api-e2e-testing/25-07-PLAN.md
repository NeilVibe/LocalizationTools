---
phase: 25-comprehensive-api-e2e-testing
plan: 07
type: execute
wave: 2
depends_on: [25-01, 25-03]
files_modified:
  - tests/api/test_gamedata.py
  - tests/api/test_codex.py
  - tests/api/test_worldmap.py
autonomous: true
requirements:
  - TEST-E2E-12
  - TEST-E2E-13
  - TEST-E2E-14

must_haves:
  truths:
    - "GameData browse returns directory tree matching mock StaticInfo structure"
    - "GameData column detection works for ALL entity types (item, character, skill, gimmick, knowledge, region, quest, sceneobject, seal)"
    - "Codex types returns all entity types from mock data"
    - "Codex entity list returns entities with Korean names and knowledge keys"
    - "WorldMap returns nodes with x/z coordinates and routes connecting them"
  artifacts:
    - path: "tests/api/test_gamedata.py"
      provides: "GameData browse + columns tests (3 endpoints)"
      min_lines: 150
    - path: "tests/api/test_codex.py"
      provides: "Codex entity tests (4 endpoints)"
      min_lines: 120
    - path: "tests/api/test_worldmap.py"
      provides: "WorldMap data tests (1 endpoint)"
      min_lines: 80
  key_links:
    - from: "tests/api/test_gamedata.py"
      to: "tests/fixtures/mock_gamedata/StaticInfo/"
      via: "Browse endpoint returns files from mock StaticInfo tree"
      pattern: "StaticInfo"
    - from: "tests/api/test_codex.py"
      to: "server/tools/ldm/routes/codex.py"
      via: "Codex endpoints aggregate data from all entity types"
      pattern: "codex"
---

<objective>
Write comprehensive pytest API tests for GameData, Codex, and WorldMap subsystems.

Purpose: These are the v3.0 game development features. GameData browse and column detection must work across ALL entity types. Codex must aggregate entities correctly. WorldMap must return geographic data. These features were added in v3.0 and need thorough testing against the expanded mock data.

Output: 3 test files covering 8 endpoints with 60+ test cases.
</objective>

<execution_context>
@/home/<USERNAME>/.claude/get-shit-done/workflows/execute-plan.md
@/home/<USERNAME>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-comprehensive-api-e2e-testing/25-CONTEXT.md

<skills>
- test-master skill (test strategy, mock data validation)
- test-generator skill (scaffold from endpoint schemas)
- fastapi-expert skill (TestClient, query parameter testing)
- xml-localization skill (validate XML structure in browse results)
- api-designer skill (REST conventions, nested resource patterns)
- python-pro skill (pytest parametrize for entity types)
</skills>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GameData browse and column detection tests</name>
  <files>tests/api/test_gamedata.py</files>
  <read_first>
    server/tools/ldm/routes/gamedata.py
    server/tools/ldm/schemas/gamedata.py
    server/tools/ldm/services/xml_parsing.py
    tests/fixtures/mock_gamedata/StaticInfo/iteminfo/iteminfo_weapon.staticinfo.xml
    tests/fixtures/mock_gamedata/StaticInfo/characterinfo/characterinfo_npc.staticinfo.xml
    tests/api/conftest.py
    tests/api/helpers/api_client.py
  </read_first>
  <action>
    **test_gamedata.py** (25+ tests):
    Read gamedata.py to find exact 3 endpoints (browse, columns/detect, save/edit).

    1. **Browse** (10 tests):
       - test_browse_root: Browse root of mock gamedata, assert directory listing
       - test_browse_staticinfo: Browse StaticInfo/, verify all subdirectories listed
       - test_browse_with_depth: Browse with depth=2, verify nested results
       - test_browse_iteminfo: Browse StaticInfo/iteminfo/, verify 4 XML files (weapon, armor, accessory, consumable)
       - test_browse_characterinfo: Verify 3 XML files (npc, monster, npc_shop)
       - test_browse_gimmickinfo: Verify nested structure (Background/, Item/, Puzzle/)
       - test_browse_questinfo: Verify 3 quest XML files (from Plan 01)
       - test_browse_sceneobjectdata: Verify 3 scene object files (from Plan 01)
       - test_browse_nonexistent_path: Browse invalid path, assert appropriate error/empty
       - test_browse_path_format: Verify paths match `StaticInfo/{type}info/{filename}.staticinfo.xml` pattern

    2. **Column Detection** (10 tests, use @pytest.mark.parametrize for entity types):
       - test_detect_columns_iteminfo: Detect columns for weapon XML, assert Key/StrKey/ItemName/ItemDesc/ItemType/Grade
       - test_detect_columns_characterinfo: assert Key/StrKey/CharacterName/CharacterDesc
       - test_detect_columns_skillinfo: assert Key/StrKey/SkillName/SkillDesc
       - test_detect_columns_gimmickinfo: assert GimmickInfo columns
       - test_detect_columns_knowledgeinfo: assert KnowledgeInfo columns
       - test_detect_columns_factioninfo: assert FactionInfo columns
       - test_detect_columns_questinfo: assert QuestInfo columns (new entity type)
       - test_detect_columns_sceneobjectdata: assert SceneObjectData columns (new entity type)
       - test_detect_columns_sealdatainfo: assert SealDataInfo columns (new entity type)
       - test_detect_columns_nonexistent: Invalid file path, assert error

    3. **Save/Edit** (5 tests):
       - test_save_inline_edit: Edit a cell value, verify saved
       - test_save_edit_preserves_brtags: Edit cell with br-tag content, verify preserved
       - test_save_edit_korean_text: Edit with Korean text
       - test_save_edit_nonexistent_entity: Invalid entity/key, assert error
       - test_save_edit_readonly_field: Attempt to edit Key field, assert rejected (if enforced)

    Use @pytest.mark.parametrize to test column detection across all entity types in a single parametrized test.
    Use mock_gamedata_path fixture to reference fixture files.
  </action>
  <verify>
    <automated>python3 -c "
import ast
tree = ast.parse(open('tests/api/test_gamedata.py').read())
tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
assert len(tests) >= 20, f'Expected >=20 tests, got {len(tests)}'
names = ' '.join(tests)
assert 'browse' in names, 'Missing browse tests'
assert 'column' in names or 'detect' in names, 'Missing column detection tests'
print(f'OK: test_gamedata.py has {len(tests)} tests')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - >= 20 tests covering browse, column detection, and save/edit
    - Browse tests verify all 10 StaticInfo subdirectories
    - Column detection tested for ALL entity types via parametrize
    - br-tag preservation in save/edit tested
    - Path format validation included
  </acceptance_criteria>
  <done>GameData test file with 25+ tests covering all 3 endpoints across all entity types</done>
</task>

<task type="auto">
  <name>Task 2: Create Codex entity tests</name>
  <files>tests/api/test_codex.py</files>
  <read_first>
    server/tools/ldm/routes/codex.py
    server/tools/ldm/schemas/codex.py
    tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_character.staticinfo.xml
    tests/fixtures/mock_gamedata/StaticInfo/knowledgeinfo/knowledgeinfo_item.staticinfo.xml
    tests/api/helpers/assertions.py
  </read_first>
  <action>
    **test_codex.py** (20+ tests):
    Read codex.py for 4 endpoints: types, list-per-type, search, get-entity.

    1. **Entity Types** (4 tests):
       - test_codex_types_list: GET codex types, assert list of entity type strings
       - test_codex_types_include_character: "character" in types list
       - test_codex_types_include_item: "item" in types list
       - test_codex_types_count: Verify expected number of types from mock data

    2. **List Entities** (6 tests):
       - test_list_character_entities: GET entities of type "character", assert list
       - test_list_item_entities: GET entities of type "item"
       - test_list_knowledge_entities: GET entities of type "knowledge"
       - test_list_entities_have_names: Each entity has a name field (Korean)
       - test_list_entities_have_knowledge_keys: Entities reference knowledge entries
       - test_list_nonexistent_type: Invalid type, assert empty/error

    3. **Search** (5 tests):
       - test_codex_search_by_name: Search Korean entity name
       - test_codex_search_by_key: Search by StrKey
       - test_codex_search_empty: Empty query returns all/error
       - test_codex_search_partial_match: Partial Korean text match
       - test_codex_search_english: Search English translation text

    4. **Get Entity** (3 tests):
       - test_get_entity_details: GET single entity, verify all fields
       - test_get_entity_cross_references: Entity links to knowledge, images, audio
       - test_get_nonexistent_entity: Invalid entity ID, assert 404

    5. **Cross-Reference Chain** (3 tests):
       - test_character_knowledge_chain: Character → KnowledgeKey → KnowledgeInfo resolves
       - test_item_knowledge_chain: Item → KnowledgeKey → KnowledgeInfo resolves
       - test_entity_type_sorting: Verify entity types sort correctly (unknown types last per FIX-09)
  </action>
  <verify>
    <automated>python3 -c "
import ast
tree = ast.parse(open('tests/api/test_codex.py').read())
tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
assert len(tests) >= 18, f'Expected >=18 tests, got {len(tests)}'
names = ' '.join(tests)
assert 'search' in names, 'Missing search tests'
assert 'cross_reference' in names or 'chain' in names or 'knowledge' in names, 'Missing cross-ref tests'
print(f'OK: test_codex.py has {len(tests)} tests')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - >= 18 tests covering types, listing, search, get entity, cross-references
    - Cross-reference chain tested: character->knowledge->details
    - Korean name search tested
    - Entity type sorting tested (unknown types last)
  </acceptance_criteria>
  <done>Codex test file with 20+ tests covering all 4 endpoints and cross-reference chains</done>
</task>

<task type="auto">
  <name>Task 3: Create WorldMap data tests</name>
  <files>tests/api/test_worldmap.py</files>
  <read_first>
    server/tools/ldm/routes/worldmap.py
    server/tools/ldm/routes/mapdata.py
    server/tools/ldm/schemas/worldmap.py
    tests/fixtures/mock_gamedata/StaticInfo/factioninfo/FactionInfo.staticinfo.xml
    tests/fixtures/mock_gamedata/StaticInfo/factioninfo/NodeWaypointInfo/NodeWaypointInfo.staticinfo.xml
    tests/api/helpers/assertions.py
  </read_first>
  <action>
    **test_worldmap.py** (15+ tests):
    Read worldmap.py and mapdata.py for all endpoints (worldmap data + map config).

    1. **WorldMap Data** (5 tests):
       - test_worldmap_returns_data: GET worldmap, assert non-empty response
       - test_worldmap_has_nodes: Response contains nodes array
       - test_worldmap_has_routes: Response contains routes array
       - test_worldmap_has_bounds: Response contains bounds (min/max coordinates)
       - test_worldmap_node_count: Verify expected node count from mock FactionInfo (14 nodes per CONTEXT)

    2. **Node Validation** (5 tests):
       - test_node_has_coordinates: Each node has x, z (or x, y) coordinate fields
       - test_node_coordinates_numeric: Coordinates parse as numbers
       - test_node_has_name: Each node has a name field
       - test_node_has_faction: Nodes reference faction data
       - test_node_korean_names: Node names contain Korean text

    3. **Route Validation** (3 tests):
       - test_route_connects_nodes: Each route has from/to node references
       - test_route_keys_unique: No duplicate route keys (FIX-07 requirement)
       - test_route_count: Expected route count from mock data (13 routes per CONTEXT)

    4. **MapData Configuration** (4+ tests):
       - test_mapdata_status: GET mapdata status endpoint
       - test_mapdata_configure: POST configuration (if endpoint exists)
       - test_mapdata_node_positions: Verify x/z positions parse correctly from FactionInfo
       - test_mapdata_bounds_calculation: Verify bounds encompass all node positions

    Use FactionInfo.staticinfo.xml and NodeWaypointInfo.staticinfo.xml mock data.
  </action>
  <verify>
    <automated>python3 -c "
import ast
tree = ast.parse(open('tests/api/test_worldmap.py').read())
tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
assert len(tests) >= 12, f'Expected >=12 tests, got {len(tests)}'
names = ' '.join(tests)
assert 'node' in names, 'Missing node tests'
assert 'route' in names, 'Missing route tests'
print(f'OK: test_worldmap.py has {len(tests)} tests')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - >= 12 tests covering data retrieval, nodes, routes, bounds, configuration
    - Node coordinate validation (numeric, present)
    - Route key uniqueness validated (prevents {#each} crashes)
    - Korean node names tested
    - Expected counts from mock data verified
  </acceptance_criteria>
  <done>WorldMap test file with 15+ tests covering nodes, routes, bounds, and map configuration</done>
</task>

</tasks>

<verification>
```bash
python3 -m pytest tests/api/test_gamedata.py tests/api/test_codex.py tests/api/test_worldmap.py --collect-only 2>&1 | tail -5
```
</verification>

<success_criteria>
- 3 test files with 55+ total tests
- GameData column detection tested for ALL entity types
- Codex cross-reference chain validated
- WorldMap nodes/routes/bounds validated
- All tests parametrized where possible for entity type coverage
</success_criteria>

<output>
After completion, create `.planning/phases/25-comprehensive-api-e2e-testing/25-07-SUMMARY.md`
</output>
