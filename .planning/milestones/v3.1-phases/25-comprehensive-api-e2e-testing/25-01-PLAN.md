---
phase: 25-comprehensive-api-e2e-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/mock_gamedata/StaticInfo/questinfo/questinfo_main.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/questinfo/questinfo_sub.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/questinfo/questinfo_daily.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/sceneobjectdata/SceneObjectData_Town.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/sceneobjectdata/SceneObjectData_Dungeon.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/sceneobjectdata/SceneObjectData_Field.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/sealdatainfo/SealDataInfo.staticinfo.xml
  - tests/fixtures/mock_gamedata/StaticInfo/regioninfo/RegionInfo.staticinfo.xml
  - tests/fixtures/mock_gamedata/stringtable/export__/System/questinfo_main.loc.xml
  - tests/fixtures/mock_gamedata/stringtable/export__/System/questinfo_sub.loc.xml
  - tests/fixtures/mock_gamedata/stringtable/export__/System/questinfo_daily.loc.xml
  - tests/fixtures/mock_gamedata/stringtable/export__/System/sceneobjectdata_town.loc.xml
  - tests/fixtures/mock_gamedata/stringtable/export__/System/sceneobjectdata_dungeon.loc.xml
  - tests/fixtures/mock_gamedata/stringtable/export__/System/sceneobjectdata_field.loc.xml
  - tests/fixtures/mock_gamedata/stringtable/export__/System/sealdatainfo.loc.xml
  - tests/fixtures/mock_gamedata/stringtable/export__/System/regioninfo.loc.xml
autonomous: true
requirements:
  - TEST-E2E-01
  - TEST-E2E-02

must_haves:
  truths:
    - "Every StaticInfo entity type has a corresponding subdirectory with at least 10 entities"
    - "All new XML files use Korean StrOrigin + English Str with br-tag examples"
    - "Mock paths exactly mirror production StaticInfo folder structure"
    - "Every new StaticInfo file has a matching .loc.xml in stringtable/export__/System/"
  artifacts:
    - path: "tests/fixtures/mock_gamedata/StaticInfo/questinfo/"
      provides: "Quest entity mock data (main, sub, daily quest types)"
      contains: "QuestInfo"
    - path: "tests/fixtures/mock_gamedata/StaticInfo/sceneobjectdata/"
      provides: "Scene object mock data (town, dungeon, field)"
      contains: "SceneObjectData"
    - path: "tests/fixtures/mock_gamedata/StaticInfo/sealdatainfo/"
      provides: "Seal data mock data"
      contains: "SealDataInfo"
    - path: "tests/fixtures/mock_gamedata/StaticInfo/regioninfo/"
      provides: "Region info mock data"
      contains: "RegionInfo"
  key_links:
    - from: "tests/fixtures/mock_gamedata/StaticInfo/questinfo/"
      to: "tests/fixtures/mock_gamedata/stringtable/export__/System/"
      via: "StrKey references in loc.xml files matching StaticInfo StrKey attributes"
      pattern: "StrKey.*STR_QUEST"
---

<objective>
Expand mock gamedata with missing StaticInfo subdirectories: questinfo, sceneobjectdata, sealdatainfo, and regioninfo.

Purpose: The existing mock_gamedata only covers 6 of 10 entity types. API tests for gamedata/browse, codex, and column detection need ALL entity types to validate complete coverage.

Output: 8 new StaticInfo XML files + 8 matching .loc.xml files, each with 10-15 entities containing Korean text and br-tags.
</objective>

<execution_context>
@/home/<USERNAME>/.claude/get-shit-done/workflows/execute-plan.md
@/home/<USERNAME>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-comprehensive-api-e2e-testing/25-CONTEXT.md

<skills>
- xml-localization skill (XML structure, br-tag handling, lxml patterns)
- test-master skill (mock data design, fixture organization)
</skills>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create questinfo StaticInfo XMLs (main, sub, daily)</name>
  <files>
    tests/fixtures/mock_gamedata/StaticInfo/questinfo/questinfo_main.staticinfo.xml
    tests/fixtures/mock_gamedata/StaticInfo/questinfo/questinfo_sub.staticinfo.xml
    tests/fixtures/mock_gamedata/StaticInfo/questinfo/questinfo_daily.staticinfo.xml
  </files>
  <read_first>
    tests/fixtures/mock_gamedata/StaticInfo/iteminfo/iteminfo_weapon.staticinfo.xml
    tests/fixtures/mock_gamedata/StaticInfo/characterinfo/characterinfo_npc.staticinfo.xml
    tests/fixtures/mock_gamedata/StaticInfo/gimmickinfo/Puzzle/GimmickInfo_Puzzle_Seal.staticinfo.xml
  </read_first>
  <action>
    Create 3 questinfo XML files following the established pattern from existing StaticInfo files.

    **questinfo_main.staticinfo.xml**: Root element `QuestInfoList`, child elements `QuestInfo`. Each entity needs:
    - Key (unique int, starting at 30001), StrKey (STR_QUEST_MAIN_XXXX), QuestName (Korean with br-tags), QuestDesc (Korean with br-tags)
    - QuestType="Main", Grade (varies), RewardKey refs
    - 12 entities total, at least 4 with `&lt;br/&gt;` in QuestName or QuestDesc

    **questinfo_sub.staticinfo.xml**: Same structure but QuestType="Sub", Keys starting at 31001, StrKey prefix STR_QUEST_SUB_. 10 entities.

    **questinfo_daily.staticinfo.xml**: Same structure but QuestType="Daily", Keys starting at 32001, StrKey prefix STR_QUEST_DAILY_. 10 entities.

    All Korean text must use real Korean characters (hangul syllables + common game vocabulary like 퀘스트, 임무, 보상, 완료). Newlines represented as `&lt;br/&gt;` (XML-escaped br-tags).

    Create the questinfo/ directory if it does not exist.
  </action>
  <verify>
    <automated>python3 -c "
from lxml import etree
import glob, os
files = glob.glob('tests/fixtures/mock_gamedata/StaticInfo/questinfo/*.staticinfo.xml')
assert len(files) == 3, f'Expected 3 quest files, got {len(files)}'
for f in files:
    tree = etree.parse(f)
    entities = tree.getroot()
    assert len(entities) >= 10, f'{f}: expected >=10 entities, got {len(entities)}'
    has_br = any('br/' in (e.get('QuestDesc','') + e.get('QuestName','')) for e in entities)
    assert has_br, f'{f}: no br-tag examples found'
print('OK: 3 questinfo files, all with >=10 entities and br-tags')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - 3 XML files exist in tests/fixtures/mock_gamedata/StaticInfo/questinfo/
    - Each file has >= 10 QuestInfo entities
    - At least 4 entities per file contain &lt;br/&gt; in attribute values
    - Korean hangul characters present in QuestName and QuestDesc
    - Keys are unique across all 3 files
  </acceptance_criteria>
  <done>3 questinfo StaticInfo XML files with 32 total entities, Korean text, and br-tag examples</done>
</task>

<task type="auto">
  <name>Task 2: Create sceneobjectdata and sealdatainfo StaticInfo XMLs</name>
  <files>
    tests/fixtures/mock_gamedata/StaticInfo/sceneobjectdata/SceneObjectData_Town.staticinfo.xml
    tests/fixtures/mock_gamedata/StaticInfo/sceneobjectdata/SceneObjectData_Dungeon.staticinfo.xml
    tests/fixtures/mock_gamedata/StaticInfo/sceneobjectdata/SceneObjectData_Field.staticinfo.xml
    tests/fixtures/mock_gamedata/StaticInfo/sealdatainfo/SealDataInfo.staticinfo.xml
  </files>
  <read_first>
    tests/fixtures/mock_gamedata/StaticInfo/factioninfo/FactionInfo.staticinfo.xml
    tests/fixtures/mock_gamedata/StaticInfo/gimmickinfo/Background/GimmickInfo_Background_Door.staticinfo.xml
  </read_first>
  <action>
    Create 4 StaticInfo XML files for the remaining missing entity types.

    **SceneObjectData_Town.staticinfo.xml**: Root `SceneObjectDataList`, child `SceneObjectData`. Attributes: Key (40001+), StrKey (STR_SOD_TOWN_XXXX), ObjectName (Korean), ObjectDesc (Korean with br-tags), ObjectType, Position (x,y,z coords as string), SceneType="Town", AliasName. 12 entities.

    **SceneObjectData_Dungeon.staticinfo.xml**: Same structure, SceneType="Dungeon", Keys 41001+. 10 entities.

    **SceneObjectData_Field.staticinfo.xml**: Same structure, SceneType="Field", Keys 42001+. 10 entities.

    **SealDataInfo.staticinfo.xml**: Root `SealDataInfoList`, child `SealDataInfo`. Attributes: Key (50001+), StrKey (STR_SEAL_XXXX), SealName (Korean), Desc (Korean with br-tags describing seal effects), Grade, GimmickKey (references to gimmick entities). 12 entities.

    Create directories if they do not exist. All Korean text with hangul syllables. At least 4 entities per file with br-tags.
  </action>
  <verify>
    <automated>python3 -c "
from lxml import etree
import os
paths = [
    'tests/fixtures/mock_gamedata/StaticInfo/sceneobjectdata/SceneObjectData_Town.staticinfo.xml',
    'tests/fixtures/mock_gamedata/StaticInfo/sceneobjectdata/SceneObjectData_Dungeon.staticinfo.xml',
    'tests/fixtures/mock_gamedata/StaticInfo/sceneobjectdata/SceneObjectData_Field.staticinfo.xml',
    'tests/fixtures/mock_gamedata/StaticInfo/sealdatainfo/SealDataInfo.staticinfo.xml',
]
for p in paths:
    assert os.path.exists(p), f'Missing: {p}'
    tree = etree.parse(p)
    assert len(tree.getroot()) >= 10, f'{p}: <10 entities'
print('OK: 4 files exist with >=10 entities each')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - SceneObjectData_Town/Dungeon/Field.staticinfo.xml exist in sceneobjectdata/
    - SealDataInfo.staticinfo.xml exists in sealdatainfo/
    - Each file has >= 10 entities with Korean text
    - Position attributes present in SceneObjectData files
    - GimmickKey references present in SealDataInfo
  </acceptance_criteria>
  <done>4 new StaticInfo XMLs covering sceneobjectdata (3 scene types) and sealdatainfo</done>
</task>

<task type="auto">
  <name>Task 3: Create regioninfo StaticInfo + all matching .loc.xml files</name>
  <files>
    tests/fixtures/mock_gamedata/StaticInfo/regioninfo/RegionInfo.staticinfo.xml
    tests/fixtures/mock_gamedata/stringtable/export__/System/questinfo_main.loc.xml
    tests/fixtures/mock_gamedata/stringtable/export__/System/questinfo_sub.loc.xml
    tests/fixtures/mock_gamedata/stringtable/export__/System/questinfo_daily.loc.xml
    tests/fixtures/mock_gamedata/stringtable/export__/System/sceneobjectdata_town.loc.xml
    tests/fixtures/mock_gamedata/stringtable/export__/System/sceneobjectdata_dungeon.loc.xml
    tests/fixtures/mock_gamedata/stringtable/export__/System/sceneobjectdata_field.loc.xml
    tests/fixtures/mock_gamedata/stringtable/export__/System/sealdatainfo.loc.xml
    tests/fixtures/mock_gamedata/stringtable/export__/System/regioninfo.loc.xml
  </files>
  <read_first>
    tests/fixtures/mock_gamedata/stringtable/export__/System/iteminfo_weapon.loc.xml
    tests/fixtures/mock_gamedata/stringtable/export__/System/characterinfo_npc.loc.xml
    tests/fixtures/mock_gamedata/stringtable/loc/languagedata_kor.xml
  </read_first>
  <action>
    **RegionInfo.staticinfo.xml**: Root `RegionInfoList`, child `RegionInfo`. Attributes: Key (60001+), StrKey (STR_REGION_XXXX), RegionName (Korean), RegionDesc (Korean with br-tags), RegionType (Town/Field/Dungeon/Special), ParentRegion (optional ref), FactionKey (refs to FactionInfo). 15 entities covering varied region types.

    **All 8 .loc.xml files**: Follow the exact pattern from existing loc.xml files. Each loc.xml uses `LanguageData > LocStr` wrapper format:
    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <LanguageData>
      <LocStr StrKey="STR_XXX_XXXX" StrOrigin="Korean source" Str="English translation" Desc="Description"/>
      ...
    </LanguageData>
    ```

    For each new StaticInfo file, create a matching .loc.xml that maps every StrKey to a LocStr with:
    - StrOrigin: Korean text (same as the StaticInfo Korean attribute)
    - Str: English translation
    - Desc: Optional description field
    - br-tags preserved as `<br/>` (NOT escaped in loc.xml - raw br-tags in LocStr format)

    Naming convention: `{entity_type}.loc.xml` in lowercase (e.g., questinfo_main.loc.xml, sealdatainfo.loc.xml).
  </action>
  <verify>
    <automated>python3 -c "
import os, glob
loc_dir = 'tests/fixtures/mock_gamedata/stringtable/export__/System'
new_locs = ['questinfo_main', 'questinfo_sub', 'questinfo_daily', 'sceneobjectdata_town', 'sceneobjectdata_dungeon', 'sceneobjectdata_field', 'sealdatainfo', 'regioninfo']
for name in new_locs:
    path = os.path.join(loc_dir, f'{name}.loc.xml')
    assert os.path.exists(path), f'Missing: {path}'
ri = 'tests/fixtures/mock_gamedata/StaticInfo/regioninfo/RegionInfo.staticinfo.xml'
assert os.path.exists(ri), f'Missing RegionInfo'
from lxml import etree
tree = etree.parse(ri)
assert len(tree.getroot()) >= 10, 'RegionInfo < 10 entities'
print('OK: RegionInfo + 8 loc.xml files created')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - RegionInfo.staticinfo.xml exists with >= 15 entities
    - 8 new .loc.xml files exist in stringtable/export__/System/
    - Each .loc.xml has LocStr entries matching the StaticInfo StrKeys
    - StrOrigin contains Korean text, Str contains English text
    - br-tags present in at least some entries
  </acceptance_criteria>
  <done>RegionInfo StaticInfo created + 8 .loc.xml files covering all new entity types</done>
</task>

</tasks>

<verification>
All StaticInfo subdirectories (10 total) have XML files with 10+ entities, br-tags, and matching loc.xml files.
```bash
python3 -c "
import os
base = 'tests/fixtures/mock_gamedata/StaticInfo'
expected = ['characterinfo', 'factioninfo', 'gimmickinfo', 'iteminfo', 'knowledgeinfo', 'skillinfo', 'questinfo', 'sceneobjectdata', 'sealdatainfo', 'regioninfo']
for d in expected:
    assert os.path.isdir(os.path.join(base, d)), f'Missing dir: {d}'
print(f'OK: All {len(expected)} StaticInfo subdirectories exist')
"
```
</verification>

<success_criteria>
- 10 StaticInfo subdirectories exist (6 existing + 4 new)
- 8 new StaticInfo XML files with 10+ entities each
- 8 new .loc.xml files matching new StaticInfo StrKeys
- All Korean text uses hangul syllables, br-tags as &lt;br/&gt;
- Total new entities: ~100+
</success_criteria>

<output>
After completion, create `.planning/phases/25-comprehensive-api-e2e-testing/25-01-SUMMARY.md`
</output>
