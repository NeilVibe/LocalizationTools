---
phase: 25-comprehensive-api-e2e-testing
plan: 10
type: execute
wave: 3
depends_on: [25-05, 25-06, 25-07, 25-08]
files_modified:
  - tests/api/test_integration_workflows.py
  - tests/api/test_brtag_roundtrip.py
  - tests/api/test_korean_unicode.py
  - tests/api/test_endpoint_coverage.py
autonomous: true
requirements:
  - TEST-E2E-23
  - TEST-E2E-24
  - TEST-E2E-25

must_haves:
  truths:
    - "Complete user workflows pass end-to-end: upload -> edit -> QA -> merge -> export"
    - "br-tag preservation is verified across every subsystem that handles text"
    - "Korean Unicode integrity is maintained through all text-handling endpoints"
    - "Endpoint coverage report confirms all 275 endpoints have at least one test"
  artifacts:
    - path: "tests/api/test_integration_workflows.py"
      provides: "Cross-subsystem workflow tests"
      min_lines: 200
    - path: "tests/api/test_brtag_roundtrip.py"
      provides: "Dedicated br-tag preservation tests across all subsystems"
      min_lines: 100
    - path: "tests/api/test_korean_unicode.py"
      provides: "Korean text integrity tests across all subsystems"
      min_lines: 100
    - path: "tests/api/test_endpoint_coverage.py"
      provides: "Meta-test verifying all endpoints have test coverage"
      min_lines: 80
  key_links:
    - from: "tests/api/test_integration_workflows.py"
      to: "tests/api/helpers/api_client.py"
      via: "Workflow tests use APIClient for multi-step operations"
      pattern: "APIClient"
    - from: "tests/api/test_endpoint_coverage.py"
      to: "server/tools/ldm/routes/"
      via: "Coverage test scans route files and matches against test files"
      pattern: "@router"
---

<objective>
Write cross-subsystem integration tests, dedicated br-tag/Korean tests, and endpoint coverage validation.

Purpose: Individual subsystem tests verify endpoints in isolation. This plan tests real user workflows spanning multiple subsystems, ensures the two most critical data integrity concerns (br-tags and Korean text) are tested exhaustively, and produces a coverage report confirming all 275 endpoints are tested.

Output: 4 test files with 60+ tests completing the test suite.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-comprehensive-api-e2e-testing/25-CONTEXT.md

<skills>
- test-master skill (integration test strategy, coverage analysis)
- test-generator skill (workflow test generation)
- xml-localization skill (br-tag verification patterns)
- fastapi-expert skill (TestClient multi-step workflows)
- api-designer skill (REST workflow validation)
- python-pro skill (parametrize, fixtures, reflection)
</skills>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cross-subsystem integration workflow tests</name>
  <files>tests/api/test_integration_workflows.py</files>
  <read_first>
    tests/api/conftest.py
    tests/api/helpers/api_client.py
    tests/api/helpers/assertions.py
    tests/api/helpers/constants.py
    tests/e2e/test_complete_user_flow.py
  </read_first>
  <action>
    **test_integration_workflows.py** (20+ tests):
    These tests simulate real user workflows spanning multiple subsystems.

    1. **Translator Workflow** (5 tests):
       - test_translator_full_workflow: Create project -> upload XML -> list rows -> edit row -> run QA -> export -> verify edits in export
       - test_translator_tm_workflow: Upload file -> register as TM -> search TM -> get suggestions -> apply suggestion -> verify
       - test_translator_pretranslate_workflow: Create TM -> add entries -> pretranslate new file -> verify rows populated
       - test_translator_merge_workflow: Upload source -> upload target -> merge with fuzzy mode -> verify merged result
       - test_translator_multi_file: Upload 3 files to project -> search across files -> QA across files

    2. **GameDev Workflow** (4 tests):
       - test_gamedev_browse_edit_workflow: Browse gamedata -> select file -> detect columns -> edit cell -> verify saved
       - test_gamedev_codex_workflow: Browse all entity types -> verify codex shows entities from each type
       - test_gamedev_worldmap_workflow: Load worldmap -> verify nodes match faction data -> check route connectivity
       - test_gamedev_cross_reference: Browse characterinfo -> get character -> lookup knowledge key -> verify knowledge entry exists

    3. **AI Workflow** (3 tests):
       - test_ai_suggestion_workflow: Upload file -> select row -> get AI suggestion -> verify suggestion format
       - test_ai_naming_workflow: Browse codex -> get entity -> check naming suggestions -> verify coherence
       - test_ai_context_workflow: Select text -> detect context -> verify context matches entity type

    4. **Data Integrity Workflow** (4 tests):
       - test_upload_edit_download_integrity: Upload -> edit multiple rows -> download -> compare all edits preserved
       - test_brtag_through_all_operations: Upload with br-tags -> edit (keep br-tags) -> QA (no br-tag warning) -> export (br-tags preserved)
       - test_korean_through_all_operations: Upload Korean -> edit Korean -> search Korean -> export -> verify
       - test_concurrent_file_operations: Upload file A and B simultaneously -> verify no cross-contamination

    5. **Error Recovery** (4 tests):
       - test_delete_project_cascades: Delete project -> verify files gone -> verify rows gone -> verify TM unlinked
       - test_file_operations_after_project_delete: Attempt file operations on deleted project -> assert proper errors
       - test_upload_then_delete_then_reupload: Full lifecycle -> verify clean state
       - test_interrupted_merge_recovery: Start merge, interrupt (if possible), verify no corruption

    Use @pytest.mark.integration on all tests. These tests are SLOW (multiple API calls each).
  </action>
  <verify>
    <automated>python3 -c "
import ast
tree = ast.parse(open('tests/api/test_integration_workflows.py').read())
tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
assert len(tests) >= 15, f'Expected >=15 tests, got {len(tests)}'
names = ' '.join(tests)
assert 'workflow' in names, 'Missing workflow tests'
assert 'integrity' in names or 'brtag' in names, 'Missing integrity tests'
print(f'OK: test_integration_workflows.py has {len(tests)} tests')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - >= 15 integration workflow tests
    - Translator, GameDev, AI, and data integrity workflows covered
    - Each workflow test spans 3+ API calls across 2+ subsystems
    - Error recovery/cascade tests included
    - All tests marked with @pytest.mark.integration
  </acceptance_criteria>
  <done>Integration workflow tests simulating 5 major user journeys with 20+ tests</done>
</task>

<task type="auto">
  <name>Task 2: Create dedicated br-tag and Korean Unicode tests</name>
  <files>
    tests/api/test_brtag_roundtrip.py
    tests/api/test_korean_unicode.py
  </files>
  <read_first>
    tests/api/helpers/assertions.py
    tests/api/helpers/api_client.py
    tests/fixtures/mock_uploads/locstr_upload_mixed_brtags.xml
    tests/fixtures/mock_uploads/eu_14col_brtags.xlsx
    tests/fixtures/mock_uploads/eu_14col_korean.xlsx
  </read_first>
  <action>
    **test_brtag_roundtrip.py** (15+ tests, all marked @pytest.mark.brtag):
    Dedicated tests verifying br-tag `<br/>` preservation across every subsystem.

    1. **Upload br-tag preservation** (3 tests):
       - test_brtag_xml_upload: Upload LocStr XML with br-tags, verify rows contain br-tags
       - test_brtag_excel_upload: Upload Excel with br-tag content, verify preserved
       - test_brtag_txt_upload: Upload TXT with br-tag content, verify preserved

    2. **Edit br-tag preservation** (3 tests):
       - test_brtag_row_update: Update row target with br-tag text, verify saved correctly
       - test_brtag_gamedata_edit: Edit gamedata cell with br-tag, verify preserved
       - test_brtag_tm_entry_create: Create TM entry with br-tags, verify stored

    3. **Search br-tag handling** (3 tests):
       - test_brtag_in_search_results: Search for text that contains br-tags, verify returned
       - test_brtag_tm_search: TM search where source/target has br-tags
       - test_brtag_in_qa_results: QA results reference text with br-tags correctly

    4. **Export br-tag preservation** (3 tests):
       - test_brtag_export_xml: Export file with br-tags as XML, verify in output
       - test_brtag_export_excel: Export as Excel, verify br-tags in cells
       - test_brtag_full_roundtrip: Upload -> edit -> merge -> export -> reimport -> compare

    5. **Edge cases** (3 tests):
       - test_brtag_multiple_per_field: Text with 3+ br-tags in one field
       - test_brtag_at_start_and_end: br-tag at beginning/end of text
       - test_brtag_with_korean_text: br-tags mixed with Korean characters

    **test_korean_unicode.py** (15+ tests, all marked @pytest.mark.korean):
    Dedicated tests verifying Korean text integrity across every subsystem.

    1. **Korean Upload** (3 tests):
       - test_korean_xml_upload: Upload Korean StrOrigin XML
       - test_korean_excel_upload: Upload Korean Excel data
       - test_korean_txt_upload: Upload Korean TXT

    2. **Korean Text Types** (4 tests):
       - test_korean_syllables: Full hangul syllables (가-힣)
       - test_korean_jamo: Individual Jamo characters (ㄱ-ㅎ, ㅏ-ㅣ)
       - test_korean_compat_jamo: Compatibility Jamo (ㅎ, ㅏ)
       - test_korean_special_punctuation: Korean punctuation (。, ！, ？, ＝)

    3. **Korean in Operations** (4 tests):
       - test_korean_search: Search Korean text, verify results
       - test_korean_qa_check: QA on Korean translations
       - test_korean_tm_search: TM search with Korean source
       - test_korean_merge: Merge files with Korean content

    4. **Korean Integrity** (4 tests):
       - test_korean_no_mojibake: Upload -> download -> verify no encoding corruption
       - test_korean_mixed_cjk: Korean mixed with Japanese and Chinese characters
       - test_korean_with_latin: Korean mixed with English/Latin text
       - test_korean_long_text: Very long Korean text (1000+ chars)
  </action>
  <verify>
    <automated>python3 -c "
import ast
for fname, min_tests in [('tests/api/test_brtag_roundtrip.py', 12), ('tests/api/test_korean_unicode.py', 12)]:
    tree = ast.parse(open(fname).read())
    tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
    assert len(tests) >= min_tests, f'{fname}: expected >={min_tests} tests, got {len(tests)}'
    print(f'{fname}: {len(tests)} tests')
print('OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - test_brtag_roundtrip.py has >= 12 tests with @pytest.mark.brtag
    - test_korean_unicode.py has >= 12 tests with @pytest.mark.korean
    - br-tag tests cover upload, edit, search, export, and round-trip
    - Korean tests cover syllables, Jamo, compat Jamo, punctuation
    - No mojibake (encoding corruption) in any round-trip test
  </acceptance_criteria>
  <done>Dedicated br-tag and Korean test files with 30+ tests for data integrity</done>
</task>

<task type="auto">
  <name>Task 3: Create endpoint coverage validation test</name>
  <files>tests/api/test_endpoint_coverage.py</files>
  <read_first>
    server/tools/ldm/routes/__init__.py
    server/tools/ldm/router.py
    tests/api/helpers/constants.py
  </read_first>
  <action>
    **test_endpoint_coverage.py** (5+ tests):
    Meta-test that verifies the test suite itself has coverage of all API endpoints.

    1. **Endpoint Discovery** (1 test):
       - test_discover_all_endpoints: Use FastAPI's app.routes or OpenAPI schema to list ALL registered endpoints. Save to variable.

    2. **Coverage Validation** (3 tests):
       - test_all_endpoints_have_tests: For each discovered endpoint path, verify at least one test file references it. Scan all test_*.py files in tests/api/ for endpoint path strings. Report any untested endpoints.
       - test_no_orphan_endpoints: Verify no test references a non-existent endpoint (stale tests)
       - test_endpoint_count_matches_expected: Verify total endpoint count is approximately 275 (within 10% tolerance for added/removed endpoints)

    3. **Coverage Report** (1 test):
       - test_coverage_report: Generate and print a coverage report:
         ```
         ENDPOINT COVERAGE REPORT
         ========================
         Total endpoints: 275
         Tested: 265 (96.4%)
         Untested: 10 (3.6%)

         Untested endpoints:
           - POST /api/v2/admin/cleanup
           - ...

         By subsystem:
           auth: 14/14 (100%)
           files: 17/18 (94%)
           ...
         ```
       This test should ALWAYS PASS (it's a report, not a hard assertion). Use capsys to print.

    Implementation approach:
    - Import FastAPI app, iterate `app.routes` to get all registered routes
    - For each route, check if it has an `endpoint` attribute and extract the path
    - Group by subsystem prefix (/api/ldm/, /api/v2/auth/, /api/v2/admin/, etc.)
    - Scan test files for string matches of each path
    - Generate the coverage report
  </action>
  <verify>
    <automated>python3 -c "
import ast
tree = ast.parse(open('tests/api/test_endpoint_coverage.py').read())
tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
assert len(tests) >= 3, f'Expected >=3 tests, got {len(tests)}'
names = ' '.join(tests)
assert 'coverage' in names or 'endpoint' in names, 'Missing coverage validation tests'
print(f'OK: test_endpoint_coverage.py has {len(tests)} tests')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - >= 3 tests: discover endpoints, validate coverage, generate report
    - Coverage report prints to stdout during test run
    - Orphan endpoint detection (tests for non-existent endpoints)
    - Endpoint count within 10% of expected 275
    - Report groups by subsystem
  </acceptance_criteria>
  <done>Endpoint coverage validation test that reports coverage percentage and identifies gaps</done>
</task>

</tasks>

<verification>
```bash
python3 -m pytest tests/api/test_integration_workflows.py tests/api/test_brtag_roundtrip.py tests/api/test_korean_unicode.py tests/api/test_endpoint_coverage.py --collect-only 2>&1 | tail -5
```
</verification>

<success_criteria>
- 4 test files with 50+ total tests
- Integration workflows span multiple subsystems per test
- br-tag preservation exhaustively tested across every operation
- Korean Unicode integrity verified (syllables, Jamo, mixed CJK)
- Endpoint coverage report identifies any testing gaps
- Complete test suite: 20 test files total across all plans
</success_criteria>

<output>
After completion, create `.planning/phases/25-comprehensive-api-e2e-testing/25-10-SUMMARY.md`
</output>
