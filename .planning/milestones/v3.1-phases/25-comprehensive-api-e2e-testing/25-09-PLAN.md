---
phase: 25-comprehensive-api-e2e-testing
plan: 09
type: execute
wave: 3
depends_on: [25-05, 25-06]
files_modified:
  - tests/api/test_merge.py
  - tests/api/test_export.py
  - tests/api/test_offline.py
  - tests/api/test_admin.py
  - tests/api/test_tools.py
autonomous: true
requirements:
  - TEST-E2E-18
  - TEST-E2E-19
  - TEST-E2E-20
  - TEST-E2E-21
  - TEST-E2E-22

must_haves:
  truths:
    - "Merge endpoints process translator merge (5 match modes) and gamedev merge"
    - "Export endpoints produce downloadable files in XML/Excel/TXT formats"
    - "Offline endpoints manage local storage, subscriptions, sync, and trash"
    - "Admin endpoints return statistics, health metrics, and server information"
    - "Tools endpoints (QuickSearch, KR-Similar, XLSTransfer) respond correctly"
  artifacts:
    - path: "tests/api/test_merge.py"
      provides: "Merge operation tests (3 endpoints)"
      min_lines: 100
    - path: "tests/api/test_export.py"
      provides: "Export pipeline tests"
      min_lines: 80
    - path: "tests/api/test_offline.py"
      provides: "Offline mode tests (22 endpoints)"
      min_lines: 200
    - path: "tests/api/test_admin.py"
      provides: "Admin/stats tests (44 endpoints)"
      min_lines: 200
    - path: "tests/api/test_tools.py"
      provides: "External tools tests (7+ endpoints)"
      min_lines: 80
  key_links:
    - from: "tests/api/test_merge.py"
      to: "server/tools/ldm/routes/merge.py"
      via: "Merge endpoints with match mode parameters"
      pattern: "merge"
    - from: "tests/api/test_offline.py"
      to: "server/tools/ldm/routes/sync.py"
      via: "Sync and offline storage endpoints"
      pattern: "sync|offline"
---

<objective>
Write pytest API tests for Merge, Export, Offline, Admin, and Tools subsystems.

Purpose: These are the remaining subsystems completing full 275-endpoint coverage. Merge is critical for translator workflow. Offline mode enables disconnected work. Admin provides monitoring. Tools wrap external scripts.

Output: 5 test files covering ~93 endpoints with 90+ test cases.
</objective>

<execution_context>
@/home/<USERNAME>/.claude/get-shit-done/workflows/execute-plan.md
@/home/<USERNAME>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-comprehensive-api-e2e-testing/25-CONTEXT.md

<skills>
- test-master skill (test strategy, coverage analysis)
- test-generator skill (scaffold from endpoint schemas)
- fastapi-expert skill (TestClient, file response handling)
- api-designer skill (REST conventions, bulk operations)
- python-pro skill (pytest fixtures, tmp_path)
- security-reviewer skill (admin endpoint authorization)
- sql-expert skill (verify offline DB state)
</skills>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create merge and export tests</name>
  <files>
    tests/api/test_merge.py
    tests/api/test_export.py
  </files>
  <read_first>
    server/tools/ldm/routes/merge.py
    server/tools/ldm/routes/files.py
    server/tools/ldm/schemas/file.py
    tests/fixtures/xml/merge_source.xml
    tests/fixtures/xml/merge_target.xml
    tests/api/conftest.py
    tests/api/helpers/api_client.py
  </read_first>
  <action>
    **test_merge.py** (15+ tests):
    Read merge.py for all merge endpoints.

    1. **Translator Merge** (7 tests):
       - test_translator_merge_exact: Merge with exact match mode
       - test_translator_merge_fuzzy: Merge with fuzzy match mode
       - test_translator_merge_hash: Merge with hash match mode
       - test_translator_merge_position: Merge with position-based mode
       - test_translator_merge_all_modes: Merge using all 5 match modes combined
       - test_translator_merge_preserves_brtags: br-tags preserved through merge
       - test_translator_merge_korean: Korean text correctly merged

    2. **GameDev Merge** (4 tests):
       - test_gamedev_merge_basic: GameDev position-based merge
       - test_gamedev_merge_preserves_attributes: All XML attributes preserved
       - test_gamedev_merge_with_new_entities: Merge handles new entities
       - test_gamedev_merge_brtag_preservation: br-tags in gamedev data preserved

    3. **File Merge with Original** (4 tests):
       - test_merge_with_original: Merge translation file with original
       - test_merge_result_format: Merged result has expected format
       - test_merge_nonexistent_source: assert error
       - test_merge_empty_source: Merge with empty source file

    **test_export.py** (12+ tests):
    Export endpoints may be part of files.py (download/convert) or separate.

    1. **Export Formats** (5 tests):
       - test_export_as_xml: Export file as XML format
       - test_export_as_excel: Export as Excel (.xlsx)
       - test_export_as_txt: Export as tab-delimited text
       - test_export_preserves_brtags: Exported content preserves br-tags
       - test_export_preserves_korean: Exported content preserves Korean Unicode

    2. **Export Options** (4 tests):
       - test_export_with_filters: Export only specific rows/status
       - test_export_selected_columns: Export subset of columns (if supported)
       - test_export_with_metadata: Exported file includes metadata
       - test_export_empty_file: Export file with no rows

    3. **Round-trip Export** (3 tests):
       - test_export_import_roundtrip_xml: Export XML → reimport → compare
       - test_export_import_roundtrip_excel: Export Excel → reimport → compare
       - test_export_brtag_roundtrip: Full br-tag round-trip through export/import
  </action>
  <verify>
    <automated>python3 -c "
import ast
for fname, min_tests in [('tests/api/test_merge.py', 12), ('tests/api/test_export.py', 10)]:
    tree = ast.parse(open(fname).read())
    tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
    assert len(tests) >= min_tests, f'{fname}: expected >={min_tests} tests, got {len(tests)}'
    print(f'{fname}: {len(tests)} tests')
print('OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - test_merge.py has >= 12 tests covering translator (5 modes), gamedev, and file merge
    - test_export.py has >= 10 tests covering XML/Excel/TXT export and round-trips
    - br-tag preservation tested in both merge and export
    - Korean text preservation tested
    - All 5 translator merge match modes tested
  </acceptance_criteria>
  <done>Merge and export test files with 25+ tests covering all merge modes and export formats</done>
</task>

<task type="auto">
  <name>Task 2: Create offline mode tests</name>
  <files>tests/api/test_offline.py</files>
  <read_first>
    server/tools/ldm/routes/sync.py
    server/tools/ldm/routes/trash.py
    server/tools/ldm/routes/maintenance.py
    server/tools/ldm/schemas/sync.py
    tests/api/conftest.py
    tests/api/helpers/api_client.py
  </read_first>
  <action>
    **test_offline.py** (25+ tests):
    Read sync.py (25 endpoints), trash.py (4 endpoints), and maintenance.py (5 endpoints).

    1. **Offline Status** (4 tests):
       - test_offline_status: GET offline mode status
       - test_offline_storage_info: Local storage size/usage
       - test_offline_mode_detection: Detect current mode (online/offline)
       - test_offline_capabilities: What's available offline

    2. **Offline Files** (5 tests):
       - test_offline_file_list: List locally cached files
       - test_offline_file_download: Download file for offline use
       - test_offline_file_save: Save changes locally
       - test_offline_file_status: File sync status (synced/pending/conflict)
       - test_offline_file_delete: Remove from local cache

    3. **Subscriptions** (4 tests):
       - test_subscribe_file: Subscribe to file for offline access
       - test_unsubscribe_file: Unsubscribe from offline file
       - test_list_subscriptions: GET all offline subscriptions
       - test_subscription_auto_sync: Subscription triggers download

    4. **Sync** (5 tests):
       - test_sync_push: Push local changes to server
       - test_sync_pull: Pull server changes to local
       - test_sync_status: GET sync queue status
       - test_sync_conflict_detection: Detect conflicting edits
       - test_sync_resolution: Resolve sync conflict

    5. **Local Storage CRUD** (4 tests):
       - test_local_create: Create local-only entry
       - test_local_read: Read from local storage
       - test_local_update: Update local entry
       - test_local_delete: Delete local entry

    6. **Trash** (4 tests):
       - test_trash_list: List trashed items
       - test_trash_move_to_trash: Soft-delete to trash
       - test_trash_restore: Restore from trash
       - test_trash_permanent_delete: Permanently remove

    7. **Maintenance** (3 tests):
       - test_maintenance_cleanup: Trigger cleanup
       - test_maintenance_reindex: Trigger reindex
       - test_maintenance_health: Maintenance health check

    Note: Many offline endpoints may return mock/placeholder responses if offline mode is not fully active in test environment. Tests should accept both full responses and graceful 501/503 responses.
  </action>
  <verify>
    <automated>python3 -c "
import ast
tree = ast.parse(open('tests/api/test_offline.py').read())
tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
assert len(tests) >= 20, f'Expected >=20 tests, got {len(tests)}'
names = ' '.join(tests)
assert 'sync' in names, 'Missing sync tests'
assert 'offline' in names, 'Missing offline tests'
assert 'trash' in names, 'Missing trash tests'
print(f'OK: test_offline.py has {len(tests)} tests')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - >= 20 tests covering status, files, subscriptions, sync, storage, trash, maintenance
    - Tests handle graceful degradation (501/503 for unavailable features)
    - Sync conflict detection and resolution tested
    - Trash lifecycle: move to trash -> restore -> permanent delete
  </acceptance_criteria>
  <done>Offline test file with 25+ tests covering all 22 offline endpoints + trash + maintenance</done>
</task>

<task type="auto">
  <name>Task 3: Create admin and tools tests</name>
  <files>
    tests/api/test_admin.py
    tests/api/test_tools.py
  </files>
  <read_first>
    server/tools/ldm/routes/platforms.py
    server/tools/ldm/routes/settings.py
    server/tools/ldm/routes/capabilities.py
    tests/api/helpers/api_client.py
    tests/api/helpers/assertions.py
  </read_first>
  <action>
    **test_admin.py** (25+ tests):
    Admin endpoints are likely under /api/v2/admin/ prefix. Read route files to find them.

    1. **Overview/Stats** (6 tests):
       - test_admin_overview: GET admin dashboard overview
       - test_admin_stats_daily: GET daily statistics
       - test_admin_stats_weekly: GET weekly statistics
       - test_admin_stats_monthly: GET monthly statistics
       - test_admin_user_count: Total user count
       - test_admin_file_count: Total file count

    2. **Performance** (4 tests):
       - test_admin_performance_metrics: GET performance data
       - test_admin_response_times: API response time stats
       - test_admin_db_health: Database health check
       - test_admin_error_rates: Error rate statistics

    3. **Server Info** (4 tests):
       - test_admin_server_info: Server version, uptime
       - test_admin_server_logs: GET recent server logs (if endpoint exists)
       - test_admin_telemetry: Telemetry data
       - test_admin_rankings: User activity rankings

    4. **Platform Management** (5 tests):
       - test_list_platforms: GET available platforms
       - test_create_platform: POST new platform
       - test_update_platform: PUT platform settings
       - test_delete_platform: DELETE platform
       - test_assign_platform_to_project: Link platform to project

    5. **Settings** (3 tests):
       - test_get_settings: GET application settings
       - test_update_settings: PUT/PATCH settings
       - test_settings_response_schema: Validate settings fields

    6. **Authorization** (3 tests):
       - test_admin_requires_auth: Admin endpoints need auth token
       - test_admin_requires_admin_role: Non-admin user gets 403
       - test_admin_endpoints_no_500: No endpoint returns 500

    **test_tools.py** (12+ tests):
    External tools: QuickSearch, KR-Similar, XLSTransfer.

    1. **QuickSearch** (4 tests):
       - test_quicksearch_health: GET QuickSearch health/status
       - test_quicksearch_dictionaries: GET available dictionaries
       - test_quicksearch_search: POST search query, assert results
       - test_quicksearch_multiline: POST multiline search

    2. **KR-Similar** (3 tests):
       - test_kr_similar_search: POST Korean text, get similar strings
       - test_kr_similar_scores: Results have similarity scores
       - test_kr_similar_empty_result: Query with no matches

    3. **XLSTransfer** (3 tests):
       - test_xlstransfer_status: Tool availability check
       - test_xlstransfer_convert: Convert between formats (if endpoint exists)
       - test_xlstransfer_health: Health endpoint

    4. **Tool Availability** (2 tests):
       - test_tools_graceful_unavailable: Tools return 503 not 500 when unavailable
       - test_tools_response_format: All tools use consistent response format
  </action>
  <verify>
    <automated>python3 -c "
import ast
for fname, min_tests in [('tests/api/test_admin.py', 20), ('tests/api/test_tools.py', 10)]:
    tree = ast.parse(open(fname).read())
    tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
    assert len(tests) >= min_tests, f'{fname}: expected >={min_tests} tests, got {len(tests)}'
    print(f'{fname}: {len(tests)} tests')
print('OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - test_admin.py has >= 20 tests covering stats, performance, platforms, settings, auth
    - test_tools.py has >= 10 tests covering QuickSearch, KR-Similar, XLSTransfer
    - Admin endpoints verified to require authentication
    - Tools handle unavailability gracefully (503, not 500)
    - No admin endpoint returns 500 Internal Server Error
  </acceptance_criteria>
  <done>Admin and tools test files with 35+ tests covering all admin stats and external tools</done>
</task>

</tasks>

<verification>
```bash
python3 -m pytest tests/api/test_merge.py tests/api/test_export.py tests/api/test_offline.py tests/api/test_admin.py tests/api/test_tools.py --collect-only 2>&1 | tail -5
```
</verification>

<success_criteria>
- 5 test files with 80+ total tests
- All 5 translator merge modes tested
- Export round-trip with br-tag preservation
- Offline sync/subscribe/trash lifecycle covered
- Admin authorization enforced
- External tools handle unavailability gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/25-comprehensive-api-e2e-testing/25-09-SUMMARY.md`
</output>
