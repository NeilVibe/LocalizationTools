---
phase: 25-comprehensive-api-e2e-testing
plan: 06
type: execute
wave: 2
depends_on: [25-03]
files_modified:
  - tests/api/test_rows.py
  - tests/api/test_tm_crud.py
  - tests/api/test_tm_search.py
  - tests/api/test_tm_entries.py
autonomous: true
requirements:
  - TEST-E2E-10
  - TEST-E2E-11

must_haves:
  truths:
    - "Row listing supports pagination, search, and filter parameters"
    - "Row target update persists and is retrievable"
    - "TM CRUD creates, lists, gets, updates, deletes translation memories"
    - "TM search returns results with similarity scores sorted descending"
    - "TM entry CRUD manages individual translation memory entries"
    - "TM 5-tier cascade search produces results at appropriate tiers"
  artifacts:
    - path: "tests/api/test_rows.py"
      provides: "Row endpoint tests (5 endpoints)"
      min_lines: 120
    - path: "tests/api/test_tm_crud.py"
      provides: "TM lifecycle tests (5 CRUD + 4 index + 5 assignment endpoints)"
      min_lines: 200
    - path: "tests/api/test_tm_search.py"
      provides: "TM search tests (3 search modes + suggest + leverage)"
      min_lines: 150
    - path: "tests/api/test_tm_entries.py"
      provides: "TM entry tests (6 entry CRUD endpoints)"
      min_lines: 120
  key_links:
    - from: "tests/api/test_tm_search.py"
      to: "server/tools/ldm/routes/tm_search.py"
      via: "Tests hit pattern, exact, and semantic search endpoints"
      pattern: "tm.*search"
    - from: "tests/api/test_rows.py"
      to: "server/tools/ldm/routes/rows.py"
      via: "Row listing and update endpoints"
      pattern: "rows"
---

<objective>
Write comprehensive pytest API tests for Rows and Translation Memory (CRUD, search, entries, indexes, assignment).

Purpose: Rows are the main data unit users edit. TM is the core CAT tool feature — search quality directly affects translator productivity. These must be thoroughly tested including the 5-tier cascade search.

Output: 4 test files covering ~42 endpoints with 80+ test cases.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-comprehensive-api-e2e-testing/25-CONTEXT.md

<skills>
- test-master skill (test strategy, coverage)
- test-generator skill (scaffold from endpoint schemas)
- fastapi-expert skill (TestClient, response validation)
- sql-expert skill (verify DB state after row updates)
- api-designer skill (REST conventions, pagination)
- python-pro skill (pytest fixtures, parametrize)
</skills>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create row endpoint tests</name>
  <files>tests/api/test_rows.py</files>
  <read_first>
    server/tools/ldm/routes/rows.py
    server/tools/ldm/schemas/row.py
    tests/api/conftest.py
    tests/api/helpers/api_client.py
    tests/api/helpers/assertions.py
  </read_first>
  <action>
    **test_rows.py** (20+ tests):
    Read rows.py to find exact endpoints (expected: list, get, update, qa-check, grammar-check).

    1. **Listing** (6 tests):
       - test_list_rows_basic: GET rows for a file, assert list response with row objects
       - test_list_rows_pagination: Use offset/limit params, verify page sizes
       - test_list_rows_search: Search by text content, verify matching results
       - test_list_rows_filter_by_status: Filter rows by translation status
       - test_list_rows_empty_file: List rows for file with no content
       - test_list_rows_nonexistent_file: assert 404

    2. **Update** (5 tests):
       - test_update_row_target: PUT/PATCH row with new Str value, assert saved
       - test_update_row_preserves_brtags: Update with br-tag content, verify preserved
       - test_update_row_korean_text: Update with Korean text, verify Unicode
       - test_update_row_empty_target: Set target to empty string
       - test_update_nonexistent_row: assert 404

    3. **Per-Row QA/Grammar** (5 tests):
       - test_row_qa_check: Trigger QA on single row, assert result
       - test_row_grammar_check: Trigger grammar check on row
       - test_row_qa_with_known_issue: Row with intentional QA violation
       - test_row_qa_clean: Row with no QA issues
       - test_row_grammar_korean: Grammar check on Korean text

    4. **Schema Validation** (4 tests):
       - test_row_response_schema: Verify all expected fields (id, string_id, str_origin, str, desc, status)
       - test_row_contains_string_id: StringID field present and non-empty
       - test_row_contains_str_origin: StrOrigin field present
       - test_row_status_values: Status is one of known values

    Tests depend on uploaded_xml_file_id fixture from conftest. If that fixture is not available, create a test-local fixture that uploads a test XML.
  </action>
  <verify>
    <automated>python3 -c "
import ast
tree = ast.parse(open('tests/api/test_rows.py').read())
tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
assert len(tests) >= 15, f'Expected >=15 tests, got {len(tests)}'
names = ' '.join(tests)
assert 'list' in names and 'update' in names, 'Missing list or update tests'
print(f'OK: test_rows.py has {len(tests)} tests')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - >= 15 test functions covering list, update, QA, grammar, schema
    - Pagination tests verify offset/limit behavior
    - br-tag and Korean text preservation tests included
    - Per-row QA and grammar endpoints tested
    - Row response schema validated against expected fields
  </acceptance_criteria>
  <done>Row test file with 20+ tests covering all 5 row endpoints plus QA/grammar per-row</done>
</task>

<task type="auto">
  <name>Task 2: Create TM CRUD and index tests</name>
  <files>
    tests/api/test_tm_crud.py
  </files>
  <read_first>
    server/tools/ldm/routes/tm_crud.py
    server/tools/ldm/routes/tm_indexes.py
    server/tools/ldm/routes/tm_assignment.py
    server/tools/ldm/routes/tm_linking.py
    server/tools/ldm/schemas/tm.py
    tests/api/conftest.py
    tests/api/helpers/api_client.py
  </read_first>
  <action>
    **test_tm_crud.py** (25+ tests):
    Read tm_crud.py, tm_indexes.py, tm_assignment.py, tm_linking.py for all endpoints.

    1. **TM Lifecycle** (7 tests):
       - test_create_tm: POST create TM, assert ID + name returned
       - test_list_tms: GET all TMs, assert list
       - test_get_tm: GET by ID, assert all fields
       - test_update_tm: PUT/PATCH TM name/settings
       - test_delete_tm: DELETE TM, verify gone
       - test_create_tm_duplicate_name: same name, assert error
       - test_get_nonexistent_tm: assert 404

    2. **TM Indexes** (5 tests):
       - test_build_tm_indexes: POST trigger index build, assert accepted/success
       - test_get_tm_index_status: GET index status
       - test_rebuild_tm_indexes: Rebuild existing indexes
       - test_tm_index_for_empty_tm: Build index for TM with no entries
       - test_get_tm_stats: TM statistics (entry count, last updated)

    3. **TM Assignment** (5 tests):
       - test_assign_tm_to_project: Link TM to project
       - test_list_project_tms: GET TMs assigned to project
       - test_unassign_tm: Remove TM from project
       - test_assign_multiple_tms: Multiple TMs on one project
       - test_assign_tm_priority: TM priority/order if supported

    4. **TM Linking** (4 tests):
       - test_link_file_to_tm: Link uploaded file as TM source
       - test_get_tm_links: List linked files
       - test_unlink_file: Remove link
       - test_link_nonexistent_file: assert error

    5. **TM Upload** (4 tests):
       - test_upload_tm_from_xml: Upload XML as TM entries
       - test_upload_tm_from_excel: Upload Excel as TM entries
       - test_tm_entry_count_after_upload: Verify entry count matches uploaded rows
       - test_upload_tm_preserves_brtags: br-tags in TM entries preserved

    Use test_tm_id fixture from conftest where available. Create test-local fixtures for tests that need specific TM configurations.
  </action>
  <verify>
    <automated>python3 -c "
import ast
tree = ast.parse(open('tests/api/test_tm_crud.py').read())
tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
assert len(tests) >= 20, f'Expected >=20 tests, got {len(tests)}'
print(f'OK: test_tm_crud.py has {len(tests)} tests')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - >= 20 tests covering CRUD, indexes, assignment, linking, upload
    - TM lifecycle: create → populate → index → assign → search → delete
    - br-tag preservation in TM entries tested
    - Error cases for nonexistent TMs and duplicate names
  </acceptance_criteria>
  <done>TM CRUD test file with 25+ tests covering full TM lifecycle</done>
</task>

<task type="auto">
  <name>Task 3: Create TM search and entry tests</name>
  <files>
    tests/api/test_tm_search.py
    tests/api/test_tm_entries.py
  </files>
  <read_first>
    server/tools/ldm/routes/tm_search.py
    server/tools/ldm/routes/tm_entries.py
    server/tools/ldm/routes/tm_leverage.py
    server/tools/ldm/routes/pretranslate.py
    server/tools/ldm/routes/semantic_search.py
    tests/api/helpers/assertions.py
  </read_first>
  <action>
    **test_tm_search.py** (18+ tests):
    Read tm_search.py for search endpoints.

    1. **Search Modes** (6 tests):
       - test_tm_search_pattern: Pattern-based search, assert results with scores
       - test_tm_search_exact: Exact string match search
       - test_tm_search_semantic: Semantic/vector search (may need FAISS index)
       - test_tm_search_returns_scores: Verify similarity scores in 0.0-1.0 range
       - test_tm_search_sorted_descending: Results sorted by score desc
       - test_tm_search_empty_query: Empty search query behavior

    2. **Suggest/Leverage** (5 tests):
       - test_tm_suggest_for_text: Get TM suggestions for input text
       - test_tm_suggest_similarity_threshold: Only results above threshold
       - test_tm_leverage_file: Leverage TM for entire file
       - test_tm_suggest_korean_text: Suggestions for Korean input
       - test_tm_suggest_with_brtags: Input text with br-tags

    3. **Pretranslate** (3 tests):
       - test_pretranslate_file: Auto-translate file using TM
       - test_pretranslate_threshold: Only apply above similarity threshold
       - test_pretranslate_preserves_existing: Don't overwrite existing translations

    4. **5-Tier Cascade** (4 tests):
       - test_cascade_hash_exact: Test hash exact match tier
       - test_cascade_faiss_whole: Test FAISS whole-sentence tier
       - test_cascade_hash_line: Test hash line-level tier
       - test_cascade_fallback: Test fallback when no match found

    **test_tm_entries.py** (15+ tests):
    Read tm_entries.py for entry CRUD endpoints.

    1. **Entry CRUD** (6 tests):
       - test_create_tm_entry: POST entry with source/target text
       - test_list_tm_entries: GET entries with pagination
       - test_get_tm_entry: GET by ID
       - test_update_tm_entry: PUT/PATCH entry
       - test_delete_tm_entry: DELETE entry
       - test_create_entry_with_brtags: Entry with br-tag content

    2. **Batch Operations** (4 tests):
       - test_bulk_add_entries: Add multiple entries at once
       - test_entry_count_accuracy: Count matches actual entries
       - test_entries_after_file_link: Entries populated from linked file
       - test_export_tm_entries: Export entries (if endpoint exists)

    3. **Search on Entries** (3 tests):
       - test_search_entries_by_source: Filter by source text
       - test_search_entries_by_target: Filter by target text
       - test_entries_contain_metadata: Entries have timestamps, IDs

    4. **Sync** (2 tests):
       - test_sync_tm: Trigger TM sync
       - test_tm_sync_status: Check sync status
  </action>
  <verify>
    <automated>python3 -c "
import ast
for fname, min_tests in [('tests/api/test_tm_search.py', 15), ('tests/api/test_tm_entries.py', 12)]:
    tree = ast.parse(open(fname).read())
    tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
    assert len(tests) >= min_tests, f'{fname}: expected >={min_tests} tests, got {len(tests)}'
    print(f'{fname}: {len(tests)} tests')
print('OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - test_tm_search.py has >= 15 tests covering all search modes
    - test_tm_entries.py has >= 12 tests covering entry CRUD + batch + search
    - Similarity scores validated as 0.0-1.0 range
    - Sort order validated as descending
    - br-tag and Korean preservation in TM operations tested
    - 5-tier cascade search tested
  </acceptance_criteria>
  <done>TM search and entry test files with 30+ tests covering search modes, cascade, and entry management</done>
</task>

</tasks>

<verification>
```bash
python3 -m pytest tests/api/test_rows.py tests/api/test_tm_crud.py tests/api/test_tm_search.py tests/api/test_tm_entries.py --collect-only 2>&1 | tail -5
```
</verification>

<success_criteria>
- 4 test files with 80+ total tests
- Row listing, update, and per-row QA/grammar covered
- Full TM lifecycle: create, populate, index, search, suggest, delete
- TM 5-tier cascade search tested
- Similarity scores validated (range and sort order)
- br-tag and Korean preservation in all TM operations
</success_criteria>

<output>
After completion, create `.planning/phases/25-comprehensive-api-e2e-testing/25-06-SUMMARY.md`
</output>
