---
phase: 25-comprehensive-api-e2e-testing
plan: 05
type: execute
wave: 2
depends_on: [25-02, 25-03]
files_modified:
  - tests/api/test_health.py
  - tests/api/test_auth.py
  - tests/api/test_projects.py
  - tests/api/test_folders.py
  - tests/api/test_files.py
autonomous: true
requirements:
  - TEST-E2E-07
  - TEST-E2E-08
  - TEST-E2E-09

must_haves:
  truths:
    - "Health endpoint returns 200 with status field"
    - "Auth login returns JWT token, register creates user, invalid creds return 401"
    - "Project CRUD works end-to-end: create, list, get, update, delete"
    - "Folder CRUD works: create, nest, move, delete with cascade"
    - "File upload accepts XML/Excel/TXT, download preserves content, delete removes file"
  artifacts:
    - path: "tests/api/test_health.py"
      provides: "Health endpoint tests"
      min_lines: 20
    - path: "tests/api/test_auth.py"
      provides: "Auth subsystem tests (14 endpoints)"
      min_lines: 150
    - path: "tests/api/test_projects.py"
      provides: "Project CRUD tests (9 endpoints)"
      min_lines: 150
    - path: "tests/api/test_folders.py"
      provides: "Folder CRUD tests (8 endpoints)"
      min_lines: 120
    - path: "tests/api/test_files.py"
      provides: "File upload/download tests (18 endpoints)"
      min_lines: 250
  key_links:
    - from: "tests/api/test_files.py"
      to: "tests/fixtures/mock_uploads/"
      via: "Upload tests use fixture files from mock_uploads directory"
      pattern: "mock_uploads"
    - from: "tests/api/test_auth.py"
      to: "server/tools/ldm/routes/files.py"
      via: "Auth headers used for protected file endpoints"
      pattern: "auth_headers"
---

<objective>
Write comprehensive pytest API tests for Health, Auth, Projects, Folders, and Files subsystems.

Purpose: These are the foundational subsystems — auth provides tokens, projects/folders organize data, files are the primary content. Testing these first ensures the base is solid before testing dependent features (rows, TM, search, etc.).

Output: 5 test files covering ~54 endpoints with 100+ individual test cases.
</objective>

<execution_context>
@/home/<USERNAME>/.claude/get-shit-done/workflows/execute-plan.md
@/home/<USERNAME>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-comprehensive-api-e2e-testing/25-CONTEXT.md

<skills>
- test-master skill (test strategy, coverage, mocking)
- test-generator skill (scaffold tests from endpoint schemas)
- fastapi-expert skill (TestClient patterns, response validation)
- security-reviewer skill (auth endpoint security: JWT, password handling)
- api-designer skill (REST conventions, error codes)
- python-pro skill (pytest best practices, parametrize)
</skills>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create health and auth test files</name>
  <files>
    tests/api/test_health.py
    tests/api/test_auth.py
  </files>
  <read_first>
    tests/api/conftest.py
    tests/api/helpers/api_client.py
    tests/api/helpers/assertions.py
    tests/api/helpers/constants.py
    server/tools/ldm/routes/health.py
    server/tools/ldm/routes/capabilities.py
  </read_first>
  <action>
    **test_health.py** (3-5 tests):
    - test_health_endpoint_returns_200: GET /health, assert 200 + "status" field
    - test_health_response_schema: Validate all expected fields present
    - test_capabilities_endpoint: GET /api/ldm/capabilities, assert feature flags returned

    **test_auth.py** (20+ tests):
    Auth routes are at /api/v2/auth/ or /api/auth/ — check the actual route prefix.

    Test categories:
    1. **Login** (5 tests):
       - test_login_valid_credentials: POST with admin/admin123, assert 200 + access_token
       - test_login_invalid_password: wrong password, assert 401
       - test_login_nonexistent_user: unknown username, assert 401
       - test_login_missing_fields: empty body, assert 422
       - test_login_form_data: login via form data (not JSON), assert 200

    2. **Token Validation** (3 tests):
       - test_protected_endpoint_with_valid_token: access /api/ldm/projects with Bearer token
       - test_protected_endpoint_without_token: access without header, assert 401/403
       - test_protected_endpoint_with_expired_token: use garbage token, assert 401/403

    3. **User Management** (5+ tests):
       - test_register_new_user: POST register, assert 201/200
       - test_register_duplicate_username: same username twice, assert 409/400
       - test_list_users: GET users list, assert array response
       - test_get_user_profile: GET current user
       - test_change_password: PUT password change

    4. **Session/Capability** (3+ tests):
       - test_capabilities_list: GET capabilities
       - test_user_permissions: Check user role/permissions

    Use pytest.mark.auth on all tests. Use parametrize for invalid credential variations.
    Import assertions from helpers/assertions.py. Use api fixture from conftest where available.
  </action>
  <verify>
    <automated>python3 -c "
import ast
for fname, min_tests in [('tests/api/test_health.py', 3), ('tests/api/test_auth.py', 15)]:
    tree = ast.parse(open(fname).read())
    tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
    assert len(tests) >= min_tests, f'{fname}: expected >={min_tests} tests, got {len(tests)}'
    print(f'{fname}: {len(tests)} tests')
print('OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - test_health.py has >= 3 test functions
    - test_auth.py has >= 15 test functions covering login, token, user management
    - All tests use pytest.mark decorators
    - Invalid credential tests use parametrize
    - Imports from helpers/ (assertions, constants)
  </acceptance_criteria>
  <done>Health and auth test files with 20+ tests covering all auth scenarios</done>
</task>

<task type="auto">
  <name>Task 2: Create project and folder test files</name>
  <files>
    tests/api/test_projects.py
    tests/api/test_folders.py
  </files>
  <read_first>
    server/tools/ldm/routes/projects.py
    server/tools/ldm/routes/folders.py
    server/tools/ldm/routes/platforms.py
    server/tools/ldm/schemas/project.py
    server/tools/ldm/schemas/folder.py
    tests/api/helpers/api_client.py
    tests/api/helpers/assertions.py
  </read_first>
  <action>
    **test_projects.py** (15+ tests):
    Read projects.py route file to find exact endpoint paths and methods.

    1. **CRUD** (7 tests):
       - test_create_project: POST, assert 201/200 + project ID returned
       - test_list_projects: GET, assert list response with at least 1 project
       - test_get_project: GET by ID, assert project fields
       - test_update_project: PUT/PATCH, change name, assert updated
       - test_delete_project: DELETE, assert success, then GET returns 404
       - test_create_project_duplicate_name: same name, assert appropriate error
       - test_get_nonexistent_project: GET invalid ID, assert 404

    2. **Platform/Tree** (4+ tests):
       - test_project_tree: GET tree structure
       - test_assign_platform: POST platform assignment (if endpoint exists)
       - test_project_stats: GET project statistics
       - test_project_with_files_count: Verify file count in project response

    3. **Edge Cases** (3+ tests):
       - test_create_project_empty_name: assert 422
       - test_create_project_long_name: 200+ char name
       - test_delete_project_with_files: cascade behavior

    **test_folders.py** (12+ tests):
    Read folders.py for exact endpoints.

    1. **CRUD** (5 tests):
       - test_create_folder: POST with project_id + name
       - test_list_folders: GET folders in project
       - test_get_folder: GET by ID
       - test_rename_folder: PUT/PATCH
       - test_delete_folder: DELETE

    2. **Nesting/Move** (4 tests):
       - test_create_nested_folder: folder inside folder
       - test_move_folder: move to different parent
       - test_folder_tree: GET full tree structure
       - test_cross_project_move: move folder between projects (if supported)

    3. **Edge Cases** (3 tests):
       - test_create_folder_without_project: assert error
       - test_delete_folder_with_files: cascade
       - test_get_nonexistent_folder: assert 404

    Use test_project_id fixture from conftest for all tests needing a project.
  </action>
  <verify>
    <automated>python3 -c "
import ast
for fname, min_tests in [('tests/api/test_projects.py', 12), ('tests/api/test_folders.py', 10)]:
    tree = ast.parse(open(fname).read())
    tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
    assert len(tests) >= min_tests, f'{fname}: expected >={min_tests} tests, got {len(tests)}'
    print(f'{fname}: {len(tests)} tests')
print('OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - test_projects.py has >= 12 tests covering CRUD + tree + edge cases
    - test_folders.py has >= 10 tests covering CRUD + nesting + edge cases
    - Tests use test_project_id fixture from conftest
    - Error cases test 404, 422, and cascade behaviors
  </acceptance_criteria>
  <done>Project and folder test files with 25+ tests covering all CRUD and tree operations</done>
</task>

<task type="auto">
  <name>Task 3: Create file upload/download test file</name>
  <files>tests/api/test_files.py</files>
  <read_first>
    server/tools/ldm/routes/files.py
    server/tools/ldm/schemas/file.py
    server/tools/ldm/file_handlers/xml_handler.py
    tests/fixtures/mock_uploads/locstr_upload_characterinfo.xml
    tests/fixtures/mock_uploads/eu_14col_sample.xlsx
    tests/api/helpers/api_client.py
    tests/api/helpers/assertions.py
  </read_first>
  <action>
    **test_files.py** (30+ tests) — This is the largest test file. Files are the core entity.

    Read files.py route to find all 18 endpoints.

    1. **Upload** (8 tests):
       - test_upload_xml_locstr: Upload LocStr XML, assert file created with correct row count
       - test_upload_excel_eu14col: Upload EU 14-col Excel, assert parsed correctly
       - test_upload_txt_tabdelimited: Upload TXT file, assert rows parsed
       - test_upload_tsv: Upload TSV file
       - test_upload_xml_with_brtags: Upload XML with br-tags, verify tags preserved in DB
       - test_upload_korean_text: Upload Korean content, verify Unicode preserved
       - test_upload_invalid_format: Upload .jpg, assert 400/415
       - test_upload_empty_file: Upload empty XML, assert appropriate error

    2. **List/Get** (5 tests):
       - test_list_files_in_project: GET files for project, assert list
       - test_get_file_details: GET file by ID, assert all schema fields
       - test_get_file_stats: File statistics (row count, language, etc.)
       - test_list_files_pagination: If pagination supported
       - test_get_nonexistent_file: assert 404

    3. **Download/Convert** (5 tests):
       - test_download_file_xml: Download as XML, verify content
       - test_download_file_excel: Download as Excel (if convert endpoint exists)
       - test_download_preserves_brtags: Upload XML with br-tags, download, verify preserved
       - test_download_preserves_korean: Upload Korean, download, verify Unicode intact
       - test_download_nonexistent_file: assert 404

    4. **Operations** (6 tests):
       - test_copy_file: Copy file to another project/folder
       - test_move_file: Move file
       - test_rename_file: Rename file
       - test_delete_file: Delete, verify 404 after
       - test_register_file_as_tm: Register uploaded file as TM source
       - test_file_convert_format: Convert between formats if endpoint exists

    5. **Round-trip** (4 tests, mark as @pytest.mark.brtag and @pytest.mark.korean):
       - test_xml_upload_download_roundtrip: Upload → download → compare content
       - test_brtag_roundtrip_preservation: Upload br-tag content → download → verify exact match
       - test_korean_roundtrip_preservation: Korean text through full cycle
       - test_excel_roundtrip: Upload Excel → download → verify

    Use mock_uploads_path fixture to locate fixture files. Create temp files for upload where needed.
  </action>
  <verify>
    <automated>python3 -c "
import ast
tree = ast.parse(open('tests/api/test_files.py').read())
tests = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]
assert len(tests) >= 25, f'Expected >=25 tests, got {len(tests)}'
# Check for key test categories
names = ' '.join(tests)
assert 'upload' in names, 'Missing upload tests'
assert 'download' in names, 'Missing download tests'
assert 'roundtrip' in names or 'round_trip' in names, 'Missing roundtrip tests'
assert 'brtag' in names or 'br_tag' in names, 'Missing br-tag tests'
print(f'OK: test_files.py has {len(tests)} tests')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - test_files.py has >= 25 test functions
    - Upload tests cover XML, Excel, TXT/TSV formats
    - Download tests verify content preservation
    - Round-trip tests for br-tags and Korean text
    - Error cases: invalid format, empty file, nonexistent file
    - Uses mock_uploads_path fixture for fixture file locations
  </acceptance_criteria>
  <done>Comprehensive file test suite with 30+ tests covering all 18 file endpoints</done>
</task>

</tasks>

<verification>
```bash
python3 -m pytest tests/api/test_health.py tests/api/test_auth.py tests/api/test_projects.py tests/api/test_folders.py tests/api/test_files.py --collect-only 2>&1 | tail -5
```
</verification>

<success_criteria>
- 5 test files with 70+ total test functions
- All auth scenarios (valid, invalid, expired, missing)
- Full CRUD coverage for projects and folders
- File upload/download with all formats (XML, Excel, TXT)
- br-tag and Korean round-trip tests
- Error cases for every endpoint
</success_criteria>

<output>
After completion, create `.planning/phases/25-comprehensive-api-e2e-testing/25-05-SUMMARY.md`
</output>
