---
phase: 21-ai-naming-coherence-placeholders
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - locaNext/src/lib/components/ldm/NamingPanel.svelte
  - locaNext/src/lib/components/ldm/PlaceholderImage.svelte
  - locaNext/src/lib/components/ldm/PlaceholderAudio.svelte
  - locaNext/src/lib/components/ldm/CodexEntityDetail.svelte
  - locaNext/src/lib/components/pages/GameDevPage.svelte
autonomous: true
requirements:
  - AINAME-01
  - AINAME-02
  - AINAME-03
  - PLACEHOLDER-01
  - PLACEHOLDER-02
  - PLACEHOLDER-03

must_haves:
  truths:
    - "Editing a Name field in Game Dev grid shows similar entity names in a panel below the grid"
    - "AI naming suggestions appear with confidence badges after a 500ms debounce"
    - "Clicking a suggestion copies the name to clipboard or applies to the editing field without auto-replace"
    - "Missing images in Codex show a styled SVG placeholder with entity name and category-specific Carbon icon"
    - "Missing audio in Codex shows a waveform SVG placeholder with entity name and [No Audio] label"
    - "Same entity always renders the same placeholder (deterministic from entityType + entityName)"
  artifacts:
    - path: "locaNext/src/lib/components/ldm/NamingPanel.svelte"
      provides: "Debounced naming suggestions panel with similar names + AI suggestions"
      min_lines: 80
    - path: "locaNext/src/lib/components/ldm/PlaceholderImage.svelte"
      provides: "Styled SVG image placeholder with category-specific Carbon icon"
      min_lines: 20
    - path: "locaNext/src/lib/components/ldm/PlaceholderAudio.svelte"
      provides: "Waveform SVG audio placeholder with entity name"
      min_lines: 20
    - path: "locaNext/src/lib/components/ldm/CodexEntityDetail.svelte"
      provides: "Updated to use PlaceholderImage and PlaceholderAudio components"
    - path: "locaNext/src/lib/components/pages/GameDevPage.svelte"
      provides: "Integrated NamingPanel below VirtualGrid, triggered on Name field edit"
  key_links:
    - from: "locaNext/src/lib/components/ldm/NamingPanel.svelte"
      to: "/api/ldm/naming/suggest/{entity_type}"
      via: "fetch with 500ms debounce + AbortController"
      pattern: "fetch.*naming"
    - from: "locaNext/src/lib/components/pages/GameDevPage.svelte"
      to: "locaNext/src/lib/components/ldm/NamingPanel.svelte"
      via: "conditional render when editing Name field"
      pattern: "NamingPanel"
    - from: "locaNext/src/lib/components/ldm/CodexEntityDetail.svelte"
      to: "locaNext/src/lib/components/ldm/PlaceholderImage.svelte"
      via: "component replacement for image-placeholder div"
      pattern: "PlaceholderImage"
    - from: "locaNext/src/lib/components/ldm/CodexEntityDetail.svelte"
      to: "locaNext/src/lib/components/ldm/PlaceholderAudio.svelte"
      via: "component replacement for [No Audio] span"
      pattern: "PlaceholderAudio"
---

<objective>
Build the frontend NamingPanel component for Game Dev mode and styled SVG placeholder components for missing images and audio, integrating them into GameDevPage and CodexEntityDetail.

Purpose: Game developers get inline naming pattern suggestions when editing entity names, and all missing media renders professionally with styled placeholders instead of broken/blank states.
Output: Three new Svelte 5 components (NamingPanel, PlaceholderImage, PlaceholderAudio) and updates to GameDevPage and CodexEntityDetail.
</objective>

<execution_context>
@/home/<USERNAME>/.claude/get-shit-done/workflows/execute-plan.md
@/home/<USERNAME>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-ai-naming-coherence-placeholders/21-RESEARCH.md
@.planning/phases/21-ai-naming-coherence-placeholders/21-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 backend -->

From server/tools/ldm/schemas/naming.py (created in Plan 01):
```python
class NamingSimilarItem(BaseModel):
    name: str
    strkey: str
    similarity: float
    entity_type: str

class NamingSimilarResponse(BaseModel):
    items: List[NamingSimilarItem]
    count: int

class NamingSuggestionItem(BaseModel):
    name: str
    confidence: float
    reasoning: str

class NamingSuggestionResponse(BaseModel):
    suggestions: List[NamingSuggestionItem]
    status: str  # "ok" | "cached" | "unavailable"
    similar_names: List[NamingSimilarItem]
```

REST Endpoints (created in Plan 01):
```
GET /api/ldm/naming/similar/{entity_type}?name=...&limit=10
  -> NamingSimilarResponse

GET /api/ldm/naming/suggest/{entity_type}?name=...
  -> NamingSuggestionResponse (includes both AI suggestions + similar names)

GET /api/ldm/naming/status
  -> { available: bool, cache_size: int, model: str }
```

From locaNext/src/lib/components/ldm/CodexEntityDetail.svelte (existing):
```svelte
<!-- Line 135-138: Current image placeholder -->
<div class="image-placeholder">
  <ImageIcon size={48} />
  <span class="placeholder-label">{entity.entity_type}</span>
</div>

<!-- Line 211: Current audio placeholder -->
<span class="no-audio">[No Audio]</span>
```

From locaNext/src/lib/components/pages/GameDevPage.svelte (existing):
```svelte
<!-- Key state variables -->
let selectedGameDevFile = $state(null);
let dynamicColumns = $state(null);
let virtualGrid = $state(null);
<!-- VirtualGrid is rendered directly, no RightPanel integration -->
```

From locaNext/src/lib/components/ldm/AISuggestionsTab.svelte (pattern to follow):
```svelte
<!-- 500ms debounce + AbortController pattern -->
let abortController = null;
let debounceTimer = null;
$effect(() => {
  if (debounceTimer) clearTimeout(debounceTimer);
  if (abortController) abortController.abort();
  debounceTimer = setTimeout(() => { /* fetch */ }, 500);
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlaceholderImage + PlaceholderAudio SVG components + CodexEntityDetail integration</name>
  <files>
    locaNext/src/lib/components/ldm/PlaceholderImage.svelte,
    locaNext/src/lib/components/ldm/PlaceholderAudio.svelte,
    locaNext/src/lib/components/ldm/CodexEntityDetail.svelte
  </files>
  <action>
    1. Create `PlaceholderImage.svelte` (Svelte 5 runes):
       - Props: `entityType = ''` and `entityName = ''` via $props()
       - Import Carbon icons for category mapping: User (character), ShoppingCart (item), Lightning (skill), GameWireless (gimmick), Earth (region). Use a const ICON_MAP object.
       - Render an SVG with viewBox="0 0 160 120":
         - Rounded rect background using `var(--cds-layer-02)` fill
         - Center the category-specific Carbon icon (48px) via foreignObject
         - Bottom text showing entityName (truncated to 20 chars) or entityType fallback, using `var(--cds-text-03)` fill
       - Style: width 100%, max-width 200px, border-radius 4px

    2. Create `PlaceholderAudio.svelte` (Svelte 5 runes):
       - Props: `entityName = ''` via $props()
       - Render a container div with:
         - SVG waveform: viewBox="0 0 200 40", 20 bars using `{#each Array(20) as _, i}`. Each bar: rect at x=i*10+2, height from Math.sin(i*0.8)*24+8, fill `var(--cds-text-03)` with opacity 0.3, rx=2 for rounded corners
         - Label span below: entityName or "[No Audio]" fallback
       - Style: flex column, align center, gap 4px, text color `var(--cds-text-03)`, font-size 12px

    3. Update `CodexEntityDetail.svelte`:
       - Import PlaceholderImage and PlaceholderAudio at top
       - Replace the image placeholder block (line 135-138, the `<div class="image-placeholder">` with `<ImageIcon>`) with `<PlaceholderImage entityType={entity.entity_type} entityName={entity.name} />`
       - Replace the audio placeholder (line 211, `<span class="no-audio">[No Audio]</span>`) with `<PlaceholderAudio entityName={entity.name} />`
       - Remove the now-unused `.image-placeholder` and `.placeholder-label` CSS rules if no longer referenced
       - Keep the `.no-audio` CSS rule removal only if PlaceholderAudio handles its own styling
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools/locaNext && npx svelte-check --threshold warning 2>&1 | tail -5</automated>
  </verify>
  <done>PlaceholderImage renders category-specific SVG with Carbon icon per entity type. PlaceholderAudio renders waveform SVG. CodexEntityDetail uses both components instead of inline placeholders. svelte-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: NamingPanel component + GameDevPage integration</name>
  <files>
    locaNext/src/lib/components/ldm/NamingPanel.svelte,
    locaNext/src/lib/components/pages/GameDevPage.svelte
  </files>
  <action>
    1. Create `NamingPanel.svelte` (Svelte 5 runes) following AISuggestionsTab pattern:
       - Props via $props(): `editingName = ''` (string), `entityType = ''` (string), `onApply = null` (callback function that receives selected name)
       - State: `similarNames = $state([])`, `aiSuggestions = $state([])`, `loading = $state(false)`, `status = $state('idle')`
       - Internal: `let abortController = null`, `let debounceTimer = null`
       - $effect reacting to editingName changes:
         - If editingName is empty or < 2 chars, clear results and return
         - Clear previous debounceTimer, abort previous abortController
         - Set loading = true
         - After 500ms debounce, fetch `GET /api/ldm/naming/suggest/${entityType}?name=${encodeURIComponent(editingName)}` with AbortController signal and auth headers (getAuthHeaders)
         - On success: parse JSON, set similarNames from response.similar_names, aiSuggestions from response.suggestions, status from response.status
         - On error (not AbortError): set status = 'error', clear results
         - Finally: loading = false
       - Template layout (vertical, compact):
         - Header: "Naming Suggestions" with InlineLoading when loading
         - Section 1 "Similar Names": List of similarNames as clickable Carbon Tag items. Each shows name + similarity % badge. onclick calls onApply(item.name).
         - Section 2 "AI Suggestions" (only if aiSuggestions.length > 0): List of suggestions with name, confidence badge (High/Medium/Low same thresholds as AISuggestionsTab: >=85% green, >=60% yellow, <60% orange), reasoning text. onclick calls onApply(suggestion.name).
         - If status === 'unavailable': Show "AI unavailable -- showing similar names only" info text
         - If no results and not loading: Show "Type at least 2 characters" or "No similar names found"
       - Style: max-height 200px, overflow-y auto, border-top 1px solid var(--cds-border-subtle), padding 8px 12px

    2. Update `GameDevPage.svelte`:
       - Import NamingPanel
       - Add state: `let editingEntityName = $state('')`, `let editingEntityType = $state('')`
       - Detect Name field editing: Listen to VirtualGrid's inline edit events. When the user starts editing a cell, check if the column/attribute is "Name" or "AliasName". If yes, set editingEntityName to the cell's current value and editingEntityType to the row's entity_type (derive from file context or row data).
       - Handle VirtualGrid edit value changes: When the Name field value changes during editing, update editingEntityName reactively.
       - On edit complete/cancel: Clear editingEntityName to '' (hides panel).
       - Render NamingPanel below VirtualGrid, conditionally: `{#if editingEntityName}` `<NamingPanel editingName={editingEntityName} entityType={editingEntityType} onApply={handleNamingApply} />` `{/if}`
       - `handleNamingApply(name)`: Apply the selected name to the currently editing cell. Since VirtualGrid handles inline editing, this should set the input value. If VirtualGrid exposes a ref or method to set cell value, use it. Otherwise, dispatch a custom event or use a bound variable. The simplest approach: copy name to clipboard and show a brief toast/notification "Name copied -- paste into field". This respects AINAME-03 (never auto-replace).
       - Style the NamingPanel container: margin-top 0, transitions for show/hide
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools/locaNext && npx svelte-check --threshold warning 2>&1 | tail -5</automated>
  </verify>
  <done>NamingPanel shows similar names + AI suggestions when editing a Name field in Game Dev grid. Suggestions are non-blocking (500ms debounce). Clicking a suggestion applies it without auto-replacing. Panel is only visible in Game Dev mode during Name field editing. svelte-check passes.</done>
</task>

</tasks>

<verification>
cd /home/<USERNAME>/LocalizationTools/locaNext && npx svelte-check --threshold warning 2>&1 | tail -10
cd /home/<USERNAME>/LocalizationTools && python -m pytest tests/unit/ldm/test_naming_service.py tests/unit/ldm/test_naming_route.py -x -q --tb=short
</verification>

<success_criteria>
1. PlaceholderImage renders category-specific SVG with correct Carbon icon per entity type (character=User, item=ShoppingCart, skill=Lightning, gimmick=GameWireless, region=Earth)
2. PlaceholderAudio renders waveform SVG with entity name or "[No Audio]" label
3. CodexEntityDetail uses PlaceholderImage/PlaceholderAudio instead of inline placeholders
4. Same entity always renders identical placeholder (deterministic from entityType + entityName)
5. NamingPanel shows similar names ranked by similarity when editing a Name field
6. NamingPanel shows AI suggestions with confidence badges after 500ms debounce
7. Clicking a suggestion applies to field without auto-replacing
8. NamingPanel only appears in Game Dev mode when editing a Name field
9. svelte-check passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/21-ai-naming-coherence-placeholders/21-02-SUMMARY.md`
</output>
