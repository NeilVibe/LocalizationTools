---
phase: 15-mock-gamedata-universe
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - tests/fixtures/mock_gamedata/generate_mock_universe.py
  - tests/fixtures/mock_gamedata/stringtable/loc/languagedata_kor.xml
  - tests/fixtures/mock_gamedata/stringtable/loc/languagedata_eng.xml
  - tests/fixtures/mock_gamedata/stringtable/loc/languagedata_fre.xml
  - tests/fixtures/mock_gamedata/stringtable/export__/System/iteminfo_weapon.loc.xml
  - tests/fixtures/mock_gamedata/stringtable/export__/System/characterinfo_npc.loc.xml
  - tests/fixtures/mock_gamedata/stringtable/export__/System/skillinfo_pc.loc.xml
  - tests/fixtures/mock_gamedata/stringtable/export__/System/knowledgeinfo_character.loc.xml
  - tests/fixtures/mock_gamedata/stringtable/export__/System/factioninfo.loc.xml
  - tests/fixtures/mock_gamedata/stringtable/export__/System/gimmickinfo_background.loc.xml
  - tests/fixtures/mock_gamedata/stringtable/export__/System/gimmickinfo_item.loc.xml
  - tests/integration/test_mock_language_data.py
  - tests/integration/test_mock_export_index.py
  - tests/integration/test_mock_roundtrip.py
autonomous: true
requirements:
  - MOCK-04
  - MOCK-05
  - MOCK-08

must_haves:
  truths:
    - "Language data files load with Korean source text and English/French translations for every mock entity"
    - "Every StringID in languagedata files appears in at least one EXPORT .loc.xml file"
    - "Every Korean text attribute in StaticInfo XML has a corresponding LocStr entry"
    - "Round-trip validation passes: parse all XML, serialize, re-parse produces identical element counts and attribute values"
  artifacts:
    - path: "tests/fixtures/mock_gamedata/stringtable/loc/languagedata_kor.xml"
      provides: "300+ LocStr entries with Korean source text"
      contains: "LocStr"
    - path: "tests/fixtures/mock_gamedata/stringtable/loc/languagedata_eng.xml"
      provides: "300+ LocStr entries with English translations"
      contains: "LocStr"
    - path: "tests/fixtures/mock_gamedata/stringtable/export__/System/iteminfo_weapon.loc.xml"
      provides: "EXPORT index mapping StringIDs to iteminfo source"
      contains: "LocStr"
    - path: "tests/integration/test_mock_language_data.py"
      provides: "Language data validation for MOCK-04"
      contains: "def test_"
    - path: "tests/integration/test_mock_export_index.py"
      provides: "EXPORT index validation for MOCK-05"
      contains: "def test_"
    - path: "tests/integration/test_mock_roundtrip.py"
      provides: "Round-trip validation for MOCK-08"
      contains: "def test_"
  key_links:
    - from: "languagedata_kor.xml LocStr.StringId"
      to: "export__/System/*.loc.xml LocStr.StringId"
      via: "StringID appears in both"
      pattern: "StringId"
    - from: "StaticInfo/**/*.xml Korean text"
      to: "languagedata_kor.xml LocStr.StrOrigin"
      via: "Korean source text mapped to LocStr entries"
      pattern: "StrOrigin"
    - from: "generate_mock_universe.py"
      to: "stringtable/**/*.xml"
      via: "Language data generation from entity registry"
      pattern: "LocStr"
---

<objective>
Generate language data files (LocStr entries), EXPORT index files (.loc.xml), and validate round-trip integrity for the entire mock universe.

Purpose: Complete the mock gamedata universe with language data that enables Translator mode testing (Phase 16 Category Clustering depends on EXPORT indexes, Phase 17 AI Suggestions needs LocStr context).
Output: 3 language data files (KOR/ENG/FRE with 300+ LocStr each), 7+ EXPORT .loc.xml index files, and 3 validation test files.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-mock-gamedata-universe/15-RESEARCH.md
@.planning/phases/15-mock-gamedata-universe/15-01-SUMMARY.md

@tests/fixtures/mock_gamedata/localization/locstr_kor.xml
@tests/fixtures/mock_gamedata/localization/locstr_eng.xml
@tests/fixtures/mock_gamedata/export/event_mapping.xml

<interfaces>
<!-- From Plan 01: The generator script and its entity registry -->
<!-- Plan 02 extends generate_mock_universe.py with language data generation functions -->
<!-- The generator maintains a CrossRefRegistry dict with ALL entity StrKeys, names, descriptions -->
<!-- Korean text is already in StaticInfo XML attributes (ItemName, CharacterName, SkillName, etc.) -->
<!-- Each entity has a unique StrKey that becomes the basis for StringID generation -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Language data + EXPORT index generation</name>
  <files>
    tests/fixtures/mock_gamedata/generate_mock_universe.py (extend)
    tests/fixtures/mock_gamedata/stringtable/loc/languagedata_kor.xml
    tests/fixtures/mock_gamedata/stringtable/loc/languagedata_eng.xml
    tests/fixtures/mock_gamedata/stringtable/loc/languagedata_fre.xml
    tests/fixtures/mock_gamedata/stringtable/export__/System/*.loc.xml (7+ files)
  </files>
  <behavior>
    - MOCK-04: languagedata_kor.xml has 300+ LocStr entries with StrOrigin containing Korean text matching StaticInfo entity names/descriptions
    - MOCK-04: languagedata_eng.xml has 300+ LocStr entries with Str containing English translations
    - MOCK-04: languagedata_fre.xml has 300+ LocStr entries with Str containing French translations
    - MOCK-05: Each staticinfo source file has a corresponding .loc.xml in export__/System/ containing StringIDs for entities in that file
    - MOCK-05: Every StringID in languagedata files appears in at least one .loc.xml EXPORT file
  </behavior>
  <action>
Extend `generate_mock_universe.py` with language data generation:

1. **StringID generation**: For each entity (item, character, skill, region, gimmick, knowledge), generate 2 StringIDs: one for Name (`SID_{TYPE}_{KEY}_NAME`) and one for Desc (`SID_{TYPE}_{KEY}_DESC`). This produces 300+ StringIDs total.

2. **LocStr format**: Each LocStr element has: `StringId`, `StrOrigin` (Korean source), `Str` (translation), `DescOrigin` (Korean desc source), `Desc` (translated desc). Korean text uses the same Korean strings already in the StaticInfo XML attributes. Multi-line descriptions use `<br/>` (literal in Python, escaped by lxml to `&lt;br/&gt;` in XML output).

3. **Translation generation**: English translations are simple transliterations/translations of the Korean names (already defined in the generator from Plan 01). French translations use basic French game text templates (e.g., "Epee de fer" for Iron Sword). These don't need to be perfect translations -- they need to be distinct per language for testing.

4. **Language data files** (3 files in `stringtable/loc/`):
   - `languagedata_kor.xml`: Root `<LocStrList>`, contains 300+ `<LocStr>` elements with Korean StrOrigin
   - `languagedata_eng.xml`: Same StringIDs, Str field has English text
   - `languagedata_fre.xml`: Same StringIDs, Str field has French text

5. **EXPORT index files** (7+ files in `stringtable/export__/System/`):
   One .loc.xml per staticinfo source file. Each contains `<LocStrList>` with `<LocStr>` elements listing StringIDs for entities in that source file. File naming: `{staticinfo_filename_stem}.loc.xml` (e.g., `iteminfo_weapon.loc.xml`).
   Files: iteminfo_weapon, iteminfo_armor, iteminfo_consumable, iteminfo_accessory, characterinfo_npc, characterinfo_npc_shop, characterinfo_monster, skillinfo_pc, knowledgeinfo_character, knowledgeinfo_item, knowledgeinfo_region, knowledgeinfo_contents, factioninfo, gimmickinfo_background, gimmickinfo_item, gimmickinfo_puzzle.

6. **Existing fixtures preservation**: Do NOT delete `localization/locstr_kor.xml` or `localization/locstr_eng.xml` or `export/event_mapping.xml`. New files go in the new `stringtable/` directory structure.

7. **Integration**: Add language data generation calls to the main `generate()` function after all StaticInfo generation. The entity registry from Plan 01 provides all the data needed.
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python tests/fixtures/mock_gamedata/generate_mock_universe.py && python -c "
from lxml import etree
from pathlib import Path
loc_dir = Path('tests/fixtures/mock_gamedata/stringtable/loc')
for lang in ['kor', 'eng', 'fre']:
    f = loc_dir / f'languagedata_{lang}.xml'
    assert f.exists(), f'Missing {f}'
    tree = etree.parse(str(f))
    count = len(tree.getroot().findall('.//LocStr'))
    assert count >= 300, f'{lang}: only {count} LocStr (need 300+)'
    print(f'{lang}: {count} LocStr entries')
export_dir = Path('tests/fixtures/mock_gamedata/stringtable/export__/System')
loc_files = list(export_dir.glob('*.loc.xml'))
assert len(loc_files) >= 7, f'Only {len(loc_files)} EXPORT files (need 7+)'
print(f'EXPORT: {len(loc_files)} .loc.xml files')
print('OK')
"</automated>
  </verify>
  <done>3 language data files with 300+ LocStr each (KOR/ENG/FRE). 7+ EXPORT .loc.xml index files mapping StringIDs to source files. Generator script idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validation tests for language data, EXPORT indexes, and round-trip</name>
  <files>
    tests/integration/test_mock_language_data.py
    tests/integration/test_mock_export_index.py
    tests/integration/test_mock_roundtrip.py
  </files>
  <behavior>
    - test_mock_language_data.py: 300+ LocStr per language, Korean StrOrigin contains Korean characters, StringIDs follow naming convention, all 3 language files have same StringID set
    - test_mock_export_index.py: Every .loc.xml file is parseable, every StringID in languagedata appears in at least one EXPORT file, EXPORT files map to real staticinfo source files
    - test_mock_roundtrip.py: Parse every XML file in StaticInfo/ and stringtable/, serialize to bytes, re-parse, compare element counts and key attribute values
  </behavior>
  <action>
Create 3 integration test files. All use `from __future__ import annotations`, `pytest`, `lxml.etree`, `Path`.

**tests/integration/test_mock_language_data.py** (MOCK-04):
- `test_korean_locstr_count()`: languagedata_kor.xml has 300+ LocStr entries
- `test_english_locstr_count()`: languagedata_eng.xml has 300+ LocStr entries
- `test_french_locstr_count()`: languagedata_fre.xml has 300+ LocStr entries
- `test_korean_text_contains_hangul()`: Every StrOrigin in KOR file contains Korean characters (regex `[\uac00-\ud7af\u1100-\u11ff\u3130-\u318f]`)
- `test_stringid_consistency()`: All 3 language files have the EXACT same set of StringIDs
- `test_stringid_naming_convention()`: All StringIDs match pattern `SID_{TYPE}_{ID}_{NAME|DESC}`
- `test_br_tag_preservation()`: At least 10 LocStr entries contain `<br/>` in StrOrigin or Str (multiline descriptions)

**tests/integration/test_mock_export_index.py** (MOCK-05):
- `test_export_files_parseable()`: Every .loc.xml under export__/System/ parses without error
- `test_export_file_count()`: At least 7 .loc.xml files
- `test_every_stringid_in_export()`: Build set of all StringIDs from languagedata_kor.xml. For each, assert it appears in at least one EXPORT .loc.xml file.
- `test_export_files_map_to_staticinfo()`: Every .loc.xml filename stem corresponds to a staticinfo XML filename stem

**tests/integration/test_mock_roundtrip.py** (MOCK-08):
- `test_staticinfo_roundtrip()`: For each XML file in StaticInfo/, parse with lxml, serialize to bytes with `etree.tostring(encoding="utf-8", xml_declaration=True)`, re-parse, assert element count matches, assert all attributes on root children match
- `test_languagedata_roundtrip()`: Same for languagedata files -- element count and StringId attribute values preserved
- `test_export_roundtrip()`: Same for EXPORT .loc.xml files
- `test_full_universe_no_parse_errors()`: Walk entire mock_gamedata tree, parse every .xml file, assert 0 failures
  </action>
  <verify>
    <automated>cd /home/neil1988/LocalizationTools && python -m pytest tests/integration/test_mock_language_data.py tests/integration/test_mock_export_index.py tests/integration/test_mock_roundtrip.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>All 3 test files pass. MOCK-04 (language data), MOCK-05 (EXPORT indexes), MOCK-08 (round-trip) validated by automated tests. Full existing test suite (478 tests) still passes.</done>
</task>

</tasks>

<verification>
Run complete validation:
```bash
cd /home/neil1988/LocalizationTools
python tests/fixtures/mock_gamedata/generate_mock_universe.py
python -m pytest tests/unit/test_mock_universe_structure.py tests/unit/test_mock_volume.py tests/unit/test_mock_media_stubs.py tests/unit/test_mock_map_data.py tests/integration/test_mock_crossref.py tests/integration/test_mock_language_data.py tests/integration/test_mock_export_index.py tests/integration/test_mock_roundtrip.py -x -v
# Also verify existing tests still pass:
python -m pytest tests/integration/test_mock_gamedata_pipeline.py -x -v
```
</verification>

<success_criteria>
1. 300+ LocStr entries per language file (KOR, ENG, FRE)
2. Korean text contains actual Hangul characters (not ASCII placeholders)
3. All 3 language files share identical StringID sets
4. 7+ EXPORT .loc.xml index files mapping StringIDs to source files
5. Every StringID appears in at least one EXPORT file
6. Round-trip parse/serialize/re-parse produces identical results for all XML files
7. Existing test suite (478 tests + test_mock_gamedata_pipeline.py) still passes
8. All 8 new test files (5 from Plan 01 + 3 from Plan 02) pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-mock-gamedata-universe/15-02-SUMMARY.md`
</output>
