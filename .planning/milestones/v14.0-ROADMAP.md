# Roadmap: v14.0 Debug & UX Overhaul

> **Goal:** Fix all debug issues, repair TM workflow, redesign navigation, add GameData tabs.
> **Approach:** Debug-first (Phase 93), then UX fixes (94-96).
> **Source:** project_pending_fixes_v2.md + STATE.md pending E2E

## Phases

- [ ] **Phase 93: Critical Debug Fixes** — Fix Codex infinite loop, remote logger cascade, verify v13.0 E2E
- [ ] **Phase 94: Grid & TM UX Fixes** — Fix TM upload, add TM assignment UI, fix yellow cell default
- [ ] **Phase 95: Navigation & Merge Redesign** — Relocate Merge button from top bar
- [ ] **Phase 96: GameData Category Tabs + Visual Polish** — Auto-parse categories into tabs, CrimsonDesert.gg reference

## Phase Details

### Phase 93: Critical Debug Fixes
**Goal**: Zero infinite loops, zero feedback cascades, all v13.0 features verified working in browser
**Depends on**: Nothing (first phase)
**Requirements**: DBG-01, DBG-02, DBG-03
**Success Criteria** (what must be TRUE):
  1. `/api/ldm/codex/list/` is called <=5 times on page load (CDP deep monitor verified)
  2. Remote logger does not cascade — 404 errors logged once, not 825x
  3. Grid loads LanguageData file without freezing (v13.0 infinite loop fix confirmed)
  4. Branch+Drive selector shows dropdowns and green/red validation status
  5. Image/Audio tabs display specific fallback reasons
  6. TM context search returns AC results on row select
**Plans:** TBD (discuss phase first)

### Phase 94: Grid & TM UX Fixes
**Goal**: TM upload works end-to-end, TM assignment is discoverable, grid default color is neutral
**Depends on**: Phase 93 (debug fixes must land first)
**Requirements**: TMUX-01, TMUX-02, TMUX-03
**Success Criteria** (what must be TRUE):
  1. User can upload a TM file and see it appear in the TM list without infinite spinner
  2. User can assign a TM to a file/folder through a clearly visible UI action
  3. Grid cells default to neutral grey, yellow only appears on explicit user action
**Plans:** TBD

### Phase 95: Navigation & Merge Redesign
**Goal**: Merge is accessible but not cluttering the top navigation bar
**Depends on**: Phase 94 (UX fixes stabilize before nav changes)
**Requirements**: NAV-01
**Success Criteria** (what must be TRUE):
  1. "Merge" button is removed from top-level navigation bar
  2. Merge functionality is accessible through an alternative path (right-click or Apps section)
  3. Merge modal still works identically to current behavior
**Plans:** TBD (Tribunal decides placement)

### Phase 96: GameData Category Tabs + Visual Polish
**Goal**: GameData categories display as tabs, visual style upgraded toward CrimsonDesert.gg quality
**Depends on**: Phase 95 (nav clean before polish)
**Requirements**: GD-01, GD-02
**Success Criteria** (what must be TRUE):
  1. GameData page shows Items/Region/Character as auto-detected tabs from XML hierarchy
  2. Tab structure derives from depth/parent-node hierarchy (not hardcoded)
  3. Data presentation style moves toward CrimsonDesert.gg quality level
**Plans:** TBD

## Progress

| Phase | Plans Complete | Status |
|-------|----------------|--------|
| 93. Critical Debug Fixes | 0/? | Pending |
| 94. Grid & TM UX Fixes | 0/? | Pending |
| 95. Navigation & Merge Redesign | 0/? | Pending |
| 96. GameData Category Tabs + Visual Polish | 0/? | Pending |
