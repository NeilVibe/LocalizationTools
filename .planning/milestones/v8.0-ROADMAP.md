# Roadmap: LocaNext v8.0 Tag Visualizer + Service Layer Extraction

## Overview

Two workstreams: (1) MemoQ-style inline tag visualization for the translation editor — makes placeholders, parameters, and escapes visible as colored pills instead of raw text. (2) Extract business logic from 6 thick API modules into service classes for testability and maintainability.

## Module Audit

| Module | Lines | Thickness | Action |
|--------|-------|-----------|--------|
| stats.py | 1371 | THICK | Phase 69 |
| auth_async.py | 629 | THICK | Phase 70 |
| admin_telemetry.py | 618 | THICK | Phase 71 |
| rankings.py | 608 | THICK | Phase 69 |
| remote_logging.py | 567 | THICK | Phase 71 |
| admin_db_stats.py | 239 | THICK | Phase 72 |
| progress_operations.py | 447 | MEDIUM | Phase 72 |
| health.py | 323 | MEDIUM | Phase 72 |
| *8 THIN modules* | 2523 | THIN | No changes needed |

## Phases

### User-Facing (Priority)
- [ ] **Phase 73: Regex Tag Visualizer** - MemoQ-style inline tag pills in VirtualGrid editor. Detect {0}, %1#, \n, {StaticInfo}, &desc; using tmx_tools.py regexes, render as colored pills.

### Architecture (Service Layer Extraction)
- [ ] **Phase 69: Stats & Rankings Service** - Extract StatsService + RankingsService from stats.py (1371) + rankings.py (608). Shared date-range helper, fix N+1 queries.
- [ ] **Phase 70: Auth Service** - Extract AuthService from auth_async.py (629). Login, register, CRUD, rate limiting, audit — all testable.
- [ ] **Phase 71: Telemetry & Remote Logging Services** - Extract TelemetryService + RemoteLoggingService from admin_telemetry.py (618) + remote_logging.py (567). Fix N+1 queries.
- [ ] **Phase 72: Infrastructure Services** - Extract DbHealthService + HealthService + ProgressService from admin_db_stats.py + health.py + progress_operations.py.

## Phase Details

### Phase 73: Regex Tag Visualizer (USER-FACING — DO FIRST)
**Goal**: Translators see inline tag pills (like MemoQ) instead of raw code patterns — `{0}` shows as `[0]` blue pill, `%1#` as `[Param1]` purple pill, `\n` as `[\n]` grey pill
**Depends on**: Nothing (independent, frontend-only)
**Requirements**: TAG-01, TAG-02, TAG-03
**Success Criteria**:
  1. `tagDetector.js` detects all 5 tag types using exact regexes from tmx_tools.py postprocessor
  2. `TagText.svelte` renders detected tags as colored inline pills (non-editable badges)
  3. VirtualGrid source/target cells show tag pills in display mode
  4. Tags are preserved exactly when editing (pills disappear in edit mode, raw text shown)
  5. All existing tag types work: `{0}`, `{Name}`, `%1#`, `\n`, `\t`, `{StaticInfo:...}`, `&desc;`
**Tag Patterns** (from `server/services/merge/tmx_tools.py`):
  - `%(\d)#` → `[ParamN]` purple pill (line 288)
  - `\{(?!Static[Ii]nfo:\w+:)([^}]+)\}` → `[content]` blue pill (line 294)
  - `\\(\w)` → `[\X]` grey pill (line 300)
  - `\{Static[Ii]nfo:(\w+):([^#}]+)#([^}]+)\}` → `[ID]` paired green pill (line 248)
  - `&amp;desc;` or `&desc;` → `[desc]` orange pill (line 309)
**Files to create/modify**:
  - CREATE: `locaNext/src/lib/utils/tagDetector.js`
  - CREATE: `locaNext/src/lib/components/ldm/TagText.svelte`
  - MODIFY: `locaNext/src/lib/components/ldm/VirtualGrid.svelte` (wire TagText into cell rendering)
**Estimated Plans**: 1-2

### Phase 69: Stats & Rankings Service
**Goal**: All stats/rankings query logic lives in service classes, route handlers are ≤10 lines, and shared date-range helper eliminates massive duplication
**Depends on**: Nothing (first phase — highest duplication, highest impact)
**Requirements**: SVC-STATS-01, SVC-STATS-02, SVC-STATS-03, SVC-STATS-04
**Success Criteria**:
  1. `server/services/stats_service.py` contains all 14 query builders from stats.py
  2. `server/services/rankings_service.py` contains all 6 query builders from rankings.py
  3. Date-range calculation is a single shared method, not duplicated 20 times
  4. No N+1 queries — top_tool_per_user uses window function instead of loop
  5. All stats/rankings endpoints return identical JSON responses as before (regression-safe)

### Phase 70: Auth Service
**Goal**: Auth logic is unit-testable without HTTP — login, register, password change, rate limiting, audit logging all encapsulated in AuthService
**Depends on**: Nothing (independent)
**Requirements**: SVC-AUTH-01, SVC-AUTH-02, SVC-AUTH-03
**Success Criteria**:
  1. `server/services/auth_service.py` handles all auth business logic
  2. Route handlers in auth_async.py are ≤10 lines each
  3. Rate limiting, password hashing, token creation are service methods
  4. Existing login/register flow works identically (regression-safe)

### Phase 71: Telemetry & Remote Logging Services
**Goal**: Telemetry queries and log ingestion pipeline extracted into services, N+1 queries fixed
**Depends on**: Nothing (independent)
**Requirements**: SVC-TELEM-01, SVC-TELEM-02, SVC-TELEM-03
**Success Criteria**:
  1. `server/services/telemetry_service.py` contains all admin_telemetry query logic
  2. `server/services/remote_logging_service.py` contains log ingestion + session tracking
  3. Installations list uses JOIN instead of N+1 per-installation queries
  4. All telemetry/logging endpoints return identical responses

### Phase 72: Infrastructure Services
**Goal**: DB health, system health, and progress operations separated into service layer
**Depends on**: Nothing (independent)
**Requirements**: SVC-INFRA-01, SVC-INFRA-02, SVC-INFRA-03
**Success Criteria**:
  1. Raw SQL in admin_db_stats.py encapsulated in `server/services/db_health_service.py`
  2. Health helper functions from health.py moved to `server/services/health_service.py`
  3. WebSocket dispatch in progress_operations.py separated from CRUD logic
  4. All endpoints return identical responses

## Progress

**Execution Order:**
Phase 73 FIRST (user-facing feature, highest user impact). Then service layer phases 69-72 (all independent, can parallelize).

| Phase | Milestone | Plans Complete | Status | Completed |
|-------|-----------|----------------|--------|-----------|
| 73. Regex Tag Visualizer | v8.0 | 0/? | Not Started | — |
| 69. Stats & Rankings Service | v8.0 | 0/? | Not Started | — |
| 70. Auth Service | v8.0 | 0/? | Not Started | — |
| 71. Telemetry & Remote Logging | v8.0 | 0/? | Not Started | — |
| 72. Infrastructure Services | v8.0 | 0/? | Not Started | — |
