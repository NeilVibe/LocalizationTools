---
phase: 84-virtualgrid-decomposition
plan: 03
type: execute
wave: 3
depends_on: ["84-02"]
files_modified:
  - locaNext/src/lib/components/ldm/grid/InlineEditor.svelte
  - locaNext/src/lib/components/ldm/grid/SearchEngine.svelte
  - locaNext/src/lib/components/ldm/grid/gridState.svelte.ts
  - locaNext/src/lib/components/ldm/VirtualGrid.svelte
autonomous: true
requirements: [GRID-02, GRID-06]

must_haves:
  truths:
    - "InlineEditor handles textarea editing, color picker, undo/redo, and save/cancel"
    - "SearchEngine handles semantic search, search config, and filter orchestration"
    - "VirtualGrid.svelte is a thin orchestrator under 300 lines"
    - "No module in grid/ exceeds 800 lines"
    - "All 7 export functions on VirtualGrid.svelte work for GridPage/LDM consumers"
    - "The app loads, displays, edits, searches, and saves grid data identically to the original monolith"
    - "Phase 83 Vitest tests pass unchanged"
  artifacts:
    - path: "locaNext/src/lib/components/ldm/grid/InlineEditor.svelte"
      provides: "Textarea editing, color picker, undo/redo, save/cancel"
      contains: "export function openEditModal"
    - path: "locaNext/src/lib/components/ldm/grid/SearchEngine.svelte"
      provides: "Semantic search, search config, filter orchestration"
      contains: "grid.activeFilter"
    - path: "locaNext/src/lib/components/ldm/VirtualGrid.svelte"
      provides: "Thin orchestrator composing all 6 modules"
      min_lines: 150
  key_links:
    - from: "InlineEditor.svelte"
      to: "gridState.svelte.ts"
      via: "writes grid.inlineEditingRowId, mutates grid.rows[index] on save"
      pattern: "grid\\.inlineEditingRowId"
    - from: "SearchEngine.svelte"
      to: "gridState.svelte.ts"
      via: "writes grid.activeFilter, grid.selectedCategories"
      pattern: "grid\\.activeFilter"
    - from: "VirtualGrid.svelte"
      to: "all 6 modules"
      via: "imports and composes via bind:this + props"
      pattern: "import.*from.*grid/"
---

<objective>
Batch 3: Extract InlineEditor and SearchEngine from VirtualGrid.svelte, completing the decomposition. VirtualGrid.svelte becomes the thin orchestrator (~230 lines per D-18).

Purpose: Final batch removes the remaining extracted logic — InlineEditor (~350 lines) handles all editing UX, SearchEngine (~100 lines) handles search/filter. After this plan, the 4293-line monolith is fully decomposed into 6 focused modules + orchestrator. GRID-02 (composable modules, no module > 800 lines) is fully satisfied.

Output: 2 new files in grid/, VirtualGrid.svelte reduced to ~230 lines, full E2E verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-virtualgrid-decomposition/84-CONTEXT.md
@.planning/phases/84-virtualgrid-decomposition/84-RESEARCH.md
@.planning/phases/84-virtualgrid-decomposition/84-01-SUMMARY.md
@.planning/phases/84-virtualgrid-decomposition/84-02-SUMMARY.md
@locaNext/src/lib/components/ldm/VirtualGrid.svelte
@locaNext/src/lib/components/ldm/grid/gridState.svelte.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract InlineEditor and SearchEngine modules</name>
  <files>
    locaNext/src/lib/components/ldm/grid/InlineEditor.svelte,
    locaNext/src/lib/components/ldm/grid/SearchEngine.svelte,
    locaNext/src/lib/components/ldm/grid/gridState.svelte.ts,
    locaNext/src/lib/components/ldm/VirtualGrid.svelte
  </files>
  <read_first>
    locaNext/src/lib/components/ldm/VirtualGrid.svelte (current state after plan 02 — should be ~1400-1600 lines),
    locaNext/src/lib/components/ldm/grid/gridState.svelte.ts (shared state),
    locaNext/src/lib/components/ldm/grid/CellRenderer.svelte (reads inlineEditingRowId for visual state),
    locaNext/src/lib/components/ldm/grid/SelectionManager.svelte (sends edit requests),
    .planning/phases/84-virtualgrid-decomposition/84-02-SUMMARY.md (what was done in batch 2),
    .planning/phases/84-virtualgrid-decomposition/84-CONTEXT.md (D-15, D-17 decisions)
  </read_first>
  <action>
    **Step 1 — Create InlineEditor.svelte (per D-15, ~350 lines):**

    1. Import from `./gridState.svelte.ts`: grid, getRowById, getRowIndexById.
    2. Import from `$lib/utils/api.js`: getAuthHeaders, getApiBase for save API calls.

    3. Props via `$props()` (per D-05): `fileId`, `fileName`, `fileType`, `onSaveComplete` callback.

    4. Extract from VirtualGrid.svelte (RESEARCH.md: lines 157-168, 1259-1760, 1802-2137):
       - `openEditModalByRowId(rowId)` — opens inline editor for a row (sets grid.inlineEditingRowId)
       - `startEditing(rowId)` — sets up editing state
       - `saveEdit()` — saves edited text to API, updates grid.rows on success
       - `cancelEdit()` — reverts editing state, clears grid.inlineEditingRowId
       - `handleEditKeyDown(event)` — keyboard shortcuts in edit mode (Ctrl+Enter to save, Escape to cancel)
       - Color picker state and logic (reads source text colors, applies to target)
       - Undo/redo stack (edit history for the current editing session)
       - Textarea auto-resize logic
       - Module-local state: `editingText`, `editingOriginalText`, `undoStack`, `redoStack`, `colorPickerVisible`

    5. Export functions the parent needs: `openEditModalByRowId` (called by parent's export wrapper).

    6. Markup: the inline editing overlay/textarea that appears when a row is being edited. Includes:
       - Textarea for target text editing
       - Color picker component (if inline)
       - Save/Cancel buttons
       - Character count, undo/redo buttons

    7. Move editing-related CSS from VirtualGrid.svelte:
       - `.inline-editor` styles
       - `.edit-textarea` styles
       - `.color-picker` styles
       - Save/cancel button positioning styles

    **Step 2 — Create SearchEngine.svelte (per D-17, D-20, ~100 lines):**

    1. Import from `./gridState.svelte.ts`: grid.
    2. Import from `$lib/utils/api.js` if needed for semantic search API calls.

    3. Props via `$props()`: `fileId`, `fileType`, `onSearchComplete` callback (to trigger ScrollEngine reload after filter change).

    4. Extract from VirtualGrid.svelte:
       - Search configuration state: `searchTerm`, `searchMode`, `searchFields`
       - Filter state writers: writes to `grid.activeFilter`, `grid.selectedCategories`
       - `performSearch()` — triggers semantic search via API
       - `clearSearch()` — resets search state
       - `handleFilterChange(filter)` — updates activeFilter
       - `handleCategoryChange(categories)` — updates selectedCategories
       - Search results handling

    5. Export functions: `performSearch`, `clearSearch`, `handleFilterChange`, `handleCategoryChange`.

    6. Markup: search input, filter dropdowns, category selector if they exist in VirtualGrid. If search UI is in a separate toolbar component, SearchEngine may be renderless (logic-only).

    7. Move search-related CSS if any.

    **Step 3 — Slim down VirtualGrid.svelte to thin orchestrator (per D-18, ~230 lines):**

    After extracting InlineEditor and SearchEngine, VirtualGrid.svelte should contain ONLY:

    1. **Imports** (~15 lines): All 6 modules from `./grid/`, gridState, stores (sync, preferences).
    2. **Props** (~10 lines): `fileId`, `fileName`, `fileType`, `activeTMs`, `onRowSelected`, etc.
    3. **Component refs** (~6 lines): `scrollEngine`, `cellRenderer`, `selectionManager`, `inlineEditor`, `statusColors`, `searchEngine`.
    4. **WebSocket sync** (~30 lines per D-19): `joinFile`/`leaveFile` lifecycle, `onCellUpdate` handler that mutates grid.rows.
    5. **Preferences** (~10 lines per D-21): reads from preferences store, passes to modules via gridState or props.
    6. **Export function wrappers** (~35 lines): All 7 functions delegating to child modules:
       - `loadRows() -> scrollEngine.loadRows()`
       - `scrollToRowById(id) -> scrollEngine.scrollToRowById(id)`
       - `scrollToRowNum(num) -> scrollEngine.scrollToRowNum(num)`
       - `openEditModalByRowId(id) -> inlineEditor.openEditModalByRowId(id)`
       - `updateRowQAFlag(id, count) -> statusColors.updateRowQAFlag(id, count)`
       - `applyTMToRow(line, text) -> statusColors.applyTMToRow(line, text)`
       - `markRowAsTMApplied(id, type) -> statusColors.markRowAsTMApplied(id, type)`
    7. **Lifecycle effects** (~20 lines): file change detection, resetGridState on new file, cleanup on destroy.
    8. **Markup** (~30 lines): The `.virtual-grid` container div with all 6 child components composed inside.
    9. **CSS** (~80 lines): Only the `.virtual-grid` container styles and CSS custom properties. All component-specific styles live in their respective modules.

    **CRITICAL cleanup:** Go through EVERY remaining function and variable in VirtualGrid.svelte. If it belongs to one of the 6 modules (scroll, cell rendering, selection, editing, status, search), it MUST be extracted. The goal is that NO business logic remains in the parent — only composition and delegation.

    **Verify the 7 exports work by checking the delegation chain:**
    - LDM.svelte calls `virtualGrid.loadRows()` -> VirtualGrid delegates -> scrollEngine.loadRows()
    - GridPage.svelte calls `virtualGrid.applyTMToRow()` -> VirtualGrid delegates -> statusColors.applyTMToRow()
  </action>
  <verify>
    <automated>cd locaNext && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "grid.inlineEditingRowId" locaNext/src/lib/components/ldm/grid/InlineEditor.svelte
    - grep -q "export function openEditModalByRowId\|export async function openEditModalByRowId" locaNext/src/lib/components/ldm/grid/InlineEditor.svelte
    - grep -q "grid.activeFilter" locaNext/src/lib/components/ldm/grid/SearchEngine.svelte
    - grep -q "import InlineEditor" locaNext/src/lib/components/ldm/VirtualGrid.svelte
    - grep -q "import SearchEngine" locaNext/src/lib/components/ldm/VirtualGrid.svelte
    - grep -c "export function" locaNext/src/lib/components/ldm/VirtualGrid.svelte returns 7
    - wc -l VirtualGrid.svelte is under 400 lines (target ~230, allow up to 400 with CSS)
    - Vitest unit tests pass
  </acceptance_criteria>
  <done>
    InlineEditor.svelte exists with textarea editing, color picker, undo/redo, save/cancel.
    SearchEngine.svelte exists with search, filter, and category management.
    VirtualGrid.svelte is a thin orchestrator (~230 lines of script + ~80 lines CSS).
    All 6 modules exist in grid/ directory.
    All 7 export functions delegate correctly.
    Vitest unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final verification — line counts, module inventory, E2E</name>
  <files>
    locaNext/src/lib/components/ldm/VirtualGrid.svelte,
    locaNext/src/lib/components/ldm/grid/gridState.svelte.ts,
    locaNext/src/lib/components/ldm/grid/ScrollEngine.svelte,
    locaNext/src/lib/components/ldm/grid/CellRenderer.svelte,
    locaNext/src/lib/components/ldm/grid/SelectionManager.svelte,
    locaNext/src/lib/components/ldm/grid/InlineEditor.svelte,
    locaNext/src/lib/components/ldm/grid/StatusColors.svelte,
    locaNext/src/lib/components/ldm/grid/SearchEngine.svelte
  </files>
  <read_first>
    locaNext/src/lib/components/ldm/VirtualGrid.svelte (final state),
    locaNext/src/lib/components/ldm/grid/gridState.svelte.ts
  </read_first>
  <action>
    **Step 1 — Verify line counts (GRID-02: no module > 800 lines):**
    ```bash
    wc -l locaNext/src/lib/components/ldm/grid/*.svelte locaNext/src/lib/components/ldm/grid/*.ts locaNext/src/lib/components/ldm/VirtualGrid.svelte
    ```
    Every file must be under 800 lines. If any file exceeds 800 lines, identify what can be split further (likely CellRenderer at ~600 lines is the only candidate, and it should be under 800).

    **Step 2 — Verify module inventory (D-23):**
    Confirm all 7 files exist in grid/:
    - gridState.svelte.ts
    - ScrollEngine.svelte
    - CellRenderer.svelte
    - SelectionManager.svelte
    - InlineEditor.svelte
    - StatusColors.svelte
    - SearchEngine.svelte

    **Step 3 — Verify export API preservation:**
    ```bash
    grep "export function" locaNext/src/lib/components/ldm/VirtualGrid.svelte
    ```
    Must show exactly 7 functions: scrollToRowById, scrollToRowNum, openEditModalByRowId, updateRowQAFlag, loadRows, applyTMToRow, markRowAsTMApplied.

    **Step 4 — Verify write-ownership documentation (D-03):**
    ```bash
    grep "WRITE OWNERSHIP" locaNext/src/lib/components/ldm/grid/gridState.svelte.ts
    ```

    **Step 5 — Run ALL Vitest unit tests:**
    ```bash
    cd locaNext && npx vitest run --reporter=verbose
    ```
    All Phase 83 tests (tagDetector, statusColors, TagText, runes-smoke) must pass.

    **Step 6 — Run key E2E Playwright tests (per D-11):**
    Run the most comprehensive grid E2E test to verify full integration:
    ```bash
    cd locaNext && npx playwright test tests/grid-status-colors.spec.ts tests/cell_context_menu.spec.ts tests/grid-save-no-overflow.spec.ts --reporter=list 2>&1 | tail -40
    ```
    Note: E2E tests require running servers. If servers aren't running, document the test commands for manual execution and report unit test results.

    **Step 7 — If any file > 800 lines, fix it:**
    The most likely candidate is CellRenderer.svelte. If it exceeds 800 lines:
    - Extract column definition constants to a `columnDefs.ts` file in grid/
    - Or extract resize logic to a helper function in gridState.svelte.ts
    - Re-verify line count after fix

    **Step 8 — Verify no orphaned code:**
    Check that VirtualGrid.svelte has no large function bodies (> 10 lines) remaining that should have been extracted. The parent should ONLY contain: imports, props, refs, WebSocket sync (~30 lines), preferences pass-through, 7 export wrappers (1-2 lines each), lifecycle effects, and markup composition.
  </action>
  <verify>
    <automated>cd locaNext && npx vitest run --reporter=verbose 2>&1 | tail -30 && echo "--- LINE COUNTS ---" && wc -l src/lib/components/ldm/grid/*.svelte src/lib/components/ldm/grid/*.ts src/lib/components/ldm/VirtualGrid.svelte && echo "--- EXPORTS ---" && grep -c "export function" src/lib/components/ldm/VirtualGrid.svelte</automated>
  </verify>
  <acceptance_criteria>
    - wc -l shows ALL files under 800 lines
    - ls grid/ shows exactly 7 files (gridState.svelte.ts + 6 .svelte modules)
    - grep "export function" VirtualGrid.svelte shows exactly 7 functions
    - grep "WRITE OWNERSHIP" gridState.svelte.ts returns a match
    - Vitest reports all tests passing
    - VirtualGrid.svelte is under 400 lines total (script + markup + CSS)
  </acceptance_criteria>
  <done>
    Full decomposition verified: 6 modules + 1 orchestrator, all under 800 lines.
    All 7 export functions preserved and delegating correctly.
    Write-ownership documented in gridState.svelte.ts.
    All Phase 83 Vitest unit tests pass.
    E2E test results documented (pass or noted as requiring server).
    Phase 84 decomposition complete.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd locaNext && npx vitest run` — all Phase 83 unit tests pass
2. Every file in grid/ under 800 lines
3. VirtualGrid.svelte under 400 lines total
4. 7 export functions preserved on VirtualGrid.svelte
5. All 7 files exist in grid/ per D-23
6. Write-ownership comments exist in gridState.svelte.ts per D-03
7. No business logic remains in VirtualGrid.svelte — only composition + delegation
</verification>

<success_criteria>
- InlineEditor owns textarea editing, color picker, undo/redo, save/cancel
- SearchEngine owns search, filters, and category selection
- VirtualGrid.svelte is a thin orchestrator (~230 lines) composing 6 modules
- ALL requirements satisfied: GRID-02 (composable, < 800 lines), GRID-06 (InlineEditor)
- Phase 83 Vitest tests pass without modification
- The decomposed grid is functionally identical to the 4293-line monolith
</success_criteria>

<output>
After completion, create `.planning/phases/84-virtualgrid-decomposition/84-03-SUMMARY.md`
</output>
