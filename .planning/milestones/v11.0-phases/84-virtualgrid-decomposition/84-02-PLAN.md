---
phase: 84-virtualgrid-decomposition
plan: 02
type: execute
wave: 2
depends_on: ["84-01"]
files_modified:
  - locaNext/src/lib/components/ldm/grid/CellRenderer.svelte
  - locaNext/src/lib/components/ldm/grid/SelectionManager.svelte
  - locaNext/src/lib/components/ldm/grid/gridState.svelte.ts
  - locaNext/src/lib/components/ldm/VirtualGrid.svelte
autonomous: true
requirements: [GRID-04, GRID-05]

must_haves:
  truths:
    - "CellRenderer handles all cell markup, column layout, resize, and TagText integration"
    - "SelectionManager handles row/cell selection, hover states, keyboard navigation, and context menu"
    - "The grid displays rows with correct column layout, tag pills, and status colors"
    - "Clicking a row selects it, keyboard arrows navigate between rows"
    - "Context menu appears on right-click with correct options"
    - "Column resize works by dragging column borders"
    - "The app works identically to before extraction"
  artifacts:
    - path: "locaNext/src/lib/components/ldm/grid/CellRenderer.svelte"
      provides: "Column layout, cell markup, text formatting, TagText integration"
      contains: "import TagText"
    - path: "locaNext/src/lib/components/ldm/grid/SelectionManager.svelte"
      provides: "Cell selection, keyboard navigation, multi-select, context menu"
      contains: "grid.selectedRowId"
  key_links:
    - from: "CellRenderer.svelte"
      to: "gridState.svelte.ts"
      via: "reads grid.rows, visibleStart/visibleEnd, hoveredRowId for rendering"
      pattern: "import.*grid.*from.*gridState"
    - from: "CellRenderer.svelte"
      to: "TagText.svelte"
      via: "renders tag pills in source/target cells"
      pattern: "import TagText"
    - from: "SelectionManager.svelte"
      to: "gridState.svelte.ts"
      via: "writes grid.selectedRowId, grid.hoveredRowId, grid.hoveredCell"
      pattern: "grid\\.selectedRowId"
    - from: "CellRenderer.svelte"
      to: "ScrollEngine (via gridState)"
      via: "reads visibleRows derived state for what to render"
      pattern: "import.*visibleRows.*from.*gridState"
---

<objective>
Batch 2: Extract CellRenderer and SelectionManager from VirtualGrid.svelte, wiring them through gridState.svelte.ts.

Purpose: CellRenderer (~600 lines) is the largest extraction — it owns all visible cell markup, column layout, and TagText integration. SelectionManager (~250 lines) owns keyboard navigation and row selection. Together they account for ~850 lines. After this plan, VirtualGrid.svelte is ~2000-2200 lines.

Output: 2 new files in grid/, a further-slimmed VirtualGrid.svelte, and passing tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-virtualgrid-decomposition/84-CONTEXT.md
@.planning/phases/84-virtualgrid-decomposition/84-RESEARCH.md
@.planning/phases/84-virtualgrid-decomposition/84-01-SUMMARY.md
@locaNext/src/lib/components/ldm/VirtualGrid.svelte
@locaNext/src/lib/components/ldm/grid/gridState.svelte.ts
@locaNext/src/lib/components/ldm/TagText.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract CellRenderer module</name>
  <files>
    locaNext/src/lib/components/ldm/grid/CellRenderer.svelte,
    locaNext/src/lib/components/ldm/grid/gridState.svelte.ts,
    locaNext/src/lib/components/ldm/VirtualGrid.svelte
  </files>
  <read_first>
    locaNext/src/lib/components/ldm/VirtualGrid.svelte (current state after plan 01),
    locaNext/src/lib/components/ldm/grid/gridState.svelte.ts (shared state),
    locaNext/src/lib/components/ldm/grid/ScrollEngine.svelte (how it connects),
    locaNext/src/lib/components/ldm/grid/StatusColors.svelte (how it connects),
    locaNext/src/lib/components/ldm/TagText.svelte (used for tag pill rendering in cells),
    .planning/phases/84-virtualgrid-decomposition/84-01-SUMMARY.md (what was done in batch 1)
  </read_first>
  <action>
    **Create CellRenderer.svelte (per D-13, ~600 lines):**

    This is the LARGEST module. It owns the visual rendering of every grid row and cell.

    1. Import from `./gridState.svelte.ts`: grid, visibleRows, measureRowHeight, rowHeightCache, tmAppliedRows, referenceData, qaFlags, getRowById.
    2. Import from `$lib/components/ldm/TagText.svelte`: the tag pill rendering component.
    3. Import Carbon components used in cell markup: Tag, InlineLoading, icons (Edit, Locked, etc.).

    4. Props via `$props()` (per D-05): `fileType` (translator vs game-dev view), `onRowClick` callback, `onRowDoubleClick` callback, `onCellContextMenu` callback, `onColumnResize` callback.

    5. Extract from VirtualGrid.svelte (RESEARCH.md: markup lines 2746-2941, column definitions 332-414, resize system 237-466):
       - Column definitions array (defines source, target, reference, status columns with widths)
       - Column resize logic (drag handlers, min/max widths, resize cursor state)
       - Row rendering markup: the `{#each visibleRows}` loop that renders `<div class="virtual-row">` elements
       - Cell rendering for each column type: source (with TagText), target (with TagText), reference, status, row number
       - `use:measureRowHeight` on row divs (imported from gridState)
       - Row click/double-click/context-menu event forwarding (calls callbacks from props)
       - Hover state display logic (reads grid.hoveredRowId, grid.hoveredCell from gridState)
       - Status color application on rows (reads StatusColors data from gridState)
       - TM match indicator display (reads tmAppliedRows from gridState)
       - QA badge display per row (reads qaFlags from gridState)

    6. Move ALL cell/row rendering CSS from VirtualGrid.svelte:
       - `.virtual-row` styles (positioning, height, hover states)
       - `.cell` styles (padding, overflow, text-overflow)
       - `.cell.source`, `.cell.target`, `.cell.reference` specific styles
       - `.row-number` styles
       - Column resize handle styles (`.resize-handle`, cursor styles)
       - Status color CSS classes (row background colors for empty/translated/confirmed)
       - QA badge positioning styles
       - TM match indicator styles

    7. **CRITICAL markup ownership:** CellRenderer renders the row content. It must receive the scroll container's visible area information from gridState (visibleStart/visibleEnd) to know which rows to render. The `{#each visibleRows}` iteration happens HERE, not in the parent.

    **Update VirtualGrid.svelte:**

    1. Add import: `import CellRenderer from './grid/CellRenderer.svelte'`.
    2. Replace the row rendering markup block with `<CellRenderer {fileType} onRowClick={handleRowClick} onRowDoubleClick={handleRowDoubleClick} onCellContextMenu={handleContextMenu} />`.
    3. Remove extracted column definitions, resize logic, and cell rendering functions.
    4. Remove extracted CSS (cell/row/column/resize styles).
    5. Keep event handler functions (handleRowClick, etc.) in VirtualGrid for now — they may delegate to SelectionManager in the next task.

    **Pitfall avoidance:**
    - CSS scoping: all `.virtual-row`, `.cell` styles MUST move to CellRenderer.svelte (Svelte scopes styles to component)
    - CSS custom properties (--grid-font-size) stay on parent div and inherit through to CellRenderer
    - `use:measureRowHeight` must be imported from gridState.svelte.ts, not from ScrollEngine
    - TagText import path doesn't change (it's still at `$lib/components/ldm/TagText.svelte`)
  </action>
  <verify>
    <automated>cd locaNext && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "import TagText" locaNext/src/lib/components/ldm/grid/CellRenderer.svelte
    - grep -q "import.*grid.*from.*gridState" locaNext/src/lib/components/ldm/grid/CellRenderer.svelte
    - grep -q "import.*visibleRows.*from.*gridState" locaNext/src/lib/components/ldm/grid/CellRenderer.svelte OR grep -q "import.*measureRowHeight.*from.*gridState" locaNext/src/lib/components/ldm/grid/CellRenderer.svelte
    - grep -q "use:measureRowHeight" locaNext/src/lib/components/ldm/grid/CellRenderer.svelte
    - grep -q "import CellRenderer" locaNext/src/lib/components/ldm/VirtualGrid.svelte
    - grep -q "\.virtual-row" locaNext/src/lib/components/ldm/grid/CellRenderer.svelte (CSS moved here)
    - Vitest unit tests pass
    - wc -l CellRenderer.svelte is between 400-800 lines
  </acceptance_criteria>
  <done>
    CellRenderer.svelte exists with column layout, cell rendering, TagText integration, resize logic, and row markup.
    All cell/row CSS moved from VirtualGrid.svelte to CellRenderer.svelte.
    VirtualGrid.svelte imports CellRenderer and delegates row rendering to it.
    Vitest unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract SelectionManager module and verify E2E</name>
  <files>
    locaNext/src/lib/components/ldm/grid/SelectionManager.svelte,
    locaNext/src/lib/components/ldm/grid/gridState.svelte.ts,
    locaNext/src/lib/components/ldm/VirtualGrid.svelte
  </files>
  <read_first>
    locaNext/src/lib/components/ldm/VirtualGrid.svelte (current state after Task 1),
    locaNext/src/lib/components/ldm/grid/gridState.svelte.ts (shared state),
    locaNext/src/lib/components/ldm/grid/CellRenderer.svelte (needs to coordinate with selection),
    .planning/phases/84-virtualgrid-decomposition/84-CONTEXT.md (D-14 decision)
  </read_first>
  <action>
    **Create SelectionManager.svelte (per D-14, ~250 lines):**

    1. Import from `./gridState.svelte.ts`: grid, getRowById, getRowIndexById.

    2. Props via `$props()`: `onRowSelected` callback (to notify parent when selection changes), `onEditRequest` callback (when user double-clicks or presses Enter to edit).

    3. Extract from VirtualGrid.svelte (RESEARCH.md: lines 217-221, 1576-1645, 2370-2402, context menu 1948-2077):
       - `handleRowClick(rowId)` — sets grid.selectedRowId
       - `handleRowDoubleClick(rowId)` — opens inline editor
       - `handleKeyDown(event)` — keyboard navigation (ArrowUp/Down to move selection, Enter to edit, Escape to deselect)
       - `handleHover(rowId, cellType)` — sets grid.hoveredRowId, grid.hoveredCell
       - `handleHoverLeave()` — clears hover state
       - Context menu state and logic: `contextMenuVisible`, `contextMenuX/Y`, `contextMenuRowId`, `handleContextMenu(event, rowId)`, `closeContextMenu()`
       - Context menu actions array (Copy, Edit, QA details, etc.)
       - Multi-select logic if present (Shift+click, Ctrl+click range selection)

    4. Export functions: `handleRowClick`, `handleRowDoubleClick`, `handleKeyDown`, `handleHover`, `handleHoverLeave`, `handleContextMenu`, `closeContextMenu`.

    5. Markup: the context menu overlay `<div class="context-menu">` with its action items. This is the only visible markup for SelectionManager.

    6. Move context menu CSS from VirtualGrid.svelte to SelectionManager.svelte.

    **Update VirtualGrid.svelte:**

    1. Add import: `import SelectionManager from './grid/SelectionManager.svelte'`.
    2. Add `let selectionManager;` ref.
    3. Wire CellRenderer callbacks to SelectionManager:
       - `<CellRenderer onRowClick={selectionManager.handleRowClick} onRowDoubleClick={selectionManager.handleRowDoubleClick} onCellContextMenu={selectionManager.handleContextMenu} />`
       OR pass selectionManager functions as callbacks.
    4. Add `<SelectionManager bind:this={selectionManager} onRowSelected={handleSelectionChange} onEditRequest={startEditing} />` in markup.
    5. Remove extracted selection/context-menu functions and state from VirtualGrid.svelte.
    6. Remove context menu CSS from VirtualGrid.svelte.

    **Step 3 — Run E2E tests (per D-11):**

    Run relevant Playwright E2E tests:
    ```bash
    cd locaNext && npx playwright test tests/cell_context_menu.spec.ts --reporter=list 2>&1 | tail -30
    ```
    Also run unit tests:
    ```bash
    cd locaNext && npx vitest run --reporter=verbose 2>&1 | tail -20
    ```

    **Pitfall avoidance:**
    - Keyboard events: the `keydown` listener may be on `window` or on the grid container. Check where it's attached in VirtualGrid.svelte and replicate in SelectionManager (likely via `$effect` with `addEventListener` on the scroll container or window).
    - Context menu positioning: `contextMenuX/Y` are pixel positions relative to the viewport. These are purely local to SelectionManager (not shared state).
    - Selection state (grid.selectedRowId) IS shared via gridState — CellRenderer reads it for visual highlighting.
  </action>
  <verify>
    <automated>cd locaNext && npx vitest run --reporter=verbose 2>&1 | tail -20 && echo "--- E2E ---" && npx playwright test tests/cell_context_menu.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "grid.selectedRowId" locaNext/src/lib/components/ldm/grid/SelectionManager.svelte
    - grep -q "handleKeyDown\|handleRowClick" locaNext/src/lib/components/ldm/grid/SelectionManager.svelte
    - grep -q "context-menu" locaNext/src/lib/components/ldm/grid/SelectionManager.svelte
    - grep -q "import SelectionManager" locaNext/src/lib/components/ldm/VirtualGrid.svelte
    - Vitest unit tests pass
    - VirtualGrid.svelte still has exactly 7 export functions
    - wc -l SelectionManager.svelte is between 150-400 lines
  </acceptance_criteria>
  <done>
    SelectionManager.svelte exists with row selection, keyboard navigation, hover tracking, and context menu.
    CellRenderer receives selection callbacks from SelectionManager (wired through parent or directly).
    VirtualGrid.svelte delegates selection handling to SelectionManager.
    All 7 export functions still work on VirtualGrid.svelte.
    Vitest unit tests pass.
    Batch 2 complete: VirtualGrid.svelte reduced by another ~800 lines (now ~1400-1600 lines).
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd locaNext && npx vitest run` — all Phase 83 unit tests pass
2. `wc -l locaNext/src/lib/components/ldm/VirtualGrid.svelte` — should be ~1400-1600 lines
3. `wc -l locaNext/src/lib/components/ldm/grid/CellRenderer.svelte` — should be ~400-700 lines (largest module)
4. `wc -l locaNext/src/lib/components/ldm/grid/SelectionManager.svelte` — should be ~150-350 lines
5. `grep -c "export function" locaNext/src/lib/components/ldm/VirtualGrid.svelte` — should be 7
6. No file in grid/ exceeds 800 lines
7. `ls locaNext/src/lib/components/ldm/grid/` — should show 4 files: gridState.svelte.ts, ScrollEngine.svelte, StatusColors.svelte, CellRenderer.svelte, SelectionManager.svelte
</verification>

<success_criteria>
- CellRenderer owns all cell markup, column layout, resize, and TagText rendering
- SelectionManager owns row selection, keyboard nav, hover, and context menu
- Both modules read/write shared state through gridState.svelte.ts (no prop drilling of reactive state)
- VirtualGrid.svelte delegates to both modules and retains all 7 export functions
- Phase 83 Vitest tests pass without modification
- No single file exceeds 800 lines
</success_criteria>

<output>
After completion, create `.planning/phases/84-virtualgrid-decomposition/84-02-SUMMARY.md`
</output>
