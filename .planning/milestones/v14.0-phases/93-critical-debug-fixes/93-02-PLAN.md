---
phase: 93-critical-debug-fixes
plan: 02
type: execute
wave: 2
depends_on: ["93-01"]
files_modified: []
autonomous: false
requirements: [DBG-03]
must_haves:
  truths:
    - "Grid loads LanguageData file without freezing or infinite API calls"
    - "Branch+Drive selector shows dropdowns and green/red path validation"
    - "Image tab shows thumbnail or specific fallback reason on row select"
    - "Audio tab shows player or specific fallback reason on row select"
    - "TM context search returns AC results on row select (if TM loaded)"
    - "Codex page loads with <=5 API calls to /api/ldm/codex/list/"
  artifacts: []
  key_links:
    - from: "DEV browser localhost:5173"
      to: "Backend localhost:8888"
      via: "All API calls verified working"
      pattern: "HTTP 200"
---

<objective>
Verify all v13.0 features work correctly in the DEV browser after the bug fixes in Plan 01. Six verification items from the D-11 checklist, each documented with CDP deep monitor measurements and screenshots.

Purpose: Confirm that infinite loops are fixed and all v13.0 features (grid, Branch+Drive, Image/Audio tabs, TM context) function correctly.
Output: Screenshots as evidence for each verification step. Pass/fail for each checklist item.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v14.0-phases/93-critical-debug-fixes/93-CONTEXT.md
@.planning/milestones/v14.0-phases/93-critical-debug-fixes/93-01-SUMMARY.md

@testing_toolkit/DEV_MODE_PROTOCOL.md
@testing_toolkit/cdp/deep_monitor.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: CDP deep monitor verification of Codex and logger fixes (per D-04, D-10)</name>
  <files></files>
  <read_first>
    testing_toolkit/DEV_MODE_PROTOCOL.md
    testing_toolkit/cdp/deep_monitor.js
    .planning/milestones/v14.0-phases/93-critical-debug-fixes/93-01-SUMMARY.md
  </read_first>
  <action>
Run CDP deep monitor against the DEV browser (localhost:5173) to verify the two bug fixes from Plan 01.

**Pre-requisite:** DEV servers must be running (check with `./scripts/check_servers.sh`). If not running, start with `./scripts/start_all_servers.sh --with-vite`.

**Test 1 -- Codex API call count (per D-04):**
1. Navigate to the Codex page in DEV browser
2. Run CDP deep monitor for 10 seconds, filtering for `/api/ldm/codex/list/`
3. Verify call count is <=5 (was 822 before fix)
4. Take screenshot of the Codex page loading correctly

**Test 2 -- Remote logger cascade (verifying D-08):**
1. Navigate to any page in DEV browser
2. Run CDP deep monitor for 10 seconds, filtering for `/api/v1/remote-logs/frontend`
3. Verify call count is <=10 per 5-second window (was 825 before fix)
4. If the endpoint returns 404, verify NO cascade occurs

Record all measurements in the SUMMARY. Include before/after comparison if baseline data is available from the context.
  </action>
  <verify>
    <automated>echo "CDP deep monitor verification -- check SUMMARY for recorded measurements"</automated>
  </verify>
  <acceptance_criteria>
    - CDP deep monitor shows /api/ldm/codex/list/ called <=5 times on Codex page load
    - CDP deep monitor shows /api/v1/remote-logs/frontend called <=10 times in any 5-second window
    - Screenshots saved to screenshots/ directory as evidence
  </acceptance_criteria>
  <done>CDP deep monitor confirms both infinite loops are eliminated. Call counts recorded in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of v13.0 feature checklist (per D-11, D-12)</name>
  <files></files>
  <action>
Present the CDP deep monitor results and screenshots from Task 1 to the user. Guide them through the 6-item verification checklist in the DEV browser at http://localhost:5173. Wait for approval or issue reports.

Checklist items:
1. Grid loads without freeze -- upload LanguageData XML, rows render within 2 seconds
2. Branch+Drive selector -- two dropdowns visible in toolbar, green/red validation on change
3. Image tab -- select row, Image tab shows thumbnail OR specific fallback reason
4. Audio tab -- select row, Audio tab shows player OR specific fallback reason
5. TM context -- select row with TM loaded, AC results appear with tier indicators
6. Codex page -- load Game World Codex, entity tabs render without freezing
  </action>
  <verify>
    <automated>echo "Human checkpoint -- approval required"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms all 6 checklist items pass
    - Or user reports specific failures for follow-up gap closure
  </acceptance_criteria>
  <done>User has approved all 6 verification items or reported specific issues for follow-up.</done>
  <what-built>
    Plan 01 fixed two infinite loop bugs (Codex API cascade, remote logger feedback loop). Task 1 of this plan verified the fixes via CDP deep monitor. Now verify all v13.0 features work correctly in the DEV browser.
  </what-built>
  <how-to-verify>
    Open DEV browser at http://localhost:5173 and verify each item:

    1. **Grid loads without freeze:** Upload a LanguageData XML file. Grid should render rows within 2 seconds, no infinite loading spinner.
    2. **Branch+Drive selector:** Look in the toolbar area. Two dropdowns (branch + drive) should be visible. Change branch -- should see green "PATHS OK" or red "PATHS NOT FOUND" status.
    3. **Image tab fallback:** Select a LanguageData row, click Image tab in right panel. Should show either a thumbnail OR a specific fallback reason like "Entity not found" / "No texture attribute" / "File not on disk".
    4. **Audio tab fallback:** Select a LanguageData row, click Audio tab in right panel. Should show either an audio player OR a specific fallback reason.
    5. **TM context (if TM loaded):** If a TM is loaded, select a row. TM tab in right panel should show AC context results with tier indicators (Exact/Line/Fuzzy). Skip if no TM available.
    6. **Codex page:** Navigate to Game World Codex. Page should load entity type tabs without freezing. No visible loading spinner stuck.

    Screenshots have been saved as evidence by Task 1.
  </how-to-verify>
  <resume-signal>Type "approved" if all items pass, or describe which items failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
All 6 checklist items from D-11 verified:
1. Grid loads without freeze
2. Branch+Drive selector shows dropdowns with validation
3. Image tab shows thumbnail or specific fallback reason
4. Audio tab shows player or specific fallback reason
5. TM context results appear on row select (if TM loaded)
6. Codex page loads without infinite API calls

CDP deep monitor measurements recorded. Human verified via checkpoint.
</verification>

<success_criteria>
- CDP deep monitor confirms <=5 codex list API calls per page load
- CDP deep monitor confirms <=10 remote log calls per 5-second window
- All 6 v13.0 feature verification items pass
- Human approval received at checkpoint
</success_criteria>

<output>
After completion, create `.planning/milestones/v14.0-phases/93-critical-debug-fixes/93-02-SUMMARY.md`
</output>
