# v14.0 Requirements — Debug & UX Overhaul

> **Source:** project_pending_fixes_v2.md (11 items) + demo testing (2026-03-28)
> **Priority:** Debug fixes first, then demo-ready, then UX changes
> **Reference:** Game encyclopedia sites for data presentation style
> **Updated:** 2026-03-28 — added TM crash, audio chain, mock data findings from live testing

---

## Phase 93: Critical Debug Fixes — DONE (Plan 01), E2E pending (Plan 02)

### DBG-01: Codex List Infinite Loop ✅ FIXED
- `$state(new Map())` tabCache + reassignment = 822 API calls
- Fix: plain Map, removed reassignment pattern (commit ecca01bf)

### DBG-02: Remote Logger Feedback Loop ✅ FIXED
- console.error interceptor + fetch failure = 825x cascade
- Fix: re-entrancy guard + rate limit 10/5s + URL pattern filter (commit e14fe56e)

### DBG-03: Pending E2E Validations ○ IN PROGRESS
- Grid loads without freeze ✅
- Branch+Drive selector ✅ green validation
- Image tabs ✅ images resolve for items/characters/regions
- Audio tabs ⚠️ WEM files exist but D11 chain empty (see AUDIO-01)
- TM context ⚠️ indexes rebuilt, search works via API but TM page crashes (see TM-04)

---

## Phase 94: Grid & TM UX Fixes + Demo Blockers

### TM-04: TM Page Crash (duplicate key) — NEW, HIGH PRIORITY
- Error: `each_key_duplicate: folder-95 at indexes 0 and 2`
- File: `TMExplorerGrid.svelte` line 1118, key: `` `${item.type}-${item.id}` ``
- Cause: `/tm-tree` API returns folder-95 twice (folder structure + TM assignment both include it)
- Fix: deduplicate `currentItems` before render, OR fix API to not return duplicates
- **Blocks entire TM page — must fix before any TM testing**

### AUDIO-01: Audio Chain D11 Empty — NEW
- WEM files exist (10 per language × 3 languages = 30 files)
- MegaIndex D10 indexes them correctly
- But D11 (event_to_stringid) is empty — mock export XMLs lack `SoundEventName` attribute
- Fix: add `SoundEventName` to `export__/Dialog/QuestDialog/questdialog_showcase.xml`
- Chain: D11 → R3 → C3 → audio resolves automatically
- No code changes needed — pure mock data fix

### AUDIO-02: Qwen3-TTS Voice Generation — OPTIONAL
- `qwen-tts` 0.1.1 installed, `tts_service.py` exists, endpoint exists
- 5 character voice profiles defined (Phase 41 CONTEXT.md)
- Can generate fresh Korean voices for demo characters
- Not a blocker — existing WEM placeholder audio works once AUDIO-01 is fixed

### TMUX-01: TM Upload Broken
- Infinite loading spinner when uploading TM
- CDP deep monitor debug needed on TMUploadModal flow

### TMUX-02: TM Assignment UI Missing
- Users can't find where to assign TM to a file
- UI for matching TM to files is unclear or buried

### TMUX-03: Yellow Cell Default Color
- Grid cells default to yellow → should be neutral grey
- Yellow only on explicit user action

---

## Phase 95: Navigation & Merge Redesign

### NAV-01: Merge Button Relocation
- Merge should NOT be a top-level nav button
- Options: right-click submenu, Apps section, or remove

---

## Phase 96: GameData Category Tabs + Visual Polish

### GD-01: GameData Categories → Tabs
- Auto-parse into TABS based on depth/parent-node hierarchy
- Pattern: QACompiler generators use cluster/mega-root-parent

### GD-02: Visual Reference for Data Presentation
- Use high-quality game encyclopedia sites as data presentation style reference

---

## Protocol Items (Non-Phase — Apply Immediately)

### PROTO-01: Debug Protocol Upgrades
- Sequential thinking, tier list review, Viking+Ruflo wiring

### PROTO-02: Playwright → Qwen+CDP
- Stop using Playwright MCP for visual reviews
- Use Qwen3-VL + CDP Node.js scripts instead

---

## Path Chain Reference (MegaIndex — 35 dicts)

### Image: StringID → DDS
```
StringID → C7(stringid_to_entity) → (type, strkey) → C1(strkey_to_image_path) → DDS
C1: D1(knowledge).UITextureName → D9(dds_by_stem) → file path
```

### Audio: StringID → WEM
```
StringID → R3(stringid_to_event) → event_name → D10(wem_by_event) → WEM path
C4/C5: event scripts (KR/ENG)
D11(event_to_stringid) must be populated from export XMLs with SoundEventName
```
