---
phase: 051-contextual-intelligence-qa-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/tools/ldm/services/glossary_service.py
  - server/tools/ldm/services/category_mapper.py
  - server/utils/qa_helpers.py
  - tests/unit/ldm/test_glossary_service.py
  - tests/unit/ldm/test_category_mapper.py
  - tests/fixtures/mock_gamedata/characterinfo_sample.xml
  - tests/fixtures/mock_gamedata/iteminfo_sample.xml
  - tests/fixtures/mock_gamedata/regioninfo_sample.xml
autonomous: true
requirements: [CTX-05, CTX-06, CTX-09, CTX-10]

must_haves:
  truths:
    - "Glossary can be extracted from game data XML (character, item, location, skill names)"
    - "Aho-Corasick automaton built from glossary detects all entity names in O(n) single pass"
    - "Word boundary check prevents false matches in Korean compound words"
    - "Category mapper assigns string types using two-tier algorithm (STORY + GAME_DATA)"
    - "Glossary terms map to staticinfo datapoints (image paths, audio paths, DESC fields)"
  artifacts:
    - path: "server/tools/ldm/services/glossary_service.py"
      provides: "GlossaryService singleton with AC automaton, entity detection, glossary extraction"
      exports: ["GlossaryService", "get_glossary_service", "DetectedEntity", "EntityInfo", "GlossaryEntry"]
    - path: "server/tools/ldm/services/category_mapper.py"
      provides: "TwoTierCategoryMapper ported from QuickCheck"
      exports: ["TwoTierCategoryMapper", "categorize_string"]
    - path: "tests/unit/ldm/test_glossary_service.py"
      provides: "Unit tests for glossary extraction and AC detection"
      min_lines: 80
    - path: "tests/unit/ldm/test_category_mapper.py"
      provides: "Unit tests for category clustering"
      min_lines: 40
  key_links:
    - from: "server/tools/ldm/services/glossary_service.py"
      to: "server/utils/qa_helpers.py"
      via: "is_isolated() for word boundary check"
      pattern: "is_isolated"
    - from: "server/tools/ldm/services/glossary_service.py"
      to: "pyahocorasick"
      via: "Automaton() for O(n) multi-pattern matching"
      pattern: "ahocorasick\\.Automaton"
---

<objective>
Build the GlossaryService that extracts entity names from game data XML files and builds an Aho-Corasick automaton for real-time O(n) entity detection, plus the TwoTierCategoryMapper for string type classification.

Purpose: This is the foundation for ALL contextual intelligence -- entity detection, context panel, and enhanced QA checks all depend on the glossary and AC automaton.
Output: GlossaryService singleton, CategoryMapper, mock gamedata fixtures, comprehensive unit tests.
</objective>

<execution_context>
@/home/<USERNAME>/.claude/get-shit-done/workflows/execute-plan.md
@/home/<USERNAME>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/051-contextual-intelligence-qa-engine/051-RESEARCH.md
@server/tools/ldm/services/mapdata_service.py
@server/utils/qa_helpers.py

<interfaces>
<!-- MapDataService singleton pattern to follow -->
From server/tools/ldm/services/mapdata_service.py:
```python
class MapDataService:
    def __init__(self):
        self._loaded: bool = False
    def initialize(self, branch, drive) -> bool: ...
    def get_status(self) -> dict: ...

_service_instance: Optional[MapDataService] = None
def get_mapdata_service() -> MapDataService: ...
```

From server/utils/qa_helpers.py:
```python
def is_isolated(text: str, start: int, end: int) -> bool: ...
def is_korean(text: str) -> bool: ...
def is_sentence(text: str) -> bool: ...
def has_punctuation(text: str) -> bool: ...
```

<!-- QuickSearch patterns to reuse (from RESEARCH.md) -->
```python
# build_automaton() pattern:
automaton = ahocorasick.Automaton()
automaton.add_word(term, (idx, term))
automaton.make_automaton()

# glossary_filter() pattern:
def glossary_filter(pairs, length_threshold=25, filter_sentences=True, min_occurrence=2): ...

# TwoTierCategoryMapper pattern:
PRIORITY_KEYWORDS = [("gimmick", "Gimmick"), ("item", "Item"), ("quest", "Quest"), ...]
# Tier 1: STORY (Dialog -> 4 sub-categories, Sequencer -> 'Sequencer')
# Tier 2: GAME_DATA (priority keywords first, then standard patterns)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GlossaryService with AC automaton and mock game data fixtures</name>
  <files>
    server/tools/ldm/services/glossary_service.py,
    tests/unit/ldm/test_glossary_service.py,
    tests/fixtures/mock_gamedata/characterinfo_sample.xml,
    tests/fixtures/mock_gamedata/iteminfo_sample.xml,
    tests/fixtures/mock_gamedata/regioninfo_sample.xml
  </files>
  <behavior>
    - Test: build_from_entity_names() creates working AC automaton from list of (name, EntityInfo) tuples
    - Test: detect_entities("The warrior enters Stormhold Castle") returns DetectedEntity for both "warrior" and "Stormhold Castle" (if in glossary)
    - Test: detect_entities() with Korean text uses is_isolated() to prevent compound word false matches
    - Test: detect_entities() returns empty list when service not loaded
    - Test: extract_glossary_from_xml(character_folder) parses characterinfo XML and extracts CharacterName values with StrKey/KnowledgeKey
    - Test: extract_glossary_from_xml(item_folder) parses iteminfo XML and extracts item names
    - Test: glossary_filter() removes entries with len > 25, sentences, punctuation, and min_occurrence < 2
    - Test: entity_index maps glossary terms to datapoint info (source_file, strkey, knowledge_key, entity_type)
    - Test: get_status() reports loaded state, entity counts per type, automaton size
  </behavior>
  <action>
    1. Create mock game data fixtures in tests/fixtures/mock_gamedata/:
       - characterinfo_sample.xml: 5 CharacterInfo entries with CharacterName, StrKey, KnowledgeKey attributes (follow QACompiler characterinfo pattern)
       - iteminfo_sample.xml: 5 ItemInfo entries with ItemName, StrKey attributes
       - regioninfo_sample.xml: 3 FactionNodeData entries with location names and KnowledgeKey

    2. Create tests/unit/ldm/test_glossary_service.py with tests for all behaviors above. Tests use mock fixtures.

    3. Create server/tools/ldm/services/glossary_service.py:
       - Dataclasses: EntityInfo(type, name, strkey, knowledge_key, source_file, datapoint_paths), DetectedEntity(term, start, end, entity), GlossaryEntry(source, target, entity_info)
       - GlossaryService class (singleton, follows MapDataService pattern):
         - build_from_entity_names(entities: List[Tuple[str, EntityInfo]]): builds AC automaton using pyahocorasick
         - detect_entities(text: str) -> List[DetectedEntity]: scans text with AC automaton, uses is_isolated() from qa_helpers
         - extract_character_glossary(folder: Path) -> List[Tuple[str, EntityInfo]]: parses characterinfo_*.staticinfo.xml
         - extract_item_glossary(folder: Path) -> List[Tuple[str, EntityInfo]]: parses iteminfo_*.staticinfo.xml
         - extract_region_glossary(folder: Path) -> List[Tuple[str, EntityInfo]]: parses factioninfo XML
         - initialize(paths: dict) -> bool: calls all extract_* methods, builds automaton from combined glossary, applies glossary_filter
         - get_glossary_terms() -> List[GlossaryEntry]: returns current glossary for Term Check usage
         - get_status() -> dict: loaded, counts per entity type, automaton size
       - Singleton: get_glossary_service() function
       - Use lxml with recovery mode for XML parsing (QACompiler pattern)
       - Glossary filter: min_occurrence=2, max_term_length=25, filter_sentences=True (from QuickSearch defaults)
       - Import is_isolated from server.utils.qa_helpers

    4. Run tests: python3 -m pytest tests/unit/ldm/test_glossary_service.py -x -v
  </action>
  <verify>
    <automated>python3 -m pytest tests/unit/ldm/test_glossary_service.py -x -v</automated>
  </verify>
  <done>GlossaryService builds AC automaton from XML game data, detects entities with word boundary checks, filters glossary with proven QuickSearch defaults, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TwoTierCategoryMapper for string type classification</name>
  <files>
    server/tools/ldm/services/category_mapper.py,
    tests/unit/ldm/test_category_mapper.py
  </files>
  <behavior>
    - Test: categorize("path/dialog/npc_quest_dialog.xml", "Hello brave warrior") returns "Dialog" (Tier 1 STORY)
    - Test: categorize("path/sequencer/seq_intro.xml", "text") returns "Sequencer" (Tier 1 STORY)
    - Test: categorize("path/iteminfo/iteminfo_sword.xml", "Iron Sword") returns "Item" (Tier 2 priority keyword)
    - Test: categorize("path/characterinfo/characterinfo_hero.xml", "Hero") returns "Character" (Tier 2 priority keyword)
    - Test: categorize("path/questinfo/questinfo_main.xml", "Main Quest") returns "Quest" (Tier 2 priority keyword)
    - Test: categorize("path/skillinfo/skillinfo_fire.xml", "text") returns "Skill" (Tier 2 priority keyword)
    - Test: categorize("path/unknown/other.xml", "text") returns "Other" (fallback)
    - Test: categorize_batch() handles list of (path, text) pairs
  </behavior>
  <action>
    Port the TwoTierCategoryMapper from QuickCheck/core/category_mapper.py to LocaNext service architecture.

    1. Create tests/unit/ldm/test_category_mapper.py with tests for all behaviors.

    2. Create server/tools/ldm/services/category_mapper.py:
       - PRIORITY_KEYWORDS list: [("gimmick", "Gimmick"), ("item", "Item"), ("quest", "Quest"), ("skill", "Skill"), ("character", "Character"), ("region", "Region"), ("faction", "Faction")]
       - DIALOG_SUBCATEGORIES: Map dialog paths to "NPC Dialog", "System Dialog", "Cutscene Dialog", "Tutorial Dialog"
       - TwoTierCategoryMapper class:
         - categorize(file_path: str, text: str = "") -> str: Tier 1 checks STORY patterns (dialog/sequencer), Tier 2 checks priority keywords in path, fallback to "Other"
         - categorize_batch(items: List[Tuple[str, str]]) -> List[str]: batch categorization
       - Module-level function: categorize_string(file_path: str, text: str = "") -> str (convenience wrapper)

    3. Run tests: python3 -m pytest tests/unit/ldm/test_category_mapper.py -x -v
  </action>
  <verify>
    <automated>python3 -m pytest tests/unit/ldm/test_category_mapper.py -x -v</automated>
  </verify>
  <done>TwoTierCategoryMapper correctly classifies strings into categories using two-tier algorithm from QuickCheck, all tests pass</done>
</task>

</tasks>

<verification>
python3 -m pytest tests/unit/ldm/test_glossary_service.py tests/unit/ldm/test_category_mapper.py -x -v
</verification>

<success_criteria>
- GlossaryService singleton builds AC automaton from game data XML with O(n) detection
- Word boundary check prevents Korean compound false matches
- Glossary filter applies proven QuickSearch defaults (min_occ=2, max_len=25, no sentences)
- Entity index maps terms to datapoint paths (strkey, knowledge_key, source_file)
- TwoTierCategoryMapper correctly classifies string types
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/051-contextual-intelligence-qa-engine/051-01-SUMMARY.md`
</output>
