---
phase: 051-contextual-intelligence-qa-engine
plan: 03
type: execute
wave: 2
depends_on: ["051-01"]
files_modified:
  - server/tools/ldm/services/context_service.py
  - server/tools/ldm/routes/context.py
  - server/tools/ldm/router.py
  - tests/unit/ldm/test_context_service.py
  - tests/unit/ldm/test_routes_context.py
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03, CTX-04, CTX-08]

must_haves:
  truths:
    - "Character names detected in string -> context shows gender, age, job, race, quest appearances"
    - "Location names detected in string -> context shows location images and map position"
    - "Audio samples shown for detected characters (direct + indirect via same character)"
    - "Image context shown for entities even when not directly linked to StringID"
    - "Context API responds with all detected entity info for a given string"
  artifacts:
    - path: "server/tools/ldm/services/context_service.py"
      provides: "ContextService that resolves entity metadata from GlossaryService detections"
      exports: ["ContextService", "get_context_service", "CharacterContext", "LocationContext", "EntityContext"]
    - path: "server/tools/ldm/routes/context.py"
      provides: "REST API endpoints for entity context by string_id or raw text"
      exports: ["router"]
    - path: "tests/unit/ldm/test_context_service.py"
      provides: "Unit tests for context resolution"
      min_lines: 60
    - path: "tests/unit/ldm/test_routes_context.py"
      provides: "Route tests for context API"
      min_lines: 40
  key_links:
    - from: "server/tools/ldm/services/context_service.py"
      to: "server/tools/ldm/services/glossary_service.py"
      via: "get_glossary_service().detect_entities() for entity detection"
      pattern: "get_glossary_service"
    - from: "server/tools/ldm/services/context_service.py"
      to: "server/tools/ldm/services/mapdata_service.py"
      via: "get_mapdata_service() for image/audio lookups by StrKey"
      pattern: "get_mapdata_service"
    - from: "server/tools/ldm/routes/context.py"
      to: "server/tools/ldm/services/context_service.py"
      via: "get_context_service() for entity context resolution"
      pattern: "get_context_service"
---

<objective>
Build the ContextService that combines GlossaryService entity detection with MapDataService media lookups to provide rich entity context (character metadata, location images, audio samples) for any translation string.

Purpose: This connects the dots between entity detection (Plan 01) and the user-facing context panel -- transforming raw AC matches into rich, actionable context for translators.
Output: ContextService with entity metadata resolution, context API routes, unit tests.
</objective>

<execution_context>
@/home/neil1988/.claude/get-shit-done/workflows/execute-plan.md
@/home/neil1988/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/051-contextual-intelligence-qa-engine/051-RESEARCH.md
@.planning/phases/051-contextual-intelligence-qa-engine/051-01-SUMMARY.md
@server/tools/ldm/services/mapdata_service.py
@server/tools/ldm/routes/mapdata.py
@server/tools/ldm/router.py

<interfaces>
<!-- From Plan 01 (GlossaryService) -->
```python
from server.tools.ldm.services.glossary_service import (
    GlossaryService, get_glossary_service,
    DetectedEntity, EntityInfo, GlossaryEntry
)

# DetectedEntity: term, start, end, entity(EntityInfo)
# EntityInfo: type, name, strkey, knowledge_key, source_file, datapoint_paths
# GlossaryService.detect_entities(text) -> List[DetectedEntity]
# GlossaryService.get_status() -> dict
```

<!-- From Phase 5 (MapDataService) -->
```python
from server.tools.ldm.services.mapdata_service import (
    MapDataService, get_mapdata_service,
    ImageContext, AudioContext
)

# MapDataService.get_image_context(string_id) -> Optional[ImageContext]
# MapDataService.get_audio_context(string_id) -> Optional[AudioContext]
```

<!-- Router registration pattern -->
```python
from .routes.context import router as context_router
router.include_router(context_router)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ContextService with entity metadata resolution</name>
  <files>
    server/tools/ldm/services/context_service.py,
    tests/unit/ldm/test_context_service.py
  </files>
  <behavior>
    - Test: resolve_context("The warrior enters Stormhold") with "warrior" in glossary returns CharacterContext with metadata fields (name, type, strkey, knowledge_key)
    - Test: resolve_context() with location entity returns LocationContext with image_context from MapDataService
    - Test: resolve_context() with character entity returns audio_context (direct StrKey lookup + indirect via KnowledgeKey)
    - Test: resolve_context() with entity having image data returns image_context even if not directly linked to StringID
    - Test: resolve_context() returns empty EntityContext when glossary service not loaded
    - Test: resolve_context_for_row(string_id, source_text) combines StringID-direct lookups with entity detection from source text
    - Test: get_status() returns service health info
  </behavior>
  <action>
    1. Create tests/unit/ldm/test_context_service.py:
       - Mock GlossaryService and MapDataService for isolation
       - Test all behaviors listed above
       - Use fixture entities with known StrKey/KnowledgeKey values

    2. Create server/tools/ldm/services/context_service.py:
       - Dataclasses:
         - CharacterContext(name, entity_type, strkey, knowledge_key, source_file, image: Optional[ImageContext], audio: Optional[AudioContext], metadata: dict)
         - LocationContext(name, entity_type, strkey, knowledge_key, source_file, image: Optional[ImageContext], metadata: dict)
         - EntityContext(entities: List[Union[CharacterContext, LocationContext]], detected_in_text: List[DetectedEntity], string_id_context: dict)
       - ContextService class (singleton):
         - resolve_context(text: str) -> EntityContext: uses GlossaryService.detect_entities(), then for each detected entity, looks up image/audio via MapDataService using StrKey and KnowledgeKey
         - resolve_context_for_row(string_id: str, source_text: str) -> EntityContext: combines direct StringID media lookup (MapDataService) with entity detection from text
         - _resolve_entity_media(entity_info: EntityInfo) -> dict: looks up image via StrKey, then KnowledgeKey; looks up audio via same keys (CTX-03: indirect matches)
         - get_status() -> dict
       - Singleton: get_context_service()
       - Key logic for CTX-04 (indirect images): when direct StrKey lookup returns None, try KnowledgeKey lookup

    3. Run tests: python3 -m pytest tests/unit/ldm/test_context_service.py -x -v
  </action>
  <verify>
    <automated>python3 -m pytest tests/unit/ldm/test_context_service.py -x -v</automated>
  </verify>
  <done>ContextService resolves entity metadata with image/audio from MapDataService, handles direct+indirect lookups, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Context API routes and router registration</name>
  <files>
    server/tools/ldm/routes/context.py,
    server/tools/ldm/router.py,
    tests/unit/ldm/test_routes_context.py
  </files>
  <behavior>
    - Test: GET /context/{string_id} returns EntityContext with detected entities and media
    - Test: POST /context/detect with {"text": "..."} returns detected entities from text scan
    - Test: GET /context/status returns service status
    - Test: GET /context/{string_id} when service not loaded returns empty context (not error)
  </behavior>
  <action>
    1. Create tests/unit/ldm/test_routes_context.py:
       - Use FastAPI TestClient with dependency_overrides (Phase 4 pattern)
       - Mock ContextService responses
       - Test all endpoints

    2. Create server/tools/ldm/routes/context.py:
       - APIRouter(tags=["LDM-Context"])
       - GET /context/{string_id}: calls context_service.resolve_context_for_row(string_id, source_text from query param)
       - POST /context/detect: accepts {"text": "..."}, calls context_service.resolve_context(text), returns detected entities
       - GET /context/status: returns context_service.get_status() + glossary_service.get_status()
       - All endpoints require auth (get_current_active_user_async)
       - Graceful degradation: return empty results when services not loaded (not 500)

    3. Register in server/tools/ldm/router.py:
       - Import: from .routes.context import router as context_router
       - Register: router.include_router(context_router)

    4. Run tests: python3 -m pytest tests/unit/ldm/test_routes_context.py -x -v
  </action>
  <verify>
    <automated>python3 -m pytest tests/unit/ldm/test_routes_context.py -x -v</automated>
  </verify>
  <done>Context API endpoints registered and working, graceful degradation when services not loaded, all tests pass</done>
</task>

</tasks>

<verification>
python3 -m pytest tests/unit/ldm/test_context_service.py tests/unit/ldm/test_routes_context.py -x -v
</verification>

<success_criteria>
- ContextService combines GlossaryService detection + MapDataService media lookups
- Character entities resolve with image + audio (direct and indirect via KnowledgeKey)
- Location entities resolve with image context
- API endpoints return rich entity context for any string
- Graceful degradation when game data not configured
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/051-contextual-intelligence-qa-engine/051-03-SUMMARY.md`
</output>
