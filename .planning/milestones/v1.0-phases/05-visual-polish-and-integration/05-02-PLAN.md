---
phase: 05-visual-polish-and-integration
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - locaNext/src/lib/components/ldm/ImageTab.svelte
  - locaNext/src/lib/components/ldm/AudioTab.svelte
  - locaNext/src/lib/components/ldm/RightPanel.svelte
  - locaNext/src/lib/components/ldm/VirtualGrid.svelte
  - locaNext/src/lib/components/pages/GridPage.svelte
  - locaNext/tests/mapdata-context.spec.ts
autonomous: false
requirements: [MAP-03, UI-05]

must_haves:
  truths:
    - "Selecting a row in the grid loads image context in the Image tab"
    - "Selecting a row in the grid loads audio context in the Audio tab"
    - "Image tab shows thumbnail with texture name when image exists, graceful empty state when not"
    - "Audio tab shows playable audio controls with script text when audio exists, graceful empty state when not"
    - "Transitions between tabs and row selections feel smooth and responsive"
    - "Overall visual quality is consistent across grid, panels, explorers, and modals"
  artifacts:
    - path: "locaNext/src/lib/components/ldm/ImageTab.svelte"
      provides: "Image context display with thumbnail and metadata"
      contains: "fetchImageContext"
    - path: "locaNext/src/lib/components/ldm/AudioTab.svelte"
      provides: "Audio context display with player controls and script text"
      contains: "fetchAudioContext"
    - path: "locaNext/tests/mapdata-context.spec.ts"
      provides: "E2E tests for Image/Audio tab rendering on row select"
      contains: "mapdata-context"
  key_links:
    - from: "locaNext/src/lib/components/ldm/ImageTab.svelte"
      to: "/api/ldm/mapdata/image/{string_id}"
      via: "fetch in $effect when selectedRow changes"
      pattern: "fetch.*mapdata/image"
    - from: "locaNext/src/lib/components/ldm/AudioTab.svelte"
      to: "/api/ldm/mapdata/audio/{string_id}"
      via: "fetch in $effect when selectedRow changes"
      pattern: "fetch.*mapdata/audio"
    - from: "locaNext/src/lib/components/ldm/RightPanel.svelte"
      to: "ImageTab.svelte, AudioTab.svelte"
      via: "component import replacing placeholder divs"
      pattern: "import.*ImageTab|import.*AudioTab"
---

<objective>
Replace RightPanel placeholder tabs with functional Image and Audio context components, then polish visual quality across all views.

Purpose: Complete the MapDataGenerator integration so translators see image/audio context alongside segments without switching tools, and ensure the entire app looks executive-demo-ready with consistent typography, spacing, and transitions.

Output: ImageTab and AudioTab components wired to MapData API, RightPanel updated, visual polish pass, E2E tests, visual checkpoint.
</objective>

<execution_context>
@/home/<USERNAME>/.claude/get-shit-done/workflows/execute-plan.md
@/home/<USERNAME>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-visual-polish-and-integration/05-RESEARCH.md
@.planning/phases/05-visual-polish-and-integration/05-01-SUMMARY.md

@locaNext/src/lib/components/ldm/RightPanel.svelte
@locaNext/src/lib/components/ldm/VirtualGrid.svelte
@locaNext/src/lib/components/pages/GridPage.svelte

<interfaces>
<!-- From Plan 01: MapData API endpoints (created in 05-01) -->
```
GET  /api/ldm/mapdata/image/{string_id}  -> { texture_name, dds_path, thumbnail_url, has_image }
GET  /api/ldm/mapdata/audio/{string_id}  -> { event_name, wem_path, script_kr, script_eng, duration_seconds }
GET  /api/ldm/mapdata/context/{string_id} -> { image: ImageContext|null, audio: AudioContext|null }
GET  /api/ldm/mapdata/status             -> { loaded, branch, drive, image_count, audio_count }
```

From RightPanel.svelte (current placeholder pattern to replace):
```svelte
{:else if activeTab === 'image'}
  <div class="placeholder-tab">
    <Image size={32} />
    <span class="placeholder-title">Image Context</span>
    <span class="placeholder-desc">Coming in Phase 5</span>
  </div>
```

From RightPanel.svelte props:
```svelte
let {
  selectedRow = $bindable(null),  // Row object with .string_id field
  tmMatches = $bindable([]),
  qaIssues = $bindable([]),
  tmLoading = false,
  qaLoading = false,
  collapsed = $bindable(false),
  width = $bindable(300),
  leverageStats = null
} = $props();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ImageTab and AudioTab components wired to MapData API</name>
  <files>
    locaNext/src/lib/components/ldm/ImageTab.svelte,
    locaNext/src/lib/components/ldm/AudioTab.svelte,
    locaNext/src/lib/components/ldm/RightPanel.svelte
  </files>
  <action>
    1. Create `ImageTab.svelte`:
       - Props: `let { selectedRow = null } = $props()`
       - State: `let imageContext = $state(null)`, `let loading = $state(false)`, `let error = $state(null)`
       - `$effect`: when `selectedRow?.string_id` changes, fetch `GET /api/ldm/mapdata/image/{string_id}` with auth headers (use same `getAuthHeaders()` pattern as other components -- check how SemanticResults or TMTab does fetch)
       - On success: show thumbnail image (use `thumbnail_url` from response), texture name, has_image badge
       - On 404/null: show graceful empty state -- Image icon + "No image context for this segment" text + if mapdata service not loaded, show "Configure paths in Settings" prompt
       - Loading state: Carbon InlineLoading spinner
       - Thumbnail: `<img>` with max-width: 100%, rounded corners, alt text = texture_name
       - Below image: texture name in small text, DDS path in smaller muted text
       - data-testid: `image-tab-thumbnail`, `image-tab-empty`, `image-tab-loading`
       - Style with Carbon design tokens (--cds-*), match RightPanel's padding/spacing

    2. Create `AudioTab.svelte`:
       - Props: `let { selectedRow = null } = $props()`
       - State: `let audioContext = $state(null)`, `let loading = $state(false)`, `let error = $state(null)`
       - `$effect`: when `selectedRow?.string_id` changes, fetch `GET /api/ldm/mapdata/audio/{string_id}` with auth headers
       - On success: show audio player (HTML5 `<audio>` element with controls, src pointing to WAV stream endpoint or converted audio URL), event name, script text (KR and ENG lines)
       - On 404/null: graceful empty state -- Music icon + "No audio context for this segment"
       - Loading state: Carbon InlineLoading spinner
       - Script text display: show script_kr in slightly larger text, script_eng below in muted text -- this helps translators understand the voice line's context
       - Duration display if available
       - data-testid: `audio-tab-player`, `audio-tab-script`, `audio-tab-empty`, `audio-tab-loading`

    3. Update `RightPanel.svelte`:
       - Import ImageTab and AudioTab at top
       - Replace Image placeholder div with `<ImageTab {selectedRow} />`
       - Replace Audio placeholder div with `<AudioTab {selectedRow} />`
       - Keep AI Context placeholder (that's Phase 5.1)
       - selectedRow is already a prop on RightPanel, just pass it through
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools/locaNext && npx svelte-check --threshold error 2>&1 | tail -5</automated>
  </verify>
  <done>Image tab renders thumbnail + metadata when row has image context, empty state otherwise. Audio tab renders player + script text when row has audio context, empty state otherwise. RightPanel wires both tabs to selectedRow. svelte-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Visual polish pass + E2E tests for MapData tabs</name>
  <files>
    locaNext/src/lib/components/ldm/VirtualGrid.svelte,
    locaNext/src/lib/components/pages/GridPage.svelte,
    locaNext/src/lib/components/ldm/RightPanel.svelte,
    locaNext/tests/mapdata-context.spec.ts
  </files>
  <action>
    1. Visual polish pass (CSS-only changes, no JS logic changes -- zero perf regression risk):
       - VirtualGrid: review spacing between rows, ensure consistent cell padding, verify status colors render crisply, check that AI badge doesn't overlap with other elements at narrow widths
       - RightPanel: add smooth tab transition (opacity/transform), ensure tab content area has consistent padding, verify QA footer separator looks clean
       - GridPage: verify the split between grid and right panel looks balanced, check resize handle visibility
       - Typography pass: ensure all font-sizes use Carbon tokens or the established size scale (0.625rem/0.6875rem/0.75rem/0.875rem), no raw px values
       - Spacing pass: ensure all padding/margin use multiples of 4px (Carbon's spacing scale), no arbitrary values
       - Transition pass: add `transition: all 0.15s ease` to interactive elements that don't already have transitions (tab switching, panel resize, hover states)
       - Color consistency: verify all colors use Carbon design tokens (--cds-*) not hardcoded hex (except the established status color constants)

    2. Create `locaNext/tests/mapdata-context.spec.ts`:
       - Use Playwright route interception to mock `/api/ldm/mapdata/image/*` and `/api/ldm/mapdata/audio/*` responses (same pattern as semantic-search.spec.ts)
       - Test: Click Image tab -> shows loading or empty state when no row selected
       - Test: Select a row + click Image tab -> mock returns image context -> thumbnail renders with data-testid="image-tab-thumbnail"
       - Test: Select a row + click Image tab -> mock returns 404 -> shows empty state with data-testid="image-tab-empty"
       - Test: Click Audio tab -> shows empty state when no row selected
       - Test: Select a row + click Audio tab -> mock returns audio context -> script text renders with data-testid="audio-tab-script"
       - Test: Tab switching between TM/Image/Audio works without errors
       - Follow existing E2E patterns: login flow, file upload via API fixture, navigation to grid
  </action>
  <verify>
    <automated>cd /home/<USERNAME>/LocalizationTools/locaNext && npx playwright test tests/mapdata-context.spec.ts --project=chromium 2>&1 | tail -10</automated>
  </verify>
  <done>E2E tests pass for Image/Audio tab rendering with mocked API. Visual polish applied consistently across grid, panels, and explorers. All CSS uses Carbon tokens. Transitions feel smooth.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification checkpoint</name>
  <action>
    What was built: Complete MapDataGenerator integration (Image + Audio tabs in RightPanel) and visual polish across all views.
    How to verify:
    1. Start dev servers: ./scripts/start_all_servers.sh --with-vite
    2. Open http://localhost:5173 and log in (admin/admin123)
    3. Navigate to a project -> folder -> open a file in the grid
    4. Click "Image" tab in right panel -- verify it shows thumbnail or graceful empty state (not "Coming in Phase 5" placeholder)
    5. Click "Audio" tab in right panel -- verify it shows audio player/script text or graceful empty state
    6. Click between different rows -- verify Image/Audio tabs update reactively
    7. Click between TM/Image/Audio tabs -- verify transitions are smooth
    8. Open Settings and verify Branch/Drive configuration modal (from Plan 01)
    9. Overall visual impression: typography consistent, spacing uniform, no rough edges, colors use dark theme properly
  </action>
  <verify>Human approves visual quality</verify>
  <done>User confirms app looks executive-demo-ready with functional Image/Audio tabs and consistent visual polish</done>
</task>
















</tasks>

<verification>
- `cd locaNext && npx playwright test tests/mapdata-context.spec.ts --project=chromium` -- E2E tests pass
- `cd locaNext && npx svelte-check --threshold error` -- zero errors
- `python3 -m pytest tests/unit/ldm/test_mapdata_service.py -x` -- backend tests still pass
- Visual checkpoint: human confirms demo-ready quality
</verification>

<success_criteria>
- Image tab shows thumbnail + metadata when row has image context, graceful empty state when not
- Audio tab shows player controls + script text when row has audio context, graceful empty state when not
- Tab switching is smooth with no flicker or layout shift
- Typography, spacing, and colors are consistent across all views
- E2E tests cover happy path and empty state for both tabs
- Human approves visual quality as executive-demo-ready
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-polish-and-integration/05-02-SUMMARY.md`
</output>
